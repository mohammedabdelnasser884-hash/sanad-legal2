# سند — تقرير حالة: المرحلة 8 (Smoke) — إغلاق نهائي

**التاريخ:** 25 يوليو 2026
**الحالة:** 🟢 المرحلة 8 مُقفلة نهائيًا
**المرجع:** `سند-خطة-تنفيذ-اختبارات-E2E-مقسمة-2.md` + تقارير الدفعات 1، 2، 3
**الزيب المرفق:** `sanad-project-updated-final-35.zip`

---

## القرار النهائي: حذف `QuickAddSessionModal.tsx`

تم التحقق من كونه كود ميت فعليًا قبل الحذف، عبر:

1. **`grep` شامل على المشروع كامل** (`.ts`/`.tsx`) — الاسم مكانش بيظهر إلا
   جوه الملف نفسه (تعريف الـ`interface` والـ`export`). مفيش `import` واحد
   ليه من أي ملف تاني.
2. **فحص فولدر `sessions-calendar/`** — مفيش `index.ts` بيعمل re-export
   يخبي الاستخدام.
3. **فحص فولدر `e2e/` كامل** — صفر إشارة له، حتى في تستات قديمة.
4. **فحص نمط الكتابة في الداتابيز** — الملف كان بيكتب في `case_sessions`
   بطريقة مباشرة (`db.from().insert()`)، مختلفة عن النمط الحالي
   (`__dbWrite` / offline queue) المستخدم في البديل الفعلي
   (`NewStandaloneSessionModal.tsx`) وباقي الملفات المشابهة. يعني حتى لو
   كان متوصل، كان هيبقى غير متوافق مع باقي النظام.
5. **لا أثر على الداتابيز نفسها** — الملف ما كانش بيعرّف عمود أو جدول
   خاص بيه، وجدول `case_sessions` بيتكتب فيه من أماكن تانية مغطاة
   ومختبرة (`NewStandaloneSessionModal`, `StandaloneSessionDetailModal`).

**النتيجة:** تم حذف `src/features/calendar/sessions-calendar/QuickAddSessionModal.tsx`
نهائيًا. صفر تأثير متوقع على أي شاشة أو تست موجود.

---

## حالة البنود المفتوحة الباقية

### بطاقة "🔴 يحتاج تدخل فوري" على الداشبورد
سيبناها من غير تست جديد — نفس مكوّني `buildSessionCard`/`buildTaskCard`
المشتركين مغطيين بالفعل عن طريق بطاقة "اليوم". الفرق بينهم بس شرط عرض
مختلف (فائت بدل النهاردة)، فمفيش قيمة إضافية حقيقية من تكرار نفس التست.
بند مفتوح اختياري لو حبينا نرجعله لاحقًا، مش عائق على إغلاق المرحلة.

### `TeamTab.tsx`
اتفقنا إنها مهمة منفصلة عن نطاق المرحلة 8 (تغطية الشاشات الموجودة فعليًا).
هتترجع كبند مستقل لما يتضاف زرار نقر حقيقي في `CommandDock.tsx` — ووقتها
هتتضاف التست في نفس دفعة الزرار، مش هترجعلها المرحلة دي.

---

## خلاصة المرحلة 8 بالكامل

كل الاثنتي عشر شاشة الأصلية في نطاق المرحلة اتغطت بتستات e2e حقيقية عبر
الدفعات التلاتة (`CalendarTab` كان متغطي من الأول، `MonthListTab`،
`MissedTab`، `ArchiveTab`، `DashboardTab`، بالإضافة لكل الشاشات
اللي اتغطت في الدفعتين الأولانيين). البند الوحيد اللي كان معلّق (قرار
`QuickAddSessionModal.tsx`) اتحسم بالحذف. `TeamTab.tsx` وبطاقة "يحتاج
تدخل فوري" خرجوا رسميًا برا نطاق المرحلة كبنود مستقبلية منفصلة.

**المرحلة 8 مُقفلة نهائيًا.**

---

## الزيب المرفق
`sanad-project-updated-final-35.zip` — فيه كل حاجة من
`sanad-project-updated-final-34.zip` زي ما هي، ناقص:
- `src/features/calendar/sessions-calendar/QuickAddSessionModal.tsx`
  (اتحذف نهائيًا).

زائد هذا التقرير.

---

## تحقق الصيغة
**لازم تتأكد بنفسك قبل الدمج (زي دايمًا):**
1. `npm run type-check` (`tsc --noEmit`) على المشروع كامل — للتأكد إن
   حذف الملف مفيهوش أي أثر جانبي (مفيش استيراد له أصلاً، لكن التأكيد
   النهائي بييجي من الـbuild نفسه).
2. `npm run test:e2e` كامل — تأكيد إن كل التستات الموجودة (بما فيها
   الأربعة الجديدة من الدفعة الأخيرة) لسه شغّالة.
3. Vercel build — للتأكد إن الحذف ما كسرش أي شيء وقت الـbundle.

---

## الخطوة الجاية
المرحلة 8 قُفلت. البنود المستقبلية (`TeamTab.tsx` + بطاقة "يحتاج تدخل
فوري") هتتفتح كمهام مستقلة لما يحين وقتها — مش جزء من أي متابعة مطلوبة
دلوقتي.
