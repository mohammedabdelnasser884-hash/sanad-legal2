# تقرير فحص لوجز CI/CD — 4 أغسطس 2026

فحصت لوجز الـrun (build-and-test ناجح بالكامل، 856 تست وحدة ✅) والـE2E (8 فشل من 100، من ضمنهم 1 flaky). الفشل التمنية دي طلعوا 4 أسباب جذرية مختلفة بس — مش 8 مشاكل منفصلة.

---

## ✅ 1. تم الفيكس فعليًا (2 تستات)

**المشكلة:** رسالة فاليديشن "المسمى القانوني الجامع" (قاعدة 6 في `casePartiesValidation.ts`) كانت نص عام/hint مش رسالة خطأ محددة بالطرف:
> "⚠️ يُكتب فقط في حالة كون أي من الطرفين الأول أو الثاني أكثر من شخص..."

بينما التستات (وده المنطقي للمستخدم) كانت متوقعة رسالة توضح **أنهي طرف بالظبط**:
> "⚠️ الطرف الأول (المدعي) فيه أكثر من شخص — لازم تكتب \"المسمى القانوني\" الجامع لهذا الطرف"

**الفيكس:** `src/shared/lib/casePartiesValidation.ts` — الرسالة بقت ديناميكية حسب `side` (مدعي/مدعى عليه).

**التستات اللي هتعدي:**
- `e2e/case-parties-and-sessions.spec.ts:15` — فاليديشن المسمى القانوني الجامع
- `e2e/standalone-sessions.spec.ts:27` — نفس الفاليديشن في الجلسة المستقلة

---

## ✅ 2. تم الفيكس فعليًا (3 تستات) — مش باج، تستات قديمة

**المشكلة:** `EditClientModal.tsx` بقى فيه خطوة تأكيد جديدة ("⚡ NEW" في الكود) — بعد الفاليديشن، بيعرض تنبيه "التعديل ده هيتطبق في كل مكان الموكل ده ظاهر فيه" ولازم ضغطة تأكيد تانية (`edit-client-impact-confirm`) قبل ما `onSave` ينفّذ فعليًا. التستات القديمة كانت بتدوس `save-client-edit-button` وتتوقع الحفظ يحصل على طول، فكانت بتوصل لتوست قديم متبقي من خطوة سابقة في نفس التست (زي "✅ تم إضافة الموكل بنجاح!" بدل "✅ تم تحديث بيانات الموكل").

**الفيكس:** عدّلت `e2e/clients.spec.ts` — التلات تستات دلوقتي بتدوس `edit-client-impact-confirm` بعد `save-client-edit-button`:
- تست الحفظ الناجح
- تست تكرار الرقم القومي (وبقى بيرجع لفورم التعديل عبر `edit-client-impact-cancel` قبل ما يتأكد إن زرار الحفظ ظاهر تاني)
- تست الدبل-كليك — اتنقلت الحماية من `save-client-edit-button` لـ`edit-client-impact-confirm` لأنه هو اللي بينفّذ الحفظ الفعلي دلوقتي

**التستات اللي هتعدي:**
- `e2e/clients.spec.ts:75` — تعديل موكل موجود بنجاح
- `e2e/clients.spec.ts:94` — تكرار الرقم القومي عند التعديل
- `e2e/clients.spec.ts:115` — دبل-كليك على حفظ التعديلات

---

## ⏳ 3. باقي — محتاج تدخل منك (مش كود)

**المشكلة:** `e2e/session-update.spec.ts:63` و `:100` بيفشلوا بتوست "❌ فشل إنشاء الجلسة الجديدة" بدل التحديث الناجح، وكمان تايم-آوت لاحق بسبب مودال تنبيه أثر التعديل (نفس البند اللي فوق) فاضل فاتح وبيقفل الضغط على عناصر تانية.

**السبب الأرجح:** كود `SessionUpdateModal.tsx` بقى بيبعت عمود `session_group_id` في الـinsert الجديد للجلسة القادمة (جزء من خطة "تسلسل الجلسة المستقلة — 3 أغسطس 2026"). الـmigration بتاعت العمود ده (`database/migrations/standalone-session-group-id-migration.sql`) موجودة في المشروع لكن — زي كل الـmigrations عندك — محتاجة تتنفذ يدوي من Supabase Dashboard. لو لسه ما اتنفذتش، الـinsert بيفشل لأن العمود مش موجود فعليًا في قاعدة البيانات (وده بيفسّر الـ500 error).

**اللي محتاجه منك:**
1. افتح Supabase Dashboard → SQL Editor وشغّل:
   ```sql
   SELECT column_name FROM information_schema.columns
   WHERE table_name='case_sessions' AND column_name='session_group_id';
   ```
2. لو رجعت صف فاضي (مفيش عمود بالاسم ده) → شغّل محتوى `database/migrations/standalone-session-group-id-migration.sql` كامل من نفس الـSQL Editor.
3. بعد كده التستات دي المفروض تعدي من غير أي تعديل كود تاني.

---

## ✅ 3. تم الفيكس فعليًا (session-update — migration اتنفذ + الكود جاهز أصلاً)

**السبب المؤكد:** عمود `session_group_id` في `case_sessions` كان ناقص فعليًا (اتأكدنا بكويري فحص في Supabase — رجع "No rows returned"). اتحل بتنفيذ `database/migrations/standalone-session-group-id-migration.sql` يدويًا من Supabase Dashboard SQL Editor (اتأكد بإعادة نفس كويري الفحص ورجع العمود موجود). الكود في `SessionUpdateModal.tsx` كان جاهز أصلاً من غير أي تعديل مطلوب.

**التستات اللي هتعدي:**
- `e2e/session-update.spec.ts:63` — تحديث جلسة مستقلة بلا موكل مربوط
- `e2e/session-update.spec.ts:100` — تحديث جلسة مستقلة مربوطة بموكل

---

## ✅ 4. تم الفيكس فعليًا (admin-backup — السبب الحقيقي)

**المشكلة:** جربنا "إنشاء نسخة احتياطية" يدوي وراجعنا Supabase Logs، ولقينا الخطأ الحقيقي:
```
canceling statement due to statement timeout
```
على الكويري بتاعة `SELECT * FROM backups ORDER BY created_at DESC LIMIT 20` (يعني قايمة النسخ نفسها، مش عملية الإنشاء).

**السبب:** `fetchBackups()` في `useAdminBackup.ts` كانت بتعمل `select('*')` — وده بيجيب عمود `data` كمان، اللي فيه نسخة JSON كاملة من كل جداول المكتب (ممكن يكون ميجابايتات لكل صف). مع تراكم صفوف `backups` بمرور الوقت (الجدول ده مش متضمن في أي تنظيف دوري)، مجرد عرض قايمة النسخ بقى بيجر كل بيانات كل نسخة موجودة، وده اللي ضرب statement timeout في بوستجرس.

**الفيكس:** `src/features/admin/backup/hooks/useAdminBackup.ts`:
- `fetchBackups()` بقت بتجيب الأعمدة الخفيفة بس (`id, created_at, created_by, created_by_name, tables_count, rows_count, size_kb`) من غير `data` — القايمة نفسها هتبقى سريعة جدًا حتى لو فيه مئات النسخ.
- `handleDownloadBackup` و`handleRestoreBackup` بقوا بيجيبوا عمود `data` لوحده بصف واحد (`select('data').eq('id', backup.id).single()`) بس وقت الحاجة الفعلية (لما المستخدم يدوس تنزيل أو يأكد استعادة) — مش مع كل القايمة.
- كمان عدّلت التستات في `useAdminBackup.test.ts` عشان تعكس السلوك الجديد ده (بما فيها تستات فشل جديدة لحالة فشل جلب الصف الكامل).

**التستات اللي هتعدي:**
- `e2e/admin-backup.spec.ts:42` — إنشاء نسخة احتياطية جديدة وظهورها في القايمة

**ملحوظة:** لو عندك نسخ قديمة كتير متراكمة في جدول `backups` من تشغيلات CI قبل كده، ينفع (اختياري، مش عاجل) تعمل تنظيف يدوي ليها من وقت للتاني عشان الجدول ميفضلش يكبر من غير داعي — بس مش لازم دلوقتي، الفيكس ده بيحل المشكلة بغض النظر عن حجم الجدول.

---

## ملخص سريع

| الفشل | الحالة | محتاج ايه |
|---|---|---|
| case-parties-and-sessions (فاليديشن) | ✅ اتصلح | ولا حاجة |
| standalone-sessions (فاليديشن) | ✅ اتصلح | ولا حاجة |
| clients ×3 (تعديل موكل) | ✅ اتصلح | ولا حاجة |
| session-update ×2 | ✅ اتصلح (migration اتنفذ) | ولا حاجة |
| admin-backup ×1 | ✅ اتصلح (select الأعمدة الخفيفة بس) | ولا حاجة |
| standalone-sessions #9 (ربط بموكل) | flaky — عدّى في retry | راقبها في الـrun الجاي، ممكن تتحل تلقائي مع فيكس session-update |

**الخطوة الجاية:** ارفع كل الملفات المعدّلة (موجودة في الزيب) على GitHub وشغّل الـCI. المفروض دلوقتي 7 من الـ8 فشل يعدّوا، والتامن (standalone-sessions #9) نراقبه في اللوج الجاي.
