# تقرير حالة — توحيد منطق "إنشاء موكل" في موديل عرض الجلسة المستقلة (محدّث)

> يحل محل `خطة-توحيد-انشاء-وربط-الموكل-في-عرض-الجلسة-المستقلة-4-8.md` (الخطة الأصلية). هذا التقرير بيوثّق اللي اتنفذ فعليًا + المتبقي.

## المشكلة الأصلية (تذكير)
دوسة "إنشاء موكل" من موديل "عرض بيانات الجلسة المستقلة" (`StandaloneSessionDetailModal` → زرار "🔗 ربط") كانت بتاخد أول طرف بس (`session.plaintiff`) وتتجاهل باقي الأطراف، وكانت بتعمل `INSERT` مباشر في `clients` بدل ما تعدّي على `NewClientModal` الموحّد — بعكس نفس السلوك في `InfoSection.tsx` (تفاصيل القضية) وفي `NewStandaloneSessionModal.tsx` (فور تسجيل جلسة مستقلة جديدة)، اللي الاتنين فيهم بيدّوا زرار مستقل لكل طرف.

---

## اللي اتنفذ فعليًا

### 1) `src/features/calendar/hooks/caseSessionLinkingShared.ts`
- إضافة `client_id?: string | null` لـ `SessionClientParty` وللـ `select()` في `fetchSessionClientParties` — تغيير إضافي بحت، مفيش أي كسر للاستخدامات القديمة (`handleLinkCase`, `movePartiesFromSessionToCase`, `matchClientsForParties`, وهوك `useClientLinking.ts`).

### 2) `src/features/calendar/hooks/useSessionLinking.ts`
- باراميتر سابع اختياري جديد: `onOpenCreateClientForSessionParty` (نوعه `OpenCreateClientForSessionParty` من `useClientLinking.ts`، بدون تكرار للنوع).
- `idlePartyList` + `linkedIdlePartyIds` (state) + `useEffect` بيجيب أطراف الجلسة (`is_client=true`) بمجرد ما الهوك يشتغل، وبيستبعد أي طرف اتربط بموكل بالفعل (`client_id` مش فاضي) — الفرق عن `useClientLinking.ts` إن هنا الجلسة *محفوظة بالفعل* فممكن تتفتح تاني بعد ما بعض الأطراف اتربطوا من قبل.
- `handleAddClientOnlyForParty(party)` — بتفتح `NewClientModal` الموحّد لطرف بعينه (بدل الـ `INSERT` المباشر القديم)، وبعد نجاح الربط بتحدّث `linkedIdlePartyIds` فيختفي زرار الطرف ده بس.
- `handleAddClientOnly` القديمة (طرف واحد، `INSERT` مباشر) **اتسابت زي ما هي** — بقت فولباك للجلسات القديمة اللي مفيهاش `case_parties` خالص (`idlePartyList.length === 0`)، نفس فلسفة `useClientLinking.ts` بالحرف (مفيش داعي منطقي نلغيها، بتغطي حالة حقيقية).
- كل المسارات التانية في نفس الملف (`handleLinkCase`, `handleLinkExistingClient`, `handleAddAndLinkClient`, `confirmLinkToExistingClient`) **زي ما هي بالظبط** — صفر تغيير سلوك.

### 3) `src/features/calendar/sessions-calendar/StandaloneSessionDetailModal.tsx`
- `LinkSessionModalProps` و`StandaloneSessionDetailModalProps`: إضافة `onOpenCreateClientForSessionParty?` (اختيارية).
- خطوة `idle` في `LinkSessionModal`: بقت بتعرض **زرار مستقل لكل طرف** من `idlePartyList` (باسمه، ويختفي هو بس بعد الربط) — فولباك كامل للزرار الواحد القديم لو الجلسة من غير أطراف متعددة (`idlePartyList.length === 0`)، بنفس نمط `postSave` في `NewStandaloneSessionModal.tsx` حرفيًا.
- تمرير البروب الجديدة لـ `useSessionLinking` وللـ render الأخير لـ `LinkSessionModal`.

### 4) توصيل البروب من فوق (App.tsx ← DashboardTab / SessionsCalendar)
لقينا `StandaloneSessionDetailModal` بيتعمله render فعليًا (مش مجرد ذكر في تعليق) في **مكانين**:
- `src/features/dashboard/DashboardTab.tsx` (تاب "الرئيسية") — ✅ اتوصلت.
- `src/features/calendar/sessions-calendar/SessionsCalendar.tsx` (تاب "الجلسات") — ✅ اتوصلت.

في الاتنين: أضفنا البروب في الـ interface + الـ destructuring + التمرير لـ `StandaloneSessionDetailModal`، وفي `App.tsx` مررنا `handleOpenCreateClientForSessionPartyOnly` (الموجودة بالفعل، مستخدمة أصلاً في `NewStandaloneSessionModal.tsx`) للاتنين.

**النتيجة:** أي مكان في التطبيق بيفتح موديل عرض الجلسة المستقلة دلوقتي بياخد نفس السلوك — زرار مستقل لكل طرف، يفتح `NewClientModal` الموحّد، يختفي بس للطرف اللي اتربط.

---

## ليه اتعمل كده (تلخيص القرارات)
- **إعادة استخدام النوع `OpenCreateClientForSessionParty` بدل تعريف نوع جديد** — نفس الـ callback (`handleOpenCreateClientForSessionPartyOnly` في App.tsx) شغال بالفعل مع `sessionId` عام (مش مرتبط بلحظة الحفظ)، فمفيش داعي لأي كود جديد في App.tsx غير التوصيل.
- **فلترة `client_id` في `useSessionLinking.ts` نفسه، مش في الدالة المشتركة `fetchSessionClientParties`** — عشان `handleLinkCase` (اللي بيستخدم نفس الدالة لنقل *كل* الأطراف للقضية الجديدة، مش بس الناقصة) يفضل شغال زي ما هو بالظبط.
- **الاحتفاظ بـ `handleAddClientOnly` القديمة كفولباك بدل حذفها** — نفس القرار المتخذ في `useClientLinking.ts` وقت "خطة توحيد إنشاء الموكل"، عشان الجلسات القديمة قبل تعدد الأطراف تفضل شغالة بنفس السلوك المضمون.

---

## المتبقي

### 1) قرار معلّق (Phase 3 من الخطة الأصلية)
"🔗 ربط بموكل موجود" في خطوة idle لسه بيربط **الجلسة كلها** (`case_sessions.client_id`)، مش طرف بعينه — نفس القيد الموجود في `InfoSection.tsx` (تفاصيل القضية) بالظبط. التوصية كانت نأجّل توسيعه لكل طرف على حدة لخطة لاحقة موحّدة (تشمل `InfoSection.tsx` كمان)، عشان الاتنين يفضلوا متسقين. **لسه محتاج قرارك.**

### 2) تنظيف/تستات (Phase 4)
- مراجعة `useSessionLinking.test.ts` — إضافة تستات للمسار الجديد: زرار لكل طرف، الفولباك للطرف الواحد، اختفاء الزرار الصحيح بس بعد `linkedIdlePartyIds`.
- مفيش كود قديم محتاج حذف فعليًا (القرار كان نسيب `handleAddClientOnly` كفولباك، مش نحذفها).

### 3) اختبار يدوي (Phase 5) — لسه ما اتعملش (محتاج بيئة فيها npm/tsc فعليًا، مش متاحة هنا)
- ⚠️ **تنويه مهم:** الفحص هنا كله كان مراجعة يدوية سطر بسطر (زي فحص الأقواس/الأكواد المتوازنة) — البيئة دي من غير `node_modules`/اتصال إنترنت، فمعرفش أشغّل `npm install` ولا `tsc --noEmit` ولا `npm run test` فعليًا. **لازم تشغيل الاتنين دول على جهازك قبل الرفع للتأكد النهائي.**
- سيناريوهات مطلوب اختبارها يدويًا:
  - جلسة مستقلة بطرف واحد بس (فولباك) — تتأكد إن السلوك زي القديم بالظبط.
  - جلسة مستقلة بأكتر من طرف (`is_client=true`) — زرار مستقل لكل طرف، يختفي هو بس بعد الإنشاء، الباقيين فاضلين.
  - إعادة فتح موديل "🔗 ربط" بعد ما بعض الأطراف اتربطوا من قبل — تتأكد إن الفلترة الجديدة (`client_id`) بتشتغل صح ومش بتعيد عرض زرار لطرف اتربط بالفعل.
  - أونلاين/أوفلاين (`_offlineTempId`/`__dbWrite`) عبر `NewClientModal`.

---

## الملفات المعدَّلة في هذه الدفعة
- `src/features/calendar/hooks/caseSessionLinkingShared.ts`
- `src/features/calendar/hooks/useSessionLinking.ts`
- `src/features/calendar/sessions-calendar/StandaloneSessionDetailModal.tsx`
- `src/features/calendar/sessions-calendar/SessionsCalendar.tsx`
- `src/features/dashboard/DashboardTab.tsx`
- `src/App.tsx`

## ترتيب مقترح للخطوة الجاية
1. تشغيل `npm install` + `tsc --noEmit` + `npm run test` على جهازك للتأكد إن مفيش أخطاء type أو تستات فاشلة (خصوصًا `useSessionLinking.test.ts` القديمة، ممكن تحتاج تحديث بسيط بسبب الباراميتر السابع الجديد في `useSessionLinking()`).
2. رأيك في القرار المعلّق (Phase 3).
3. الاختبار اليدوي بالسيناريوهات فوق.
