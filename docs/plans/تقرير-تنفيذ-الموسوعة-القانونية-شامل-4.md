# تقرير تنفيذ شامل — قسم "الموسوعة القانونية"

**التاريخ:** 13 سبتمبر 2026
**الحالة العامة:** المراحل 1 إلى 5 (قاعدة البيانات، الفانكشنز، واجهة الإدارة، تصفح المستخدمين، اختبارات E2E) منفذة في الكود. كل الخطوات اليدوية (migration، الباكت، نشر الفانكشنين) **تمت ✅**. الاختبار الحقيقي (unit + E2E) لسه محتاج تشغيل فعلي منك (الساندبوكس هنا من غير `node_modules`/شبكة).

---

## 1. القرارات النهائية اللي اتقفلت

1. **عداد التحميلات** — عمود `download_count` على `encyclopedia_forms`، يبدأ من 0، يظهر للسوبر أدمن بس.
2. **الترتيب** — أبجدي تلقائي بالكامل (`name_ar` للمجلدات، `title` للنماذج). اتشال عمود `sort_order` من التصميم لأنه مش لازم.
3. **حذف مجلد فيه محتوى** — Cascade كامل: حذف مجلد رئيسي بيشيل معاه أي مجلد فرعي وكل النماذج المرتبطة، من غير تأكيد على مستوى القاعدة (التأكيد بقى مسؤولية الواجهة في مرحلة 3، واتنفذ فعلاً).
4. **حد حجم الملف** — **2 ميجا**، منفصل تمامًا عن حد الـ1 ميجا الخاص بمستندات القضايا. ده كان سؤال مفتوح في تقرير مرحلة 1 واتقفل بقرارك في مرحلة 2.

---

## 2. مرحلة 1 — قاعدة البيانات والتخزين

**Migration جديدة:** `database/migrations/sql-migrations-phase28/01-encyclopedia-tables.sql`

- جدول `encyclopedia_categories` (المجلدات): مستويين بس، مفروضين عبر تريجر `encyclopedia_enforce_two_level_depth`، مع `ON DELETE CASCADE` على `parent_id`.
- جدول `encyclopedia_forms` (النماذج): `ON DELETE CASCADE` على `category_id`، وعمود `download_count`.
- تريجر `updated_at` تلقائي على الجدولين عبر دالة محلية جديدة `encyclopedia_set_updated_at` (مش بتلمس أي جدول تاني).
- **RLS:** قراءة مفتوحة لأي `authenticated`، كتابة مقصورة على `is_super_admin()` — استُخدمت الدالة الجاهزة الموجودة فعلاً في المشروع بدل `EXISTS` يدوي كان مقترح أصلاً.
- **`src/database.types.ts`:** أُضيف تعريف الجدولين (Row/Insert/Update) في ترتيبهم الأبجدي الصحيح بين `clients` و`fee_payments`.

---

## 3. مرحلة 2 — Edge Function

**ملف جديد:** `supabase/functions/encyclopedia-admin/index.ts` — فانكشن واحدة، بتتحقق `is_super_admin === true` قبل أي عملية (نفس نمط `admin-actions`)، وبتغطي الأفعال الستة:

`createCategory` / `updateCategory` / `deleteCategory` / `uploadForm` / `updateForm` / `deleteForm`

نقاط مهمة:

- **حد حجم الملف 2 ميجا** (`MAX_FILE_SIZE_BYTES`) مُطبّق جوه الفانكشن نفسها، منفصل تمامًا عن حد الـ1 ميجا الخاص بمستندات القضايا (`validateUploadFile` في `src/shared/lib/storage.ts` — لم يُلمس).
- **تنظيف التخزين عند الحذف/الاستبدال** (الـcascade في القاعدة بيمسح صفوف الـDB بس، الفانكشن مسؤولة عن الملفات الفعلية):
  - `deleteCategory` بيجمع كل النماذج (نفس المجلد + أي مجلد فرعي جواه) ويمسح ملفاتها كلها *قبل* حذف المجلد من الـDB.
  - `deleteForm` بيمسح الملف الأول بعدين الصف.
  - `updateForm` (لو فيه ملف جديد): بيرفع الجديد الأول، يعدّل الصف، وبعد نجاح التعديل بس بيمسح القديم — عشان لو التعديل فشل مايضيعش الملف الأصلي.
  - `uploadForm`: لو الـinsert فشل بعد نجاح الرفع، بيمسح الملف اليتيم (rollback).
- **مسار التخزين:** `{category_id}/{form_id}.{ext}` بالظبط زي ما الخطة نصّت.
- **RLS مش كافية لوحدها هنا:** رفع/حذف ملف على باكت private لازم `service_role`، وده سبب وجود الفانكشن أصلًا — طبقة إضافية فوق RLS مش بديل عنها.
- **اختبار وحدة:** `index.test.ts` (15 حالة) يغطي رفض غير السوبر أدمن، رفض الحساب المعطّل، إنشاء/تعديل/حذف مجلد (بما فيها رسالة تريجر منع المستوى التالت)، رفض نوع/حجم ملف غير صالح، رفع/تعديل/حذف نموذج بنجاح، وحالات rollback عند الفشل الجزئي.

---

## 4. مرحلة 3 — واجهة الإدارة (سوبر أدمن)

### ملفات جديدة

- **`src/features/admin/encyclopedia/hooks/useAdminEncyclopedia.ts`** — هوك كامل يدير القسم: القراءة مباشرة من الجداول (RLS مفتوحة)، والكتابة كلها عن طريق `encyclopedia-admin`.
  - `fetchEncyclopedia` — قراءة `encyclopedia_categories` + `encyclopedia_forms`.
  - `handleSaveCategory` / `handleDeleteCategory` — إنشاء/تعديل/حذف مجلد.
  - `handleSaveForm` / `handleDeleteForm` — رفع/تعديل/حذف نموذج، تحويل الملف لـ base64 قبل الإرسال.
  - فحص مبكر في الواجهة لنوع وحجم الملف (PDF/docx، 2 ميجا) قبل التحويل لـ base64 — نفس القيم المطبّقة في الفانكشن، لتفادي انتظار رد السيرفر لملف هيتم رفضه أصلاً.
  - تسجيل نشاط (`logActivity`) على كل عملية، بنفس نمط باقي أقسام لوحة الإدارة.
- **`src/features/admin/encyclopedia/EncyclopediaCategoryModal.tsx`** — مودال إضافة/تعديل مجلد؛ بيخفي اختيار "المجلد الأب" لو المجلد رئيسي، كإجراء وقائي إضافي فوق قيد المستويين المفروض من التريجر.
- **`src/features/admin/encyclopedia/EncyclopediaFormModal.tsx`** — مودال رفع/تعديل نموذج؛ اختيار المجلد يشمل كل المجلدات (رئيسية وفرعية).
- **`src/features/admin/encyclopedia/EncyclopediaSection.tsx`** — واجهة تصفح بمستويين: القائمة الجذر (مجلدات رئيسية)، جوه مجلد رئيسي (مجلداته الفرعية + نماذجه المباشرة)، جوه مجلد فرعي (نماذجه بس)، مع breadcrumb وأزرار تعديل/حذف على كل بطاقة.

### ملفات معدّلة

- **`src/types.ts`** — إضافة `EncyclopediaCategoryRow` و`EncyclopediaFormRow`.
- **`src/supabaseClient.ts`** — إضافة `callEncyclopediaAction` + نوع `EncyclopediaActionPayload` (دالة مستقلة عن `callAdminAction`، بنفس منطق استخراج رسالة الخطأ العربية).
- **`src/features/admin/AdminPanel.tsx`** — قسم جديد (`encyclopedia`): مقصور على نفس حساب السوبر أدمن (`SUPER_ADMIN_EMAIL`)، Nav card كهرماني تحت "المكتبة القانونية" مباشرة بعداد إجمالي النماذج، وجلب البيانات فقط عند فتح القسم فعليًا.
- **`src/features/admin/AdminPanelModals.tsx`** — ربط مودالي المجلد والنموذج + تأكيدي حذف بتحذير Cascade مخصص (رئيسي vs فرعي).

---

## 5. الخطوات اليدوية المتبقية (مجمّعة من كل المراحل)

1. **تشغيل الـmigration** `database/migrations/sql-migrations-phase28/01-encyclopedia-tables.sql` على Supabase (SQL Editor) — الملف فيه خطوات تأكيد (`pg_class`/`pg_policies`) للتأكد من تفعيل RLS ووجود السياسات الأربعة.
2. **إنشاء باكت Storage يدويًا:** الاسم `encyclopedia-forms`، **Private**.
3. **نشر الفانكشن:** `supabase functions deploy encyclopedia-admin` (متغيرات البيئة متاحة تلقائيًا، زي باقي الفانكشنز — مفيش إعداد إضافي).
4. **تشغيل التست الحقيقي** (`npm test` / `npm run build`) — الساندبوكس هنا من غير `node_modules`/شبكة، فالمراجعة كانت يدوية بس (توازن الأقواس، تطابق الـsignatures مع `logActivity`/`buildFieldDiff`/`showErrorToast`).

---

## 6. مرحلة 4 — واجهة تصفح/تحميل لكل المستخدمين (نُفّذت الآن)

القرار: نفّذنا أول بند في "الخطوة الجاية" الأصلية بدل اختبار E2E — لأن من غيرها القسم كله (حتى العرض) كان مقصور على السوبر أدمن، يعني الميزة مالهاش أي قيمة فعلية للمكاتب المستخدمة للنظام. اختبار E2E ليه قيمة أعلى بعد ما تبقى الميزة مستخدَمة فعليًا.

### المشكلة التقنية اللي واجهناها

باكت `encyclopedia-forms` **private** ومفيهوش أي سياسة Storage تسمح لغير `service_role` بالقراءة (اتأجلت من مرحلة 1 ولسه ما اتعملتش). ده معناه إن أي مستخدم عادي مش هيقدر يولّد رابط تحميل موقّع مباشرة من المتصفح (زي `getSignedUrl` المستخدمة في أماكن تانية بالمشروع)، وكمان تحديث `download_count` مقفول بـRLS على سوبر أدمن بس. الحل: فانكشن جديدة منفصلة تمامًا عن `encyclopedia-admin`.

### اللي اتنفذ

- **`supabase/functions/encyclopedia-download/index.ts`** (فانكشن جديدة، منفصلة كليًا عن `encyclopedia-admin`): متاحة لأي مستخدم مسجّل دخول وحسابه فعّال (`is_active`)، **من غير اشتراط سوبر أدمن** — الفرق الجوهري عن كل عمليات الإدارة. بتاخد `form_id`، تتأكد إن النموذج موجود، تولّد رابط موقّع صالح 5 دقايق (`createSignedUrl` عبر service_role، نفس نمط `signStorageUrl` في `client-portal-api`)، وتزوّد `download_count` بواحد (best-effort — فشل تحديث العداد مايمنعش نجاح التحميل نفسه).
- **`supabase/functions/encyclopedia-download/index.test.ts`** — 8 حالات (رفض جلسة منتهية، رفض حساب معطّل، قبول مستخدم عادي مش سوبر أدمن، رفض `form_id` فاضي، رفض نموذج غير موجود، نجاح مع تزويد العداد، فشل التوقيع، فشل تحديث العداد كـbest-effort). زي كل مرة، الساندبوكس هنا من غير `node_modules`/شبكة فمقدرش أشغّله فعليًا.
- **`src/supabaseClient.ts`** — دالة جديدة `callEncyclopediaDownload(formId)`، مستقلة عن `callEncyclopediaAction` (شكل payload/رد مختلف كليًا: `form_id` واحد → رابط + اسم ملف).
- **`src/features/encyclopedia/hooks/useEncyclopediaBrowse.ts`** (ملف جديد) — هوك قراءة فقط: `fetchEncyclopedia` (نفس استعلام القراءة المباشرة بتاع نسخة الأدمن)، و`handleDownloadForm` بيستدعي الفانكشن الجديدة ويفتح الرابط في تاب جديد.
- **`src/features/encyclopedia/EncyclopediaBrowseSection.tsx`** (ملف جديد) — نفس واجهة تصفح المستويين بتاعة الأدمن بصريًا، بس للقراءة/التحميل بس: بطاقة مجلد للتنقل بس (بدون تعديل/حذف)، وبطاقة نموذج بزرار "تحميل" واحد (مع حالة تحميل جارٍ).
- **التنقل:** أُضيف `'encyclopedia'` كتاب جديد في `useNavigation.ts` (`VALID_TABS`/`TAB_PATHS`، مسار `/encyclopedia`)، وزرار جديد "الموسوعة القانونية" في قائمة "المزيد" بـ`CommandDock.tsx` — **متاح لكل المستخدمين بدون أي شرط `isAdmin`** (بخلاف زرار "لوحة الإدارة" جنبه).
- **`src/App.tsx`** — استيراد `EncyclopediaBrowseSection` كـ`React.lazy` (نفس نمط `ArchiveTab`)، تفعيل `useEncyclopediaBrowse()` وجلب البيانات تلقائيًا أول ما تاب `encyclopedia` يتفتح فعليًا (نفس فكرة باقي التابات — مفيش استعلام زيادة من غير داعي)، وربط التاب في شجرة الرندر جنب `tab === 'documents'`.

### لسه محتاج منك

1. **نشر الفانكشن الجديدة:** `supabase functions deploy encyclopedia-download`.
2. **تشغيل التست الحقيقي** (`npm test` / `npm run build`) — راجعت الكود يدويًا بس، زي كل مرة.
3. باقي الخطوات المتراكمة من قبل (قسم 5 فوق) لو لسه ما اتعملتش.

### الخطوة الجاية

- اختبار E2E شامل (تصفح المستخدم العادي + إدارة السوبر أدمن سوا).

## 7. مرحلة 5 — اختبارات E2E + إصلاح ثغرة في السايدبار (نُفّذت الآن)

### ثغرة اتكشفت أثناء الشغل على E2E

قسم "المزيد" في `CommandDock.tsx` (شريط الموبايل السفلي) اتحدّث بزرار "الموسوعة القانونية" (راجع مرحلة 4)، لكن السايدبار على الديسكتوب (`DesktopSidebar.tsx`) بيقرا عناصره من ملف منفصل (`src/app/shell/navConfig.ts`) — مش من `CommandDock.tsx` مباشرة. من غير تحديث الملف ده، مستخدمي الديسكتوب (وده يشمل اختبارات E2E نفسها، اللي بتشتغل على فيوبورت Desktop Chrome حصريًا) مكانوش هيشوفوا الزرار خالص. **تم الإصلاح:** أُضيف عنصر `encyclopedia` في `moreNavItems` بملف `navConfig.ts` (بدون `adminOnly`، زي ما هو متوقع)، وده تلقائيًا بيوّلد `desktop-nav-encyclopedia` في السايدبار من غير أي تعديل تاني على `DesktopSidebar.tsx` نفسه.

### اختبارات E2E الجديدة

- **`e2e/admin-encyclopedia.spec.ts`** — نفس نمط `admin-legal-library.spec.ts` بالحرف: يتأكد إن قسم *إدارة* الموسوعة (`admin-section-encyclopedia`) مش ظاهر خالص في لوحة الإدارة لحساب أدمن مكتب عادي (مش سوبر أدمن).
- **`e2e/encyclopedia-browse.spec.ts`** — تستين:
  1. حساب أدمن عادي: زرار "الموسوعة القانونية" ظاهر (بعكس "الأتعاب"/"لوحة الإدارة" اللي مقصورين عليه هو تحديدًا كأدمن)، والتاب بيفتح من غير كراش.
  2. حساب **lawyer** حقيقي (اتعمل runtime عبر `createTestUser` + بدون أي صلاحيات إدارية): تاب "الأتعاب" مختفي (`can_view_fees` مقفول)، لكن "الموسوعة القانونية" لسه ظاهرة وشغالة — ده الإثبات الفعلي إن القسم متاح لأي role مش بس للأدمن. تنظيف تلقائي للحساب التجريبي في آخر التست.

**⚠️ ملحوظة مهمة على حدود التغطية:** مفيش حساب سوبر أدمن في بنية اختبارات E2E الحالية في المشروع أصلًا (`admin-legal-library.spec.ts` نفسه مقتصر على نفس النوع من التست السلبي لنفس السبب). يعني عمليات إدارة الموسوعة الفعلية (إنشاء/تعديل/حذف مجلد، رفع/تعديل/حذف نموذج عبر `encyclopedia-admin`) **لسه مش مغطاة بأي E2E حقيقي** — مغطاة بس باختبارات الوحدة (`index.test.ts`، مراجعة يدوية بدون تشغيل فعلي). لو حبيت تغطية E2E فعلية للإدارة، محتاجين حساب سوبر أدمن مخصص للاختبار (secret منفصل، زي `E2E_TEST_EMAIL`) — قرار يستحق نقاشه قبل التنفيذ لأنه بيفتح باب لحساب صلاحياته واسعة جدًا داخل بيئة CI.

### لسه محتاج منك

1. تشغيل `npx playwright test admin-encyclopedia.spec.ts encyclopedia-browse.spec.ts` فعليًا (نفس قيود الساندبوكس هنا — من غير `node_modules`/شبكة).
2. باقي الخطوات المتراكمة من المراحل الفاتت (قسم 5) لو لسه ما اتعملتش، ونشر `encyclopedia-download` (تم ✅ حسب تأكيدك).

---

## 8. الحالة النهائية — 13 سبتمبر 2026

- **تأكيد المستخدم:** كل الخطوات اليدوية (migration، الباكت، نشر الفانكشنين) **تمت ✅** فعليًا على الإنتاج.
- **قرار نهائي بخصوص تغطية E2E لعمليات إدارة الموسوعة (حساب سوبر أدمن مخصص للاختبار):** **مؤجَّل نهائيًا برضا المستخدم — مش هيتنفذ.** الاكتفاء بالتغطية الحالية (اختبار الوحدة `index.test.ts` + المراجعة اليدوية + E2E السلبي في `admin-encyclopedia.spec.ts`).
- **النتيجة:** خطة "الموسوعة القانونية" (المراحل 1 إلى 5) **مقفولة بالكامل**. لا يوجد بند تنفيذي متبقٍ.

---

## 9. باج مكتشف بعد الإغلاق — 13 سبتمبر 2026: تعارض بين فحص الواجهة وفحص الفانكشن لصلاحية السوبر أدمن

**الأعراض:** محاولة إضافة مجلد جديد من واجهة إدارة الموسوعة فشلت برسالة "تعذّر حفظ المجلد" (توست ثابت من `useAdminEncyclopedia.ts`، مبيعكسش سبب الفشل الحقيقي من السيرفر).

**التشخيص:** فحص لوج الفانكشن على Supabase (Invocations) أظهر رد **403** من `encyclopedia-admin`. بمراجعة الكود:

- **`AdminPanel.tsx`** بيقرر ظهور قسم إدارة الموسوعة بمقارنة الإيميل فقط: `profile.email === SUPER_ADMIN_EMAIL` ('m.gemy4231@gmail.com').
- **`encyclopedia-admin/index.ts`** (الفانكشن) بيفحص حاجة مختلفة تمامًا: عمود `is_super_admin` في جدول `profiles` (`caller.is_super_admin !== true` → 403 "العملية دي مقصورة على سوبر أدمن فقط").

الفحصين مش متزامنين مع بعض — حساب ممكن يطابق الإيميل الثابت في الواجهة (فيشوفله زرار/قسم الإدارة) لكن عموده `is_super_admin` في القاعدة لسه `false`/`NULL` (مش متظبط يدويًا) فيترفض من الباك إند.

**الحل:** استعلام يدوي واحد لضبط العمود على حساب السوبر أدمن الفعلي:

```sql
UPDATE public.profiles
SET is_super_admin = true
WHERE email = 'm.gemy4231@gmail.com';
```

**لا يوجد تعديل كود مطلوب** — الباج في تزامن البيانات (عمود قاعدة البيانات) مش في المنطق نفسه. القرار: الاكتفاء بتشغيل الاستعلام يدويًا، من غير توحيد الفحصين في تعديل كود إضافي.

---

## 10. إضافة — زرار "معاينة" قبل التحميل (13 سبتمبر 2026)

**الطلب:** المستخدم يقدر يعاين النموذج قبل ما يحمّله، مش زرار تحميل واحد بس.

**التنفيذ:**

- **`supabase/functions/encyclopedia-download/index.ts`** — دعم `mode: 'view'` اختياري في الـpayload: نفس رابط التحميل الموقّع بالظبط (نفس التحقق من الجلسة/الحساب الفعّال، نفس مدة الصلاحية 5 دقايق)، الفرق الوحيد إنه **مبيزودش `download_count`** — المعاينة مش تحميل فعلي. أي قيمة تانية لـ`mode` (أو غيابها) بتفضل بالسلوك الأصلي 100% (زرار "تحميل" صفر تغيير عليه).
- **`index.test.ts`** — تستان جداد: المعاينة بترجع رابط صح من غير ما تزوّد العداد، وأي mode غير 'view' لسه بيزوّد العداد زي الأصل.
- **`src/supabaseClient.ts`** — `callEncyclopediaDownload(formId, mode)` بقت بتاخد باراميتر `mode` اختياري (افتراضي `'download'`، صفر كسر للاستخدام الحالي).
- **`src/features/encyclopedia/hooks/useEncyclopediaBrowse.ts`** — `handlePreviewForm` جديدة: بتنادي الفانكشن بـ`mode: 'view'`، وبتفتح النتيجة في تاب جديد. **PDF** بيتفتح مباشرة (المتصفح بيعرضه inline لوحده). **Word (docx)** — المتصفح مش بيعرضه لوحده، فبيتلف برابط **Google Docs Viewer** (`docs.google.com/gview?embedded=true&url=...`) حوالين الرابط الموقّع، بيشتغل طول ما الرابط لسه صالح وقت الفتح (5 دقايق كفاية).
- **`EncyclopediaBrowseSection.tsx`** — بطاقة النموذج بقت فيها زرارين جنب بعض: "معاينة" (رمادي، أيقونة عين) و"تحميل" (زي ما كان)، كل واحد بيتعطّل وقت ما التاني شغال.
- **`App.tsx`** — تمرير `previewingFormId`/`onPreview` الجداد لـ`EncyclopediaBrowseSection`.

**ملحوظة:** معاينة الـWord عبر Google Docs Viewer محتاجة إن جهاز المستخدم يقدر يوصل لـ`docs.google.com` (مفيش بديل داخلي للـpreview بدون خدمة خارجية، ومفيش قرار سابق بالمشروع يمنع الاعتماد على خدمة خارجية زي كده).

**لسه محتاج منك:** تشغيل التست الحقيقي (نفس قيود الساندبوكس)، ونشر نسخة `encyclopedia-download` المحدثة (`supabase functions deploy encyclopedia-download`) — التعديل هنا في نفس الفانكشن الموجودة، مش فانكشن جديدة.

---

## 11. إضافة — زر رجوع (ظاهر + فعلي بالموبايل) — 13 سبتمبر 2026

**الطلب:** مفيش زرار رجوع واضح للمستخدم جوه مجلد فرعي، وزر رجوع الموبايل الفعلي كان بيقفل التاب كله بدل ما يرجع خطوة خطوة.

**التنفيذ (ملف واحد: `EncyclopediaBrowseSection.tsx`، صفر migration/فانكشن):**

- **زرار رجوع ظاهر** — أيقونة سهم جنب مسار التنقل (breadcrumb)، بينده `goBackOneLevel()`: من مجلد فرعي → مجلده الرئيسي، من مجلد رئيسي → القائمة الكاملة.
- **زر رجوع الموبايل الفعلي** — استخدمنا آلية جاهزة أصلاً بالمشروع (`useNestedModalBackButton` / `registerNestedModal` في `useNavigation.ts`، نفس اللي بتتعامل مع مودالات فرعية جوه مودالات رئيسية). اتسجّل مستويين مستقلين: "داخل أي مجلد" و"داخل فرعي تحديدًا" — فالضغطة الأولى ترجع للمجلد الرئيسي بس، والتانية ترجع للقائمة الجذرية، بدل ما تخرج بره التاب كله دفعة واحدة.

---

## 12. إضافة — بحث خاص بالموسوعة + تأكيد عدم الظهور في البحث العام — 13 سبتمبر 2026

**الطلب:** (1) بحث داخل الموسوعة نفسها. (2) التأكد إن نماذج الموسوعة مش بتظهر في نتائج البحث العام للتطبيق.

**(2) التأكيد — بدون أي تعديل كود:** مراجعة كاملة لـ`src/shared/modals/hooks/useUniversalSearch.ts` (المصدر الوحيد للبحث العام) أظهرت إنه بيستعلم بس من: `cases`, `clients`, `case_documents`, `case_sessions`, `case_notes`, `case_parties`, `case_fees`, `reminders`. **مفيش أي إشارة لـ`encyclopedia_categories`/`encyclopedia_forms` خالص** — القسم أصلاً معزول تمامًا عن البحث العام من غير أي تعديل مطلوب.

**(1) التنفيذ (نفس الملف `EncyclopediaBrowseSection.tsx`، صفر migration/فانكشن/استعلام إضافي):**

- صندوق بحث نصي فوق واجهة التصفح، شغّال في أي وقت (مستوى جذري أو جوه مجلد).
- البحث بيدوّر في `title` و`description` لكل النماذج المحمّلة أصلاً في الذاكرة (`forms` — نفس البيانات اللي `fetchEncyclopedia` بيجيبها مرة واحدة، صفر round-trip إضافي للسيرفر).
- النتيجة **مسطّحة** (عبر كل المجلدات مع بعض، مش مقصورة على المجلد المفتوح حاليًا) — كل نتيجة بتوريك اسم مجلدها (رئيسي، أو "رئيسي / فرعي") كـcontext، لأن النتيجة بتظهر من غير التنقل جوه الشجرة.
- وضع البحث بيحجب عرض المجلدات/breadcrumb العادي مؤقتًا؛ مسح البحث (زرار "×" أو تفريغ الحقل) بيرجّع العرض العادي بمكانه اللي كان فيه (المجلد المفتوح لسه محفوظ في الحالة).
- زر رجوع الموبايل وقت البحث مسجّل كمستوى مستقل تالت (بنفس آلية `useNestedModalBackButton`) — بيقفل البحث الأول قبل ما يرجع لمنطق التنقل بين المجلدات.

**لسه محتاج منك:** رفع الملف بس على GitHub — مفيش أي خطوة يدوية تانية (لا migration، لا نشر فانكشن).



---

## 13. ميزة جديدة — الرفع المتعدد للنماذج (Batch Upload) — 13 سبتمبر 2026

**الطلب:** تسهيل رفع عدة نماذج دفعة واحدة بدل واحد واحد، بدون أي migration أو فانكشن سيرفر جديدة.

### الفكرة المتفق عليها

1. زرار "رفع متعدد" جنب زرار "نموذج جديد" — **الاتنين هيظهروا دايمًا** (في القائمة الرئيسية وجوه أي مجلد)، واختيار المجلد نفسه بقى بيتم من جوه المودال (بدل ما يكون مقصور على وجود مجلد مفتوح فعلاً).
2. مجلد واحد لكل الدفعة (أبسط) — يتحدد من جوه المودال.
3. اختيار كذا ملف PDF/docx من الموبايل مرة واحدة (`<input type="file" multiple>`).
4. اسم مقترح تلقائي لكل ملف (من اسم الملف، بعد شيل الامتداد واستبدال `_`/`-` بمسافة) — قابل للتعديل بحرية في قائمة مراجعة قبل أي رفع فعلي، مع إمكانية حذف ملف من الدفعة.
5. عند "رفع الكل": رفع **تسلسلي واحد واحد** (نفس action الحالية `uploadForm`، صفر فانكشن سيرفر جديدة)، مع مؤشر تقدّم ("٣ من ١٠")، ولو ملف فشل (حجم/نوع غلط) الباقي يكمل عادي ويوضح مين فشل بالظبط.

### الملفات المتأثرة (5)

1. ✅ **`useAdminEncyclopedia.ts`** — منفّذ (مرحلة 1)
2. ⏳ `EncyclopediaBatchUploadModal.tsx` (جديد) — المودال نفسه
3. ⏳ `EncyclopediaSection.tsx` — زرار "رفع متعدد" + جعل زرار "نموذج جديد"/"رفع متعدد" يظهروا دايمًا (مش مقصورين على وجود مجلد مفتوح)
4. ⏳ `AdminPanelModals.tsx` — تركيب المودال الجديد
5. ⏳ `AdminPanel.tsx` — تمرير الـprops الجديدة

### مرحلة 1 — `useAdminEncyclopedia.ts` (منفّذة)

- **نوع جديد** `EncyclopediaBatchFileResult { fileName, title, status: 'success'|'error', error? }` — نتيجة كل ملف في الدفعة على حدة.
- **State جديدة:** `showBatchUploadModal`/`setShowBatchUploadModal`، `batchModalCategoryId`/`setBatchModalCategoryId` (المجلد الافتراضي عند الفتح من جوه مجلد معيّن)، `batchUploading`، `batchProgress: {current, total} | null`، `batchResults: EncyclopediaBatchFileResult[] | null`.
- **دالة جديدة `handleUploadBatch(categoryId, items)`** حيث `items = {file: File; title: string}[]`:
  - لوب `for` تسلسلي (ملف يخلص قبل ما التاني يبدأ)، بيحدّث `batchProgress` قبل كل ملف.
  - لكل ملف: نفس تحقق الامتداد (`pdf`/`docx`) والحجم (2 ميجا) المستخدم في `handleSaveForm`، بعدين `fileToBase64` ثم نفس نداء `callEncyclopediaAction({action:'uploadForm', ...})` المستخدم في الرفع الفردي بالظبط — **مفيش أي تعديل في `encyclopedia-admin` (الفانكشن) ولا أي migration**.
  - نجاح: `logActivity` (نفس نص لوج الرفع الفردي) + نتيجة `success`. فشل: مسك الخطأ برسالته العربية الجاهزة (من `callEncyclopediaAction`) + نتيجة `error`، من غير ما يوقف اللوب.
  - في الآخر: `fetchEncyclopedia()` + toast ملخّص (نجاح الكل / فشل الكل / نجاح جزئي مع العدد).
- **ملاحظة:** مفيش تشغيل فعلي لـ`tsc`/`vitest` في الساندبوكس (نفس القيد المعروف) — فحص توازن أقواس يدوي بس على الملف. التأكيد النهائي من الـbuild/CI الفعلي عندك.

### مرحلة 2 — `EncyclopediaBatchUploadModal.tsx` (منفّذة، ملف جديد)

مودال مستقل بنفس أسلوب `EncyclopediaFormModal.tsx` بصريًا (نفس `useModalPresentation`، نفس شكل الرفع بالـdashed border):

- **اختيار المجلد** عبر `Sel` — نفس منطق `categoryOptions` المستخدم في `EncyclopediaFormModal.tsx` (كل المجلدات رئيسية وفرعية، الفرعية بادئة "— ").
- **اختيار الملفات:** `<input type="file" multiple>` — كل مرة تختار ملفات بتتضاف لقائمة المراجعة (مش بتستبدل القديمة)، وتفريغ الـ`value` بعد كل اختيار عشان تقدر تختار نفس الملف تاني لو شيلته بالغلط.
- **اسم مقترح تلقائي** عبر `suggestTitle()`: بيشيل الامتداد ويستبدل `_`/`-` بمسافة — دالة محلية بسيطة، مفيش استيراد إضافي.
- **قائمة المراجعة:** كل صف فيه اسم الملف الأصلي (للمرجعية) + حقل نص للعنوان (قابل للتعديل بحرية) + زرار حذف الصف.
- **زرار "رفع الكل (عدد الملفات)"** — بينادي `onUpload(categoryId, items)` من الأب (`handleUploadBatch` في الـhook، من مرحلة 1). معطّل لو مفيش ملفات أو مفيش مجلد مختار أو الرفع شغّال فعلاً.
- **أثناء الرفع:** شريط تقدّم نصي + بصري ("جاري الرفع... ٣ من ١٠") مبني على `progress` الجاي من الـhook.
- **بعد الانتهاء:** قائمة نتائج لكل ملف (✅ نجاح / ❌ فشل مع رسالة الخطأ العربية الجاهزة من السيرفر)، وزرار "تمام" يقفل المودال.
- **إغلاق أثناء الرفع ممنوع** (`canClose = !uploading`) — نفس فكرة `saving` في مودالات تانية بالمشروع، عشان محدش يقفل المودال ويسيب حالة `batchResults` معلّقة نص الطريق.
- `data-testid` جديدة على كل عنصر تفاعلي (`admin-encyclopedia-batch-*`) بنفس نمط تسمية بقية اختبارات الموسوعة، جاهزة لو حبيت تضيف E2E لاحقًا.

### مرحلة 3 — `EncyclopediaSection.tsx` (منفّذة)

- **Props جديدة:** `setBatchModalCategoryId`, `setShowBatchUploadModal` (نفس نمط `setFormModalCategoryId`/`setShowFormModal` الموجودين).
- **إعادة تنظيم صف الأزرار:**
  - زرار "مجلد رئيسي جديد"/"مجلد فرعي جديد" فضل بنفس الشرط القديم بالظبط (رئيسي في القائمة الجذر، فرعي جوه مجلد رئيسي، مفيش زرار مجلد جوه مجلد فرعي).
  - زراري **"نموذج جديد" و"رفع متعدد" بقوا ظاهرين دايمًا** — حتى في القائمة الرئيسية برة أي مجلد (كان "نموذج جديد" قبل كده مقصور على وجود مجلد مفتوح). الاثنين في صف مستقل تحت زرار المجلد.
  - عند الضغط من غير مجلد مفتوح، بيتبعت `null` كـ`category_id` الافتراضي للمودال — و`EncyclopediaFormModal`/`EncyclopediaBatchUploadModal` الاتنين أصلاً بيرجعوا لأول مجلد في القايمة (`categories[0]?.id`) لو الافتراضي `null`، فمفيش أي تعديل إضافي مطلوب في المودالين.
  - "رفع متعدد" بلون مميز (indigo) عن "نموذج جديد" (teal) عشان التفرقة البصرية.

### مرحلة 4 — `AdminPanelModals.tsx` (منفّذة)

- استيراد `EncyclopediaBatchUploadModal` الجديد + نوع `EncyclopediaBatchFileResult` من الـhook.
- **Props جديدة** في `AdminPanelModalsProps` (بعد props النموذج الفردي مباشرة): `showBatchUploadModal`, `setShowBatchUploadModal`, `batchModalCategoryId`, `setBatchModalCategoryId`, `batchUploading`, `batchProgress`, `batchResults`, `setBatchResults`, `handleUploadBatch` — كلها بتتمرر لاحقًا من `AdminPanel.tsx` (مرحلة 5).
- **تركيب المودال:** `showBatchUploadModal && React.createElement(EncyclopediaBatchUploadModal, {...})` بعد مودال تأكيد حذف النموذج مباشرة — نفس نمط تركيب `EncyclopediaFormModal` فوقه بالظبط.
- **`onClose`:** بيقفل المودال + يصفّر `batchModalCategoryId` و`batchResults` سوا، عشان لو المستخدم فتح المودال تاني بعدين ميلاقيش نتايج دفعة قديمة ظاهرة بالغلط.

### مرحلة 5 (الأخيرة) — `AdminPanel.tsx` (منفّذة)

- **Destructure:** إضافة `showBatchUploadModal, setShowBatchUploadModal, batchModalCategoryId, setBatchModalCategoryId, batchUploading, batchProgress, batchResults, setBatchResults, handleUploadBatch` من `encyclopedia` (نفس نتيجة `useAdminEncyclopedia`) — بعد سطر destructure النموذج الفردي مباشرة.
- **تمرير لـ`EncyclopediaSection`:** إضافة `setBatchModalCategoryId, setShowBatchUploadModal` (الزرار الجديد محتاج بس فتح المودال وتحديد المجلد الافتراضي — الباقي حالة داخلية في المودال نفسه).
- **تمرير لـ`AdminPanelModals`:** إضافة كل الحالات التسعة كاملة (`showBatchUploadModal` ... `handleUploadBatch`) بعد props النموذج الفردي مباشرة.
- صفر تغيير في أي منطق تاني — نفس نمط التوصيل الموجود للمجلد/النموذج الفردي بالظبط، من غير أي إعادة هيكلة.

### الحالة النهائية للميزة — 13 سبتمبر 2026

**✅ مقفولة بالكامل، المراحل 1-5 كلها منفذة.** الملفات الخمسة كلها اتسلّمت:

1. `useAdminEncyclopedia.ts` (معدّل)
2. `EncyclopediaBatchUploadModal.tsx` (جديد)
3. `EncyclopediaSection.tsx` (معدّل)
4. `AdminPanelModals.tsx` (معدّل)
5. `AdminPanel.tsx` (معدّل)

**لا يوجد migration ولا فانكشن سيرفر جديدة** — الميزة كلها مبنية فوق `action='uploadForm'` الموجودة أصلاً في `encyclopedia-admin`، بالظبط زي ما كان متفق من البداية.

**تأكيد المستخدم (13 سبتمبر 2026):** الملفات الخمسة اتستبدلت في الريبو، والميزة اتجرّبت وشغّالة بالكامل.

**النتيجة: ميزة "الرفع المتعدد للنماذج" مقفولة بالكامل — لا يوجد بند تنفيذي متبقٍ.**

---

## 14. فصل إدارة الموسوعة عن شبكة أقسام لوحة الإدارة (13 سبتمبر 2026)

**الطلب:** كرت "الموسوعة القانونية" يختفي كقسم مستقل من شبكة أقسام لوحة الإدارة بالكامل، وبدل منه زرار جوه صفحة الموسوعة العادية (اللي أي مستخدم بيدخلها) يفتح نفس شاشة الإدارة (بنفس المودالز وزرار "رفع متعدد" وكل الوظائف بلا أي تغيير)، على إن الزرار ده يظهر لحساب السوبر أدمن بس.

### القرار الفني

بدل تكرار كود شاشة الإدارة (`EncyclopediaSection` + المودالز التلاتة بتاعتها + الهوك `useAdminEncyclopedia`) في مكان جديد، اتعمل توصيل غير مباشر بيسمح لنفس الكود يتفتح من غير ما يمر على شبكة الأقسام:

1. **`src/features/admin/AdminPanel.tsx`**:
   - كرت "الموسوعة القانونية" (`admin-section-encyclopedia`) اتشال بالكامل من شبكة الأقسام — مش موجود لأي حساب خالص، ولا حتى السوبر أدمن.
   - كل كود القسم نفسه (الهوك، جلب البيانات عند فتح القسم، الرندر الفعلي لـ`EncyclopediaSection` وكل المودالز، لون الهيدر، عنوان "الموسوعة القانونية" في الهيدر) **متلمسش خالص**.
   - `AdminPanelProps` اتزودت بـ`initialSection?: 'encyclopedia' | null` و`onInitialSectionConsumed?: () => void`. `useEffect` جديد: لو `initialSection` جاية و`isSuperAdminUser` صح، بيفتح القسم مباشرة وينادي `onInitialSectionConsumed` عشان الحالة ترجع فاضية في الأب (فرجوع عادي لتاب الإدارة بعد كده مايفتحش القسم تلقائيًا تاني). حماية `isSuperAdminUser` الأصلية (في الـ`useEffect` اللي بيقفل القسم لغير السوبر أدمن، وفي شرط الرندر الفعلي للقسم) فضلت زي ما هي بالظبط — خط الدفاع الحقيقي.

2. **`src/features/encyclopedia/EncyclopediaBrowseSection.tsx`** (صفحة الموسوعة العادية):
   - Prop اختيارية جديدة `onManageEncyclopedia?: () => void`.
   - زرار "إدارة الموسوعة" (`encyclopedia-manage-button`) جديد فوق شريط البحث — بيتعمله render بس لو الـprop موجودة، فأي حساب متبعتلوش الـprop مابيشوفش الزرار خالص (مش مجرد معطّل).

3. **`src/App.tsx`** (نقطة الربط):
   - State جديدة `adminInitialSection`. `onManageEncyclopedia` بيتبعت لـ`EncyclopediaBrowseSection` بس لو `isAISuperAdmin` (نفس حساب `m.gemy4231@gmail.com` المستخدم فعلاً في الملف لقفل قسم الـAI — نفس الحساب المستخدم في `SUPER_ADMIN_EMAIL` بـ`AdminPanel.tsx`، اتعاد استخدامه هنا من غير تكرار السطر).
   - الضغط على الزرار: `setAdminInitialSection('encyclopedia')` + `nav.navigateTo('admin')` → `AdminPanel` بيستقبلها كـ`initialSection` ويفتح القسم فورًا.

### اختبارات E2E

- **`e2e/admin-encyclopedia.spec.ts`** — التست الأصلي (الكرت مش ظاهر لحساب مكتب عادي) لسه صحيح تقنيًا وبنفس الـassertion، لكن التعليق اتحدّث ليعكس إن الكرت اتشال بالكامل من الشبكة (لأي حساب) مش مجرد مخفي عن حساب معيّن.
- **`e2e/encyclopedia-browse.spec.ts`** — الدالة المشتركة `assertEncyclopediaTabOpensCleanly` (بتتنادى من تست الأدمن العادي وتست الـlawyer) اتزودت بسطر جديد: التأكد إن `encyclopedia-manage-button` مالوش وجود خالص لغير السوبر أدمن.
- **نفس ملحوظة التغطية القديمة (قسم 7):** مفيش حساب سوبر أدمن في بنية E2E الحالية، فالمسار الإيجابي (ظهور الزرار فعليًا والانتقال الناجح لشاشة الإدارة) لسه مش مغطى بـE2E حقيقي — نفس القرار المؤجَّل نهائيًا من قسم 8.

### التسليم

- زيب كامل للمشروع بعد التعديل، وبعدها زيب منفصل فيه الملفات الخمسة المعدّلة بس (كل ملف في مساره الصحيح من جذر الريبو) للاستبدال المباشر: `src/App.tsx`, `src/features/admin/AdminPanel.tsx`, `src/features/encyclopedia/EncyclopediaBrowseSection.tsx`, `e2e/admin-encyclopedia.spec.ts`, `e2e/encyclopedia-browse.spec.ts`.

**لسه محتاج منك:** استبدال الملفات الخمسة في الريبو (تم ✅ حسب تسليم الزيب)، وتشغيل `npm run build` + `npx playwright test admin-encyclopedia.spec.ts encyclopedia-browse.spec.ts` فعليًا (نفس قيد الساندبوكس المعروف — من غير `node_modules`/شبكة هنا).

**النتيجة: فصل إدارة الموسوعة عن شبكة أقسام لوحة الإدارة — مقفول بالكامل، لا يوجد بند تنفيذي متبقٍ غير التأكيد المحلي.**
