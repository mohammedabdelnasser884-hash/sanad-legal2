# سند — خطة تنفيذ اختبارات E2E مقسمة على مراحل (تحديث حالة شامل)

**التاريخ:** 25 يوليو 2026
**الحالة:** 🟡 قيد التنفيذ — المراحل 1-6 مكتملة، المرحلة 7 قيد التصنيف، المرحلة 8 لسه ما بدأتش
**مرجع أصلي:** `سند-خطة-تنفيذ-اختبارات-E2E-مقسمة-1-2.md` (24 يوليو) + `سند-جرد-اختبارات-E2E-الشامل-1.md`

هذا الملف نسخة محدّثة من الخطة الأصلية، بيوضّح بالظبط اللي خلص فعليًا
واللي لسه باقي، من غير تغيير في القرارات المعتمدة الأصلية (مبيّنة تحت
كما هي).

---

## القرارات المعتمدة (زي الأصل، من غير تغيير)

1. مساعد الذكاء الاصطناعي — وضع `generate` بس، بـ mock لاستجابة الـAPI. باقي الثمانية أوضاع بره النطاق نهائيًا.
2. `SecuritySection`/`PortalSection` كانوا مصنّفين Tier 2 في الجرد الأصلي — لكن اتغطوا فعليًا كجزء من "المرحلة 6" الموسّعة (شرح تحت).
3. حالات الفشل الحقيقية جوّه Tier 2 تتقيّم فردًا فردًا، مفيش تصنيف جماعي.

---

## المرحلة 1 — الموكلين — ✅ مكتملة (24 يوليو)
`e2e/clients.spec.ts` (6 تستات): إنشاء ناجح، تكرار رقم قومي عند الإضافة/التعديل، حماية دبل-كليك في الحالتين. هيلبر `createClient()` جديد.

## المرحلة 2 — الجلسة المستقلة — ✅ مكتملة
`NewStandaloneSessionModal.tsx` (كان صفر testid، أكبر بند في الخطة) + `StandaloneSessionDetailModal.tsx`: مستقلة/ربط بقضية، مودال "تحويل لقضية؟"، إضافة موكل من جوّه، أوفلاين.

## المرحلة 3 — القضايا (أطراف متعددة) — ✅ مكتملة
`NewCaseModal.tsx`/`EditCaseModal.tsx`: طرف واحد/أكتر، فاليديشن، دبل-كليك. `CaseDetailView.tsx` تبويب الجلسات: إضافة/تعديل/حذف، تعارض تعديل، أوفلاين.

## المرحلة 4 — الأتعاب — ✅ مكتملة
تسجيل دفعة (جزئية/كاملة/أكبر من المتبقي)، `FeesTab.tsx` مرتبطة بقضية/أونلاين-أوفلاين، `InvoiceModal.tsx` إصدار/طباعة.

## المرحلة 5 — مساعد الذكاء الاصطناعي (توليد مستند، mock) — ✅ مكتملة
اختيار نوع مستند + تعبئة تلقائية من القضية (أطراف متعددين) + توليد mock (اعتراض `api.anthropic.com` بـ`page.route`) + نسخ/طباعة/PDF.

## المرحلة 6 — الأدمن — ✅ مكتملة بالكامل (تسعة أقسام)
الخطة الأصلية كانت حاصرة المرحلة 6 في المستخدمين + الأرشيف بس (`السطر
89-97` من الخطة الأصلية)، وسايبة الأمان/البوابة Tier 2 (مرحلة 7)،
وسايبة النشاط/المكتبة القانونية/المكتب/الجلسات/النسخ الاحتياطي لمرحلة
8 (Smoke). عمليًا، دفعات المرحلة 6 اتنفذت بنطاق أوسع من كده وغطت **كل
تسعة أقسام لوحة الإدارة** بتستات كاملة (مش smoke بس):

| القسم | دفعة | الحالة |
|---|---|---|
| `ArchiveSection` | مبكرة | ✅ |
| النشاط + المكتبة القانونية + المكتب | دفعة 1 | ✅ |
| المستخدمون + الجلسات | دفعة 2 | ✅ |
| الأمان + بوابة الموكل | دفعة 3، جزء 1 | ✅ |
| النسخ الاحتياطي (الجزء الآمن فقط — بدون تنفيذ استعادة فعلي، قرار متعمد) | دفعة 3، جزء 2 | ✅ |

**نتيجة عملية:** بنود "الأمان"/"بوابة الموكل" من مرحلة 7 الأصلية، وكل
بنود مرحلة 8 الأصلية الخاصة بالأدمن (نشاط، جلسات أدمن، نسخ احتياطي،
مكتب، مكتبة قانونية) **خلصت بالفعل** ضمن المرحلة 6 — ملغاة من مرحلة 7/8
تحت، عشان مفيش تكرار.

---

## المرحلة 7 — باقي Tier 2 (مُفرزة) — 🟡 التصنيف مقترح، لسه محتاج تأكيدك

بعد استبعاد بندي الأمان/البوابة (خلصوا في مرحلة 6)، الباقي من الخطة
الأصلية:

**مقترح Tier 2 (فشل حقيقي/حماية بيانات) — بالترتيب المقترح للتنفيذ:**

| # | الملف | الحالات | testid حاليًا |
|---|---|---|---|
| 1 | `NotesSection.tsx` (`case-detail/`) | إضافة/تعديل/حذف ملاحظة | 0 |
| 2 | `DocsSection.tsx` (`case-detail/`) | رفع/حذف مستند، **منع الرفع أوفلاين** | 0 |
| 3 | `SessionUpdateModal.tsx` (`calendar/sessions-calendar/`) | **تعارض تعديل** لجلسة قادمة | 5 (جزئي) |
| 4 | `EditReminderModal.tsx` | **تعارض تعديل** تذكير | 0 |
| 5 | `ChecklistSection.tsx` (`case-detail/`) | فحص الأطراف المتعددة الجديد | 0 |

**مقترح Smoke (عرض بيانات، مسار واحد بلا تفرّع خطر) — بينزل لمرحلة 8:**
`ClientDetailModal.tsx` (2 testid جزئي)، `QuickAddSessionModal.tsx` (0)، `AddReminderForm.tsx` (0)، `ArchiveTab.tsx` بتاع `dashboard/` (مستندات)، `UniversalSearchModal.tsx` بتاع `shared/modals/`.

⚠️ لسه مفيش تنفيذ ولا تستيد اتضاف لأي بند من الخمسة دول — التصنيف
مقترح بس، مطروح عليك في نفس الرسالة اللي فاتت، ومحتاج تأكيدك قبل ما
نبدأ فعليًا بـ`NotesSection.tsx`.

---

## المرحلة 8 — Tier 3 (Smoke دفعة واحدة، آخر حاجة) — ⬜ لسه ما بدأتش

بعد استبعاد بنود الأدمن اللي خلصت في مرحلة 6، الباقي الفعلي:
`DashboardTab`، `TeamTab`، تبويبات التقويم (شهر/أسبوع/فائتة)،
`SummaryModal`، `ViewReminderModal`، `RemindersTab`، `PdfViewerModal`
— زائد الخمسة بنود Smoke المرحّلة من مرحلة 7 فوق (لو تأكدت تصنيفهم).
اختبار واحد بسيط لكل شاشة: تفتح، تعرض بيانات، تتقفل من غير كسر.

---

## ملاحظة تشغيلية معلّقة (لسه من غير قرار)
قرار وقت التشغيل في CI + تنظيف بيانات التينانت التجريبي بعد كل تست —
لسه معلّق من الخطة الأصلية. مش عامل عرقلة لحد دلوقتي لأن كل التستات
بتتشغّل يدويًا بواسطتك مش جوّه CI، لكن لازم يتحسم قبل أي تفعيل مستقبلي
لـCI.
