# تقرير مراجعة نظام صلاحيات أعضاء المكتب — سند

**تاريخ المراجعة:** 16 أغسطس 2026 (آخر تحديث: نفس اليوم — بعد تنفيذ خطة التفعيل الكامل لنظام الصلاحيات التفصيلية، قسم 7)
**نطاق المراجعة:** نظام الأدوار (roles) والصلاحيات (permissions) الخاص بأعضاء المكتب الواحد — الكود (Frontend + Edge Functions) + سياسات RLS في Supabase.

---

## 1. ملخص تنفيذي

النظام مبني على 3 أدوار (`admin` / `lawyer` / `viewer`) مخزّنة في `profiles.role`، مع طبقة صلاحيات تفصيلية (`profiles.permissions`) بتتحكم في أفعال محددة (إضافة/تعديل/حذف قضايا، عرض/تعديل أتعاب، إلخ).

المراجعة كشفت **ثغرة حرجة واحدة (تم إصلاحها بالكامل — الـ trigger + توثيقه كملف migration + حماية الواجهة من أثره الجانبي)**، وفجوة تصميمية كان نظام الصلاحيات التفصيلية فيها مش مفعّل فعليًا — **البند ده اتحل لاحقًا بالتفعيل الكامل (الخيار 1، مش الإخفاء المؤقت)، تفاصيله كاملة في قسم 7**، بالإضافة لملاحظات أصغر على تجربة المستخدم وجودة الكود، **كلها اتصلحت**. الأجزاء الخاصة بعمليات الأدمن الحساسة (إنشاء/حذف مستخدم، تغيير باسورد) اتضح إنها مبنية بعناية وآمنة.

**✅ كل بنود جدول التوصيات (قسم 5) اتنفّذت.** الباقي المتبقي فعليًا هو خطوات تشغيلية غير برمجية (Deploy Edge Function نهائي، رفع GitHub، تشغيل e2e في CI) — مفصّلة في قسم 7.6.

---

## 2. الأمان (Security)

### 2.1 🔴 [تم إصلاحه] تصعيد صلاحيات ذاتي عبر تحديث profiles

**الحالة:** تم اكتشافه وإصلاحه بـ trigger أثناء هذه الجلسة (منفّذ بالفعل على قاعدة الإنتاج).

**المشكلة:** سياسة `profiles_update` كانت:
```
USING / WITH CHECK: (user_id = auth.uid()) OR (tenant_id = current_tenant_id() AND get_my_role()='admin') OR is_super_admin()
```

الشرط الأول `user_id = auth.uid()` بيسمح لأي مستخدم (حتى `viewer`) يعدّل صف نفسه **بدون أي قيد على الأعمدة**، لأن RLS في Postgres يعمل على مستوى الصف مش العمود. عمليًا، أي مستخدم عادي كان يقدر — من كونسول المتصفح مباشرة، بدون المرور بالواجهة — يشغّل:

```js
await supabase.from('profiles')
  .update({ is_super_admin: true, role: 'admin' })
  .eq('user_id', myUserId);
```

ويصبح سوبر أدمن يتحكم في المنصة كلها، أو على الأقل أدمن في مكتبه، أو حتى يغيّر `tenant_id` بتاعه ويقفز لمكتب تاني.

نفس الفكرة اتقفلت قبل كده على `INSERT` (migration 11، بند 8) لكن اتنسيت على `UPDATE`.

**الإصلاح المطبّق:** trigger `prevent_self_privilege_escalation` بيمنع تعديل الأعمدة الحساسة (`role`, `is_super_admin`, `tenant_id`, `is_active`, `permissions`) إلا لو المُعدِّل أدمن حقيقي بيعدّل حساب غيره في نفس المكتب، أو سوبر أدمن. تعديل الحساب الشخصي (الاسم مثلًا) لسه مسموح عادي.

**✅ توثيق الـ migration:** الملف موجود بالفعل في `database/migrations/sql-migrations-phase5/02-prevent-profiles-self-privilege-escalation.sql` (idempotent، نفس نص الـ trigger المنفّذ على الإنتاج).

---

### 2.1.ب ✅ [تم إصلاحه] أثر جانبي على الواجهة: الأدمن يقدر "يقفل نفسه" بغير قصد

بعد تفعيل التريجر، ظهر أثر جانبي مباشر: زرار "تفعيل/تعطيل" السريع في قائمة المستخدمين وشاشة "تعديل مستخدم" **مكانوش فيهم أي استثناء لصف الأدمن نفسه** (بعكس زرار الحذف اللي كان مستثنيه من الأول). يعني لو الأدمن فتح تعديل حسابه وحاول يغيّر دوره أو يعطّل نفسه، هياخد رسالة خطأ عامة (`❌ فشل الحفظ`) بدل رسالة التريجر الحقيقية وواضحة، ومن غير أي توضيح إن ده أصلًا مش مسموح بيه.

**الإصلاح المطبّق:**
- `UsersSection.tsx` — زرار "تفعيل/تعطيل" السريع اتخفى على صف الأدمن نفسه، بنفس نمط زرار الحذف بالظبط.
- `EditUserModal.tsx` — أضيف prop باسم `isSelf`: لو `true` بيقفل أزرار اختيار الدور وسويتش الحالة (نشط/معطّل) ويظهر ملاحظة توضيحية ("مايمكنش تغيّر دورك أو حالة حسابك بنفسك — لازم أدمن تاني يعمل ده"). الاسم لسه قابل للتعديل عادي.
- `AdminPanelModals.tsx` / `AdminPanel.tsx` — تم تمرير `profile` (المستخدم الحالي) لحساب `isSelf` عند فتح مودال التعديل.

بيك درة: الحماية دلوقتي على مستويين — التريجر في القاعدة (خط الدفاع الأساسي، شغال حتى لو حصل نداء مباشر لـ Supabase)، والواجهة (تجربة مستخدم واضحة، ومنع الوصول لحالة الخطأ من الأساس).

---

### 2.2 🟢 [تم التعامل معه] نظام `permissions` التفصيلي مش مفعّل فعليًا — واجهة وهمية

**المكان:** `EditUserModal.tsx`, `useAdminUsers.ts`, `icons.ts` (`PERMISSION_LABELS`)

الأدمن عنده واجهة كاملة لضبط 8 صلاحيات تفصيلية لكل مستخدم:
`can_add_cases`, `can_edit_cases`, `can_delete_cases`, `can_view_fees`, `can_edit_fees`, `can_add_clients`, `can_view_reports`, `can_export_data`.

**بحث شامل في الكود كله أثبت إن الحقل `permissions` مش بيتقرأ أو يتفحص في أي مكان تاني غير شاشة التعديل نفسها.** يعني:

- محامي بصلاحية `can_delete_cases: false` **لسه يقدر يحذف أي قضية عادي**.
- `viewer` بصلاحية `can_view_fees: false` (القيمة الافتراضية) **لسه يشوف الأتعاب كاملة**.
- الفرق العملي بين دور `lawyer` و `viewer` في كل الفرونت إند = **صفر**. الاتنين نفس الصلاحيات بالظبط.

هذا خطر مزدوج: (أ) وهم أمان — الأدمن هيحس إنه ضابط وصول حساس زي الأتعاب وهو مش ضابط حاجة فعليًا، (ب) تعرّض قانوني/تجاري محتمل لو مكتب حط موظف "مشاهد فقط" على بيانات أتعاب حساسة وهو فاكر إنه مقفول.

**التوصية:** قرار واحد من اتنين لازم يتاخد قريب:
1. **تفعيل النظام فعليًا** — بناء هوك `usePermission(key)` واستخدامه في كل نقطة حساسة (زرارات الحذف، شاشة الأتعاب، التصدير)، **وفرضه كمان على مستوى RLS** مش بس الفرونت إند (لأن أي فحص فرونت إند لوحده سهل الالتفاف عليه بنداء مباشر لـ Supabase).
2. **إخفاء الواجهة مؤقتًا** لحد ما يتفعّل، عشان ما تدّيش انطباع خاطئ للأدمن.

**✅ القرار المطبّق (نهائي):** تم في البداية اختيار الخيار 2 مؤقتًا (إخفاء الواجهة)، ثم — في جلسة تنفيذ لاحقة بتاريخ 16 أغسطس 2026 — تقرر الانتقال للخيار 1 (**التفعيل الكامل فعليًا على مستوى Frontend + RLS**)، بعد ما المكتب احتاج فرق حقيقي بين `lawyer`/`viewer`. التنفيذ الكامل (دالة `has_permission()` مركزية، تقسيم سياسات RLS لأوامر منفصلة، `usePermission()` hook، توصيل 5 نقاط في الواجهة، إعادة تفعيل قسم الصلاحيات في `EditUserModal.tsx`، Matrix Test شامل 34/34 نجحوا فعليًا على الإنتاج) موثّق بالتفصيل في قسم 7 من التقرير ده. القيمة الصريحة المحفوظة في `permissions` بقت فعليًا فعّالة (مش مجرد مخزّنة للمستقبل).

---

### 2.3 ✅ [تم إصلاحه] عمليات حساسة على `profiles` بتتم مباشرة من الفرونت إند بدل `admin-actions`

**المكان:** `useAdminUsers.ts` — `handleEditUser`, `toggleUserActive`, `handleToggleLock`

على عكس `create_lawyer`/`delete_user`/`change_password`/`force_signout` (بتمر كلها عبر Edge Function `admin-actions` بفحص صريح للـ tenant والدور)، العمليات دي بتعمل `db.from('profiles').update(...)` مباشرة من المتصفح، معتمدة بالكامل على RLS كخط دفاع وحيد.

بعد إصلاح 2.1، الوضع بقى آمن، لكن الاعتماد الكامل على RLS كخط دفاع وحيد (بدون توثيق مركزي/logging موحّد لكل تغيير حساس) أضعف من نمط `admin-actions` المستخدم في باقي العمليات.

**التوصية:** نقل الثلاث عمليات دي لتمر عبر `admin-actions` (دفاع مضاعف + تسجيل موحّد في activity log بنفس معايير باقي العمليات الإدارية).

**✅ [تم إصلاحه]** الثلاث دوال (`handleEditUser`, `toggleUserActive`, `handleToggleLock` في `useAdminUsers.ts`) بقوا فعليًا بيمروا عبر `admin-actions` (actions: `update_profile`/`toggle_lock`) بدل `db.from('profiles').update()` المباشر — نفس نمط `create_lawyer`/`delete_user`/`change_password`/`force_signout` بالظبط. `handleDeleteUser` كمان اتصلح بنفس المناسبة (كان بيحذف صف `profiles` بس ويسيب حساب `auth.users` معلّق).

---

### 2.4 🟢 نقاط قوة مؤكدة

- **`admin-actions` Edge Function**: تحقق صارم من هوية المستدعي (`getCallerProfile`)، فحص `is_active`، عزل `tenant_id` بين المكاتب (`authorizeOnTarget`)، منع حذف/تعديل الحساب الشخصي، ومنع حقن `tenant_id` تاني عند إنشاء مستخدم جديد (السطر بياخد `tenant_id` من حساب الأدمن نفسه مش من الـ body).
- **`current_tenant_id()`**: بيتحقق من `is_active` وحالة اشتراك المكتب (`suspended`/`trial` منتهي) على مستوى القاعدة نفسها، مش بس الواجهة — خط دفاع قوي.
- **`profiles_delete`/`profiles_select`**: منطق العزل بين المكاتب سليم ومتسق مع باقي الجداول.
- **RLS مفعّلة فعليًا** على `profiles` (`rls_enabled_on_profiles: true`) — مش جدول مكشوف بالغلط.

---

## 3. تجربة المستخدم (UX)

### 3.1 ✅ [تم حله] دور `viewer` بلا معنى فعلي
~~زي ما اتوضح في 2.2، اختيار دور "مشاهد" في واجهة الأدمن بيوهم بقيود مش موجودة فعليًا.~~ **تم الحل:** بعد تفعيل نظام الصلاحيات فعليًا (قسم 7)، دور `viewer` بقى له فرق حقيقي ومطبّق على مستوى RLS نفسه (مش بس الواجهة) — بيتأكد ده فعليًا فى PART D من Matrix Test (قسم 7.5): محاولة `viewer` عمل INSERT/UPDATE/DELETE على `cases` بترفض من القاعدة.

### 3.2 ✅ [تم حله] عدم وجود تمييز بصري بين "دور" و"صلاحية معطّلة تلقائيًا"
~~في `EditUserModal.tsx`، لما تختار دور `admin` كل الصلاحيات بتتفعّل تلقائيًا وتتقفل (disabled) — ده منطقي. لكن لما تختار `viewer`، الصلاحيات الافتراضية بتتحط بس المستخدم يقدر يغيّرها فورًا — مفيش أي توضيح في الواجهة إن التغييرات دي (حاليًا) شكلية وغير مُفعّلة فعليًا في النظام.~~ **تم الحل كجزء من مرحلة 4 (قسم 7):** الصلاحيات دلوقتي فعّالة فعليًا (مش شكلية)، و`EditUserModal.tsx` بقى فيه تمييز بصري واضح — تلوين ذهبي للـ checkbox لو فيه استثناء صريح محفوظ، رمادي عادي لو لسه على افتراضي الدور، مع legend فوق القايمة يوضّح الفرق. لو الدور المختار `admin`، بيتعرض نص توضيحي بدل الـcheckboxes بدل ما نعرض كل المفاتيح `true` بشكل مضلّل.

### 3.3 رسالة نجاح "تسجيل خروج قسري" ثابتة حتى لو فشل جزء منها
في `toggleUserActive` (تعطيل مستخدم)، لو `force_signout` فشل، بيظهر toast تحذيري صحيح (`⚠️ تم تعطيل الحساب، لكن تعذر إنهاء جلساته الحالية`) — ده تصميم كويس فعلاً، مذكور كـ fix سابق في الكود. لا توجد ملاحظة سلبية هنا، بالعكس هذه ممارسة جيدة تستحق الإشارة.

### 3.4 ✅ [تم إصلاحه] عدم وجود تأكيد إضافي عند تغيير دور مستخدم لـ `admin`
~~حاليًا تغيير دور أي مستخدم من `lawyer` لـ `admin` بيتم بنفس سهولة تغيير الاسم — زرار واحد "حفظ التعديلات" بلا تأكيد إضافي.~~ **تم الإصلاح:** `EditUserModal.tsx` دلوقتي بيتطلب ضغطة تأكيد صريحة تانية ("متأكد إنك عايز تدّي [الاسم] صلاحيات مدير كاملة؟") قبل ما الترقية لـ`admin` تتحفظ فعليًا — بيظهر بس لو الدور فعليًا هيتغيّر لـadmin (مش لو المستخدم أدمن أصلًا).

---

## 4. جودة الكود (Code Quality)

### 4.1 ✅ [تم إصلاحه] ازدواجية منطق تحديد "الأدمن"
~~`isAdmin = profile?.role === 'admin'` مكرر حرفيًا في `useAppData.ts` و`App.tsx`.~~ **تم الإصلاح:** استُخرج فى دالة موحّدة `isAdminRole()` جوه `src/shared/lib/permissions.ts`، والاتنين (`useAppData.ts`/`App.tsx`) بيستوردوها بدل التكرار.

### 4.2 ✅ [تم إصلاحه] نوع `permissions` غير محكم (`Record<string, boolean>`)
~~مفيش أي type-safety أو enum يربط مفاتيح `PERMISSION_LABELS` بنوع `EditUserForm.permissions`.~~ **تم الإصلاح:** اتعرّف `PERMISSION_KEYS` (مصفوفة ثابتة بالـ8 مفاتيح) و`PermissionKey` (union type مشتق منها بـ`typeof`) فى `src/shared/lib/permissions.ts` — نفس النوع مستخدم دلوقتي فى `usePermission()` و`EditUserModal.tsx` (`EDITABLE_PERMISSION_KEYS`) وباقي نقاط التوصيل، فمفيش مصدرين منفصلين تانى.

### 4.3 ✅ [تم إصلاحه] عمود `rbac_role` ميت في قاعدة البيانات
~~موجود في `database.types.ts` وفي schema فعليًا، لكن غير مستخدم في أي منطق حالي.~~ **تم الإصلاح:** اتضاف تعليق صريح جوه `database.types.ts` يوضّح إن الدور الفعلي المستخدم فى كل الكود هو `role`، مش `rbac_role` (legacy/غير مستخدم).

### 4.4 ✅ [تم إصلاحه جزئيًا] سياسات RLS الأساسية غير موثّقة كملفات migration في الريبو
~~سياسات `profiles_select` / `profiles_update` / `profiles_delete` وباقي الجداول الأساسية مش موجودة كملفات SQL فى الريبو.~~ **تم الإصلاح:** سياسات `profiles` الأساسية اتوثّقت فى `sql-migrations-phase5/04-document-profiles-update-base-policy.sql` و`05-document-remaining-rls-policies.sql`. وكجزء من خطة التفعيل الكامل (قسم 7)، سياسات `cases`/`clients`/`case_fees`/`fee_payments` الجديدة اتكتبت من الأول كملف migration رسمي (`sql-migrations-phase6/02-rls-split-cases-clients-fees-payments.sql`) بدل التنفيذ اليدوي المباشر على Supabase Dashboard — المبدأ العام بقى مطبّق فعليًا فى آخر مرحلتين تنفيذ.

---

## 5. جدول توصيات مرتّب بالأولوية

| # | الأولوية | الوصف | الحالة |
|---|---------|-------|--------|
| 1 | 🔴 حرجة | Trigger منع تصعيد الصلاحيات الذاتي على `profiles` | ✅ تم التنفيذ |
| 2 | 🔴 حرجة | توثيق الـ trigger كملف migration رسمي في الريبو | ✅ تم — موجود في `sql-migrations-phase5/02-...sql` |
| 2.ب | 🔴 حرجة | حماية الواجهة من الأثر الجانبي للتريجر (الأدمن يقفل/يغيّر نفسه) | ✅ تم — `UsersSection.tsx` + `EditUserModal.tsx` + تمرير `profile` |
| 3 | 🟠 عالية | قرار: تفعيل نظام `permissions` فعليًا (Frontend + RLS) أو إخفاء واجهته مؤقتًا | ✅ تم — **الخيار 1 (تفعيل كامل) اتنفّذ فعليًا لاحقًا، تفاصيله فى قسم 7** |
| 4 | 🟡 متوسطة | نقل `handleEditUser`/`toggleUserActive`/`handleToggleLock` لتمر عبر `admin-actions` | ✅ تم — `useAdminUsers.ts` |
| 5 | 🟡 متوسطة | تأكيد إضافي عند ترقية مستخدم لدور `admin` | ✅ تم — `EditUserModal.tsx` |
| 6 | 🟢 منخفضة | توحيد منطق `isAdmin`، تعريف type موحّد لمفاتيح الصلاحيات، توثيق `rbac_role` كـ legacy | ✅ تم — قسم 4.1/4.2/4.3 |
| 7 | 🟢 منخفضة | تسجيل باقي سياسات RLS الأساسية كملفات migration في الريبو | ✅ تم جزئيًا — قسم 4.4 |

---

## 6. الخطوة التالية المقترحة (تاريخي — قبل تنفيذ قسم 7)

~~كل البنود الحرجة والعالية (1، 2، 2.ب، 3، 5) خلصت. الباقي بنود متوسطة/منخفضة الأولوية ومش عاجلة: الأنسب البدء بالبند 4 (نقل عمليات profiles الحساسة تمر عبر admin-actions)... بند 6 و7 تحسينات كود/توثيق مش مستعجلة.~~

**كل بنود الجدول أعلاه اتنفّذت فعليًا.** الخطوة اللي فعلاً حصلت بعد كده كانت أكبر من البند 4 وحده: بند 3 اتفتح تاني وتقرر التفعيل الكامل بدل الإخفاء المؤقت — التفاصيل الكاملة، بما فيها ثغرة إضافية اتكتشفت أثناء التنفيذ (RPCs الأتعاب بتتخطى RLS)، موجودة فى قسم 7.

---

## 7. تحديث لاحق — تفعيل نظام الصلاحيات التفصيلية فعليًا (16 أغسطس 2026)

بعد ما القرار فى البند 3 اتفتح تاني، اتنفّذت خطة كاملة (Frontend + RLS معًا) لتفعيل نظام الـ8 مفاتيح صلاحيات (`can_add_cases`, `can_edit_cases`, `can_delete_cases`, `can_view_fees`, `can_edit_fees`, `can_add_clients`, `can_view_reports`, `can_export_data`) فعليًا بدل ما تفضل واجهة مقفولة بلا أثر. ملخص النتائج:

### 7.1 مصفوفة الصلاحيات الافتراضية المعتمدة

| المفتاح | admin | lawyer | viewer |
|---|:---:|:---:|:---:|
| can_add_cases | ✅ | ✅ | ❌ |
| can_edit_cases | ✅ | ✅ | ❌ |
| can_delete_cases | ✅ | ❌ | ❌ |
| **can_view_fees** | ✅ | **❌ (مقفول بلا استثناء)** | ❌ |
| **can_edit_fees** | ✅ | **❌ (مقفول بلا استثناء)** | ❌ |
| can_add_clients | ✅ | ✅ | ❌ |
| can_view_reports | ✅ | ✅ | ✅ |
| can_export_data | ✅ | ❌ | ❌ |

الأتعاب (`can_view_fees`/`can_edit_fees`) هي الاستثناء الوحيد المقفول تمامًا لغير الأدمن بلا استثناءات صريحة، حتى لو الأدمن حاول يفتحها لمستخدم معيّن. `admin`/`is_super_admin` بايباس كامل دايمًا.

### 7.2 طبقتين دفاع، مش طبقة واحدة
- **قاعدة البيانات (RLS):** دالة مركزية `has_permission(key text)` (`SECURITY DEFINER`)، وسياسات RLS لـ`cases`/`clients`/`case_fees`/`fee_payments` اتقسّمت من `FOR ALL` لأوامر منفصلة (SELECT/INSERT/UPDATE/DELETE) كل واحد بمفتاحه الخاص.
- **الفرونت إند:** `usePermission(profile, key)` hook فى `src/shared/lib/permissions.ts`، بنفس منطق `has_permission()` بالضبط (بايباس admin → قفل الأتعاب → قيمة صريحة → افتراضي الدور) — موصّل فعليًا فى 5 نقاط: حذف/إضافة/تعديل قضية، إضافة موكل، وتاب الأتعاب بالكامل (render-gate + إخفاء من التنقل، مش بس تعطيل الزرار).

### 7.3 🔴 ثغرة إضافية اتكتشفت واتصلحت أثناء التنفيذ
دالتين `record_fee_payment` و`create_fee_with_advance` (`SECURITY DEFINER`) كانوا بيتحققوا بس من عزل الـtenant بلا أي فحص دور — يعني `lawyer` كان يقدر يتخطى قفل `case_fees`/`fee_payments` بنداء الـRPC مباشرة. اتصلح بإضافة فحص `has_permission('can_edit_fees')` جوه الدالتين قبل أي `INSERT`/`UPDATE`.

### 7.4 سلوك تصفير الصلاحيات عند تغيير الدور
أي تغيير `role` لمستخدم من `admin-actions` (`update_profile`) بيصفّر عمود `permissions` الخاص بيه تلقائيًا (`{}`) كجزء من نفس العملية، عشان استثناء قديم (زي `can_export_data: true` لمحامي) ما يفضلش شغال بعد ما دوره يتغيّر لـ`viewer`.

### 7.5 اختبار فعلي على الإنتاج
Matrix Test SQL (`database/tests/phase6-permissions-matrix-test.sql`، كامل جوه `BEGIN...ROLLBACK`) اتشغّل فعليًا: **34/34 نجحوا، صفر فشل** — تغطية `has_permission()` (24 حالة: 3 أدوار × 8 مفاتيح)، قفل الأتعاب الأساسي، رفض RLS الفعلي لـ`viewer`/`lawyer` غير المصرّح، تأكيد إصلاح ثغرة الـRPC (7.3)، وبايباس `admin` الكامل على مستوى RLS نفسه.

### 7.6 الباقي — خطوات تشغيلية فقط (مش برمجية)
- تشغيل `e2e/permissions-matrix.spec.ts` فعليًا فى CI/محليًا (محتاج حساب E2E_TEST_EMAIL أدمن).
- رفع الملفات المعلّقة على GitHub (`EditUserModal.tsx`، ملفات مرحلة 5).
- Deploy نهائي لـEdge Function `admin-actions` (لو مكانش جزء من نفس خطوة الاستبدال اللي حصلت بالفعل).
- مراجعة يدوية بحساب `lawyer`/`viewer` تجريبي للتأكد إن السلوك زي المتوقع فعليًا فى الواجهة.

**المرجع الكامل لكل تفاصيل التنفيذ:** `خطة-تفعيل-نظام-الصلاحيات-التفصيلية-10.md`.
