# سند — تقرير حالة: المرحلة 6 (الأدمن) — دفعة 3، جزء 1: الأمان + بوابة الموكل

**التاريخ:** 25 يوليو 2026
**الحالة:** ✅ اكتمل
**المرجع:** `سند-تقرير-حالة-إصلاح-حذف-المستخدم-1.md`
**الزيب المرفق:** `sanad-project-updated-final-25.zip`

---

## الخلفية

دفعة 3 هي الأخيرة في قسم الأدمن، وفيها ثلاث شاشات: **الأمان**، **بوابة
الموكل**، **النسخ الاحتياطي**. باتفاق معاك، بدأنا بالأمان والبوابة (الاتنين
عمليات معزولة بأمان زي دفعة المستخدمين)، وأجّلنا التخطيط للنسخ الاحتياطي
لآخر حاجة لأنه الأخطر (استبدال كامل للبيانات).

## اللي خلص

### 1) الأمان (`SecuritySection.tsx` + `AdminPanelSectionConfirms.tsx`)
اتضافت `data-testid` لكل حاجة:
- بطاقة المستخدم (`admin-security-card`) وحالة "لا يوجد مستخدمون"
  (`admin-security-empty`)
- الأزرار التلاتة على كل بطاقة: تغيير كلمة المرور
  (`admin-security-changepass`)، تسجيل خروج من جميع الأجهزة
  (`admin-security-signout`)، قفل/فتح الحساب (`admin-security-lock-toggle`)
- مودالي التأكيد في `AdminPanelSectionConfirms.tsx`:
  `admin-security-signout-confirm/cancel` و
  `admin-security-lock-confirm/cancel`

زرار "تغيير كلمة المرور" هنا بيفتح نفس `ChangePasswordModal.tsx` اللي
اتغطى بالكامل في دفعة 2 (نفس testids `admin-changepass-*`) — مفيش داعي
لتكرار تست الحفظ الكامل، تست واحد بس بيتأكد إن المودال الصحيح بيفتح.

**تست جديد:** `e2e/admin-security.spec.ts` (5 حالات، بمستخدم تجريبي
`createTestUser` خاص بكل تست):
1. بطاقة المستخدم التجريبي بتظهر بالأزرار التلاتة
2. قفل الحساب ثم فتحه — بادچ "🔒 مقفول" بيظهر ويختفي، وتوستات
   `🔒 تم قفل الحساب` / `🔓 تم فتح الحساب`
3. إلغاء تأكيد القفل ما بيغيّرش الحالة
4. تسجيل خروج من جميع الأجهزة — نفس منطق دفعة 2 (مستخدم لسه ما سجّلش
   دخول، فـ`force_signout` بيعدي بنجاح من غير جلسة حقيقية يقفلها)
5. زرار تغيير كلمة المرور بيفتح `ChangePasswordModal` الصحيح

### 2) بوابة الموكل (`PortalSection.tsx` + `AddPortalUserModal.tsx` +
`ClientPortalModal.tsx` + `AdminPanel.tsx`)
اتضافت `data-testid` لكل حاجة:
- زرار "وصول جديد" في هيدر القسم (`admin-portal-new-button`) —
  ⚠️ كان الزرار موجود فعليًا في الكود من قبل (`setShowAddPortalUser(true)`)
  بس من غير testid، يعني الميزة موجودة بس مغطّاش بتست قبل كده
- حقل البحث (`admin-portal-search`)، بطاقة الموكل
  (`admin-portal-card`)، حالة "لا يوجد موكلون" (`admin-portal-empty`)،
  زرار الإعداد/التعديل (`admin-portal-setup-button`)
- `AddPortalUserModal.tsx`: حقل البحث (`admin-portal-add-search`)،
  خيار الموكل (`admin-portal-add-client-option`)، حقل PIN
  (`admin-portal-add-pin`)، توليد (`admin-portal-add-genpin`)، حفظ
  (`admin-portal-add-submit`)، إغلاق (`admin-portal-add-close`)
- `ClientPortalModal.tsx`: حقل PIN (`admin-portal-edit-pin`)، توليد
  (`admin-portal-edit-genpin`)، مفتاح التفعيل
  (`admin-portal-edit-active-toggle`)، حفظ (`admin-portal-edit-save`)،
  إغلاق (`admin-portal-edit-close`)

عملية إعداد/تعديل بوابة موكل معزولة بالكامل (RPC `set_portal_pin` على
صف `client_portal_pins` بتاع الموكل التجريبي بس)، فمفيش خطورة في
تغطيتها بالكامل زي دفعة المستخدمين — عكس الأمان (تسجيل الخروج/القفل)
اللي فيها لمسة لحسابات Auth حقيقية.

**تست جديد:** `e2e/admin-portal.spec.ts` (4 حالات، بموكل تجريبي
`createClient` خاص بكل تست):
1. إعداد بوابة موكل لأول مرة (الزرار "إعداد" → PIN → حفظ → بادچ
   "✓ مفعّل")
2. تعديل بعد الإعداد (الزرار بقى "تعديل") وتعطيل الوصول → بادچ
   "✗ معطّل"
3. المسار البديل: "وصول جديد" من هيدر القسم (`AddPortalUserModal`)
   بدل الضغط على بطاقة موكل موجود
4. إغلاق مودال التعديل من غير حفظ → الموكل يفضل من غير وصول

## تحقق الصيغة
فحصت `SecuritySection.tsx`, `AdminPanelSectionConfirms.tsx`,
`AdminPanel.tsx`, `PortalSection.tsx`, `AddPortalUserModal.tsx`,
`ClientPortalModal.tsx`, وملفي التست الجداد بـ `tsc --noEmit --ignoreConfig`
منفردين — نفس قايمة أخطاء بيئة الساندبوكس المعروفة من كل التقارير
السابقة (`react`/`@playwright/test`/`@supabase/supabase-js` غير مثبتين،
`import.meta.env`، خصائص `window` المخصصة، `@types/node` غير مثبت،
زائد شوية implicit-any قديمة في ملفات تانية (`useAdminArchive.ts`،
`OfficeNotificationsTab.tsx`، `useNavigation.ts`) موجودة من الأصل ومش
حاجة لمستها الدفعة دي)، صفر أخطاء صياغة في أي سطر اتعدّل فعليًا.

**لازم تتأكد بنفسك قبل الدمج:**
1. `npm test` محليًا — نفس ملحوظة الدفعات اللي فاتت: التعديلات كلها
   إضافة `data-testid` بس، صفر تغيير منطق.
2. `npm run test:e2e` للملفين الجداد (`admin-security.spec.ts`،
   `admin-portal.spec.ts`).
3. تجربة "تسجيل خروج من جميع الأجهزة" يدويًا من الواجهة على حساب فيه
   جلسة نشطة فعلاً (مش تجريبي)، للتأكد من سلوك `force_signout` الكامل
   في بيئتك.

---

## حالة خطة E2E الكاملة (تحديث)

| # | المرحلة | الحالة |
|---|---|---|
| 1 | الموكلين | ✅ مكتملة |
| 2 | الجلسة المستقلة | ✅ مكتملة |
| 3 | القضايا (أطراف متعددة) | ✅ مكتملة |
| 4 | الأتعاب | ✅ مكتملة |
| 5 | مساعد الذكاء الاصطناعي (وضع `generate`) | ✅ مكتملة |
| 6 | الأدمن — الأرشيف | ✅ مكتملة (مرحلة سابقة) |
| 6 | الأدمن — نشاط + مكتبة قانونية + مكتب (دفعة 1) | ✅ مكتملة |
| 6 | الأدمن — المستخدمين والجلسات (دفعة 2) | ✅ مكتملة |
| 6 | الأدمن — الأمان + بوابة الموكل (دفعة 3، جزء 1) | ✅ مكتملة (هذا التقرير) |
| 6 | الأدمن — النسخ الاحتياطي (دفعة 3، جزء 2 — الأخيرة) | ⬜ لسه ما بدأتش |
| 7 | باقي Tier 2 | ⬜ لسه ما بدأتش |
| 8 | Tier 3 — Smoke دفعة واحدة | ⬜ لسه ما بدأتش |

## الزيب المرفق
`sanad-project-updated-final-25.zip` — فيه كل حاجة من
`sanad-project-updated-final-24.zip` زي ما هي، زائد:
- `src/features/admin/security/SecuritySection.tsx` (testids)
- `src/features/admin/AdminPanelSectionConfirms.tsx` (testids مودالي تأكيد الأمان)
- `src/features/admin/AdminPanel.tsx` (testid زرار "وصول جديد")
- `src/features/admin/portal/PortalSection.tsx` (testids)
- `src/features/admin/portal/AddPortalUserModal.tsx` (testids)
- `src/features/admin/portal/ClientPortalModal.tsx` (testids)
- `e2e/admin-security.spec.ts` (جديد — 5 تستات)
- `e2e/admin-portal.spec.ts` (جديد — 4 تستات)

---

## الخطوة الجاية
**دفعة 3، جزء 2 (الأخيرة في المرحلة 6 كلها)** — النسخ الاحتياطي. زي ما
اتفقنا، محتاجين الأول نخطط إزاي نختبر "استعادة نسخة احتياطية"
(`confirmRestore`/`handleRestoreBackup`) بأمان من غير ما نأثر على بيانات
بيئة التست الحقيقية — العملية دي بتستبدل البيانات الحالية بالكامل ومفيش
تراجع عنها. الجزء الآمن (إنشاء نسخة، عرض القايمة، تحميل نسخة) ممكن
يتغطى عادي زي أي قسم تاني؛ الاستعادة الفعلية هي اللي محتاجة تفكير.
تحب تبدأ بيه دلوقتي؟
