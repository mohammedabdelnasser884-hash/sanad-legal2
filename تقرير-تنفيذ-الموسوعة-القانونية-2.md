# تقرير تنفيذ — قسم "الموسوعة القانونية" (مرحلة 2: Edge Function)

**التاريخ:** 13 سبتمبر 2026

## اللي اتنفذ

`supabase/functions/encyclopedia-admin/index.ts` — فانكشن واحدة بتتحقق `is_super_admin === true` قبل أي عملية (نفس نمط `admin-actions`)، وبتغطي الأفعال الستة من الخطة:

- `createCategory` / `updateCategory` / `deleteCategory`
- `uploadForm` / `updateForm` / `deleteForm`

نقاط مهمة في التنفيذ:

- **حد حجم الملف 2 ميجا** (قرارك) — مُطبّق داخل الفانكشن نفسها (`MAX_FILE_SIZE_BYTES`)، منفصل تمامًا عن حد الـ1 ميجا الخاص بمستندات القضايا (`validateUploadFile` في `src/shared/lib/storage.ts` — لم يُلمس).
- **تنظيف الاستوريج عند الحذف/الاستبدال:** الـcascade في القاعدة (migration 28) بيمسح صفوف الـDB بس. الفانكشن هنا مسؤولة عن مسح الملفات الفعلية من باكت `encyclopedia-forms`:
  - `deleteCategory` بيجمع كل النماذج (نفس المجلد + أي مجلد فرعي جواه) ويمسح ملفاتهم كلها *قبل* حذف المجلد من الـDB.
  - `deleteForm` بيمسح الملف الأول بعدين الصف.
  - `updateForm` (لو فيه ملف جديد) بيرفع الجديد الأول، يعدّل الصف، وبعد نجاح التعديل بس بيمسح القديم — عشان لو فشل التعديل مايضيعش الملف الأصلي.
  - `uploadForm`: لو فشل الـinsert بعد نجاح الرفع، بيمسح الملف اليتيم اللي اتحمّل (rollback).
- **مسار التخزين:** `{category_id}/{form_id}.{ext}` بالظبط زي ما الخطة نصّت.
- **RLS مش كافية لوحدها هنا:** حتى لو RLS بتمنع غير سوبر أدمن من الكتابة على الجداول، رفع/حذف ملف على باكت private لازم service_role — ده سبب وجود الفانكشن أصلًا، مش بديل عن RLS (RLS لسه خط الدفاع الأساسي، الفانكشن طبقة إضافية).
- **اختبار وحدة:** `index.test.ts` (15 حالة) — يغطي رفض غير السوبر أدمن، رفض الحساب المعطّل، إنشاء/تعديل/حذف مجلد (بما فيها ظهور رسالة تريجر "منع المستوى التالت" زي ما هي)، رفض نوع/حجم ملف غير صالح، رفع/تعديل/حذف نموذج بنجاح، وحالات الـrollback عند الفشل الجزئي.

## لسه محتاج منك

- **نشر الفانكشن:** `supabase functions deploy encyclopedia-admin` (المتغيرات البيئية SUPABASE_URL/ANON_KEY/SERVICE_ROLE_KEY متاحة تلقائيًا على مستوى المشروع، زي باقي الفانكشنز — مفيش إعداد إضافي مطلوب).
- **تشغيل التست الحقيقي** (`npm test`) — زي كل مرة، الساندبوكس هنا من غير node_modules/شبكة فمقدرش أشغّله فعليًا.

## الخطوة الجاية (مرحلة 3)

واجهة الإدارة (سوبر أدمن): قسم إدارة جديد لإدارة المجلدات + شاشة رفع/تعديل/حذف النماذج، بتستخدم الفانكشن دي عن طريق wrapper زي `callAdminAction` الموجود بالظبط.
