# خطة تنفيذ: إعادة هيكلة "الموسوعة القانونية" إلى "الموارد القانونية"

## 0. نظرة عامة

القسم الحالي "الموسوعة القانونية" هيتحول لقسم أب اسمه **"الموارد القانونية"**، وتحته قسمان:

1. **الصيغ والنماذج** — نفس شاشة التصفح الموجودة حاليًا بالضبط، بدون أي تغيير في منطقها أو بياناتها.
2. **دليل المحامي** — دليل خدمات رسمية قابل للبحث والتصنيف (وزارة العدل، الشهر العقاري، الضرائب... إلخ)، مبني من الصفر.

---

## 1. القرارات المؤكدة

| القرار | التفصيل |
|---|---|
| آلية الاختيار | الضغط على "الموارد القانونية" يفتح بوكس منبثق (نفس تصميم بوكس "المزيد" الحالي في الموبايل) فيه أيقونتين: الصيغ والنماذج / دليل المحامي. الضغط على واحدة يفتح صفحتها ويقفل البوكس |
| الرجوع | زرار الرجوع من جوه أي قسم يرجع مباشرة للقائمة الرئيسية، مش لبوكس الاختيار |
| صلاحيات "دليل المحامي" | نفس نموذج "الصيغ والنماذج": القراءة مفتوحة لأي مستخدم مسجّل دخول، الإضافة/التعديل/الحذف مقصورة على السوبر أدمن فقط (`is_super_admin()`) |
| تنظيم "دليل المحامي" | **تصنيفات**، مش قائمة مسطحة — كل رابط له تصنيف (زي: قضاء ونيابة، شهر عقاري وتوثيق، ضرائب...) |
| بحث | نفس نمط البحث الموجود في "الصيغ والنماذج" (بحث في العنوان/الوصف عبر كل التصنيفات دفعة واحدة) |
| بيانات كل رابط | عنوان، وصف مختصر، نوع الجهة، تاريخ آخر مراجعة (يدوي من الأدمن، مش تلقائي) |
| هيكل التصنيفات | التقسيم الثماني الكامل معتمد كما هو، بكل روابطه الفرعية داخل كل تصنيف (التفصيل الكامل في الملحق أ) — بدون اختصار أو تقليل. عدد التصنيفات والروابط بيانات تُدخل من شاشة الأدمن بعد التنفيذ، مش قيد على الكود |
| المفضلة ⭐ | **مؤجلة لمرحلة تانية بعد الإطلاق**، مش جزء من v1 — لأنها فعليًا مشروع فرعي مستقل (بيانات خاصة بكل مستخدم، جدول وصلاحيات RLS مختلفة تمامًا عن باقي القسم اللي كله بيانات عامة يديرها السوبر أدمن) |
| تجميع محتوى الروابط نفسه | مسؤولية بي (تدقيق الروابط الرسمية وصحتها وتحديثها) — مش جزء من التنفيذ التقني |
| التعامل مع الخدمات الفرعية بدون بوابة مستقلة | مفيش روابط مخترعة أو مركّبة لخدمة مش ليها بوابة رسمية ثابتة — راجع التفصيل الكامل في الملحق أ |

## 2. قرارات لسه مفتوحة

| # | القرار | الخيارات | ملاحظة |
|---|---|---|---|
| 1 | ترتيب الروابط داخل كل تصنيف | أبجدي تلقائي — أو ترتيب يدوي (الأهم يفضل فوق) | **الافتراض المبدئي المُطبَّق في مرحلة 3**: أبجدي (بالعنوان)، لأن جدول `lawyer_guide_links` مالوش عمود `sort_order` أصلاً (بعكس `lawyer_guide_categories`). لو قرارك ترتيب يدوي، محتاجين ميجريشن صغير إضافي (`sort_order` على `lawyer_guide_links`) — قوليلي وأضيفه |

**قرار #2 (شكل بوكس الاختيار على الديسكتوب) — محسوم:** Popover بسيط جنب "الموارد القانونية" في الـSidebar. هيتفصّل في مرحلة 4.

---

## 3. المراحل التقنية

### المرحلة 1 — قاعدة البيانات — ✅ منفّذ
جدولا `lawyer_guide_categories`/`lawyer_guide_links` + RLS (قراءة مفتوحة لأي authenticated، كتابة `is_super_admin()` بس). طُبّق على الإنتاج وتأكّد.

### المرحلة 2 — Edge Function — ✅ منفّذ
توسيع `encyclopedia-admin` بـ6 actions: `createLinkCategory`/`updateLinkCategory`/`deleteLinkCategory`، `createLink`/`updateLink`/`deleteLink`. محتاج `supabase functions deploy encyclopedia-admin` (لو لسه مش متعمول).

### المرحلة 3 — واجهة الأدمن — ✅ منفّذ (هذا التسليم)
- هوك جديد `useAdminLawyerGuide.ts` — نفس نمط `useAdminEncyclopedia.ts` بالظبط (نفس `callEncyclopediaAction`)، لكن أبسط: مستوى واحد بس من التصنيفات (بدون `parent_id`)، وروابط نصية بحتة بدون أي رفع ملفات/Storage.
- مكوّنين مودال جديدين: `LawyerGuideCategoryModal.tsx` (اسم + أيقونة اختيارية) و`LawyerGuideLinkModal.tsx` (تصنيف، عنوان، رابط، وصف، نوع الجهة، تاريخ آخر مراجعة).
- شاشة إدارة جديدة `LawyerGuideSection.tsx` — نفس نمط `EncyclopediaSection.tsx` (بطاقات تصنيف تُفتح لعرض روابطها، بحث موحّد يتخطى شكل التصنيفات، زرار رجوع فعلي مسجّل في `useNestedModalBackButton`).
- إضافة أيقونة `ExternalLink` جديدة في `constants.ts` (لسه مفيش أيقونة رابط خارجي جاهزة في المشروع).
- ربط كل ده في `AdminPanel.tsx`/`AdminPanelModals.tsx`: `SectionId` جديد `lawyer_guide`، كرت عريض جديد في شبكة الأقسام (بعد كرت "المكتبة القانونية" مباشرة، مقصور على السوبر أدمن زي باقي الأقسام الحساسة)، هيدر ملوّن (لون العنبر نفسه المستخدم مع الموسوعة)، وكل المودالات وتأكيدات الحذف.
- تحديث `database.types.ts` (تعريف الجدولين الجديدين يدويًا زي باقي الجداول) و`types.ts` (`LawyerGuideCategoryRow`/`LawyerGuideLinkRow`) و`supabaseClient.ts` (توسيع union `EncyclopediaActionPayload` بالـ6 actions الجديدة).
- **ملاحظة على مكان الدخول**: القسم دلوقتي بيظهر ككرت عادي في شبكة أقسام لوحة الإدارة (زي "المكتبة القانونية")، مش عن طريق زرار "إدارة" من صفحة تصفح عادية زي الموسوعة القانونية — لأن صفحة التصفح العادية لسه مش موجودة (هتتعمل في مرحلة 4). لما مرحلة 4 تتنفذ، ممكن نراجع مكان الدخول ده يتوحّد مع نفس أسلوب الموسوعة لو حبيت.
- **بيئة التسليم**: الـsandbox من غير إنترنت وقت هذا التسليم (زي تسليم دفعة B في خطة المدفوعات سابقًا) — الكود اتراجع يدويًا بس (فحص أقواس/توازن)، من غير تشغيل `npm run build`/`vitest` فعليًا. محتاج تشغيل CI بنفسك قبل النشر.

### المرحلة 4 — واجهة المستخدم وإعادة هيكلة التنقل — ⬜ لم يبدأ
`TabName` جديد، بوكس الموبايل، Popover الديسكتوب (قرار #2 محسوم)، مكوّن عرض "دليل المحامي" للمستخدم العادي.

### المرحلة 5 — تسميات ونصوص (Rename sweep) — ⬜ لم يبدأ

### المرحلة 6 — اختبارات (E2E + Edge Function) — ⬜ لم يبدأ

### المرحلة 7 (مؤجلة — بعد الإطلاق) — ⬜ مؤجلة عمدًا
المفضلة ⭐ + توسّع تدريجي لاحق.

---

## 3.1 جدول حالة تنفيذ المراحل

| المرحلة | الوصف | الحالة | ملاحظات |
|---|---|---|---|
| 1 | قاعدة البيانات | ✅ منفّذ | — |
| 2 | Edge Function | ✅ منفّذ | — |
| 3 | واجهة الأدمن (إدارة التصنيفات والروابط) | ✅ منفّذ | هذا التسليم — محتاج تشغيل CI منك قبل النشر |
| 4 | واجهة المستخدم + إعادة هيكلة التنقل | ⬜ لم يبدأ | — |
| 5 | تسميات ونصوص (Rename sweep) | ⬜ لم يبدأ | — |
| 6 | اختبارات (E2E + Edge Function) | ⬜ لم يبدأ | — |
| 7 | المفضلة ⭐ + توسّع تدريجي | ⬜ مؤجلة عمدًا | خارج نطاق v1 بقرار مؤكد |

---

## 3.2 طريقة التسليم
كما هو محدد في النسخة السابقة من هذا التقرير — زيب بالمسارات الحقيقية + هذا التقرير محدّثًا. لا يوجد ملفات SQL/Edge Function جديدة في هذا التسليم (مرحلة 3 واجهة فقط)، فمفيش نسخة منفصلة مطلوبة.

**ملفات هذا التسليم:**
- `src/features/admin/lawyer-guide/hooks/useAdminLawyerGuide.ts` (جديد)
- `src/features/admin/lawyer-guide/LawyerGuideCategoryModal.tsx` (جديد)
- `src/features/admin/lawyer-guide/LawyerGuideLinkModal.tsx` (جديد)
- `src/features/admin/lawyer-guide/LawyerGuideSection.tsx` (جديد)
- `src/features/admin/AdminPanel.tsx` (معدّل)
- `src/features/admin/AdminPanelModals.tsx` (معدّل)
- `src/types.ts` (معدّل)
- `src/supabaseClient.ts` (معدّل)
- `src/database.types.ts` (معدّل)
- `src/constants.ts` (معدّل)

لا يوجد ملفات تحتاج حذف يدوي في هذا التسليم.

---

## 4. ملاحظة عامة
لا يوجد أي تغيير في منطق أو بيانات "الصيغ والنماذج" الحالية. القسم الجديد بالكامل هو "دليل المحامي"، ومحتواه الفعلي (الروابط والتصنيفات المدرجة) مسؤولية بي وليس جزءًا من هذا التنفيذ التقني.

---

## ملحق أ — خريطة التصنيفات والروابط الكاملة
بدون تغيير عن النسخة السابقة من هذا التقرير (نفس التقسيم الثماني الكامل بروابطه).
