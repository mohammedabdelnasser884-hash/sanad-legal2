# سند — تقرير حالة: إصلاح المشكلة المعلقة (حذف المستخدم يسيب حساب Auth يتيم)

**التاريخ:** 25 يوليو 2026
**الحالة:** ✅ اكتمل
**المرجع:** `سند-تقرير-حالة-المرحلة-6-دفعة-2-الأدمن-1.md` (قسم "ملحوظة مهمة لازم تعرفها")
**الزيب المرفق:** `sanad-project-updated-final-24.zip`

---

## المشكلة

`handleDeleteUser` في `useAdminUsers.ts` كان بيحذف صف `profiles` مباشرة
(`db.from('profiles').delete()`) وبس. حساب `auth.users` المرتبط ببيانات
الدخول (البريد/كلمة السر) كان فاضل موجود وشغال فعليًا في Supabase Auth،
رغم إن المستخدم مختفي من واجهة الأدمن. النتيجة عمليًا: أي "حذف نهائي"
كان بيسيب بيانات دخول صالحة معلّقة، وكل تشغيل لتست `admin-users.spec.ts`
(حالة "حذف نهائي") كان بيراكم حساب Auth يتيم إضافي في قاعدة التست.

## الحل

اتنقلت عملية الحذف بالكامل للـ Edge Function `admin-actions` (نفس نمط
`force_signout`/`change_password`/`create_lawyer` الموجود أصلاً)، بدل
ما تتنفذ من المتصفح مباشرة:

**Edge Function (`supabase/functions/admin-actions/index.ts`)** — action
جديد `delete_user`:
1. تحقق صلاحية: نفس فحص `authorizeOnTarget` (أدمن نفس المكتب أو سوبر
   أدمن)، زائد منع صريح إن الأدمن يحذف حسابه هو نفسه.
2. **بيحذف حساب Auth الأول** عبر `DELETE /auth/v1/admin/users/:id`
   بصلاحية `service_role` — ده بيقفل بيانات الدخول القديمة فورًا. لو
   فشلت الخطوة دي (وماكانتش 404 يعني الحساب مش موجود أصلاً) بيوقف
   ويرجّع خطأ **من غير** ما يحذف صف البروفايل — قرار متعمد: حساب Auth
   يتيم شغال من غير بروفايل أخطر بكتير من مستخدم لسه ظاهر في القايمة.
3. بعدين بيحذف صف `profiles`. لو فشلت الخطوة دي بعد نجاح حذف Auth،
   بيرجّع `ok:true` + تحذير واضح للأدمن بدل ما يختفي الخطأ بصمت.
4. حالة خاصة: بروفايل قديم بلا `user_id` مرتبط (بيانات تالفة من الأصل) —
   بيتفحص نطاق المكتب مباشرة من صف البروفايل، وبيتحذف الصف بس من غير
   محاولة حذف Auth (مفيش حساب Auth يتحذف أصلاً).

**العميل (`useAdminUsers.ts`)** — `handleDeleteUser` بقى بيستدعي
`callAdminAction({ action: 'delete_user', profile_id, user_id })` جوه
`try/catch` مع `showErrorToast` (نفس نمط `handleAddUser`)، بدل الاستدعاء
المباشر لـ `db.from('profiles').delete()`. الواجهة والتوستات وسلوك
المودال ما اتغيّروش من منظور المستخدم — الفرق كله في الباك-إند.

**النوع (`supabaseClient.ts`)** — اتضاف عضو `delete_user` لـ
union type `AdminActionPayload` (نفس أسلوب الأنواع التلاتة التانية).

## الاختبارات

اتضافت 9 حالات جديدة في `supabase/functions/admin-actions/index.test.ts`
(نفس نمط `describe` الموجود لكل action):
- من غير `profile_id` → error
- محاولة حذف الحساب الشخصي → error، صفر حذف
- تينانت مختلف → غير مسموح، صفر حذف
- فشل حذف Auth → error، البروفايل يفضل موجود (مفيش DELETE عليه)
- Auth مش موجود أصلاً (404) → يُعامل كنجاح، وبيكمل يحذف البروفايل
- المسار الكامل الناجح → بيتأكد من ترتيب الحذف (Auth الأول، بروفايل تاني)
- نجاح حذف Auth + فشل حذف البروفايل → `ok:true` + تحذير
- بروفايل يتيم بلا `user_id`، أدمن نفس المكتب → مسموح
- بروفايل يتيم بلا `user_id`، أدمن مكتب مختلف → غير مسموح
- بروفايل يتيم بلا `user_id`، سوبر أدمن → مسموح بغض النظر عن التينانت

`e2e/admin-users.spec.ts` (حالة "حذف نهائي" من دفعة 2) هتفضل تشتغل
زي ما هي من غير أي تعديل — بتتفاعل مع الواجهة بس (`data-testid`s)،
ومش عارفة ولا لازم تعرف إن التنفيذ الداخلي اتغيّر. الفرق الوحيد: دلوقتي
فعليًا بتتأكد إن حساب Auth بيتقفل معاها، مش بس صف البروفايل.

## تحقق الصيغة
فحصت `admin-actions/index.ts`، `admin-actions/index.test.ts`،
`useAdminUsers.ts`، و`supabaseClient.ts` بـ `tsc --noEmit --ignoreConfig`
منفردين — نفس قايمة أخطاء بيئة الساندبوكس المعروفة من التقارير السابقة
(`Deno` global، `vitest` غير مثبت، `import.meta.env`، `@supabase/supabase-js`
غير مثبت)، صفر أخطاء صياغة في أي سطر اتعدّل فعليًا.

**لازم تتأكد بنفسك قبل الدمج:**
1. `npm run test:unit` (أو الأمر المكافئ) — تأكد إن الـ 9 حالات الجديدة
   بتعدي.
2. `npm run test:e2e -- admin-users.spec.ts` — تأكد إن حالة "حذف نهائي"
   لسه بتعدي بنفس السلوك.
3. تجربة يدوية: احذف مستخدم تجريبي من الواجهة، وتأكد من لوحة تحكم
   Supabase Auth إن الحساب فعلاً اختفى من قسم Users مش بس من `profiles`.

## ⚠️ نطاق الإصلاح — حاجة واحدة برّه النطاق عمدًا
المستخدمين اللي بيتعملهم `createTestUser` في تستات "تعديل"/"تعطيل-تفعيل"/
"تغيير كلمة السر" (مش تست "حذف") لسه بيتسابوا في قاعدة بيانات التست من
غير ما يتحذفوا في نهاية التست — ده تصميم متعمد من دفعة 2 (عزل كل تست
بمستخدم خاص بيه) مش نفس الباگ اللي اتصلح هنا. لو حابب نضيف تنظيف تلقائي
(`afterEach` بيحذف المستخدمين التجريبيين) كبند منفصل، قولّي.

---

## الخطوة الجاية
زي ما اتسأل في نهاية تقرير دفعة 2: **دفعة 3 من المرحلة 6** (بوابة الموكل
+ الأمان + النسخ الاحتياطي) هي الأخيرة في قسم الأدمن. النسخ الاحتياطي
فيها استعادة كاملة للبيانات (`confirmRestore`) وهي الأخطر في القسم كله
لأنها بتستبدل البيانات الحالية. محتاج قرارك: نبدأ دفعة 3 على طول
(الأمان الأول زي ما هو أسهل وأأمن، والنسخ الاحتياطي آخر حاجة)، ولا نراجع
الأول إزاي نختبر "استعادة نسخة احتياطية" بأمان من غير ما نأثر على بيانات
بيئة التست؟
