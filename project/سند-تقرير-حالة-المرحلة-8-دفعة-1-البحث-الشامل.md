# سند — تقرير حالة: المرحلة 8 (Smoke) — دفعة 1

**التاريخ:** 25 يوليو 2026
**الحالة:** 🟡 جزء منها اكتمل (موثّق تحت بالتفصيل)، والباقي قيد التنفيذ
**المرجع:** `سند-خطة-تنفيذ-اختبارات-E2E-مقسمة-2.md`
**الزيب المرفق:** `sanad-project-updated-final-32.zip`

---

## الخلفية

بعد ما اتأكد إن **المرحلة 7 (Tier 2) خلصت بالكامل** (5 بنود، آخرهم
`ChecklistSection.tsx`)، واتأكد كمان إن البندين 6 و7 من مرحلة 7 الأصلية
(`ClientDetailModal.tsx`، `QuickAddSessionModal.tsx`) بينزلوا لمرحلة 8
كـ"Smoke" زي ما الخطة اقترحت من غير نقاش إضافي — بدأت في مرحلة 8.

قايمة مرحلة 8 الكاملة (12 شاشة): `DashboardTab`، `TeamTab`، تبويبات
التقويم (شهر/أسبوع/فائتة)، `SummaryModal`، `ViewReminderModal`،
`RemindersTab`، `PdfViewerModal`، زائد الخمسة المرحّلة من مرحلة 7
(`ClientDetailModal`، `QuickAddSessionModal`، `AddReminderForm`،
`ArchiveTab` بتاع `dashboard/`، `UniversalSearchModal`).

فحصت الاثنتي عشر شاشة كلها قبل ما أكتب أي كود (نفس منهج بند 5: فحص
الفجوة الحقيقية الأول). طلع اكتشافين مهمين، وخلصت شاشة واحدة كاملة
بالكود + التست. الباقي لسه قيد التنفيذ.

---

## ✅ اللي خلص فعليًا (بكود + تست + تحقق صيغة)

### `UniversalSearchModal.tsx` (البحث الشامل)
كانت من غير أي `data-testid` خالص. اتضاف:
- `universal-search-modal` (حاوية المودال).
- `universal-search-input` (حقل البحث).
- `universal-search-close` (زرار الإغلاق).
- `universal-search-case-result` (كل نتيجة قضية في نتائج البحث).
- `header-search-open` (زرار فتح البحث في الهيدر — `AppHeader.tsx`،
  كان محتاج testid كمان عشان نقدر نفتح المودال أصلاً من واجهة حقيقية).

**تست جديد:** `e2e/universal-search.spec.ts` (تست واحد، Smoke):
إنشاء قضية → فتح البحث الشامل من الهيدر → كتابة جزء من عنوان القضية →
التأكد من ظهور نتيجة القضية فعليًا (بيانات حقيقية، مش شكل فاضي) →
إغلاق المودال → التأكد إن التطبيق رجع لحالته الطبيعية من غير كسر.

اخترنا نتيجة "قضية" بالذات (`matchedCases`) لأنها بتترشّح من الـ`props`
المحمّلة أصلاً في الذاكرة (متزامنة)، عكس نتائج الجلسات/الملاحظات/
المستندات اللي محتاجة round-trip فعلي لقاعدة البيانات (`debounce`) —
اختيار أثبت وأسرع لتست Smoke بسيط زي ما الخطة حددت نطاقه ("مسار واحد
بلا تفرّع خطر").

---

## 🔍 اكتشافين مهمين (فحص الفجوة الحقيقية — قبل الكتابة)

### 1. `QuickAddSessionModal.tsx` — كود ميت فعليًا
بحثت عن أي استدعاء له (`import`/رندر) في كل المشروع — **صفر نتائج**.
الملف مش متستخدم في أي مكان في التطبيق خالص، لا من الداشبورد ولا من
أي شاشة تانية. يعني **مفيش أي طريقة توصله من واجهة المستخدم الفعلية**،
لا بالنقر ولا حتى برابط مباشر. كتابة تست E2E له مستحيلة فعليًا من غير
ما نضيفه هو نفسه لمكان في التطبيق أولًا (خارج نطاق "كتابة تستات" —
ده قرار منتج/كود مش قرار تستنج).

**التوصية:** يتراجع مع الفريق — إما فعلاً المكوّن ده لازم يتوصل بزرار
حقيقي في مكان ما (وبعدين نغطيه)، أو هو بقايا كود قديم اتستبدل بمكوّن
تاني (زي `NewStandaloneSessionModal.tsx` من مرحلة 2) ولازم يتشال.

### 2. `TeamTab.tsx` — تاب موجود بس مش متاح من واجهة التنقل
`'team'` موجود في `TabName` وفي `TAB_PATHS` (`/team`) — يعني الراوتينج
بيدعمه فعليًا. **لكن** فحصت `CommandDock.tsx` (شريط التنقل السفلي بكل
أزراره، بما فيها قايمة "المزيد") ومفيش أي زرار بيودّي لـ`setTab('team')`
— عكس `clients`/`fees`/`documents`/`admin` اللي كلهم موجودين في قايمة
"المزيد". يعني الوصول له من تفاعل مستخدم حقيقي (نقر) **مش متاح حاليًا**،
بس بيفضل قابل للاختبار عن طريق تنقّل مباشر بالرابط (`page.goto('/team')`)
لأنه route حقيقي مدعوم فعليًا في التطبيق — مش نفس فئة `QuickAddSessionModal`
(اللي معندهوش حتى مسار وصول بديل).

**سؤال محتاج قرارك:** نغطي `TeamTab` بتست يعتمد على `page.goto('/team')`
المباشر (تنقّل حقيقي مدعوم في التطبيق، بس مش من زرار)، ولا نأجله برضه
لحد ما يتضاف زرار نقر فعلي زي باقي التابات؟

---

## ⬜ لسه باقي في المرحلة 8 (اتفحص، التعديل لسه ما اتكتبش)

فحصت الملفات دي وحددت بالظبط الـtestid المطلوبة ومكانها، لكن الكود لسه
ما اتعدّلش:
- `ClientDetailModal.tsx` — محتاج testid واحد بس على زرار الإغلاق
  (`client-detail-close`) — كل حاجة تانية (فتح المودال + عرض البيانات)
  متغطاة أصلاً بـ`client-detail-view`/`client-edit-trigger` الموجودين.
- `ViewReminderModal.tsx` — صفر testid، محتاج testid على الحاوية وزرار
  الإغلاق.
- `SummaryModal.tsx` + `FeesTab.tsx` — نفس الشيء، زائد testid على زرار
  فتح الملخص المالي في `FeesTab.tsx`.
- `PdfViewerModal.tsx` — صفر testid على الحاوية/زرار الإغلاق، لكن زرار
  الفتح نفسه (`doc-view-trigger`) موجود بالفعل كأثر جانبي من بند
  DocsSection (مرحلة 7).
- `AddReminderForm.tsx` — عنده testid بالفعل (أثر جانبي من مرحلة 7)،
  بس محتاج تست Smoke مخصص (إنشاء تذكير → فتحه من القائمة → قفل العرض)
  بدل ما يفضل مغطّى ضمنيًا بس كخطوة تجهيز جوّه تست تاني.

**ما اتفحصش لسه خالص:** `ArchiveTab.tsx` (دشبورد/مستندات)، تبويبات
التقويم الثلاثة (`MonthListTab`، `MissedTab`، `CalendarTab`)،
`DashboardTab.tsx` (أكبر ملف في المرحلة، ~500 سطر).

---

## تحقق الصيغة
`tsc --noEmit` على `src/` بالكامل: **صفر أخطاء حقيقية** (نفس تحذير
`baseUrl` deprecation المعروف من نسخة الكومبايلر المحلية 6.0.3 بس).

`tsc --noEmit` على `e2e/` (بنفس الإعدادات + `include: ["e2e"]`): **صفر
أخطاء خالص** بما فيها `universal-search.spec.ts` الجديد — في البيئة
الحالية اللي شغّال فيها الفحص، `@playwright/test` كان قابل للـresolve
فعليًا (مختلف عن ملاحظة بند 5 اللي وصفت بيئة تانية معندهاش الباكدج).

**لازم تتأكد بنفسك قبل الدمج:**
1. `npm run test:e2e -- universal-search.spec.ts`.
2. `npm test` محليًا — تأكيد إن الـ`data-testid` الجديدة في
   `UniversalSearchModal.tsx`/`AppHeader.tsx` مفيهاش أثر جانبي.

---

## الزيب المرفق
`sanad-project-updated-final-32.zip` — فيه كل حاجة من
`sanad-project-updated-final-31.zip` زي ما هي، زائد:
- `e2e/universal-search.spec.ts` (جديد).
- `data-testid` في `UniversalSearchModal.tsx` (5 مواضع) و`AppHeader.tsx`
  (زرار البحث).
- هذا التقرير.

---

## الخطوة الجاية
1. **قرارك في `TeamTab`** (السؤال فوق) — `page.goto('/team')` مباشر،
   ولا تأجيل؟
2. لو موافق، هكمل بنفس الترتيب: `ClientDetailModal` → `ViewReminderModal`
   → `SummaryModal`/`FeesTab` → `PdfViewerModal` → تذكير `AddReminderForm`
   Smoke مخصص → `ArchiveTab` → تبويبات التقويم → `DashboardTab` (الأكبر،
   آخر واحد).
3. اكتشاف `QuickAddSessionModal.tsx` (كود ميت) محتاج قرار منتج منك —
   مش حاجة هقدر أحسمها من جوّه نطاق "كتابة تستات".
