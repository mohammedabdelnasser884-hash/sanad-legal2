# تقرير تدقيق حالة خطة "توحيد قفل الطرف" — 8 أغسطس 2026

> ده مش تقرير تنفيذ جولة جديدة — ده **تدقيق** على الزيب المرفوع
> (`sanad-fix-cases-orphan-lookup-v3-1.zip`) للتأكد فعليًا (بالكود، مش
> بالذاكرة/الافتراض) من حالة المراحل اللي كانت مسجّلة كـ"معلّقة" في
> آخر ملخص عندي (مراحل 3، 5، 6 من خطة توحيد قفل الطرف). طلع إنها كلها
> **منفّذة فعليًا في الكود** — الملخص القديم كان قديم/غير دقيق.

---

## ✅ اتأكد فعليًا (بالـgrep على الكود، مش بس بالتقارير الداخلية)

| البند | مكان التأكيد بالكود |
|---|---|
| مرحلة 3 — `getPartyStateBadge()` (Badges/UI) | `src/shared/parties/partyDomainService.ts:166` — مستخدمة فعليًا في `EditCaseModal.tsx`/`StandaloneSessionDetailModal.tsx` |
| بادج `CasesTab.tsx` (كان موثّق كـ"قرار مفتوح خارج النطاق") | `src/features/dashboard/CasesTab.tsx` — بادج "🟠 موكل محذوف" على كارت القضية، باستخدام `isOrphanedLink`+`getPartyStateBadge('ORPHAN')`، نطاق ضيّق عمدًا (الموكل الأساسي بس، بلا استعلام إضافي) |
| مرحلة 5 — RPC ذرّية `create_fee_with_advance` | ملف الهجرة `database/migrations/sql-migrations-phase5/01-create-fee-with-advance-rpc.sql` موجود + `src/features/fees/hooks/useFeesActions.ts` بينادي `db.rpc('create_fee_with_advance', ...)` فعليًا (سطر ~260) |
| مرحلة 6 — تنبيه فشل تسجيل الجلسة الأولى (إنشاء + تعديل) | `src/features/cases/hooks/useCaseActions.ts:790` — توست `⚠️ التعديل اتحفظ، بس فشل تسجيل الجلسة الجديدة...` موجود في `handleUpdateCase` (وأصل الفيكس في `handleSaveCase` قبله) |
| إصلاح orphan `auth.users` عند حذف مستخدم | `supabase/functions/admin-actions/index.ts` (action: `delete_user`) — بيحذف حساب Auth الأول ثم صف البروفايل، مع فشل صريح (مش صامت) لو حذف Auth فشل. الـclient (`src/features/admin/users/hooks/useAdminUsers.ts:85`) بينادي `admin-actions` بـ`action: 'delete_user'` فعليًا — مش دالة `delete_lawyer` منفصلة زي ما كان مسجّل، الاسم الفعلي `delete_user` جوه `admin-actions` الموحّدة |

**كل البنود دي عندها نفس التنبيه التنفيذي المتكرر في تقاريرها الأصلية
جوه الزيب:** الكود اتراجع منطقيًا يدويًا + فحص صياغة معزول (`tsc`/
`transpileModule`)، لكن **`npm test`/`npm run build` الحقيقيين ما
اتشغلوش** (بيئة التنفيذ بلا نت/`node_modules`). مرحلة 5 كمان محتاجة
تشغيل ملف الـmigration (`01-create-fee-with-advance-rpc.sql`) على
القاعدة الحية قبل النشر.

---

## ⏳ لسه فعلاً مفتوح (مفيش أي تقرير داخل الزيب بيوثّق حل ليهم)

1. **تست Playwright متعثر:** `handleLinkCase` — timeout.
2. **تست Playwright متعثر:** `DashboardTab` — accordion state.

مفيش أي إشارة `test.skip`/`test.fixme` أو تعليق فيكس مرتبط بيهم في
`e2e/` — يعني لسه محتاجين تشخيص فعلي (مش مجرد مراجعة كود).

---

## خلاصة للمتابعة

الخطة الأساسية لـ"توحيد قفل الطرف + فلو الأتعاب" (بمسارتيها) **مكتملة
على مستوى الكود بالكامل** بما فيها الفجوات اللي كانت مؤجّلة (بادج
CasesTab). الباقي كله تحقق محلي عندك (`build`/`test`/migration)، بلا
أي منطق تطبيقي جديد مطلوب على المسار ده.

**التالي:** تشخيص التستين المتعثرين، أو أي ملف/مراجعة تانية عندك.
