# التقرير الشامل — مستقبل "المستندات القانونية" في سند
### من التحليل للنقد للرؤية للتنفيذ — كل حاجة في مكان واحد
*(نسخة 24 — قسم 25 الجديد: إلغاء "عقود" نهائيًا — **آخر تصنيف مفتوح، كل
تصنيفات المستندات القانونية بقت محسومة**)*

**التاريخ:** 25 أغسطس 2026 (آخر تحديث: 26 أغسطس 2026 — "عقود" اتلغى نهائيًا؛ كل تصنيفات مستقبل "المستندات القانونية" محسومة الآن)
**الحالة:** أولوية 1، أولوية 2، أولوية 3 (كل الملفات السبعة)، أولوية 4 (الترتيب حسب
نوع القضية)، وقسم 17 (سجل النشاط + تيليجرام) منفذين بالكامل ✅. **كل قرارات 17.4
الأربعة اتقفلت دلوقتي بالكامل ✅**: بند 6 (صلاحية `can_generate_documents`)، بند 5
(حفظ المستند في حافظة القضية — طلع منفّذ بالفعل، اكتشاف مش شغل جديد)، بند 8 (زرار
قضية مقيّدة/مؤرشفة — اتأكَّد، صفر تغيير)، و**بند 4 (الأوفلاين) نُفّذ بالكامل ✅**
("خيار أ" الموصوف في القسم 17.6 — راجعه للتفاصيل الكاملة والملفات المعدَّلة). الثلاث
SQL migrations (بما فيهم `can_generate_documents`) اتشغّلوا فعليًا على الإنتاج (قسم
18). **🆕 تأكيد يدوي (`npm run build` + `npm test`/E2E) اتحل فعليًا — راجع قسم 36**:
جيمي رفع لوجات CI حقيقية (نفس الكوميت `4b3dec0`) — unit tests 1066/1066 ✅، build
(lint+tsc+vite) سليم بالكامل ✅، E2E 104/106 (2 skipped مش مرتبطين بالتقرير) ✅.
راجع القسم 16/17 لتفاصيل كل ملف وقايمة test IDs الكاملة.
**🆕 تحديث 26 أغسطس 2026 (سادس جلسة — نهائي):** أولوية 7 ("تجهيز ملف تسوية
أسرة") **اتلغت بالكامل ونهائيًا**. بعد جلسات سكوفولد وتوثيق حقول (راجع
19.1–19.3.2)، جيمي اكتشف إن كل محكمة/مكتب تسوية ليه نموذج رسمي مختلف ومش
بيقبل حتى نسخة مطابقة، يعني الفيتشر عمليًا ملوش قيمة استخدام حقيقية. كل
الكود اتشال، وميجريشن رولباك (`02-family-settlement-rollback.sql`) موجودة
تشيل الجدول من الإنتاج. راجع قسم 19.5 للتفاصيل الكاملة.
**🆕 تحديث 26 أغسطس 2026 (مساءً):** كل القرارات المفتوحة المتبقية من قسم 12/13/17.4
اتحسمت **مبدئيًا** (سجل القرارات، بنود 3–8) عشان نقدر نكمل التنفيذ من غير توقف —
دي قرارات سريعة، مش تقفيل نهائي كامل. هنرجعلها في النهاية (بعد القرارات الأكبر:
نسخ المكتب، حزم المستندات، المحتوى القانوني الفعلي) لمراجعة أدق.
**🆕 تحديث 26 أغسطس 2026 (ليلاً):** التنفيذ الفعلي لقرارات 17.4 بدأ. بنود 5/6/8
اتقفلت، بند 4 كان قيد التنفيذ بعد ما التحليل الفعلي للكود كشف إن العملية أعقد من
الافتراض الأصلي.
**🆕 تحديث 26 أغسطس 2026 (ليلاً، تكملة):** بند 4 اتقفل بالكامل — الـ4 خطوات
المخطَّطة في القسم 17.6 اتنفَّذت (كاش محلي + تعديل `useGenerateDocument.ts` +
توسيع `DbWriteTable` + قفل زرار التصدير في شاشة المعاينة)، مع تحديث تستات
`useGenerateDocument.test.ts` (موك جديد لـ`resolveTemplateVersion`/
`getCurrentTenantId`/`__dbWrite` + 4 تستات جديدة للسيناريوهات الأربعة: كاش
موجود/كاش غير موجود × تحميل الفورم/التوليد الفعلي). فحص نحوي كامل (نفس منهج بند
6: `node --experimental-strip-types --check`) سليم على كل الملفات المعدَّلة. تشغيل
`npm test`/`npm run build` الحقيقيين لسه مطلوب منك — نفس قيد السندبوكس من الأول.
**🆕 تحديث 26 أغسطس 2026 (تاني جلسة):** جيمي حسم قرارين مباشرين (خارج جلسة قسم 14
اللي كانت متوقّعة لأولوية 6):
1. **حزم المستندات (أولوية 6) اتلغت نهائيًا** — مش مؤجّلة، مش هتترجع تاني كفكرة
   ضمن نطاق هذا التقرير. راجع قسم 18.1.
2. **SourceModeSelector بقت واجبة الظهور دايمًا** — تخطيها التلقائي القديم لما
   المستخدم داخل من قضية مفتوحة (القسم 9.5 الأصلي) اتلغى بالكامل ونُفّذ الكود
   الفعلي. راجع قسم 18.2 للتفاصيل والملفات المعدَّلة.
**🆕 تحديث 26 أغسطس 2026 (رابع جلسة):** التنفيذ الفعلي لإزالة "توكيلات" من الإنتاج (قسم 23.3) اتقفل.
الأربعة ملفات كود اتعدّلوا فعليًا بعد فحص مباشر لكل مرجع حقيقي في الكودبيز، وmigration جديدة
(`sql-migrations-phase8/01-remove-tawkilat-category.sql`) اتكتبت بطريقة **archive مش delete** — بتحل
نقطة القرار الحرجة اللي كانت مفتوحة في 23.3 بند 3 من غير ما تحتاج تأكيد إضافي منك (مفيش أي مستند
قديم بيتلمس). راجع قسم 26 الجديد للتفاصيل الكاملة. **لسه محتاج منك:** تشغيل الـmigration فعليًا على
الإنتاج + تأكيد `npm run build`/`npm test` (نفس قيد السندبوكس المعتاد).

**نطاق التقرير:** تصميم تجربة الاستخدام والمعمارية العامة. **محتوى/صياغة كل قالب مستند هيتحدد في
جلسة عمل منفصلة بينا** — أي اسم مستند أو حقل جوه التقرير ده هو للتوضيح بس، مش نص نهائي.

---

## الفهرس

1. الهدف من التقرير
2. تحليل الوضع الحالي في سند
3. نقد صريح للتصميم المرجعي
4. مبادئ التصميم الحاكمة
5. الرؤية: أفكار أصيلة مش موجودة في المرجع
6. البنية المعمارية المستهدفة (شاشة بشاشة)
7. تعيين التصنيفات (Category Mapping)
8. دليل مسارات الاستخدام بالتفصيل
9. التغييرات التقنية المطلوبة (ملفات، مكونات، backend)
10. خطة التنفيذ على مراحل بالأولوية
11. المخاطر والاعتبارات
12. قرارات مفتوحة محتاجة تأكيدك
13. مراجعة ذاتية نقدية — إيه الناقص عشان التقرير يبقى كامل
14. الخطوة الجاية
15. سجل القرارات المؤكدة (بيتحدّث أول بأول)
16. سجل التنفيذ الفعلي (بيتحدّث بعد كل ملف — أولوية 3 وما بعدها)
17. إغلاق فجوتين من المراجعة الذاتية (سجل النشاط + تيليجرام + قرارات 17.4)
18. قرارات جيمي المباشرة (26 أغسطس 2026، تاني جلسة) — إلغاء الحزم + SourceModeSelector واجبة
19. أولوية 7 — تجهيز ملف تسوية أسرة (26 أغسطس 2026، تالت جلسة) — بدء التنفيذ (اتلغت بالكامل، راجع 19.5)
20. قسم 14 — جلسة تحديد محتوى تصنيف "إعلانات" (26 أغسطس 2026) — **مقفول بالكامل** (6 نماذج موثّقة)، توثيق فقط، صفر تنفيذ كود
21. تصنيف "عرائض" (26 أغسطس 2026) — **مقفول توثيقيًا بنموذج فاضي واحد**؛ الصيغ الجاهزة الشائعة مؤجَّلة
22. تصنيفَي "إنذارات" و"جنح مباشرة" (26 أغسطس 2026) — **مقفولين توثيقيًا**، نفس تقسيمة "إعلانات/عرائض"، نموذج فاضي واحد لكل تصنيف
23. إلغاء "أشكال"/"تظلمات" نهائيًا + قرار إزالة "توكيلات" من الإنتاج (26 أغسطس 2026) — خطة تنفيذ مبدئية، تنفيذ فعلي مؤجَّل
24. تصنيف "طلبات" — النموذج الموحَّد (26 أغسطس 2026) — **مقفول توثيقيًا**
25. إلغاء "عقود" نهائيًا (26 أغسطس 2026) — **آخر تصنيف مفتوح؛ كل التصنيفات محسومة الآن**
26. تنفيذ إزالة "توكيلات" من الإنتاج (26 أغسطس 2026، رابع جلسة) — **✅ الكود جاهز، محتاج تشغيل الـmigration منك**

---

## 1. الهدف من التقرير

جيمي زوّد لقطات شاشة من تطبيق تاني كمرجع بصري لإعادة تصميم قسم "المستندات القانونية" في سند. بدل ما
نقلّد الشكل ده حرفيًا، التقرير ده بيعمل 3 حاجات مع بعض:

- **يحلل** الكود الفعلي لسند دلوقتي (مش افتراضات).
- **ينقد** التصميم المرجعي بصراحة — بياخد اللي كويس، ويرفض اللي خطر في سياق قانوني.
- **يضيف** أفكار مش موجودة في المرجع خالص، مبنية على واقع استخدام محامي مصري فعلي.

---

## 2. تحليل الوضع الحالي في سند

### الفلو الحالي (4 خطوات، من غير Stepper بصري)

```
TemplatePicker (شبكة قوالب + dropdown تصنيف + بحث)
  → SourceModeSelector (قضية مفتوحة / إدخال يدوي / نموذج فاضي)
    → DynamicFieldsForm (تعبئة الحقول)
      → DocumentPreviewEditor (المعاينة والتصدير)
```

الملف المسؤول: `src/pages/LegalDocumentsPage.tsx` — state machine بسيطة (`type Step = 'templates' |
'sourceMode' | 'fields' | 'preview'`).

### التصنيفات الموجودة فعليًا

`src/features/documentGeneration/hooks/useDocumentTemplates.ts`:
```ts
export const DOCUMENT_CATEGORIES = ['إنذارات', 'توكيلات', 'عرائض', 'طلبات'] as const;
```
4 تصنيفات بس، كل واحد فيه **قالب واحد** مزروع فعليًا (migration
`sql-migrations-phase7/02-document-generation-seed.sql`)، وكل قالب له **نسخة منشورة واحدة بالظبط**
(`current_published_version_id`).

### مسارين دخول موجودين

1. **من CaseDetailView** (`initialCaseId` موجود): بيدخل على `TemplatePicker` مباشرة، وبعد اختيار
   القالب بيتخطى `SourceModeSelector` تمامًا (`sourceMode='case_bound'` تلقائي) — قرار مقصود (القسم
   9.5 من الخطة الأصلية).
2. **من الصفحة نفسها** (`initialCaseId=null`): زرار "+ مستند جديد" بيفتح `SourceModeSelector` **قبل**
   اختيار القالب — قرار موثّق في الكود نفسه كـ"غير مؤكد" لأنه بيعارض ترتيب "القسم/المستند الأول".
   *(✅ محسوم — سجل القرارات بند 2: الزرار ده هيتشال بالكامل.)*

### قيود مهمة في الـ backend

- `document_templates.category` عمود `varchar(100)` **من غير CHECK constraint** — إضافة تصنيفات
  جديدة مايحتاجش migration على الـ schema، بس محتاج seed data فعلية وإلا هيفضل التصنيف فاضي.
- **مفيش تعدد نسخ فعلي لأي قالب حاليًا** — نسخة واحدة منشورة بس لكل قالب، وفيه unique index بيمنع
  أكتر من نسخة `published` لنفس القالب (`idx_one_published_version_per_template`).

---

## 3. نقد صريح للتصميم المرجعي

### 3.1 خطير ولازم يتشال تمامًا: "النسخة العشوائية"

العبارة "سيتم اختيار نسخة عشوائية من النسخ المتاحة" هي **أخطر فكرة في التصميم المرجعي**. في سياق
قانوني:

- لو عندك أكتر من صياغة لنفس المستند (مختصرة/مفصّلة مثلاً)، والمحامي مش بيختار هو أنهي واحدة، ممكن
  يطبع ويقدم للمحكمة صياغة مختلفة عن اللي كان ناوي يستخدمها من غير ما ياخد باله.
- المحاماة مبنية على **الدقة والتحكم في الصياغة**، مش variety عشوائي زي تطبيقات المحتوى العامة.
- أي خطأ ناتج عن صياغة "طلعت عشوائي" مسؤوليته على المحامي، وسند بيبقى السبب.

**البديل المقترح — "نسخ المكتب" (Office Variants):** ~~كل مكتب يحفظ صياغته المفضلة لنفس نوع
المستند...~~ *(تم تأجيل هذا البديل بالكامل لمرحلة منفصلة — راجع سجل القرارات بند 1. القرار النهائي
لهذه المرحلة: نسخة واحدة ثابتة لكل قالب، زي الوضع الحالي بالظبط، من غير أي عشوائية.)*

### 3.2 كويس ومقتبس: التصفح بالتصنيف

فكرة "قسم → مستند" بتحل مشكلة حقيقية: لما يكبر عدد المستندات، شبكة واحدة كبيرة (زي سند دلوقتي)
بتبقى مرهقة. التصنيف الأول بيقلل المجال بسرعة — هنستخدمها.

### 3.3 ناقصة تمامًا في المرجع: مفيش ذكاء في الوصول للمستند

التصميم المرجعي بالكامل تصفح يدوي بحت — لازم تعرف بالظبط في انهي قسم المستند اللي عايزه. مفيش أي
مساعدة توصلك للمستند الصح. دي أكبر فرصة ضايعة (تفصيلها في القسم 5).

### 3.4 محدودة في المرجع: المايك مربوط بفيتشر واحد بس

المايك في "تجهيز ملف تسوية أسرة" فكرة ممتازة، بس مقصورة على فيتشر واحد. محامي بيملي بيانات وهو ماشي
أو قاعد قدام موكل محتاج المايك في **كل** فورم بيانات طويل، مش بس في الفيتشر ده.

---

## 4. مبادئ التصميم الحاكمة

1. **السرعة أهم من الجمال** — الاستخدام غالبًا وسط جلسة أو مكالمة، مش تصفح هادي.
2. **التحكم أهم من التلقائية** — أي اقتراح ذكي لازم يفضل قابل للتعديل والرفض بسهولة.
3. **التخصيص بيتراكم مع الوقت** — كل مكتب له أسلوب صياغة؛ النظام يتعلّم منه بدل ما يفرض نمط واحد.
4. **مفيش عشوائية في مستند قانوني** — أي تنويع لازم يكون قرار واعي من المحامي.
5. **الموبايل أول حاجة** — الاستخدام الفعلي هيبقى غالبًا من التليفون وسط الشغل.

---

## 5. الرؤية: أفكار أصيلة مش موجودة في المرجع

### 5.1 البحث والاقتراح الذكي بدل التصفح فقط

> 🔒 **قرار مؤكد (سجل القرارات، بند 1):** الجزء الخاص بـ"نسخ المكتب" في الفقرة دي **اتأجل بالكامل**
> لمرحلة مستقبلية منفصلة، بعد الانتهاء من التصميم الحالي خالص. النظام هيمشي دلوقتي بنسخة واحدة ثابتة
> لكل قالب (زي الوضع الحالي بالظبط) — إحنا بس شايلين فكرة "النسخة العشوائية" من التصميم المرجعي من
> غير ما نستبدلها بأي بديل دلوقتي. باقي الفقرة (البحث الموحّد، الترتيب حسب نوع القضية) لسه سارية.

**المشكلة:** محامي عنده قضية نفقة عايز "دعوى نفقة زوجية" بيفكر بالموقف ("موكلتي عايزة نفقة")، مش
بيفكر "أنا محتاج أدخل قسم عرائض وأدور في قايمة 15 اسم".

**الحل:** شريط بحث موحّد فوق شبكة التصنيفات، بيدوّر في أسماء **كل** المستندات مهما كان تصنيفهم:

- كتابة "نفقة" → يطلع "دعوى نفقة زوجية بأنواعها" و"دعوى نفقة مصاريف مدارس" (عرائض) + "جنحة امتناع
  عن أداء النفقة" (جنح مباشرة) — من تصنيفات مختلفة في نفس القايمة.
- مفيش تعقيد تقني إضافي — `getActiveTemplates()` أصلًا بيجيب كل القوالب دفعة واحدة، فده مجرد فلترة
  client-side على `name_ar` + `description` عبر كل التصنيفات مش بس المختار.
- **بديل اختياري**، مش إجباري — التصنيفات تفضل موجودة للي بيحب التصفح.

**خطوة أبعد — ترتيب حسب نوع القضية:** لو داخل توليد مستند من جوه قضية مفتوحة (`hasCaseContext`)،
النظام يرتّب المستندات الأكتر ارتباطًا بـ`case_type` بتاع القضية **أول القايمة** (مش يخفي الباقي).
ده mapping ثابت بسيط بين نوع القضية وتصنيفات المستندات، خفيف ومفهوم، من غير أي API خارجي أو latency.

### 5.2 حزم المستندات (Document Bundles) — ⛔ اتلغت نهائيًا (قرار جيمي، 26 أغسطس 2026)

> ⛔ **الفكرة دي اتلغت بالكامل من نطاق التقرير — راجع قسم 18.1 للقرار الكامل.**
> النص الأصلي تحت فاضل موجود كتوثيق تاريخي بس (إيه اللي كان مقترح وليه)، مش خطة
> فعلية حاليًا.

قضايا كتير محتاجة **مجموعة** مستندات مع بعض، مش مستند واحد. مثال من خبرتك: قضية نفقة زوجية غالبًا
محتاجة صحيفة الدعوى + توكيل + إعلان مع بعض. بدل توليدهم 3 مرات منفصلة، حزمة واحدة بتاخدك عليهم
بالترتيب (اختيار المصدر مرة واحدة، والبيانات المشتركة زي اسم الموكل بتتملى تلقائيًا في الـ3).

~~محتاج جدول جديد (`document_bundles` + `document_bundle_items`)، فهي مرحلة لاحقة منفصلة~~ —
ملغاة، مفيش جدول جديد هيتضاف لأجلها.

### 5.3 تعميم المايك (مش تقصيره على فيتشر واحد)

بدل ما المايك يبقى خاص بـ"تجهيز ملف تسوية أسرة" بس، يبقى **قدرة عامة**:

- مكوّن واحد `VoiceInputField` (wrapper حوالين أي `<input>`/`<textarea>` موجود، مش تغيير جذري)
  بيستخدم `Web Speech API` — مفيش تكلفة API خارجية.
- أول تطبيق ليه في `DynamicFieldsForm.tsx` الموجود فعليًا (حقول `textarea` طويلة زي "موضوع الإنذار")
  — مش لازم ننتظر فيتشر تسوية الأسرة عشان نستفيد.
- تحسين بسيط نسبيًا (component واحد قابل لإعادة الاستخدام) بعائد كبير على الاستخدام اليومي.

---

## 6. البنية المعمارية المستهدفة (شاشة بشاشة)

```
شاشة المستندات القانونية (الصفحة الرئيسية)
│
├── شريط بحث موحّد (اختياري — يقفز مباشرة لنتائج من كل التصنيفات)
│
├── ① القسم — شبكة كاردز (الأكثر استخدامًا للمكتب أول حاجة، بعدين باقي التصنيفات)
│
├── ② المستند — قايمة داخل التصنيف المختار (أو نتائج بحث)
│     └── لو جاي من قضية مفتوحة: ترتيب حسب صلة نوع القضية
│
├── ③ المصدر — من قضية مفتوحة / إدخال يدوي / نموذج فاضي (زي ما هو دلوقتي)
│
├── ④ البيانات — فورم ديناميكي + VoiceInputField على الحقول الطويلة
│
└── ⑤ المعاينة — تعديل مباشر + تصدير
```
*(السطر الخاص بـ"أضف مستند تاني للحزمة" اتشال — فكرة الحزم ملغاة، قسم 18.1)*

**"حافظة مستندات"** كارت منفصل تمامًا بره الويزارد ده — بينقّل مباشرة لتاب المستندات الموجود
(`ArchiveTab`)، مش بيولّد حاجة (تفصيله في القسم 8.2).

**"تجهيز ملف تسوية أسرة"** (فورم المايك + PDF من التصميم المرجعي) **مش جزء من هذا التقرير** — فيتشر
منفصل تمامًا هيتبنى كمرحلة تالية بعد ما البنية دي تتنفذ (schema مختلف بالكامل عن `document_templates`،
فورم حر مش قالب بحقول ثابتة).

---

## 7. تعيين التصنيفات (Category Mapping)

من الـ 10 كاردز اللي ظهرت في التصميم المرجعي:

| المجموعة | الكاردز | القرار |
|---|---|---|
| **تصنيفات قوالب حقيقية** (تدخل الويزارد) | إنذارات، توكيلات، عرائض، طلبات (موجودين فعليًا) + إعلانات، أشكال، تظلمات، جنح مباشرة، عقود (جداد) | تتضاف كـ `DOCUMENT_CATEGORIES` — 9 تصنيفات إجمالي |
| **تنقّل مباشر لفيتشر موجود** | حافظة مستندات | كارت بيودّي مباشرة لـ`ArchiveTab` — مش بيدخل الويزارد خالص |
| **فيتشر مستقل غير موجود** | تجهيز ملف تسوية أسرة | خارج نطاق هذا التقرير — مرحلة منفصلة تالية |

---

## 8. دليل مسارات الاستخدام بالتفصيل

*(توثيق تجربة الاستخدام فقط — بيوضّح المحامي بيضغط إيه وبيشوف إيه، مش صياغة قانونية)*

### 8.1 المسار العام (النمط الموحّد لكل مستند)

أي مستند في سند بيعدي بنفس الـ5 خطوات بالظبط. الفرق بين مستند وتاني هو بس: (أ) في انهي تصنيف
هيتلاقي، و(ب) أسماء الحقول في خطوة "البيانات".

- **① القسم:** شبكة كاردز (إنذارات، توكيلات، عرائض، طلبات، إعلانات، أشكال، تظلمات، جنح مباشرة، عقود)
  + كارت "حافظة مستندات" منفصل. دوس على التصنيف، أو استخدم البحث الموحّد لو عارف الاسم بالظبط.
- **② المستند:** قايمة بأسماء مستندات التصنيف المختار بس + خانة بحث لو القايمة طويلة.
- **③ المصدر:** 3 خيارات — *من قضية مفتوحة* (البيانات بتتملى تلقائيًا)، *إدخال يدوي*، أو *نموذج
  فاضي* للطباعة والتعبئة بخط اليد. *(لو داخل أصلًا من جوه قضية مفتوحة، الخطوة دي بتتخطى تلقائيًا.)*
- **④ البيانات:** فورم بحقول المستند ده بس؛ أي حقل اتملى تلقائيًا من القضية يقدر يتعدّل. الزرار
  معطّل لحد ما الحقول المطلوبة تتملى.
- **⑤ المعاينة:** شكل المستند النهائي بشعار المكتب، قابل للتعديل المباشر قبل التصدير PDF/طباعة.

### 8.2 مسار "حافظة مستندات" (مختلف جوهريًا)

ده **مش توليد مستند** — ده أرشفة مستند جاهز عندك بالفعل (عقد موقّع، حكم صادر، مستند رسمي استلمته).
المسار الفعلي (موجود بالفعل في سند دلوقتي، موثّق من الكود الحالي):

1. دوس كارت "حافظة مستندات" → تروح لتاب "المستندات" مباشرة (بره الويزارد بالكامل).
2. تشوف قايمة كل المستندات المؤرشفة، بفلترة بالتصنيف وبحث بالاسم.
3. دوس "رفع مستند" → فورم صغير: اختر الملف من جهازك، اديله اسم/تصنيف، واختياريًا **اربطه بقضية
   موجودة** من قايمة منسدلة (أو سيبه "غير مرتبط بقضية").
4. دوس "☁️ رفع في نظام سند" → الملف بيتخزن ويظهر في القايمة فورًا.
5. بعد كده تقدر تعاين المستند (لو صورة/PDF) بضغطة، أو تحذفه لو غلط.

**الفرق الجوهري:** هنا المحامي هو اللي جايب المستند من بره، سند بس بيحفظه وينظّمه ويربطه بالقضية.

### 8.3 مثال تفصيلي كامل: توليد "صحيفة دعوى مبسطة" (تصنيف عرائض)

**① القسم:** دوس كارت "عرائض".
**② المستند:** تفتح قايمة مستندات عرائض، دوس "صحيفة دعوى مبسطة".
**③ المصدر:** عندك قضية موكل مفتوحة بخصوص الموضوع، فاختار "من قضية مفتوحة" → دوّر باسم الموكل أو
رقم القضية → دوس عليها.
**④ البيانات:** فورم بالحقول دي (القالب الحالي المزروع فعليًا):

| الحقل | مصدره | اتملى تلقائي؟ |
|---|---|---|
| اسم المحكمة | يكتبه المحامي | لأ |
| اسم الموكل (المدعي) | من بيانات القضية | **آه** |
| رقم القضية | من بيانات القضية | **آه** |
| الوقائع | يكتبها المحامي (نص طويل) | لأ |
| الطلبات | يكتبها المحامي (نص طويل) | لأ |
| اسم المكتب | من إعدادات المكتب | **آه** (اختياري) |

بتكتب الوقائع والطلبات، وتراجع باقي الحقول الجاهزة، وتدوس "التالي" (الزرار متفعّلش غير لما كل
المطلوب يتملى).

**⑤ المعاينة:** تشوف صحيفة الدعوى كاملة بشعار مكتبك، تعدّل أي حاجة أخيرة مباشرة في المعاينة، وتصدّرها
PDF جاهزة للطباعة والتقديم.

### 8.4 أمثلة مختصرة (لإثبات ثبات النمط)

- **إنذار على يد محضر** (إنذارات): القسم → المستند → المصدر → البيانات (اسم المنذَر إليه + موضوع
  الإنذار، والباقي تلقائي لو من قضية) → المعاينة والتصدير.
- **توكيل عام** (توكيلات): نفس المسار، البيانات هنا: اسم المحامي الموكَّل + نطاق التوكيل (نص حر).
- **التصنيفات الجديدة** (إعلانات، أشكال، تظلمات، جنح مباشرة، عقود): نفس المسار بالضبط — الفرق الوحيد
  إن أسماء المستندات وحقولها **لسه محتاجة نتفق عليها مع بعض** (القسم 13).

---

## 9. التغييرات التقنية المطلوبة

### ملفات جديدة

| الملف | الغرض | الحالة |
|---|---|---|
| `src/shared/ui/Stepper.tsx` | مكوّن عام لعرض 5 دوائر مرقّمة RTL بحالة نشط/منجز/لسه — قابل لإعادة الاستخدام في أي ويزارد تاني بسند | ✅ منفذ |
| `src/features/documentGeneration/components/CategoryPicker.tsx` | شبكة كاردز التصنيفات (خطوة "القسم") — شامل كارت "حافظة مستندات" اللي بينقّل بره الويزارد + شريط البحث الموحّد الجديد (راجع القسم 16 لتفاصيل قرار مكانه) | ✅ منفذ |
| `src/features/documentGeneration/components/TemplatePicker/TemplateRow.tsx` | صف كامل العرض لعرض مستند واحد جوه تصنيف مقفول (خطوة "المستند") | ✅ منفذ |
| `src/shared/ui/VoiceInputField.tsx` | Wrapper عام لأي حقل نصي بمايك (Web Speech API) — متوصّل فعليًا في `DynamicFieldsForm.tsx` على كل حقول `textarea` | ✅ منفذ (أولوية 2 خلصت قبل ما نبدأ أولوية 3) |

### ملفات معدّلة

| الملف | التعديل | الحالة |
|---|---|---|
| `useDocumentTemplates.ts` | توسيع `DOCUMENT_CATEGORIES` من 4 لـ 9 + قبول `initialCategory` اختياري بدل `'الكل'` الثابتة | ✅ منفذ |
| `TemplatePicker.tsx` | prop اختياري `lockedCategory` — لما يكون موجود: يخفي dropdown التصنيف ويعرض صفوف (`TemplateRow`) بدل شبكة. لما مايكونش موجود (مسار hasCaseContext): يفضل زي ما هو بالظبط من غير أي تغيير | ✅ منفذ |
| `LegalDocumentsPage.tsx` | إضافة خطوة `'categories'` أول الـ state machine + دمج `Stepper` + **حذف زرار "+ مستند جديد" القديم نهائيًا** (✅ محسوم — سجل القرارات بند 2؛ البديل: شريط البحث الموحّد فوق شبكة التصنيفات يغطي نفس الاحتياج بترتيب متسق) | ⏳ لسه |
| `TemplateCard.tsx` | توسيع `CATEGORY_BADGE_STYLE` لتغطية التصنيفات الجديدة (مستخدم بس في مسار hasCaseContext) | ⏳ لسه |

### منطق الدخول

- **من CaseDetailView:** زي ما هو — يتخطى خطوة "القسم" بالكامل، يدخل على "المستند" مباشرة (كل
  التصنيفات)، والـ Stepper بيعلّم "القسم" منجزة تلقائيًا.
- **من الصفحة نفسها:** يبدأ من "القسم" دايمًا. زرار الرجوع في كل خطوة يرجّع للي قبلها بدون فقد الاختيارات.

### تغييرات الـ Backend/Seed

مفيش تغيير schema (`category` مرن أصلًا). المطلوب:
1. Migration جديد يزرع على الأقل قالب عيّنة واحد تحت كل تصنيف جديد (إعلانات/أشكال/تظلمات/جنح
   مباشرة/عقود) — عشان الكارت ميبقاش فاضي لما يتفتح.
2. **قرار مطلوب:** نضيف قوالب حقيقية بمحتوى كامل من دلوقتي، ولا نكتفي بعيّنات placeholder لحد ما
   نجهّز المحتوى القانوني الفعلي (شغل محتوى منفصل، القسم 13)؟

---

## 10. خطة التنفيذ على مراحل بالأولوية

| الأولوية | العنصر | ليه هنا بالذات | الحجم |
|---|---|---|---|
| **1** | شريط البحث الموحّد فوق الكل | أكبر تحسين فعلي في السرعة اليومية، وأرخص حاجة تقنيًا | صغيرة |
| **2** | `VoiceInputField` عام | عائد كبير، مستقل عن أي فيتشر تاني، ممكن يتوصّل بسرعة | صغيرة–متوسطة |
| **3** | إعادة تصميم الويزارد بالتصنيفات (القسم/المستند/المصدر/البيانات/المعاينة) | الأساس البصري المتفق عليه | متوسطة |
| **4** | ترتيب حسب نوع القضية | تحسين تدريجي فوق الويزارد، مش يغيّره | صغيرة |
| **5** | "نسخ المكتب" بدل النسخة العشوائية | ⛔ **مؤجّلة بالكامل** (سجل القرارات بند 1) — مرحلة مستقلة منفصلة بعد الانتهاء من كل ما سبق، مش جزء من هذا الروادماب | متوسطة–كبيرة (schema جديد) |
| **6** | ~~حزم المستندات~~ | ⛔ **اتلغت نهائيًا** (قرار جيمي، 26 أغسطس 2026 — قسم 18.1) — مش مؤجّلة، برّه نطاق التقرير تمامًا | — |
| **7** | فيتشر "تجهيز ملف تسوية أسرة" (⏳ بدأت — قسم 19؛ **بدون مايك** بقرار بي) | مرحلة مستقلة كاملة بعد كل ما فات | كبيرة |

**الترتيب المقترح للتنفيذ الفعلي:** 1 → 2 → 3 (المكونات التقنية بالقسم 9) → 4 → ثم 7 كمشروع
مستقل لاحقًا. البند 5 ("نسخ المكتب") مؤجّل بالكامل — مش جزء من أي ترتيب تنفيذ حاليًا (راجع سجل
القرارات بند 1). البند 6 ("حزم المستندات") ملغي نهائيًا — مش هيرجع كبند في أي ترتيب تنفيذ لاحق.

---

## 11. المخاطر والاعتبارات

- **المسؤولية القانونية:** أي فيتشر بيقترح مستند أو صياغة تلقائيًا (ترتيب حسب نوع القضية، اقتراح
  نسخة سابقة) لازم يفضل **اقتراح قابل للتجاهل**، مش قرار نهائي — المحامي هو المسؤول قانونيًا عن
  المحتوى النهائي دايمًا.
- **دقة الترتيب الذكي:** الـ mapping بين `case_type` وتصنيفات المستندات لازم يكون مبني على خبرتك
  المهنية انت بالذات، مش تخمين.
- **VoiceInputField ودقة اللهجة المصرية:** `Web Speech API` جودته بتختلف حسب المتصفح، خصوصًا على
  الموبايل — لازم اختبار فعلي على مصطلحات قانونية باللهجة المصرية ("نفقة"، "استئناف") قبل اعتباره
  جاهز.

---

## 12. قرارات مفتوحة محتاجة تأكيدك

1. ✅ **[محسوم — سجل القرارات بند 1]** ~~"نسخ المكتب" بدل العشوائية...~~ تم تأجيلها بالكامل لمرحلة
   منفصلة بعد الانتهاء من كل التصميم الحالي. نمشي بنسخة واحدة ثابتة لكل قالب.
2. ✅ **[محسوم — سجل القرارات بند 2]** ~~موافق على حذف زرار "+ مستند جديد" القديم بالكامل؟~~ نعم،
   هيتشال بالكامل — البحث الموحّد فوق شبكة التصنيفات بيغطي نفس الاحتياج بترتيب متسق.
3. ⚡ **[مبدئي — سجل القرارات بند 3]** ~~التصنيفات الخمسة الجديدة — نبدأ بعيّنة placeholder...~~
   نعم، placeholder واحد لكل تصنيف دلوقتي، والمحتوى الحقيقي بعدين على مراحل (قسم 14).

---

## 13. مراجعة ذاتية نقدية — إيه الناقص عشان التقرير يبقى كامل

القسم ده هو التقرير بيراجع نفسه بـ3 عيون مختلفة: مطوّر (تفاصيل تنفيذ)، مهندس (قرارات معمارية)،
مستخدم (تجربة فعلية). كل نقطة هنا حقيقية ومبنية على حاجات موجودة بالفعل في سند (مش تخمين)، والتقرير
اللي فات كان ساكت عنها.

### 13.1 بعين المطوّر — تفاصيل تنفيذ ناقصة

1. **الأوفلاين مش متذكور خالص.** سند فيه بنية Offline Queue كاملة (اتبنت وتوسّعت على مراحل)،
   و`ArchiveTab` نفسه فيه `createFetchGuard` وcache محلي لكل تصنيف. توليد مستند من قضية أوفلاين —
   هيشتغل ولا هيتقفل؟ لو المحامي في المحكمة وسط الجلسة ومفيش نت، ده أكتر وقت محتمل يحتاج يولّد مستند
   فيه بالظبط. القرار ده لازم يتحدد صراحة، مش يتسكت عنه.
2. **الصلاحيات (RBAC) مش متذكورة.** سند عنده نظام صلاحيات فعلي (`isAdminRole()`,
   `PERMISSION_KEYS`, أدوار lawyer/viewer). هل توليد المستندات مفتوح لكل الأدوار ولا محتاج permission
   key مخصوص (زي `can_generate_documents`)؟ لو مكتب فيه محامي مساعد بصلاحية viewer، مفروض يقدر يولّد
   مستندات ولا لأ؟
3. **حفظ المسودة أثناء التعبئة.** سند عنده نمط راسخ (`useFormDraft`) بيحفظ مسودة أي فورم تلقائيًا
   عشان لو المحامي اتقاطع (مكالمة، حد داخل عليه) ميضيعش اللي كتبه. خطوة "البيانات" في الويزارد الجديد
   (خصوصًا لو فيها نص طويل زي الوقائع والطلبات) **لازم** يبقى فيها نفس الحماية دي، ومتذكرتش في
   التقرير الأصلي خالص.
4. **تأكيد قبل فقد التقدّم.** سند بدّل `window.confirm` بـ`UnsavedChangesConfirmModal` مخصص في كل
   المودالز التانية. لو المحامي داس رجوع/إغلاق وسط الويزارد وعنده بيانات متملية، لازم نفس التأكيد ده
   يظهر — التقرير الأصلي افترض إن "زرار الرجوع يرجّع للخطوة اللي قبلها" من غير ما يقول إيه اللي بيحصل
   للبيانات المتملية.
5. **بعد التصدير — إيه اللي بيحصل بالظبط؟** التقرير وصف "بتصدّرها PDF" بس ماوضحش: المستند المولّد
   بيتحفظ تلقائيًا في "حافظة مستندات" بتاعة القضية دي (فيه `CaseDocumentLink` type موجود فعليًا في
   الكود بيوحي إن ده المفروض يحصل)، ولا بيبقى ملف منفصل المحامي لازم يرفعه هو بنفسه لو عايز يأرشفه؟
   الفرق ده مهم جدًا للمستخدم ولازم يتحدد بوضوح.
6. **تأثير التصميم الجديد على الاختبارات الموجودة.** سند فيه E2E suite ضخمة (Playwright) بتغطي
   المستندات، من ضمنها اختبارات صلاحيات وسيناريوهات desktop/mobile. أي تغيير في بنية الخطوات
   (`doc-gen-search-input`, `doc-gen-category-filter`) هيكسر أي test موجود بيعتمد على السلوك القديم
   (شبكة واحدة بلا خطوة تصنيف). التقرير محتاج قايمة صريحة بالـtest IDs المتأثرة قبل أي تنفيذ.
7. **حالات الفراغ والخطأ مش موصوفة.** إيه اللي المحامي بيشوفه لو: دخل تصنيف مفيش تحته ولا مستند
   واحد لسه (التصنيفات الجديدة قبل ما يتزرع فيها محتوى)؟ البحث الموحّد مرجعش نتايج؟ توليد المستند
   فشل (مشكلة شبكة، صلاحية)؟ دي كلها شاشات لازم تتصمم، مش تتسيب فاضية.

### 13.2 بعين المهندس — قرارات معمارية ناقصة

1. **"نسخ المكتب" بتتعارض مع قرار معماري موجود فعليًا.** الكود الحالي فيه تعليق صريح إن **مفيش شاشة
   لإدارة القوالب في هذه الخطة إطلاقًا** (`templatesApi.ts`). فكرة "نسخ المكتب" (القسم 5.1) معناها
   ضمنيًا إن المحامي هيقدر يحفظ نسخته المعدّلة كـ"افتراضية" ليه — وده **فعليًا شاشة إدارة قوالب
   جزئية**، حتى لو مش كاملة. لازم نعترف إن الفكرة دي بتفتح باب كان مقفول عن قصد، ونصمم الحد الأدنى
   المطلوب ليها (مين يقدر يحفظ نسخة مكتب؟ فين بتتخزن؟ إزاي بترجع تاني؟).
2. **"الأكثر استخدامًا" محتاج مصدر بيانات مش موجود.** ترتيب الكاردز حسب الاستخدام (القسم 6) بيفترض
   إن فيه عداد استخدام لكل قالب لكل مكتب. ده مش موجود دلوقتي — محتاج يتحدد: عداد جديد مخصوص، ولا
   استعلام على عدد المستندات المولّدة فعليًا لكل `template_id` (لو موجود جدول `generated_documents`)؟
3. **البحث الموحّد اتوصف بـ"semantic matching" بشكل مبالغ فيه.** التقرير الأصلي قال البحث هيشتغل
   بمطابقة دلالية مش بس نصية، بس فعليًا وصف تنفيذه كـ"فلترة client-side على النص" — ده **مطابقة نص
   بسيطة، مش semantic فعلي**. الحقيقة الأهم: سند عنده بالفعل edge function اسمه `embed-query`
   (موجود ومختبر) ممكن يتستخدم لبحث دلالي حقيقي (يفهم "نفقة" و"إعالة" كمعنى قريب حتى لو مفيش تطابق
   حرفي) — دي فرصة حقيقية مش مجرد وصف تسويقي، ولازم تتحدد كقرار: نبدأ بمطابقة نص بسيطة (أرخص)، ولا
   نستخدم `embed-query` من الأول (أدق بس فيه تكلفة/زمن استجابة إضافي)؟
4. **قابلية التوسع لو القوالب زادت بكتير.** `getActiveTemplates()` بيجيب كل القوالب دفعة واحدة
   ويفلتر client-side — ده تمام لعدد قوالب صغير (عشرات)، بس لو كل مكتب بدأ يضيف نسخه الخاصة (نتيجة
   فكرة "نسخ المكتب")، العدد ممكن يكبر بسرعة. لازم حد أدنى معلوم (مثلاً 500 قالب) نبدأ بعده نفكر في
   فلترة من السيرفر بدل التحميل الكامل.
5. **عزل المستأجرين (Tenant Isolation) للتصنيفات الجديدة.** لازم تأكيد صريح إن الـseed بتاع
   التصنيفات الخمسة الجديدة هتتزرع كـ"قوالب نظام" (`tenant_id IS NULL`) متاحة لكل المكاتب، مش مربوطة
   بمكتب معيّن بالغلط.

### 13.3 بعين المستخدم — حاجات في تجربة الاستخدام محتاجة توضيح

1. **غيّرت رأيك في نوع المستند نفسه وسط التعبئة؟** لو المحامي وصل لخطوة "البيانات" وقرر إن المستند
   ده مش اللي محتاجه فعلاً (فيه واحد أنسب في نفس التصنيف)، هل في طريقة سريعة يرجع لقايمة "المستند"
   من غير ما يفقد البيانات اللي اتملت أصلاً لو الحقول متشابهة (زي اسم الموكل ورقم القضية)؟ التقرير
   الأصلي بيفترض رحلة خطية بس، من غير معالجة لتغيير الرأي.
2. **أول استخدام لمكتب جديد.** فكرة "الأكثر استخدامًا أول القايمة" (القسم 6) هتعرض إيه لمكتب لسه
   بادئ من الصفر ومفيش له تاريخ استخدام؟ لازم ترتيب افتراضي منطقي (مثلاً حسب تكرار الاستخدام عبر كل
   سند، مش بس المكتب ده) لحد ما يتراكم تاريخ فعلي.
3. **مكتب فيه أكتر من محامي.** "نسخ المكتب" (القسم 5.1) قال "بيفتكر آخر نسخة استخدمها **المحامي ده
   بالذات**" — ده يعني الذاكرة على مستوى المستخدم الفرد، مش المكتب ككل. لو ده مقصود، لازم يتوضح في
   التسمية نفسها ("نسخة المحامي" مش "نسخة المكتب")؛ ولو المقصود إن كل المكتب يشارك نفس التفضيل
   (مفيد لو المكتب عايز اتساق في الصياغة بين المحامين)، ده قرار مختلف تمامًا لازم يتحدد بوضوح.
4. **ربط المستند المولّد بتذكيرات/جلسات.** سند عنده فيتشر تنبيهات جلسات (session-alerts) قائم
   بالفعل. فرصة مش مستغلة: لو عندك جلسة بكرة ومحتاج مذكرة، ممكن تنبيه استباقي "عندك جلسة بكرة —
   محتاج تجهّز مذكرة؟" يودّي مباشرة لقسم المستندات بالسياق المناسب. ده مش جزء أساسي من الخطة، بس
   فرصة تحسين حقيقية تستاهل تتسجل كفكرة مستقبلية.
5. **إيه اللي بيتفقد فعليًا لو قفلت الويزارد فجأة؟** غير موضوع التأكيد قبل الفقد (13.1 بند 4)،
   المستخدم لازم يعرف بوضوح: لو خرج من نص الرحلة، هل في مكان يرجع يكمل منه تاني (مسودة محفوظة)، ولا
   لازم يبدأ من الأول تمامًا؟ دي معلومة تجربة استخدام أساسية مش تفصيلة تقنية بس.

## 14. الخطوة الجاية

بعد ما نتفق على القرارات المفتوحة (القسم 12) + القرارات الإضافية اللي طلعت من المراجعة الذاتية
(القسم 13)، الخطوة الجاية هي جلسة عمل منفصلة بينا نحدد فيها:

1. **قايمة المستندات الفعلية** تحت كل تصنيف جديد (إعلانات/أشكال/تظلمات/جنح مباشرة/عقود).
2. **حقول كل مستند** (إيه اللي المحامي هيكتبه بنفسه، وإيه اللي هيتملى تلقائيًا من القضية).
3. **الصياغة القانونية الكاملة** لكل مستند — ده شغل محتوى مباشر بينا، مبني على خبرتك، مش تصميم واجهة.

---

## 15. سجل القرارات المؤكدة

*(القسم ده بيتحدّث أول بأول كل ما نحسم سؤال من قسم 12/13. كل بند بيوضح السؤال، القرار النهائي،
وتاريخ الحسم.)*

### بند 1 — نظام النسخ: "نسخ المكتب" ولا نسخة واحدة ثابتة؟ ✅ محسوم (25 أغسطس 2026)

**السؤال:** التصميم المرجعي كان فيه "نسخة عشوائية" رُفضت لخطورتها القانونية (قسم 3.1). البديل
المقترح كان "نسخ المكتب" — المحامي يحفظ صياغته المفضلة والنظام يفتكرها. لكن الفكرة دي بتتعارض مع
قرار معماري موجود بالفعل في كود سند: **"مفيش شاشة لإدارة القوالب في هذه الخطة إطلاقًا"** (قسم 13.2).

**القرار:** "نسخ المكتب" **اتأجلت بالكامل** لمرحلة مستقبلية منفصلة، تتحدد تفاصيلها بعد ما ننهي
التصميم الحالي خالص. المرحلة الحالية بتمشي بنسخة واحدة ثابتة لكل قالب — زي الوضع الحالي بالظبط،
من غير أي عشوائية. الأثر بتاع القرار ده على باقي التقرير:
- قسم 5.1 — الجزء الخاص بـ"نسخ المكتب" اتحط عليه علامة تأجيل صريحة.
- قسم 10 (خطة التنفيذ) — بند "نسخ المكتب" بقى ⛔ مؤجّل، وطلع بره ترتيب التنفيذ الحالي.
- قسم 12 — البند ده بقى ✅ محسوم، مبقاش سؤال مفتوح.

**السبب:** فتح باب "إدارة القوالب" شغل كبير (مين يعدّل؟ إزاي بيتحفظ؟ إزاي يرجع للأصل لو غلط؟)
ومستاهلش يتقرر بسرعة وسط خطة إعادة تصميم واجهة. الأولوية دلوقتي: نلغي الخطر (العشوائية) بس، من غير
ما نستعجل ببديل معقّد.

### بند 2 — زرار "+ مستند جديد" القديم: يتشال ولا يتصلح؟ ✅ محسوم (25 أغسطس 2026)

**السؤال:** الزرار ده كان بيفتح `SourceModeSelector` (اختيار المصدر) **قبل** اختيار المستند نفسه —
ترتيب معكوس عن الويزارد الجديد (القسم → المستند → المصدر)، وموثّق في الكود الأصلي كـ"قرار غير مؤكد".
الاحتمالين كانوا: يتشال تمامًا، أو يتسيب كاختصار بس بترتيب مُصلَّح.

**القرار:** يتشال بالكامل. مع وجود شريط البحث الموحّد (أولوية 1 في خطة التنفيذ، قسم 10) فوق شبكة
التصنيفات، أي حد عارف اسم المستند بالظبط هيكتبه في البحث ويوصله على طول — نفس فايدة الزرار القديم،
من غير ترتيب مختلف يلخبط المستخدم. الأثر على باقي التقرير:
- قسم 2 (تحليل الوضع الحالي) — اتحط عليه علامة "محسوم" ووُصِل بسجل القرارات.
- قسم 9 (التغييرات التقنية) — `LegalDocumentsPage.tsx` هيتضمن حذف الزرار كجزء صريح من التعديل.
- قسم 12 — البند ده بقى ✅ محسوم.

**السبب:** الاحتفاظ بزرارين بترتيبين مختلفين (واحد بيمشي بالتصنيف، وواحد بيقفز على المصدر) هيلخبط
المستخدم أكتر ما يفيده. مسار دخول واحد متسق (القسم دايمًا) أوضح وأسهل يتعلّم.

### بند 3 — Seed التصنيفات الخمسة الجديدة: placeholder ولا من غير seed؟ ⚡ مبدئي (26 أغسطس 2026)

> ⚡ **ملاحظة على طبيعة القرار:** ده قرار مبدئي وسريع اتاخد عشان نكمل نمشي في التنفيذ، مش قرار نهائي
> مقفّل. هنرجعله تاني في النهاية (بعد ما باقي القرارات المفتوحة الأكبر — نسخ المكتب، حزم المستندات،
> المحتوى القانوني الفعلي — تتقفل) عشان نراجعه بشكل كامل وأدق قبل أي إنتاج فعلي.

**السؤال:** (قسم 9، قسم 12 بند 3) لما نزرع التصنيفات الخمسة الجديدة (إعلانات/أشكال/تظلمات/جنح
مباشرة/عقود)، نضيف قالب placeholder واحد لكل واحدة عشان الكارت ميبقاش فاضي لما يتفتح، ولا نسيبها من
غير seed خالص لحد ما نتفق على المحتوى القانوني الفعلي (القسم 14)؟

**القرار:** نزرع قالب placeholder واحد لكل تصنيف من الخمسة دلوقتي — عشان تجربة التصفح تبقى كاملة
وملموسة (مفيش كارت فاضي)، على إن المحتوى القانوني الفعلي لكل مستند يتحدد لاحقًا في جلسة عمل منفصلة
(القسم 14) ويستبدل الـplaceholder وقتها. الأثر:
- قسم 9 (تغييرات الـBackend/Seed) — Migration الـseed هيتنفذ بالـplaceholder دلوقتي، مش هيتأجل.
- قسم 16 (سجل التنفيذ) — القرار المفتوح رقم 1 هناك اتحسم بعكس الميل المبدئي المسجّل فيه (كان
  "من غير seed وهمي")؛ اتغيّر بعد نقاش مباشر مع صاحب القرار.

**السبب:** كارت تصنيف فاضي تمامًا وقت العرض الأول بيدي انطباع إن الفيتشر ناقص/عطلان، مش إنه لسه
محتاج محتوى. Placeholder واضح المصدر (مسمّى بوضوح إنه مؤقت) بيحل المشكلة البصرية من غير ما يوهم حد
إنه جاهز للاستخدام الفعلي.

### بند 4 — توليد مستند وإحنا أوفلاين: نسمح ولا نقفل الزرار؟ ✅ نُفّذ (26 أغسطس 2026)

**السؤال:** (قسم 13.1 بند 1) سند فيه بنية Offline Queue كاملة. توليد مستند من قضية مفتوحة والمحامي
أوفلاين (زي وسط جلسة محكمة، أكتر وقت محتمل يحتاج المستند فيه بالظبط) — هيشتغل عن طريق الـqueue، ولا
هيتقفل لحد ما يرجع النت؟

**القرار النهائي المنفَّذ ("خيار أ" — راجع القسم 17.6 للتفاصيل الكاملة):** نسمح جزئيًا — التخزين نعم
(المسودة بتتبني محليًا وتتقيّد في نفس Offline Queue)، التصدير الفوري لأ (زرار PDF/Word يتقفل لحد ما
يتزامن). القرار الأصلي ("يشتغل زي قضية/جلسة جديدة أوفلاين، بلا استثناء") اتعدّل بعد اكتشاف تقني: توليد
المستند سلسلة قراءات (نسخة القالب + حقولها + بيانات القضية) قبل أي إدراج، والتصدير نفسه بيعيد قراءة
الصف بالـid الحقيقي من السيرفر — التفاصيل والتنفيذ الكامل في القسم 17.6.

### بند 5 — بعد التصدير: يتحفظ تلقائيًا في حافظة القضية ولا يفضل منفصل؟ ⚡ مبدئي (26 أغسطس 2026)

> ⚡ **ملاحظة على طبيعة القرار:** ده قرار مبدئي وسريع اتاخد عشان نكمل نمشي في التنفيذ، مش قرار نهائي
> مقفّل. هنرجعله تاني في النهاية عشان نراجعه بشكل كامل وأدق قبل أي إنتاج فعلي.


**السؤال:** (قسم 13.1 بند 5) بعد ما المحامي يصدّر PDF، هل المستند المولّد بيتربط تلقائيًا بحافظة
مستندات القضية (`CaseDocumentLink`)، ولا يفضل ملف منفصل والمحامي لازم يرفعه هو بنفسه لو عايز
يؤرشفه (زي مسار "حافظة مستندات" اليدوي، قسم 8.2)؟

**القرار:** يتحفظ تلقائيًا ومربوط بالقضية — مفيش خطوة رفع يدوي إضافي مطلوبة من المحامي بعد التوليد
من قضية مفتوحة.

**السبب:** مستند اتولّد من جوه قضية مفتوحة أصلًا معروف انتماءه لها من اللحظة الأولى؛ تحميل المحامي
خطوة رفع يدوي إضافي لحاجة النظام عارفها فعليًا بيضيف احتكاك من غير فايدة. (المسار اليدوي في قسم 8.2
يفضل زي ما هو — ده لمستندات جايه من بره سند أصلًا، مش لمستند اتولّد من جواه.)

### بند 6 — مفتاح صلاحية `can_generate_documents`: مين يقدر يولّد؟ ⚡ مبدئي (26 أغسطس 2026)

> ⚡ **ملاحظة على طبيعة القرار:** ده قرار مبدئي وسريع اتاخد عشان نكمل نمشي في التنفيذ، مش قرار نهائي
> مقفّل. هنرجعله تاني في النهاية عشان نراجعه بشكل كامل وأدق قبل أي إنتاج فعلي.


**السؤال:** (قسم 13.1 بند 2 / قسم 17.4 بند 1) البنية التحتية لنظام الصلاحيات التفصيلية جاهزة
(`permissions.ts` + `has_permission()` على القاعدة). توليد المستندات يبقى مفتوح لكل الأدوار
(زي `can_add_cases`)، مقفول للمحامي/الأدمن بس بلا استثناء (زي القفل الأساسي بتاع `can_view_fees`)،
ولا متاح افتراضيًا بس قابل للسحب لدور معينة (زي `can_edit_fees`)؟

**القرار:** مقفول لدوري lawyer/admin بس، بلا استثناء — دور `viewer` ما يقدرش يولّد مستندات نهائيًا،
حتى لو أدمن حاول يفتحها له بقيمة صريحة في `profiles.permissions`. نفس نمط القفل الأساسي بتاع
`can_view_fees`/`can_edit_fees` (فحص الدور بيسبق أي قيمة صريحة محفوظة)، مش نمط `can_edit_fees`
الافتراضي القابل للتخصيص.

**الأثر التقني (يتحدد وقت التنفيذ):**
- `permissions.ts`: إضافة `'can_generate_documents'` لمصفوفة `PERMISSION_KEYS`، وإضافة فحص القفل
  الأساسي ليها في `checkPermission()` بجانب `can_view_fees`/`can_edit_fees` (سطر الـ`if` مباشرة)،
  بدل الاعتماد على `ROLE_DEFAULT_PERMISSIONS` وحدها.
- `has-permission-function.sql`: نفس التعديل بالظبط على `has_permission()` (السطر اللي فيه
  `p_key IN ('can_view_fees', 'can_edit_fees')` يتوسّع لـ`can_generate_documents`) — لازم
  الاتنين (فرونت إند + RLS) يتزامنوا زي ما التعليق التوثيقي في `permissions.ts` بيحذّر.
- زرار "توليد مستند" (من `CaseDetailView` والصفحة المستقلة) يتقفل/يتخفي لدور `viewer`.

**السبب:** توليد مستند قانوني بيحمل نفس وزن المسؤولية القانونية اللي "الأتعاب" بتحمله تقريبًا —
مستند خارج من سند بشعار المكتب واسمه، فمنطقي يتقفل بنفس صرامة `can_view_fees` بدل ما يتسيب قابل
للتخصيص الفردي.

### بند 7 — البحث الموحّد: مطابقة نص بسيطة ولا `embed-query` دلالي؟ ⚡ مبدئي (26 أغسطس 2026)

> ⚡ **ملاحظة على طبيعة القرار:** ده قرار مبدئي وسريع اتاخد عشان نكمل نمشي في التنفيذ، مش قرار نهائي
> مقفّل. هنرجعله تاني في النهاية عشان نراجعه بشكل كامل وأدق قبل أي إنتاج فعلي.


**السؤال:** (قسم 13.2 بند 3) البحث الموحّد الحالي مطابقة نص بسيطة (`name_ar`/`description`)، مش
semantic فعلي رغم إن التقرير الأصلي وصفه بمصطلحات semantic. سند عنده `embed-query` edge function
جاهزة ممكن تدّي بحث دلالي حقيقي (يفهم "نفقة"/"إعالة" كمعنى قريب). نبدأ بالبسيط، ولا نربط
`embed-query` من الأول؟

**القرار:** نفضّل بمطابقة النص البسيطة الموجودة فعليًا دلوقتي. `embed-query` **مؤجّل**، مش جزء من
النطاق الحالي.

**السبب:** أرخص، بدون latency إضافي، وموجودة وشغالة بالفعل (`TemplatePicker.tsx`/`CategoryPicker.tsx`).
عدد القوالب الحالي صغير (تحديدًا لسه أقل من عشرات) فمفيش فايدة ملموسة من بحث دلالي دلوقتي — نفس منطق
قسم 13.2 بند 4 (حد أدنى معلوم قبل أي تعقيد سيرفر إضافي). يتراجع فيه لاحقًا لو عدد القوالب/التنويعات
كبر فعلاً (خصوصًا لو "نسخ المكتب" المؤجّلة، بند 1، اتفعّلت يومًا).

### بند 8 — زرار "توليد مستند" جوه قضية مقيّدة/مؤرشفة: يتقفل ولا يفضل متاح؟ ⚡ مبدئي (26 أغسطس 2026)

> ⚡ **ملاحظة على طبيعة القرار:** ده قرار مبدئي وسريع اتاخد عشان نكمل نمشي في التنفيذ، مش قرار نهائي
> مقفّل. هنرجعله تاني في النهاية عشان نراجعه بشكل كامل وأدق قبل أي إنتاج فعلي.


**السؤال:** (قسم 17.4 بند 2) هل زرار "توليد مستند" جوه `CaseDetailView` لقضية مقيّدة/مؤرشفة لازم
يتقفل زي باقي أزرار التعديل (تعديل بيانات القضية، إضافة جلسة...)، ولا يفضل متاح لأن التوليد مش
تعديل على القضية نفسها؟

**القرار:** يفضل متاح — مفيش قفل جديد على زرار "توليد مستند" لقضية مقيّدة/مؤرشفة.

**السبب:** توليد مستند بيقرأ من بيانات القضية (اسم الموكل، رقم القضية...) من غير ما يعدّل أي حقل
فيها — نفس منطق باقي أزرار "القراءة/الإخراج" (زي المعاينة والتصدير) اللي فضلت متاحة في القضايا
المقيّدة، عكس أزرار "التعديل" الفعلي. قضية مؤرشفة أو مقيّدة غالبًا بالظبط الوقت اللي محامي محتاج
يطلع منها مستند نهائي (مذكرة ختامية، إلخ) — قفلها هنا هيمنع استخدام مشروع.

**ملحوظة تنفيذ:** يحتاج فحص فعلي لمكان الزرار بالظبط في `CaseDetailView.tsx` (زي ما قسم 17.4 بند 2
الأصلي حذّر) للتأكد إنه مش لاصق بمنطق قفل التعديل العام أصلًا بشكل بيصعب فصله.

---

## 16. سجل التنفيذ الفعلي

*(القسم ده بيتحدّث بعد كل ملف يتسلّم فعليًا أثناء أولوية 3 وما بعدها — مصدر الحقيقة
الوحيد لحالة التنفيذ، بدل تقارير تسليم منفصلة متفرقة.)*

### أولوية 1 (شريط البحث الموحّد) وأولوية 2 (`VoiceInputField`)

اتأكد بالمراجعة المباشرة للكود (مش افتراض) إنهم **منفذين بالكامل** قبل ما نبدأ أولوية
3: البحث الموحّد شغال في `TemplatePicker.tsx` (`doc-gen-search-input`)، و
`VoiceInputField` متوصّل فعليًا على كل حقول `textarea` في `DynamicFieldsForm.tsx`.
الملاحظة الوحيدة: مفيش e2e test لسلوك المايك نفسه لحد دلوقتي (مؤجّل بقرارك).

### أولوية 3 — إعادة تصميم الويزارد بالتصنيفات

**1. `src/shared/ui/Stepper.tsx` (جديد) — ✅ 26 أغسطس 2026**
مكوّن عام، بياخد `steps` + `currentStepIndex` بس. القرار بتاع "إيه اللي منجز" بالكامل
مسؤولية المكوّن الأب — مهم عشان مسار الدخول من قضية مفتوحة بيتخطى خطوة "القسم"
تلقائيًا (لازم يظهر منجز من غير ما المستخدم يختارها فعليًا).

**2. `src/features/documentGeneration/components/CategoryPicker.tsx` (جديد) — ✅ 26 أغسطس 2026**
شاشة "① القسم" — شبكة كاردز التصنيفات (9، من `DOCUMENT_CATEGORIES`) + كارت "حافظة
مستندات" منفصل (بينقّل بس، مش بيولّد — القسم 8.2) + شريط بحث موحّد جديد.

**⚠️ قرار تصميم اتخد أثناء التنفيذ (مستنتج من رسم القسم 6، مش سؤال مفتوح، لكن
موثّق هنا عشان يبقى واضح ليك):** شريط البحث الموحّد الجديد اتحط في
`CategoryPicker.tsx` نفسه (فوق شبكة التصنيفات) — مش في `TemplatePicker.tsx`. سبب
القرار: البحث لازم "يقفز مباشرة لنتايج من كل التصنيفات" *قبل* ما المستخدم يختار
تصنيف، وده بالظبط اللي عوّض زرار "+ مستند جديد" القديم (سجل القرارات بند 2).

**الأثر على البحث الموحّد القديم (أولوية 1):** فضل زي ما هو تمامًا من غير أي
تعديل — لسه موجود وشغال في `TemplatePicker.tsx`، وهيفضل يظهر بس في مسار الدخول من
قضية مفتوحة (`hasCaseContext`) اللي بيتخطى شاشة "القسم" بالكامل (زي قسم 9 "منطق
الدخول"). يعني صفر تغيير في سلوك مسار القضية المفتوحة، وصفر خطر على
`e2e/document-generation.spec.ts` الموجود — هيتأكد فعليًا بس بعد ما نخلص كل
الملفات ونقدر نراجع/نشغّل تحقق شامل.

**تكرار كود مقبول:** فيه تكرار بسيط (~15 سطر) في عرض شبكة/حالات تحميل بين
`CategoryPicker.tsx` و`TemplatePicker.tsx` — مقبول، متسق مع إن الكودبيز مش بيعمل
abstraction زيادة عن اللزوم في أماكن مشابهة تانية.

**3. `src/features/documentGeneration/components/TemplatePicker/TemplateRow.tsx` (جديد) — ✅ 26 أغسطس 2026**
صف كامل العرض لخطوة "② المستند" جوه تصنيف مقفول — بديل شبكة `TemplateCard` في
المسار الجديد. من غير بادچ تصنيف (مش لازم، التصنيف أصلًا معروف ومقفول). سهم رجوع
RTL يوضّح إنه قابل للضغط، زي أسلوب `CategoryPicker`/`TemplateCard` الحاليين.

**4. `useDocumentTemplates.ts` (تعديل) — ✅ 26 أغسطس 2026**
`DOCUMENT_CATEGORIES` وسّعت من 4 لـ9 (نفس ترتيب جدول قسم 7 بالظبط). الهوك بقى
بياخد `initialCategory` اختياري (`useDocumentTemplates(initialCategory?)`) — لو
مش متمرر، السلوك زي ما كان تمامًا (`'الكل'` افتراضي). التعديل ده تحضير لملف 5
(`TemplatePicker.tsx` — `lockedCategory`) اللي هيستخدمه، فمفيش أي مستهلك حالي
(`TemplatePicker.tsx`/`CategoryPicker.tsx`) اتأثر لأنهم بينادوا الهوك من غير
argument أصلًا.

**5. `TemplatePicker.tsx` (تعديل) — ✅ 26 أغسطس 2026**
prop اختياري جديد `lockedCategory`. **لما مش موجود** (مسار `hasCaseContext` من
CaseDetailView): الكود **متطابق حرفيًا** مع السلوك القديم — نفس الـtestids
(`doc-gen-search-input`, `doc-gen-category-filter`)، نفس شريط البحث/الفلتر، نفس
شبكة `TemplateCard`. **لما موجود** (المسار الجديد من `CategoryPicker`): يخفي شريط
البحث ودروب-داون التصنيف بالكامل (مفيش داعي، أصلًا مقفول)، وبيعرض `TemplateRow`
(صفوف) بدل شبكة، مفلترة على التصنيف ده بس عن طريق تمرير `lockedCategory`
كـ`initialCategory` لـ`useDocumentTemplates` (ملف 4). حالة القايمة الفارغة ليها
نص مختلف في الوضع المقفول ("مفيش مستندات في التصنيف ده لسه") من غير زرار "امسح
الفلاتر" (مش منطقي في وضع مقفول).

### قرارات لسه مفتوحة (من ضمن التنفيذ، بالإضافة لقسم 12/13)

1. ⚡ **[مبدئي — سجل القرارات بند 3]** ~~Seed التصنيفات الخمسة الجديدة...~~ placeholder واحد لكل
   تصنيف جديد، دلوقتي.
2. **خانة بحث محلية جوه تصنيف مقفول** (قسم 8.1 — "لو القايمة طويلة") — مش هتتنفذ
   في هذه الدفعة (كل تصنيف حاليًا قالب واحد بس، مفيش داعي فعلي دلوقتي). هتتسجل
   كتحسين مؤجل. *(لسه مفتوح — مش جزء من دفعة القرارات دي.)*

**6. `LegalDocumentsPage.tsx` (تعديل) — ✅ 26 أغسطس 2026**
خطوة `'categories'` اتضافت أول الـ state machine (`Step` بقت 5 قيم بدل 4)،
`Stepper` (ملف 1) متوصّل فوق كل الويزارد بـ`STEP_ORDER` ثابت — نفس المنطق
المستنتج وقت بناء `Stepper.tsx` نفسه: `currentStepIndex` مبني على ترتيب
الخطوات بس، فلما مسار `hasCaseContext` يتخطى `'categories'` (وكمان
`'sourceMode'` زي ما كان)، الخطوتين ديه بيظهروا "منجزين" تلقائيًا من غير
أي state إضافي. زرار "+ مستند جديد" القديم (`doc-gen-new-document-btn`)
وكل منطق `pendingSourceModeOnly` المرتبط بيه **اتشالوا بالكامل** — البحث
الموحّد الجديد في `CategoryPicker` (ملف 2) بيغطي نفس الاحتياج (سجل
القرارات بند 2). ربط فعلي:
- `hasCaseContext=true`: يبدأ من `'templates'` مباشرة من غير `lockedCategory`
  (كل التصنيفات، زي ما كان بالظبط قبل أولوية 3).
- `hasCaseContext=false`: يبدأ من `'categories'`. اختيار كارت تصنيف
  (`onSelectCategory`) → `'templates'` بـ`lockedCategory` مقفول. نتيجة
  بحث موحّد من جوه `CategoryPicker` (`onSelectTemplate`) بتقفز مباشرة
  لـ`'sourceMode'`/`'fields'` زي أي اختيار قالب تاني، من غير المرور
  بخطوة "المستند" خالص. كارت "حافظة مستندات" (`onOpenArchive`) بينادي
  `nav.navigateTo('documents')` — اتأكد بالكود إن تاب `'documents'` في
  `App.tsx` بيرندر `ArchiveTab` فعليًا (`DocsTab = React.createElement(ArchiveTab, ...)`)،
  يعني القسم 8.2 (تنقّل بره الويزارد، مش توليد) شغال بالظبط زي الموصوف.
- زرار رجوع جديد (`doc-gen-back-to-categories-btn`) من خطوة "المستند"
  لخطوة "القسم" — بيصفّر `selectedCategory` و`selectedTemplate` معًا.

**7. `TemplateCard.tsx` (تعديل) — ✅ 26 أغسطس 2026**
`CATEGORY_BADGE_STYLE` اتوسّعت من 4 لـ9 مدخلات، بنفس الألوان المستخدمة في
`CategoryPicker.tsx` (`CATEGORY_CARD_STYLE`) عشان بادچ التصنيف على الكارت
(مسار `hasCaseContext`) يبقى متسق بصريًا مع كارت التصنيف نفسه (مسار
التصفح العادي). الـfallback الرمادي (`bg-white/10 text-slate-300`) فضل
زي ما هو لأي تصنيف مستقبلي غير متسجّل.

**تحديث تست (`LegalDocumentsPage.test.tsx`):** التست القديم كان بيفترض إن
اختيار قالب من غير `initialCaseId` بيدخل على `TemplatePicker` مباشرة —
ده بقى غلط بعد ملف 6 (بيبدأ من `CategoryPicker` دلوقتي). اتعدّل التست
عشان يمر بخطوة اختيار التصنيف الأول، واتضافوا 5 تستات جديدة (`describe`
منفصل) بتغطي: البداية من `CategoryPicker`، تخطي "القسم" كامل لمسار
`hasCaseContext`، قفزة نتيجة البحث الموحّد، تنقّل كارت "حافظة مستندات"،
وزرار الرجوع لـ"القسم". **ملحوظة صريحة:** ده تشغيل يدوي/منطقي بس — مفيش
`node_modules` ولا نت في السندبوكس (`npm install` مرفوض شبكيًا) فمقدرتش
أشغّل `npm test` أو `npm run build` فعليًا؛ التستات مكتوبة ومتأكد منها
بمراجعة الأنواع والـmocks يدويًا بس محتاجة تشغيل فعلي عندك قبل الدمج.

### الباقي: صفر — قسم 9 (7 ملفات) اتقفل بالكامل ✅

كل الملفات السبعة المذكورة في قسم 9 منفذة. الخطوة الجاية: **تأكيد يدوي
كامل عندك** — تشغيل `npm run build` (بيعمل `lint` + `tsc --noEmit` +
`vite build` مع بعض) و`npm test`/E2E فعليًا، لأن السندبوكس ده مفيهوش نت
ولا `node_modules` مثبتة. لو أي حاجة فشلت في build/lint هرجعلها فورًا.

**قايمة test IDs جديدة/متأثرة كاملة (لأي تحديث E2E مستقبلي):**

| test ID | الملف | ملاحظة |
|---|---|---|
| `doc-gen-stepper` | `LegalDocumentsPage.tsx` (عبر `Stepper.tsx`) | جديد — الحاوية العامة للـStepper |
| `doc-gen-stepper-circle-categories` … `-preview` | `Stepper.tsx` | جديد — 5 دوائر، `testId` مركّب |
| `doc-gen-category-search-input` | `CategoryPicker.tsx` | جديد — البحث الموحّد الأساسي (خطوة "القسم") |
| `doc-gen-category-grid` | `CategoryPicker.tsx` | جديد |
| `doc-gen-category-card-<اسم التصنيف>` | `CategoryPicker.tsx` | جديد — 9 كاردز |
| `doc-gen-archive-card` | `CategoryPicker.tsx` | جديد — كارت "حافظة مستندات" |
| `doc-gen-template-row-<template.id>` | `TemplateRow.tsx` | جديد — خطوة "المستند" جوه تصنيف مقفول |
| `doc-gen-back-to-categories-btn` | `LegalDocumentsPage.tsx` | جديد |
| `doc-gen-new-document-btn` | ~~`LegalDocumentsPage.tsx`~~ | **اتشال بالكامل** — أي E2E test بيدوّر عليه هيفشل، لازم يتشال |
| `doc-gen-search-input` | `TemplatePicker.tsx` | **بلا تغيير** — لسه شغال بس في مسار `hasCaseContext` (بدون `lockedCategory`) |
| `doc-gen-category-filter` | `TemplatePicker.tsx` | **بلا تغيير** — نفس الملاحظة أعلاه |
| `doc-gen-template-card-<template.id>` | `TemplateCard.tsx` | **بلا تغيير** في الـtestid نفسه — لكن الألوان (بادچ) اتوسّعت لـ9 تصنيفات |

أي `e2e/document-generation.spec.ts` قديم بيعتمد على إن الصفحة بتبدأ
بشبكة قوالب مباشرة (من غير خطوة تصنيف) هيحتاج تحديث لمسار `hasCaseContext=false` — مسار `hasCaseContext=true` (من CaseDetailView) لسه
بنفس السلوك القديم تمامًا.

### أولوية 4 — الترتيب حسب نوع القضية (القسم 5.1)

**8. `src/features/documentGeneration/lib/caseTypeCategoryPriority.ts` (جديد) — ✅ 26 أغسطس 2026**
دالة pure واحدة (`getCategoryPriorityForCaseType`) بتاخد `case_type` نص حر
وبترجّع array مرتّبة من التصنيفات الأكتر صلة (أو فاضية لو مفيش تطابق).

**✅ محسوم — 26 أغسطس 2026 (مساءً):** جيمي راجع جدول `RULES` بصفته محامي
استئناف واعتمده كما هو (نفس الـmapping المقترح، من غير أي تعديل). التحذير
اللي كان هنا خلاص اتقفل — الجدول مش مسودة تخمينية بعد كده، ده قرار مهني
معتمد. علّمت الملف نفسه (`caseTypeCategoryPriority.ts`) بنفس الاعتماد.

⚠️ اكتشاف مهم أثناء التنفيذ: `case_type` (`cases.type` في الفرونت إند)
عمود **نص حر (`text`)** في سند، مفيش `enum` ولا dropdown ثابت
(`NewCaseModal.tsx` → حقل "تصنيف الدعوى" `input` عادي). يبقى المطابقة
بالضرورة `substring/keyword` مرنة، مش تطابق حرفي كامل على قايمة مقفولة.

**9. `useDocumentTemplates.ts` (تعديل إضافي) — ✅ 26 أغسطس 2026**
باراميتر اختياري تاني `categoryPriority` بعد `initialCategory` — بيرتّب
`filteredTemplates` بس لما `category === 'الكل'` (مفيش `lockedCategory`)
ومفيش بحث فعّال، عن طريق `Array.prototype.sort` **مستقر** (stable، ES2019+)
فالقوالب اللي مالهاش أولوية بتفضل بترتيبها الأصلي بعد اللي ليها أولوية —
**ترتيب مش فلترة**، زي ما القسم 5.1 نص بالظبط.

**10. `TemplatePicker.tsx` (تعديل إضافي) — ✅ 26 أغسطس 2026**
prop اختياري `categoryPriority` بيتمرر مباشرة لـ`useDocumentTemplates`.
لو `lockedCategory` موجود، `categoryPriority` بيتجاهل تلقائيًا (الهوك
بيتأكد إن `category === 'الكل'` قبل ما يرتّب) — يعني مفيش تعارض بين
الميزتين.

**11. `LegalDocumentsPage.tsx` (تعديل إضافي) — ✅ 26 أغسطس 2026**
prop اختياري جديد `initialCaseType` (نص حر أو `null`)، بيتحوّل لـ
`categoryPriority` **مرة واحدة عند mount** (نفس نمط تجميد `hasCaseContext`
بالظبط — `useState(() => getCategoryPriorityForCaseType(initialCaseType))`)
وبيتمرر لـ`TemplatePicker` **بس** لما `hasCaseContext=true` (مسار
"كل التصنيفات" أصلًا، ملهوش معنى في مسار `lockedCategory`).

**12. `App.tsx` (تعديل) — ✅ 26 أغسطس 2026**
`onGenerateDocument` (زرار "توليد مستند" جوه `CaseDetailView`) دلوقتي بيحسب
`legalDocsInitialCaseType` من `casesWithExtras.find(...)?.type` وقت الدخول
(القيمة متاحة في سكوب `App.tsx` أصلًا، مش محتاجة تمرير `cases` array كاملة
لـ`LegalDocumentsPage`)، وبيمررها كـ`initialCaseType` — بترجع `null` مع
`legalDocsInitialCaseId` في `onInitialCaseConsumed` بنفس التوقيت.

**تستات جديدة:**
- `caseTypeCategoryPriority.test.ts` (جديد بالكامل) — تغطية كاملة للدالة
  الـpure: حالة فاضية/null، substring matching، وتأكيد إن كل تصنيف مقترح
  فعليًا موجود في `DOCUMENT_CATEGORIES` الحقيقية (حماية من typo في الجدول).
- `LegalDocumentsPage.test.tsx` — نموك `TemplatePicker` اتوسّع يعرض
  `categoryPriority` المستلمة كـ`data-category-priority` attribute، و3
  تستات جديدة: تطابق قاعدة معروفة، `initialCaseType=null` → فاضية، ومسار
  `hasCaseContext=false` بيتجاهل `categoryPriority` تمامًا حتى لو
  `initialCaseType` اتبعتت بالغلط.

### الباقي من خطة قسم 10: أولوية 7 (تجهيز ملف تسوية أسرة)

⏳ **بدأت فعليًا (26 أغسطس 2026، تالت جلسة) — راجع قسم 19 الجديد للتفاصيل
الكاملة.** جلسة تحديد متطلبات مباشرة مع بي حسمت طبيعة الفيتشر (تسوية ودّية قبل
رفع دعوى، مفيش مايك، ملف مستقل بلا `case_id`، قالب رسمي واحد ثابت)، وسكوفولد
أولي (سكيما + هيكل الفيتشر + ربط تنقّل) اتنفّذ بالحقول المؤكدة بس. **متوقف
حاليًا** في انتظار قايمة الحقول التفصيلية الكاملة لكل طرف + شكل القالب الرسمي
من بي — القسم 14 (جلسة تحديد المحتوى) بقى جزئيًا مستهلك لأولوية 7 تحديدًا،
لسه ساري لباقي التصنيفات الخمسة الجديدة. البند 5 ("نسخ المكتب") لسه مؤجّل
بالكامل زي ما هو (سجل القرارات بند 1). أولوية 6 ("حزم المستندات") **اتلغت
نهائيًا** ومش جزء من الباقي (قرار جيمي، قسم 18.1) — مبقاش مطلوب ليها أي جلسة
عمل أو معمارية خالص.

---

## 17. إغلاق فجوتين من المراجعة الذاتية (26 أغسطس 2026)

بعد مراجعة إضافية للتقرير ده (خارج القسم 13 نفسه) طلعت 4 فجوات جديدة مش
متذكورة حتى في المراجعة الذاتية: سجل النشاط، إشعار تيليجرام، تقاطع مع
نظام الصلاحيات التفصيلية، وسلوك القضية المقيّدة/المؤرشفة. اتنين منهم
اتنفذوا فعليًا في هذه الجلسة (كود بس، بلا قرار محتوى/معمارية جديدة)،
والباقي اتوثّق كقرارات لسه مفتوحة.

### 17.1 سجل النشاط (Activity Log) — ✅ نُفّذ

**الفجوة:** سند عنده نمط ثابت (`logActivity`) بيسجّل كل فعل رئيسي (إضافة
قضية، جلسة، رفع مستند أرشيف — كلهم بـ`entity_type: 'document'` فعليًا
لحالة الأرشيف تحديدًا). توليد مستند قانوني كان الاستثناء الوحيد الغير
مسجّل، رغم إن التعليق التوثيقي جوه `dataAccess.ts` نفسه كان أصلًا بيذكر
`'document'` كقيمة ممكنة لـ`entity_type` — يعني البنية التحتية كانت جاهزة
ومحدّش وصلها.

**التنفيذ:** استدعاء واحد لـ`logActivity(db, 'توليد مستند قانوني', {...})`
في `handleGenerate` (`LegalDocumentsPage.tsx`)، بعد نجاح `genState.generate()`
مباشرة — نفس مكان واحد، صفر تعديل على منطق التوليد نفسه (`useGenerateDocument.ts`/
`generationApi.ts`). البيانات المسجّلة: `entity_type: 'document'`،
`entity_id: doc.id`، و`details` فيها اسم القالب + تصنيف المصدر (من قضية
مفتوحة/إدخال يدوي/نموذج فاضي). بدون `await` — نفس نمط `ArchiveTab.tsx`
بالحرف (`logActivity` بتتعامل مع أخطاءها داخليًا، فمفيش خطر على مسار
التوليد نفسه لو فشل السجل).

### 17.2 إشعار تيليجرام — ✅ نُفّذ

**الفجوة:** كل فعل رئيسي مشابه (قضية جديدة، جلسة جديدة) بيبعت إشعار
تيليجرام عبر `sendTelegram` (من `useTelegramAlerts`). توليد مستند مرشح
طبيعي بنفس المنطق، ومكانش موصّل خالص.

**التنفيذ:**
- `prop` اختياري جديد `sendTelegram?: (text: string) => void` في
  `LegalDocumentsPageProps` — اختياري تمامًا عشان صفر تأثير على أي مكان
  تاني بيستخدم الصفحة من غيره (تست، مثلًا).
- `App.tsx` بيمرر `sendTelegram` الموجود فعليًا في نفس السكوب (`useTelegramAlerts(profile)`)
  لـ`LegalDocumentsPage` عند `tab === 'legalDocs'` — سطر واحد إضافي، صفر
  تغيير على أي تاب تاني.
- الرسالة بتتبني بنفس نمط `NewStandaloneSessionModal.tsx` بالحرف
  (`escapeTelegramHtml` على كل نص متغيّر): اسم القالب + التصنيف + تصنيف
  المصدر. بتتبعت بس لو `sendTelegram` متمرر فعليًا (فحص `if (sendTelegram && selectedTemplate)`)
  — صفر خطر كسر لو الصفحة اتنادت من مكان مالوش `sendTelegram`.

### 17.3 تستات جديدة (`LegalDocumentsPage.test.tsx`)

`describe` جديد كامل (4 تستات): تأكيد استدعاء `logActivity` بالبيانات
الصح عند نجاح التوليد، تأكيد إن الفشل (`generate` بترجع `null`) مايستدعيش
`logActivity` خالص ولا يتقدم لخطوة المعاينة، تأكيد إن غياب `sendTelegram`
مايكسرش الصفحة، وتأكيد إن وجوده بيستدعيه برسالة فيها اسم القالب والتصنيف.
`logActivity` نفسها اتموكت على مستوى الموديول (زي `useGenerateDocument`
أصلًا) عشان نتحقق من الاستدعاء بمعزل عن تأثيرها الحقيقي على قاعدة
البيانات. مفيش `node_modules`/نت في السندبوكس فمقدرتش أشغّل `npm test`
فعليًا — تحقق نحوي بس (`node --experimental-strip-types --check`) على
الملفين المعدّلين والتست، سليم بالكامل. **تشغيل `npm test` الحقيقي لسه
مطلوب منك قبل الدمج.**

### 17.4 قرارات كانت مفتوحة — ⚡ اتحسمت مبدئيًا (26 أغسطس 2026، سجل القرارات بنود 6 و8)

> ⚡ نفس ملاحظة سجل القرارات: البندين دول قرارات مبدئية وسريعة عشان نكمل التنفيذ، مش تقفيل نهائي —
> هيتراجعوا بشكل كامل وأدق مع باقي القرارات المبدئية (بند 3–8) في مرحلة تالية.

1. ⚡ **`can_generate_documents` permission key** — **مبدئي**: مقفول لدوري
   lawyer/admin بس بلا استثناء (نفس نمط القفل الأساسي بتاع `can_view_fees`،
   مش نمط `can_edit_fees` القابل للتخصيص). التفاصيل والأثر التقني الكامل
   في سجل القرارات، بند 6. **لسه مش منفّذ في الكود** — قرار موثّق بس.
2. ⚡ **قضية مقيّدة/مؤرشفة** — **مبدئي**: زرار "توليد مستند" جوه
   `CaseDetailView` يفضل متاح، مفيش قفل جديد عليه. التفاصيل في سجل
   القرارات، بند 8. **لسه مش منفّذ/متأكد منه في الكود** — محتاج فحص فعلي
   لمكان الزرار في `CaseDetailView.tsx` قبل التنفيذ (زي ما البند نفسه
   بيوضّح).

### 17.5 التسليم (26 أغسطس، مساءً)

`LegalDocumentsPage.tsx`، `App.tsx`، `LegalDocumentsPage.test.tsx` —
تعديلات فقط (صفر ملف جديد). مسلّمة كزيب مشروع كامل
(`sanad-legal-docs-activity-telegram-1.zip`) + هذا التقرير المحدّث.

---

## 17.6 تنفيذ قرارات 17.4 الأربعة (26 أغسطس 2026، ليلاً)

### بند 6 — صلاحية `can_generate_documents` — ✅ نُفّذ بالكامل

**الملفات المعدَّلة:**

| الملف | التعديل |
|---|---|
| `src/shared/lib/permissions.ts` | `'can_generate_documents'` أُضيفت لـ`PERMISSION_KEYS`. قفل أساسي جديد في `checkPermission()`: `if (key === 'can_generate_documents') return profile?.role === 'lawyer';` — بعد فحص admin (اللي بيرجع true فوق أصلاً)، يعني عمليًا "lawyer/admin بس، بلا استثناء صريح ممكن يفتحها لـviewer". `ROLE_DEFAULT_PERMISSIONS` اتحدّثت بالمفتاح الجديد لكل الأدوار الثلاثة (توثيقي بس — القفل الأساسي بيسبقها). |
| `database/migrations/sql-migrations-phase7/03-can-generate-documents-permission.sql` (**جديد**) | نفس منطق القفل في `has_permission()` على القاعدة (SQL) — فرع منفصل عن `can_view_fees/can_edit_fees` لأنه بيسمح لـlawyer صراحة (مش برفض الكل). **+ تشديد `tenant_write_generated_docs` policy** (INSERT على `generated_documents`) بإضافة `AND has_permission('can_generate_documents')` — ده الدفاع الحقيقي؛ من غيره أي مستخدم (بما فيهم viewer) يقدر يدرج صف مباشرة عبر الـAPI حتى لو الواجهة مخفية عنه الزرار. |
| `src/app/shell/navConfig.ts` | `NavItem` بقى فيه `requiresCanGenerateDocuments?: boolean` (منفصل عن `adminOnly` لأنه مش مقصور على admin وحده). عنصر `legalDocs` في `moreNavItems` اتحط عليه العلم ده. `navItemsFor()` بقت بتاخد `canGenerateDocuments` كباراميتر تاني وتفلتر بيه. |
| `src/app/shell/DesktopSidebar.tsx` / `TabletDrawer.tsx` | prop جديد `canGenerateDocuments: boolean`، بيتستخدم في فلترة `moreNavItems` جنب `isAdmin` (نفس النمط). |
| `src/app/CommandDock.tsx` | نفس الفكرة للموبايل — عنصر `legalDocs` اتحوّل من عنصر ثابت في المصفوفة لـspread شرطي `...(canGenerateDocuments ? [...] : [])`، بنفس نمط `fees`/`admin` الموجود جنبه بالظبط. |
| `src/app/shell/AppShell.tsx` | prop اختياري جديد `canGenerateDocuments?: boolean`، بيتمرر لـ`DesktopSidebar`/`TabletDrawer`. |
| `src/App.tsx` | `const canGenerateDocuments = checkPermission(profile, 'can_generate_documents');` (نفس نمط `canViewFees` السطر اللي فوقه بالظبط). ثلاث نقط استخدام: (1) بتتمرر لـ`AppShell` و`CommandDock`، (2) `onGenerateDocument` الممرّرة لـ`CaseDetailView` بقت `canGenerateDocuments ? (caseId) => {...} : undefined` — لو viewer، الزرار جوه `DocsSection` مايترندرش أصلًا (شرط `onGenerateDocument &&` الموجود هناك من الأول)، (3) تاب `legalDocs` نفسه بقى محكوم بنفس نمط تاب `fees` (`canGenerateDocuments ? <LegalDocumentsPage/> : "غير مصرح لك بهذا القسم"`) — **دفاع فعلي ضد الوصول المباشر بالرابط** (deep linking عبر `PATH_TABS`)، مش بس إخفاء زرار. |

**فحص نحوي:** كل الملفات المعدَّلة اتفحصت بـ`node --experimental-strip-types --check` (بعد نسخ مؤقت لـ`.ts` للملفات اللي فيها JSX/`.tsx` — الكودبيز كله `React.createElement` بحت من غير JSX خام، فالفحص ده كافٍ نحويًا). سليم بالكامل. **تشغيل `npm run build`/`npm test` الحقيقي لسه مطلوب منك** — نفس قيد السندبوكس المذكور من الأول.

**ملحوظة:** لو حابب تختبر يدويًا، فيه فرق بين مسارين: (أ) viewer بيحاول يدخل تاب `legalDocs` من غير أي رابط قضية → هيشوف "غير مصرح لك بهذا القسم"، (ب) viewer بيفتح تفاصيل قضية → زرار "توليد مستند" جوه تبويب المستندات مش هيظهر خالص.

### بند 5 — حفظ المستند في حافظة القضية — ✅ اكتشاف: منفّذ بالفعل، صفر شغل جديد

فحصت `src/features/documentGeneration/api/exportApi.ts` (`uploadExportedFile`)
ولقيت إنه بيربط `case_document_links` تلقائيًا وقت تصدير PDF/Word لو `case_id`
موجود (سطور 116-126 من الملف، منفّذة من مرحلة سابقة — القسم 3 بند 3 من خطة
التوليد الأصلية). يعني القرار المسجّل في القسم 15 بند 5 ("يتحفظ تلقائيًا ومربوط
بالقضية") **متحقق فعليًا في الكود من غير أي تدخل إضافي مطلوب دلوقتي**. الفجوة
الوحيدة اللي كانت باقية هي التوثيق (قسم 17.4 كان مسجّلها كـ"لسه مش منفّذ") —
اتصححت هنا.

### بند 8 — زرار "توليد مستند" لقضية مقيّدة/مؤرشفة — ✅ اتأكَّد، صفر تغيير

فحصت `CaseDetailView.tsx` (تعريف `onGenerateDocument` وتمريره لـ`DocsSection`)
و`DocsSection.tsx` (رندر `GenerateDocumentButton`) — الزرار **مش مربوط بأي شرط
قفل** (لا `canEditCase`، ولا حالة أرشفة/تقييد القضية). يعني السلوك الفعلي في
الكود مطابق للقرار المسجّل بالفعل (القسم 15 بند 8: "يفضل متاح، مفيش قفل جديد
عليه") من غير أي حاجة نغيّرها. الملحوظة التقنية اللي طلبت الفحص ("زي ما البند
نفسه بيوضّح") اتقفلت.

### بند 4 — الأوفلاين — ✅ نُفّذ بالكامل (26 أغسطس 2026، ليلاً — تكملة)

**الاكتشاف:** فحص `generateDocument()` (`generationApi.ts`) و`useGenerateDocument.ts`
أثبت إن توليد المستند **مش عملية كتابة بسيطة على جدول واحد** زي إضافة قضية/جلسة
(اللي طابور الأوفلاين `__dbWrite`/`offlineQueue.ts` مصمم أصلًا ليها). هي سلسلة
قراءات متتالية من السيرفر (نسخة القالب المنشورة، حقولها، بيانات القضية/الأطراف
لو `case_bound`) **قبل** أي إدراج فعلي — أقرب لنمط `case_fees`/`fee_payments`
(المستثنيين عمدًا من الطابور لنفس السبب بالظبط، راجع تعليق `offlineQueue.ts`
سطر 24-32) من نمط `cases`/`case_sessions` (كتابة صف واحد مباشرة).

**اكتشاف تاني أهم:** تصدير PDF/Word نفسه (`exportApi.ts` → `loadExportContext`)
بيعيد قراءة صف `generated_documents` **من السيرفر بالـid** بعد التوليد مباشرة —
مش بيستخدم البيانات المحلية اللي اتولّدت بيها المعاينة. يعني حتى لو قيّدنا خطوة
التوليد نفسها في طابور الأوفلاين، **زرار "تصدير PDF" هيفضل معطّل لحد ما النت
يرجع ويتزامن** — لأن الـid الحقيقي للصف مش موجود قبل كده، والتصدير مبني على
استعلام سيرفر مش على `document_content_json` المحلي المتاح أصلًا في الذاكرة.

**القرار المعدَّل (بموافقة صاحب المشروع، 26 أغسطس 2026 ليلاً) — "خيار أ":**
نخزّن المسودة أوفلاين (نص المستند كامل، قابل للقراءة في شاشة المعاينة، ومحفوظ
فعليًا في طابور الأوفلاين عشان ميضيعش)، **بدون** محاولة تفعيل تصدير PDF/Word
أوفلاين في هذه الدفعة. الخطوات الأربعة المخطَّطة اتنفَّذت كلها بالكامل:

**الملفات المعدَّلة/الجديدة:**

| الملف | التعديل |
|---|---|
| `src/features/documentGeneration/lib/offlineTemplateCache.ts` (**جديد**) | Cache محلي (`sessionStorage`، مفتاح `templateId:caseId:sourceMode`) بيتخزن نسخة القالب المنشورة (`id`+`body_template`)، حقولها، و`initialValues` بعد دمج `office_name` — بيتحفظ تلقائيًا مع كل تحميل ناجح أونلاين. فيه كمان `makeOfflineGeneratedDocId()`/`isOfflineGeneratedDocId()` — بادئة `offline-doc-` لـid محلي مؤقت للمستند المتولّد أوفلاين، بنفس نمط `offlineTempId` الموجود فعليًا في `useCaseActions.ts` (بدل ما نضيف حقل جديد لـ`GeneratedDocument` في `types.ts` — الملف ده مقفول عن قصد عن أي إضافة حقول من غير تحديث الخطة الموحّدة الأصلية أولاً). |
| `src/features/documentGeneration/api/generationApi.ts` | استبدال placeholders (كان جزء داخلي من `generateDocument()`) اتخلع لدالة pure منفصلة مُصدَّرة `renderDocumentContent(bodyTemplate, templateFields, values)` — صفر تغيير سلوكي، `generateDocument()` نفسها بقت بتنادي الدالة دي بدل الحلقة المكرّرة. الهدف: `useGenerateDocument.ts` يقدر يبني `document_content_json` محليًا من غير شبكة. |
| `src/lib/offlineQueue.ts` | `'generated_documents'` أُضيفت لـ`DbWriteTable` — تعليق موثّق بنفس أسلوب باقي الجداول يوضّح إن الإدراج النهائي (بعد ما المحتوى اتبنى محليًا من الكاش) وحيد الخطوة بلا FK متسلسل، نفس نمط `case_notes`، بعكس سلسلة *بناء* المحتوى نفسها (أعقد، زي `case_fees`). |
| `src/features/documentGeneration/hooks/useGenerateDocument.ts` | (أ) خطوة تحميل الحقول: لو `guard.offline`، بتقرا من `offlineTemplateCache.ts` بدل الرفض الفوري — لو موجود، تعرض الفورم من الكاش (`usingOfflineCache: true`، خاصية جديدة في نتيجة الهوك)؛ لو مش موجود، نفس رسالة "أنت أوف لاين" القديمة بالظبط. لو أونلاين، بتنادي `resolveTemplateVersion()` كمان (زيادة عن قبل) وتحفظ كل حاجة في الكاش بعد كل تحميل ناجح. (ب) `generate()`: لو أوفلاين وعندنا نسخة قالب محفوظة (`templateVersionRef`)، بتتحقق من الحقول المطلوبة، تبني `document_content_json` عبر `renderDocumentContent()`، وتقيّد الإدراج في `window.__dbWrite({table:'generated_documents', ...})` بدل ما تنادي `generateDocument()` الشبكية — وترجّع مستند محلي (`id` ببادئة `offline-doc-`) للمعاينة الفورية. لو مفيش نسخة محفوظة أصلاً، رسالة واضحة تطلب فتح الشاشة أونلاين مرة واحدة على الأقل الأول. |
| `src/features/documentGeneration/components/DynamicFieldsForm.tsx` | prop اختياري جديد `usingOfflineCache` — بانر تنبيه كهرماني واضح لما البيانات المعروضة جايه من الكاش المحلي مش من السيرفر. |
| `src/features/documentGeneration/components/DocumentPreviewEditor.tsx` | بتكتشف المستند المتولّد أوفلاين عن طريق بادئة الـid (`isOfflineGeneratedDocId`)، وتقفل زراير "تصدير PDF"/"تصدير Word" (`disabled` + `title` توضيحي) مع بانر تنبيه أسفل المعاينة يوضّح إن التصدير هيبقى متاح بعد المزامنة عبر حافظة القضية. auto-save (`scheduleSave`) بقى بيتخطى محاولة الشبكة بالكامل لو المستند أوفلاين (كانت بتحاول وتفشل بصمت قبل كده — دلوقتي منتحاولش من الأساس). |
| `src/pages/LegalDocumentsPage.tsx` | تمرير `genState.usingOfflineCache` لـ`DynamicFieldsForm` — سطر واحد. |
| `src/features/documentGeneration/__tests__/useGenerateDocument.test.ts` | موك جديد لـ`resolveTemplateVersion`/`renderDocumentContent`/`getCurrentTenantId`/`window.__dbWrite`، و`sessionStorage.clear()` في `beforeEach` (عشان تستات الأوفلاين متتأثرش ببعض). 4 تستات جديدة: كاش بيتبني أونلاين وبيتقرا أوفلاين، أوفلاين من غير كاش (نفس الرسالة القديمة)، `generate()` أوفلاين بيبني المستند ويقيّده في `__dbWrite`، و`generate()` أوفلاين من غير كاش (رسالة واضحة، مفيش نداء لـ`__dbWrite`). |

**اختيار متعمَّد اتسجل هنا (لسه محتاج مراجعتك):** زرار التصدير بيفضل مقفول
لنفس نسخة الشاشة دي **حتى لو النت رجع** أثناء ما المستخدم لسه فاتح شاشة
المعاينة — مفيش آلية مربوطة تستبدل الـid المحلي المؤقت بالـid الحقيقي بعد ما
الطابور يتزامن (ده هيحتاج wiring إضافي مع `window.__syncOfflineQueue` مش جزء من
الخطوات الأربعة الأصلية). البانر بيوضّح للمستخدم إنه هيقدر يصدّر من حافظة القضية
بعد المزامنة بدل كده — قرار بسيط ومقصود يتجنب تعقيد إضافي، لكنه اختيار تنفيذ مني
مش قرار صريح منك، فمحتاج تأكيدك أو تعديلك.

**فحص نحوي:** كل الملفات المعدَّلة اتفحصت بـ`node --experimental-strip-types --check`
(نفس منهج فحص بند 6 بالظبط). سليمة بالكامل. **تشغيل `npm run build`/`npm test`
الحقيقي لسه مطلوب منك** — نفس قيد السندبوكس المذكور من الأول في التقرير.

**الأثر على قسم 15 بند 4 (سجل القرارات):** القرار الأصلي ("نسمح — هيتخزن في نفس
Offline Queue") **قائم من حيث المبدأ ومنفَّذ فعليًا**، بنطاقه الموضَّح: التخزين
نعم، التصدير الفوري لأ (لحد المزامنة، ثم من حافظة القضية).

### بند 9 — حزم المستندات (أولوية 6): تتأجل ولا تتلغى؟ ⛔ محسوم — إلغاء نهائي (26 أغسطس 2026)

**السؤال:** أولوية 6 كانت التالية في ترتيب التنفيذ (قسم 10)، ومحتاجة جلسة تحديد
محتوى منفصلة (قسم 14) قبل أي كود.

**القرار:** إلغاء نهائي، مش تأجيل. التفاصيل الكاملة في قسم 18.1.

### بند 10 — تخطي SourceModeSelector تلقائيًا من قضية مفتوحة: يفضل ولا يتلغى؟ ✅ محسوم — يتلغى (26 أغسطس 2026)

**السؤال:** القسم 9.5 الأصلي كان بيخطّي شاشة اختيار المصدر تلقائيًا (`case_bound`
جاهز) لما المستخدم داخل من قضية مفتوحة.

**القرار:** يتلغى — الاختيار الكامل (من قضية/يدوي/فاضي) يظهر دايمًا، من غير
افتراضي محدد مسبقًا. القضية المعروفة بتتمرر كـ`presetCaseId` بس لتسهيل اختيار "من
القضية" لو اتاخد، مش لتخطي الاختيار نفسه. نُفّذ بالكامل في الكود — التفاصيل
والملفات المعدَّلة في قسم 18.2 (وفيه كمان تأكيد إن الحقول التلقائية كانت أصلاً
قابلة للتعديل الحر من غير حاجة لتغيير كود).

---

## 18. قرارات جيمي المباشرة (26 أغسطس 2026، تاني جلسة)

*(جلسة منفصلة عن جلسة قسم 14 المتوقّعة — جيمي حسم قرارين مباشرين بره ترتيب
التنفيذ المتوقّع، اتنفّذوا فورًا كوده.)*

### 18.1 حزم المستندات (أولوية 6) — ⛔ إلغاء نهائي

**السياق:** بعد ما اقتُرحت أولوية 6 (قسم 5.2، قسم 10) كخطوة تالية في ترتيب
التنفيذ — جلسة تحديد قايمة المستندات/الحقول اللي هتتضم في كل حزمة (بداية قسم
14) — جيمي راجع الفكرة نفسها ورفضها من الأساس، مش بس أجّلها.

**القرار:** فكرة "حزم المستندات" (Document Bundles) اتلغت نهائيًا من نطاق هذا
التقرير بالكامل. مش تأجيل زي "نسخ المكتب" (سجل القرارات بند 1) — دي إلغاء كامل،
الفكرة مش هترجع كبند مستقبلي.

**الأثر على التقرير:**
- قسم 5.2 — النص الأصلي فضل موجود كتوثيق تاريخي بس، بعلامة إلغاء واضحة.
- قسم 6 (الرسم المعماري) — السطر الخاص بـ"[مستقبلي] أضف مستند تاني للحزمة دي" اتشال.
- قسم 10 (خطة التنفيذ) — بند 6 بقى ⛔ ملغي (مش ⏳ مؤجّل)، وطلع بره أي ترتيب تنفيذ
  حالي أو مستقبلي.
- قسم 16 ("الباقي من خطة قسم 10") — تحديث: الباقي الفعلي بقى أولوية 7 بس (+ بند
  5 المؤجّل من قبل كده).
- **صفر تأثير على الكود الفعلي** — الفكرة مكنش لسه اتنفّذ منها أي سطر كود
  (كانت لسه في مرحلة "قبل جلسة تحديد المحتوى")، فمفيش أي تراجع أو حذف مطلوب.

**السبب:** قرار جيمي المباشر — الحزم مش حاجة هتفرق في شغله اليومي حاليًا.

### 18.2 SourceModeSelector واجبة الظهور دايمًا — ✅ نُفّذ بالكامل

**السياق:** القسم 9.5 الأصلي (ومن قبله سجل التنفيذ في القسم 16، ملف 6) كان بيوثّق
قرار متعمَّد: لما المستخدم بيدخل توليد المستند من جوه قضية مفتوحة (زرار "توليد
مستند" في `CaseDetailView`)، الشاشة كانت بتتخطى `SourceModeSelector` بالكامل
وتحط `sourceMode = 'case_bound'` تلقائيًا — افتراض إن المستخدم أكيد عايز يستخدم
بيانات نفس القضية دايمًا.

**القرار:** جيمي رفض الافتراض ده. عايز الاختيار الكامل (من قضية مفتوحة / إدخال
يدوي / نموذج فاضي) يظهر **في كل مرة**، حتى لو داخل من قضية مفتوحة بالفعل — من
غير أي اختيار محدد مسبقًا (لا حتى "من القضية" كافتراضي)، المحامي هو اللي يقرر في
كل مرة.

**سؤال تابع اتسأل واتأكَّد:** هل الحقول اللي بتتملى تلقائيًا من بيانات القضية (لما
يختار "من قضية مفتوحة") بتفضل قابلة للتعديل بحرية، ولا بتتقفل؟ **الإجابة: فحص
الكود الفعلي (`DynamicFieldsForm.tsx`) أثبت إنها أصلاً فاضلة قابلة للتعديل بالكامل
من غير أي قفل** — حقول النص/التاريخ/الرقم اللي `auto-filled` بس بياخدوا خلفية
بنفسجية خفيفة وبادچ "auto" صغير للتوضيح البصري، من غير `disabled` خالص. الاستثناء
الوحيد هو حقول `party_ref` (قوايم منسدلة لأطراف القضية) اللي بتتقفل بس *لما مفيش
قضية مربوطة أصلًا* (`sourceMode !== 'case_bound'`) — لأنها محتاجة قايمة أطراف من
قضية عشان تظهر خالص، مش قفل تعمّدي على بيانات موجودة. يعني الجزء ده كان **صفر
تغيير كود مطلوب** — سلوك موجود بالفعل ومطابق لطلب جيمي.

**الملفات المعدَّلة:**

| الملف | التعديل |
|---|---|
| `src/pages/LegalDocumentsPage.tsx` | (أ) `sourceMode` initial state بقى `null` دايمًا (كان `hasCaseContext ? 'case_bound' : null`). (ب) `handleSelectTemplate`: اتشال شرط `if (hasCaseContext) { setStep('fields'); return; }` — دلوقتي `setStep('sourceMode')` دايمًا، للمسارين. (ج) `SourceModeSelector` بيستلم prop جديد `presetCaseId={hasCaseContext ? caseId : null}` — القضية المعروفة بتتمرر، مش عشان تتخطى الشاشة، لكن عشان اختيار "من قضية مفتوحة" (لو المحامي اختاره) يستخدمها على طول من غير بحث تاني. (د) زرار الرجوع من خطوة "البيانات" بسّط لـ`() => setStep('sourceMode')` دايمًا (كان بيرجّع لخطوة "المستند" ويصفّر القالب في مسار hasCaseContext تحديدًا — مبقاش لازم بعد ما الرجوع بقى بيتجه لـsourceMode زي المسار التاني بالظبط). |
| `src/features/documentGeneration/components/SourceModeSelector.tsx` | prop اختياري جديد `presetCaseId?: string \| null`. لو موجود وضغط "من قضية مفتوحة"، `onSelectMode('case_bound', presetCaseId)` بينادى على طول من غير ما يفتح شاشة البحث عن قضية (`pickingCase`) — القضية أصلاً معروفة. نص وصف الخيار (`desc`) بيتغيّر ديناميكيًا لـ"تعبئة تلقائية من بيانات هذه القضية" (بدل "قضية موجودة" العام) لما يكون فيه `presetCaseId`، عشان يبقى واضح للمحامي إنه هيستخدم نفس القضية اللي هو فاتحها. |
| `src/pages/LegalDocumentsPage.test.tsx` | الـdescribe الكامل اللي كان بيوثّق ويتأكد من التخطي التلقائي القديم (القسم 9.5) اتستبدل بالكامل بـdescribe جديد يتأكد من السلوك العكسي: SourceModeSelector تظهر دايمًا، `presetCaseId` بيتمرر صح في الحالتين (موجود/فاضي)، واختيار أي مصدر بيودّي لخطوة البيانات. موك `SourceModeSelector` نفسه اتوسّع (زرارين تجريبيين: "من قضية مفتوحة"/"إدخال يدوي" بينادوا `onSelectMode` بدل ما يكون `<div>` ثابت). 4 تستات تانية (سجل النشاط/تيليجرام) اتعدّلت عشان تضيف كليك على "من قضية مفتوحة" قبل الضغط على "توليد" — كانت بتعتمد على التخطي التلقائي القديم ضمنيًا. تستات أولوية 4 (الترتيب حسب نوع القضية) متأثرتش — بتتأكد من `categoryPriority` عند اختيار القالب بس، قبل خطوة المصدر. |

**فحص نحوي:** الملفات التلاتة اتفحصت بـ`tsc --noEmit` (JSX + module resolution
مفلترة يدويًا لغياب `node_modules`/النت في السندبوكس — نفس القيد المذكور من
الأول في التقرير) للتأكد من خلو الكود من أخطاء syntax/type حقيقية. سليمة بالكامل.
**تشغيل `npm test`/`npm run build` الحقيقيين لسه مطلوب منك.**

### ✅ الثلاث SQL migrations اتشغّلوا فعليًا على الإنتاج (26 أغسطس 2026)

`01-document-generation-schema.sql`، `02-document-generation-seed.sql`،
`03-can-generate-documents-permission.sql` اتشغّلوا بالترتيب على Supabase
الحقيقي، بمشاكل بسيطة اتحلّت في اللحظة:

- **01** أول محاولة رجّعت خطأ `document_templates already exists` — طلع إن
  الجدول اتعمل من قبل كده في جلسة سابقة (21 أغسطس). اتحلّت بنسخة idempotent
  (`CREATE TABLE IF NOT EXISTS` + `DROP POLICY IF EXISTS` قبل كل `CREATE POLICY`)
  أكملت باقي الـ4 جداول الناقصة. اتأكّد بـ`information_schema.tables`: الـ5
  جداول موجودين.
- **02** لما اتشغّل، طلع إن الـseed كان اتزرع أصلاً من نفس جلسة 21 أغسطس —
  فالنتيجة كانت تكرار كامل (8 قوالب/8 نسخ/40 حقل بدل 4/4/20). اتشخّص بكويري
  عدّ + كويري مقارنة (كل قالب ظاهر مرتين، بتاريخي 21 و26 أغسطس)، واتنضّف
  بمسح النسخة الأحدث من كل قالب (سيب الأقدم، الـcascade مسح النسخ/الحقول
  المرتبطة تلقائيًا). اتأكّد رجوع العدد لـ4/4/20.
- **03** اتشغّل من غير أي مشاكل.

**الحالة النهائية: الثلاث migrations متأكّد منهم بالكامل على الإنتاج.**
باقي بس تأكيد يدوي من التطبيق نفسه (زرار "توليد مستند" ظاهر لـlawyer
ومختفي لـviewer).

**ملحوظة UX يستاهل انتباهك:** دلوقتي أي توليد مستند من جوه قضية مفتوحة بقى محتاج
ضغطة إضافية واحدة (اختيار "من قضية مفتوحة" في شاشة المصدر) قبل ما يوصل لفورم
البيانات — بعد ما كان بيقفز مباشرة. ده الأثر المباشر لقرارك، موثّق هنا عشان يبقى
واضح إنه تريد-أوف واعي (شفافية زيادة/تحكم أكتر، مقابل ضغطة واحدة إضافية في أكتر
مسار استخدام شيوعًا) مش نتيجة جانبية غير مقصودة.

---

## 19. أولوية 7 — تجهيز ملف تسوية أسرة (26 أغسطس 2026، تالت جلسة) — بدء التنفيذ

*(جلسة منفصلة عن جلستَي قسم 17/18 — بي طلب نكمل بنود التقرير، واختار أولوية 7
كبند تالي. زي ما قسم 6/14 وثّقوا من الأول: معمارية مستقلة بالكامل عن
`document_templates`، محتاجة جلسة تحديد متطلبات مباشرة قبل أي كود.)*

### 19.1 نتيجة جلسة تحديد المتطلبات مع بي

**السياق المؤكد:** الملف ده لتسوية ودّية **قبل** رفع أي دعوى — بديل عن التقاضي،
مش خطوة بعده. المخرج النهائي ملف بيانات بيتقدّم لمكتب تسوية المنازعات الأسرية،
بقالب رسمي موحّد (نفس القالب لكل مكاتب سند، مش قابل لتخصيص كل مكتب — قرار بي
المباشر، عكس فكرة "نسخ المكتب" المؤجّلة في سجل القرارات بند 1).

**قرارات مؤكدة صراحة من بي:**

| السؤال | القرار |
|---|---|
| المايك (كان مقترح في القسم 5.3/6 كفيتشر مستقل)؟ | **مرفوض تمامًا** — مفيش صوت خالص في الفيتشر ده |
| سند بيمثّل طرف واحد بس ولا الاتنين؟ | ممكن يمثّل **الطرفين معًا** في بعض الحالات |
| نوع/سبب النزاع؟ | قايمة شائعة محدّدة مسبقًا **+ خيار "آخر" نص حر** |
| محامي واحد ولا للطرفين؟ | **محامي واحد بس** (بتاع الموكّل) — الطرف التاني بلا تمثيل بالملف |
| مرتبط بقضية (case) موجودة في سند؟ | **لأ خالص** — الملف بيتعمل قبل ما أي قضية توجد أصلاً |
| لو التسوية فشلت ورفعنا دعوى، نربط الملف بالقضية الجديدة؟ | **لأ** — ملف منفصل بالكامل، مفيش تحويل تلقائي |
| متابعة حالة الملف (قدّمنا/اتسوى/فشلت)؟ | **مش مطلوبة** — مجرد توليد وطباعة الملف بس |
| القالب قابل لتخصيص كل مكتب؟ | **لأ** — قالب واحد ثابت لكل المكاتب دلوقتي |

**الأثر المعماري لهذه القرارات مجتمعة:** الفيتشر ده أبسط بكتير من ويزارد
المستندات القانونية (أولوية 1-4) — مفيش تصنيفات، مفيش قوالب متعددة، مفيش
`SourceModeSelector` (مفيش قضية أصلاً يتربط بيها)، ومفيش تتبع حالة. عمليًا:
فورم واحد ثابت → معاينة → تصدير. نفس فلسفة "قالب رسمي واحد" اللي القسم 3.1 كان
رافضها لفكرة "النسخة العشوائية" — هنا الثبات مقصود ومطلوب من بي نفسه.

### 19.2 السكوفولد المُنفَّذ (بالحقول المؤكدة فقط)

**قرار تصميم صريح:** الحقول التفصيلية الكاملة لكل طرف + شكل القالب الرسمي **لسه
مش وصلوا من بي** (بي هيجهّزهم). عشان منبنيش على تخمين، السكوفولد اتوقف عمدًا عند
الحقول المؤكدة صراحة بس (اسم/رقم قومي/عنوان/تليفون لكل طرف)، وأي حقل إضافي محجوز
بتعليق `TODO` واضح في السكيما والنوع (`types.ts`) بدل ما يتكتب افتراضيًا.

**الملفات الجديدة:**

| الملف | المحتوى |
|---|---|
| `database/migrations/sql-migrations-phase8/01-family-settlement-schema.sql` | جدول `family_settlement_files` — مستقل تمامًا، **بدون** `case_id` (تأكيد بي). أعمدة: `party1_*`/`party2_*` (name/national_id/address/phone)، `represented_party` (`party1`\|`party2`\|`both`)، `dispute_type` + `dispute_type_other`، `dispute_details`، `rendered_html`. RLS بنفس نمط tenant isolation المعتاد + **قفل كتابة يعيد استخدام `has_permission('can_generate_documents')`** الموجود بالفعل (قرار مني، محتاج تأكيدك — نفس فئة النشاط: توليد مستند قانوني بشعار المكتب). تعليق توثيقي كامل في رأس الملف بكل القرارات أعلاه. |
| `src/features/familySettlement/types.ts` | `FamilySettlementParty`/`FamilySettlementFile`/`FamilySettlementFormValues` بالحقول المؤكدة، `DISPUTE_TYPES` (قايمة أولية: نفقة/حضانة/طلاق/رؤية/آخر — **مؤقتة**، مستنية تأكيد بي النهائي). |
| `src/features/familySettlement/api/familySettlementApi.ts` | `createFamilySettlementFile()`/`getFamilySettlementFile()` — CRUD أساسي جاهز وشغال. `renderFamilySettlementContent()` **لسه STUB صريح** — بيرجّع HTML بانر تحذيري واضح ("القالب الرسمي لسه مش متوفر") + عرض خام للبيانات المُدخلة، مش القالب الفعلي. |
| `src/features/familySettlement/components/FamilySettlementForm.tsx` | فورم بالحقول المؤكدة بس (كارت منفصل لكل طرف + `represented_party` + نوع النزاع + تفاصيل حرة). تصميم كارت-لكل-طرف مقصود عشان يستحمل حقول إضافية بعدين من غير إعادة هيكلة. |
| `src/pages/FamilySettlementPage.tsx` | صفحة خطوتين بس (بيانات → معاينة) — أبسط من ويزارد المستندات القانونية الخماسي، بما يعكس عدم وجود تصنيفات/قوالب متعددة/مصدر قضية هنا. زرار "تصدير PDF" موجود بصريًا بس **معطّل عمدًا** (`disabled` + tooltip يوضّح إنه هيتفعّل بعد اعتماد القالب الرسمي). |

**الملفات المعدَّلة (ربط تنقّل، بنفس نمط `legalDocs` بالحرف):**

| الملف | التعديل |
|---|---|
| `src/useNavigation.ts` | `'familySettlement'` أُضيفت لـ`TabName` union وللمصفوفة اللي بتولّد `PATH_TABS` (دعم deep-linking زي أي تاب تاني). |
| `src/app/shell/navConfig.ts` | عنصر جديد في `moreNavItems` بـ`requiresCanGenerateDocuments: true` — نفس قفل `legalDocs`. |
| `src/app/CommandDock.tsx` | زرار موبايل جديد في شريط "المزيد"، بنفس نمط الـspread الشرطي `...(canGenerateDocuments ? [...] : [])` المستخدم لـ`legalDocs`/`fees`/`admin`. |
| `src/App.tsx` | استيراد `FamilySettlementPage` + بلوك `tab === 'familySettlement'` جديد بنفس دفاع deep-linking بتاع `legalDocs` بالظبط (فحص `canGenerateDocuments` قبل الرندر، مش بس إخفاء الزرار) — بيمرر `currentUserId: profile?.id ?? null`. |

**فحص نحوي:** ملفات `.ts` الصرفة (`types.ts`, `familySettlementApi.ts`) اتفحصت
بـ`node --experimental-strip-types --check` وسليمة بالكامل. ملفات `.tsx`
(الفورم والصفحة) ما قدرتش تتفحص آليًا — `esbuild`/أي أداة TSX check مش متاحة في
السندبوكس (مفيش `node_modules`، ومحاولة `npx esbuild` اترفضت بسبب عدم وجود نت).
اتراجعت يدويًا بس، ومبنية بنفس بنية/أنماط JSX الموجودة فعليًا في
`SourceModeSelector.tsx`/`DynamicFieldsForm.tsx` (نفس المكتبة، نفس أسلوب
الـstate/props). **تشغيل `npm run build`/`npm test` الحقيقيين مطلوب منك** — نفس
قيد السندبوكس المذكور من الأول في التقرير.

### 19.3 قرارات مفتوحة ومعلّقة — محتاجة منك

1. ⏳ **[الأهم — بيوقف الاستكمال]** قايمة الحقول التفصيلية الكاملة لكل طرف
   (بي أكّد إن فيه زيادة عن الأربعة الأساسية — مثال مذكور: بيانات الأولاد
   المشتركين لو فيه).
2. ⏳ **[الأهم — بيوقف الاستكمال]** شكل/قالب مكتب تسوية المنازعات الأسرية
   الرسمي (صورة أو نسخة) — عشان `renderFamilySettlementContent()` تُكتب
   فعليًا بدل الـSTUB الحالي، والتصدير النهائي (jsPDF، نفس مكتبة
   `exportApi.ts` الموجودة) يتفعّل.
3. ⚡ **[افتراض مني، محتاج تأكيدك]** إعادة استخدام قفل `can_generate_documents`
   الموجود لتوليد المستندات القانونية على الفيتشر ده كمان (بدل صلاحية مستقلة
   جديدة). السبب: نفس فئة النشاط قانونيًا (توليد مستند بشعار المكتب)، لكنه مش
   قرار صريح منك لهذا الفيتشر بالذات.
4. ⚡ **[افتراض مني، محتاج تأكيدك]** قايمة `DISPUTE_TYPES` الأولية
   (نفقة/حضانة/طلاق/رؤية/آخر) — مبنية على خبرتك العامة المذكورة سابقًا في
   التقرير (قسم 5.1)، مش تأكيد صريح لهذا الفيتشر. هتترجع للمراجعة مع قايمة
   الحقول التفصيلية.

### 19.3.1 تحديث — قرارات 3 و4 اتحسمت (26 أغسطس 2026، رابع جلسة)

جيمي حسم البندين "افتراض مني" في 19.3:

- **بند 3 (الصلاحية):** ✅ مؤكد — نفس `can_generate_documents` الموجودة، مفيش
  صلاحية مستقلة جديدة. الميجريشن والتعليق التوثيقي اتحدّثوا.
- **بند 4 (أنواع النزاع):** ↩️ **تغيير عن المقترح الأصلي** — مفيش قايمة/dropdown
  مقفولة خالص. `dispute_type` بقى **عمود نص حر بالكامل** (schema وType)،
  والأمثلة الشائعة (نفقة/حضانة/طلاق/رؤية) بتتعرض كـ`placeholder` استرشادي جوه
  الفورم بس، مش خيارات يختارها المستخدم. `dispute_type_other` اتشال تمامًا —
  مبقاش له داعي مع النص الحر.

**الملفات المعدَّلة:**

| الملف | التعديل |
|---|---|
| `database/migrations/sql-migrations-phase8/01-family-settlement-schema.sql` | `dispute_type` بقى `text NOT NULL` بدل `varchar(100)`، حذف عمود `dispute_type_other`، إضافة `CHECK (btrim(dispute_type) <> '')`، تحديث تعليقات التوثيق (الصلاحية بقت مؤكدة مش افتراض). |
| `src/features/familySettlement/types.ts` | حذف `DISPUTE_TYPES`/`DisputeTypeKey`، إضافة `DISPUTE_TYPE_EXAMPLES` (مصفوفة نصوص للـ placeholder بس)، `disputeType`/`FamilySettlementFormValues.disputeType` بقوا `string`، حذف `disputeTypeOther` من كل الأنواع. |
| `src/features/familySettlement/components/FamilySettlementForm.tsx` | الـ`<select>` بتاع نوع النزاع اتحوّل لـ`<input>` نص حر بـ`placeholder="مثال: نفقة / حضانة / طلاق / رؤية"`، حذف حقل "آخر" الشرطي بالكامل. `isValid` بقت بتتحقق من `disputeType.trim() !== ''` بدل شرط `other`. |
| `src/features/familySettlement/api/familySettlementApi.ts` | حذف `disputeTypeOther` من `rowToFile`/`createFamilySettlementFile`/الـinsert payload، `renderFamilySettlementContent` بقت بتطبع `values.disputeType` مباشرة من غير شرط `other`. |

**فحص نحوي:** `node --experimental-strip-types --check` سليم على `types.ts` و
`familySettlementApi.ts`. `FamilySettlementForm.tsx` اتراجع يدويًا (نفس قيد
عدم توفر أداة TSX check في السندبوكس المذكور في 19.2).

**الحالة الآن:** لسه واقفين عند نفس البندين 1/2 في 19.3 (قايمة الحقول
التفصيلية الكاملة + شكل القالب الرسمي) — دول لسه الوحيدين اللي بيوقفوا
الاستكمال الفعلي (توسيع الـschema، كتابة `renderFamilySettlementContent`
الحقيقية، تفعيل زرار التصدير).

### 19.3.2 توثيق — قايمة الحقول التفصيلية الكاملة وصلت (26 أغسطس 2026، خامس جلسة)

جيمي بعت لقطات شاشة من تطبيق مرجعي تاني (اسم الشاشة فيه: "تجهيز ملف تسوية
اسرة") فيه استمارة إدخال بيانات كاملة. ده **البند 1** من قسم 19.3 (قايمة
الحقول التفصيلية) اتحسم بيها، بالإضافة لقرارين إضافيين مباشرين. **البند 2**
(شكل القالب الرسمي النهائي بعد التوليد) **لسه مستني** — الصور دي شكل استمارة
الإدخال بس، مش شكل المستند بعد التصدير.

**⚠️ توثيق بس في هذه المرحلة — لسه مفيش أي تنفيذ كود.** بي طلب صراحة نوثّق
الأول وبعدين ننفّذ لما شكل الملف الرسمي (البند 2) يوصل.

#### أ) الحقول الكاملة لكل طرف (نفس الحقول تتكرر لـ"المدعي" ولـ"المدعي عليه")

كل الحقول دي **نص حر بالكامل** — قرار جيمي المباشر: "مش عايز دروب داون عايزها
نص حر". ده بيلغي أي dropdown كان متصوَّر في السكوفولد الأولي (19.2) خالص، مش
بس `dispute_type` (اللي كان اتحسم في 19.3.1). المصطلحات "مدعي"/"مدعي عليه" في
الصور بتقابل `party1`/`party2` عندنا (التسميات النهائية في الـUI هترجع تتراجع
مع شكل القالب الرسمي).

| # | الحقل | ملاحظات |
|---|---|---|
| 1 | الاسم كاملاً | موجود فعليًا (`name`) |
| 2 | تاريخ الميلاد | حقل تاريخ — جديد |
| 3 | الوظيفة | نص حر — جديد |
| 4 | المؤهل الدراسي | نص حر — جديد |
| 5 | العنوان | موجود فعليًا (`address`) |
| 6 | الحالة الاجتماعية | **نص حر** (كانت شكلها dropdown في الصور، لكن جيمي رفض أي dropdown) — جديد |
| 7 | تاريخ الزواج أو الطلاق | حقل تاريخ — جديد |
| 8 | عدد الأولاد من الزواج الحالي | **نص حر** (كان شكله dropdown في الصور) — جديد |
| 9 | عدد مرات الزواج السابق | **نص حر** — جديد |
| 10 | عدد مرات الطلاق | **نص حر** — جديد |
| 11 | عدد الأولاد من الزواج السابق | **نص حر** — جديد |
| 12 | رقم الهاتف (وسائل الاتصال) | موجود فعليًا (`phone`) |

**ملاحظة:** الرقم القومي (`nationalId`) الموجود عندنا فعليًا في السكوفولد
الأولي (19.2) **مش ظاهر** في استمارة التطبيق المرجعي. قرار مبدئي: **نسيبه**
لحد ما نتأكد من جيمي — ممكن يكون موجود في مكان تاني من الاستمارة (الصور
المرفقة مقطوعة/scroll جزئي) أو يكون فعلاً مش مطلوب هنا. هيتراجع مع تأكيد شكل
القالب الرسمي.

#### ب) استمارة بيانات المنازعة الأسرية (قسم مشترك واحد، مش لكل طرف)

كل الحقول دي كمان **نص حر بالكامل** (تأكيد جيمي: "الحقول كلها... عايزها نص
حر" — شامل حقول الاستمارة دي مش بس بيانات الأطراف):

| # | الحقل |
|---|---|
| 1 | نوع النزاع (الحقل الموجود فعليًا `dispute_type` من 19.3.1 — يفضل زي ما هو) |
| 2 | أسباب النزاع |
| 3 | الغرض من تقديم طلب التسوية |
| 4 | مقترحات مقدم الطلب في خصوص التسوية |
| 5 | ما يركن إليه مقدم الطلب في الإثبات |
| 6 | بيان المستندات المرفقة بالطلب |

#### ج) "تحميل بيانات موكل" / "تحميل بيانات خصم" — تكامل فعلي مع عملاء سند

**قرار جيمي المباشر:** الزرارين دول مش مجرد نص من التطبيق المرجعي — المطلوب
فعليًا سحب بيانات **موكل موجود بالفعل في جدول العملاء (`clients`) بتاع سند**
لتعبئة حقول "بيانات المدعي" (وبالمثل زرار "تحميل بيانات خصم" لحقول "بيانات
المدعي عليه" — الأرجح انه بيسحب من نفس جدول `clients` كمان، بفرض إن الخصم
غالبًا طرف تاني مسجّل كعميل أو جهة اتصال في سند؛ ده افتراض مني محتاج تأكيدك
الصريح وقت التنفيذ). الأثر المعماري: الفيتشر ده **مش هيفضل معزول تمامًا** عن
باقي سند زي ما كان مخطَّط أصلاً في 19.1 ("الملف مش مرتبط بقضية") — الاستقلال
هنا لسه قائم بخصوص الـ`case`/القضية بس، لكن هيحتاج ربط قراءة (read-only) مع
`clients` وربما جدول الخصوم/الأطراف المقابلة لو موجود. التفاصيل التقنية
(انهي حقول `clients` بتتوافق مع انهي حقول هنا، إزاي شكل الـUI للاختيار من
قايمة عملاء) هتتحدد وقت التنفيذ الفعلي.

#### د) لسه الوحيد المطلوب فعليًا قبل بدء الكود

**البند 2 القديم في 19.3 لسه قائم:** شكل الملف الرسمي **بعد** التعبئة
والتوليد (مش شكل استمارة الإدخال، ده وصلنا بالفعل في القسم ده) — جيمي وعد
يبعته في الرسالة الجاية. بمجرد وصوله، الاستكمال الفعلي هيبدأ (تفاصيله في
19.4 تحت، محدَّثة لتشمل التغييرات دي).

### 19.5 ❌ إلغاء أولوية 7 بالكامل (26 أغسطس 2026، سادس جلسة) — قرار نهائي

**جيمي قرر إلغاء فيتشر "تجهيز ملف تسوية أسرة" نهائيًا** — مش تأجيل، مش
تجميد، إلغاء كامل. السبب الموثّق بالحرف:

> "كل محكمة ليها نموذج مختلف لازم يبقى نفس الشكل ونفس البيانات وكل حاجة ومش
> بتقبل حتى نسخة مطابقة. ف عمليًا ده صعب جدًا، وع الوضع ده هتبقى فيتشر
> ملهاش أي قيمة ومحدش هيستخدمها نهائي."

ده بيلغي الافتراض الأساسي اللي الفيتشر كله كان مبني عليه من 19.1 ("المخرج
النهائي: ملف بيانات يتقدّم لمكتب تسوية المنازعات الأسرية بقالب رسمي موحّد").
لو الجهات المستقبِلة (المحاكم/مكاتب التسوية) كل واحدة فيها ليها نموذجها
الرسمي الخاص ومش بتقبل غيره حتى لو مطابق شكليًا، يبقى مفيش "قالب واحد" ممكن
نبنيه يخدم كل الحالات — والمستند الناتج هيتقال عليه "مرفوض" عمليًا في أول
استخدام حقيقي. القرار متسق مع فلسفة سند المعلَنة: كل فيتشر لازم يبقى قوي
ومفيد عمليًا، مش موجود على الورق بس.

**الإجراء المتخذ في نفس الجلسة:**

| الإجراء | التفاصيل |
|---|---|
| حذف الكود بالكامل | `src/features/familySettlement/` (كل المجلد: `types.ts`، `api/familySettlementApi.ts`، `components/FamilySettlementForm.tsx`) و`src/pages/FamilySettlementPage.tsx` اتشالوا نهائيًا. |
| تنظيف التنقّل | `src/useNavigation.ts` (شيل `'familySettlement'` من `TabName` union ومن مصفوفة `PATH_TABS`)، `src/app/shell/navConfig.ts` (شيل عنصر `moreNavItems`)، `src/app/CommandDock.tsx` (شيل زرار الموبايل)، `src/App.tsx` (شيل الـimport وبلوك الرندر بتاع `tab === 'familySettlement'`) — بحث شامل (`grep -rn familySettlement src/`) اتأكّد إنه رجع فاضي تمامًا، صفر أثر متبقي. |
| رولباك قاعدة البيانات | ميجريشن جديدة `database/migrations/sql-migrations-phase8/02-family-settlement-rollback.sql` — بتشيل الـ3 RLS policies بعدين `DROP TABLE family_settlement_files`. **لازم تشغيلها على الإنتاج** لأن جيمي أكّد إن ميجريشن `01-family-settlement-schema.sql` كانت اتشغّلت فعليًا هناك قبل كده. |
| ملف الإنشاء الأصلي | `01-family-settlement-schema.sql` **اتسيب زي ما هو** (مش هيتشال) — للتوثيق التاريخي بس (نفس منطق أي migration سابقة في سند، الميجريشنز مالهاش رجوع للخلف بتعديل، بس بميجريشن تانية فوقها). |
| فحص نحوي | `node --experimental-strip-types --check` سليم على `src/useNavigation.ts` بعد التعديل. |

**الحالة الآن:** أولوية 7 مقفولة نهائيًا. كل البنود المفتوحة السابقة في
19.3/19.3.2 (قايمة الحقول، شكل القالب الرسمي، تكامل عملاء سند) **بطلت
تلقائيًا** — محتاجة صفر متابعة.

### 19.4 الخطوة الجاية (محدَّثة بعد 19.3.2 — ⚠️ لاغية، راجع 19.5)

بمجرد ما شكل القالب الرسمي (البند الوحيد المتبقي) يوصل من جيمي، الاستكمال
المباشر هيكون:

- توسيع `family_settlement_files` schema (migration تاني، `ALTER TABLE`)
  بالحقول الـ10 الجديدة لكل طرف (كلها `text`، مفيش `CHECK`/enum لأنها نص حر) —
  راجع 19.3.2(أ)، مع تأكيد نهائي على مصير `national_id` (يفضل ولا يتشال).
- توسيع `family_settlement_files` بحقول استمارة المنازعة الخمسة الإضافية
  (19.3.2ب) — `dispute_type` يفضل زي ما هو من 19.3.1.
- توسيع `FamilySettlementParty`/`FamilySettlementFormValues` في `types.ts`
  بالحقول الجديدة، وتوسيع `FamilySettlementForm.tsx` (كارت كل طرف + قسم
  استمارة المنازعة) — كل حقل `<input type="text">` أو `<input type="date">`
  حسب الحقل، **بدون أي `<select>`**.
- تنفيذ زرار "تحميل بيانات موكل"/"تحميل بيانات خصم" (19.3.2ج) — قراءة من
  جدول `clients` الموجود، مع تأكيد جيمي وقت التنفيذ على تفاصيل الربط (خصوصًا
  مصدر بيانات "الخصم" لو مختلف عن `clients`).
- كتابة `renderFamilySettlementContent()` الفعلية مطابقة للقالب الرسمي.
- تفعيل زرار "تصدير PDF" (ربط `exportApi.ts`/jsPDF الموجود فعليًا، بنفس نمط
  المستندات القانونية).

---

## 20. قسم 14 — جلسة تحديد محتوى تصنيف "إعلانات" (26 أغسطس 2026، سابع جلسة)

⚠️ **هذا القسم توثيق قرارات فقط — صفر تنفيذ كود حتى إشعار آخر.** جيمي طلب
صراحة نتفق ونوثّق كل حاجة في التقرير الأول، قبل أي بداية تنفيذ فعلي لأولوية
7 القديمة رقمها اتغيّر... لأ، المقصود هنا قسم 14 (محتوى المستندات تحت
التصنيفات الخمسة الجديدة). التنفيذ الفعلي (schema/مكونات/رندر القالب) هيبدأ
في جلسة لاحقة منفصلة بعد ما التوثيق يكتمل لكل تصنيف.

### 20.1 القرار العام: التقسيمة البصرية لكل مستند (صندوق "الموضوع" + متن رسمي)

جيمي بعت صورة نموذج ورقي حقيقي ("نموذج إعلان بأصل الصحيفة") كمرجع للشكل
النهائي المطلوب. الشكل بيتكوّن من جزئين:

**أ) صندوق جانبي ثابت ("الموضوع")** — يظهر كملخص/بطاقة تعريف للمستند:
- ترويسة "مكتب الأستاذ / المحامي" أعلى الصندوق
- "الموضوع: [نوع الإعلان] في الدعوى" + خانات تصنيف رقم القضية (رقم / لسنة /
  نوع القضية — تظهر كتيك بوكسات أو حقول في النموذج الورقي)
- "وذلك لجلسة" + تاريخ الجلسة القادمة
- "وكيل الطالب / المحامي" أسفل الصندوق

**ب) المتن الرسمي (نص الإعلان الكامل)** — بالصياغة القانونية التقليدية:
- الديباجة: "إنه في يوم ... الموافق .../.../٢٠١ الساعة ... بناحية ..."
- "بناء على طلب السيد/ [الطالب] [الصفة] المقيم [العنوان]، ومحله المختار مكتب
  الأستاذ/ [المحامي]"
- "أنا محضر محكمة [كذا] قد انتقلت وأعلنت: السيد/ [الخصم] [الصفة] المقيم
  [العنوان]"
- "مخاطبًا مع: ____"
- "وأعلنته بالآتي: [نص خاص بنوع الإعلان — يختلف من نموذج للتاني]"
- "بناء عليه" — فقرة التكليف بالحضور: المحكمة/الجهة المختصة (مثلاً شئون
  الأسرة)، العنوان، تاريخ الجلسة، الساعة، رقم الدائرة
- "ولأجل العلم /"

**✅ قرار مؤكد:** التقسيمة دي (صندوق "الموضوع" + متن رسمي) **خاصة بتصنيفَي
"إعلانات" و"عرائض" بس** — باقي التصنيفات (توكيلات/طلبات/أشكال/تظلمات/جنح
مباشرة/عقود) هيكون لها شكل مختلف يتحدد لما نوصلهم في جلسات لاحقة، مش نفس
القالب البصري ده.

### 20.2 تصنيف "إعلانات" — القايمة النهائية للنماذج (5 + نموذج فاضي)

جيمي حدد إن تصنيف "إعلانات" يحتوي على 5 نماذج جاهزة لأكتر أنواع الإعلانات
شيوعًا في شغله، بالإضافة لنموذج سادس عام غير مخصص:

| # | اسم النموذج | الحالة |
|---|---|---|
| 1 | إعلان بأصل الصحيفة | ✅ مرجعي مصوّر (راجع 20.1 والصورة المرفقة من جيمي) |
| 2 | إعادة إعلان | ⏳ لسه محتاج صياغة المتن الخاصة به |
| 3 | إعلان بالدعوى المدنية | ⏳ لسه محتاج صياغة المتن الخاصة به |
| 4 | إعادة إعلان وعرض صلح (أسرة) | ⏳ لسه محتاج صياغة المتن الخاصة به |
| 5 | إعلان بالأجل الإداري | ⏳ لسه محتاج صياغة المتن الخاصة به |
| 6 | نموذج فاضي (إعلان غير مخصص) | ⏳ الشكل لسه هيتحدد لاحقًا |

**ملاحظة:** ده استبدال لسياسة "عيّنة placeholder واحدة لكل تصنيف" المذكورة
في سجل القرارات بند 3 (قسم 12) — بالنسبة لتصنيف "إعلانات" تحديدًا، جيمي
حدد المحتوى الحقيقي الكامل (5 نماذج + فاضي) بدل الـplaceholder المؤقت.

### 20.3 الحقول المشتركة بين النماذج الخمسة

جيمي أكّد الحقول دي كمشتركة في كل الإعلانات الخمسة (زيادة على أي حقل خاص
بكل نموذج لوحده):

| # | الحقل | التفاصيل |
|---|---|---|
| 1 | الطالب | اسم + صفة + عنوان |
| 2 | الخصم (المعلن إليه) | اسم + صفة + عنوان |
| 3 | رقم القضية | صيغة مركبة: رقم / لسنة / نوع القضية + المحكمة (مثال: "245 لسنة 2026 جنح محكمة كذا") |
| 4 | الجلسة السابقة | تاريخ |
| 5 | الجلسة القادمة | تاريخ |

### 20.4 نموذج مرجعي — إعلان بأصل الصحيفة (النموذج الأول)

جيمي رفع صورة نموذج ورقي فعلي بعنوان "نموذج إعلان بأصل الصحيفة" ليكون
القالب المرجعي البصري لباقي النماذج. التفاصيل الكاملة موثّقة في القسم 20.1
أعلاه. هذا النموذج **ثابت الآن كمرجع نهائي** — باقي النماذج الأربعة هتُبنى
بنفس التقسيمة البصرية، مع اختلاف نص المتن حسب نوع الإعلان.

### 20.5 نموذج مرجعي — إعادة إعلان (النموذج الثاني)

جيمي رفع صورة نموذج ورقي فعلي بعنوان "14- صيغة إعادة اعلان". نفس تقسيمة
صندوق "الموضوع" من 20.1 (إعادة اعلان المعلن اليه / جلسة .../.../.... /
كطلب الطالب / وتحت مسئوليته / وكيل الطالب)، مع **فرق جوهري في المتن** عن
النموذج المرجعي الأول:

- بعد فقرة "أنا محضر محكمة ... قد انتقلت وأعلنت"، بيدخل مباشرة في فقرة
  **"أقام الطالب ضد المعلن إليه الدعوى رقم ... أمام محكمة ... الدائرة ( )
  طالبًا في ختام صحيفة افتتاحها الحكم له بـ: ..."** — ده وصف للدعوى الأصلية
  نفسها، مش موجود بنفس الصيغة في نموذج "بأصل الصحيفة".
- الفقرة اللي بعدها بتوضّح **سبب** إعادة الإعلان تحديدًا: "وتحدد لنظر
  الدعوى جلسة .../.../...، وبتلك الجلسة قررت المحكمة التأجيل لإعادة إعلان
  المدعى عليه لجلسة .../.../... لتخلفه عن الحضور رغم سبق إعلانه قانونًا."
- بيختم بفقرة استشهاد قانوني ثابتة (نص المادة 84/1 من قانون المرافعات) عن
  حكم تخلف المدعى عليه عن الحضور في الجلسة الأولى رغم إعلانه لشخصه.
- **مفيش فقرة "بناء عليه" / التكليف بالحضور الرسمية** بنفس صيغة النموذج
  الأول — الاستشهاد القانوني بالمادة هو اللي بيختم المستند هنا بدل كده.

### 20.6 نموذج مرجعي — إعادة إعلان وعرض صلح (النموذج الرابع، خاص بالأسرة)

جيمي رفع صورة نموذج ورقي فعلي (فيه ترويسة مكتب محامٍ حقيقي كمثال — هتتبدل
ببيانات مكتب كل مستخدم فعليًا). نفس تقسيمة صندوق "الموضوع"، مع خصوصية
واضحة كونه مستند **أسرة**:

- المتن بيذكر إقامة الدعوى، تحديد **جلسة سابقة** حضرها الطالب/الطالبة فقط
  ("مثلت الطالبة أمام المحكمة ولم يمثل المعلن إليه")، فقررت المحكمة
  التأجيل **"لإعادة الإعلان وعرض الصلح"** تحديدًا — ده اللي بيميّزه عن
  نموذج "إعادة إعلان" العام (20.5) اللي بيقول سبب التأجيل "تخلف عن
  الحضور" بس من غير ذكر عرض صلح.
- فقرة "والطالبة تنتظر قرار هذه المحكمة" — جملة وجدانية غير موجودة في أي
  نموذج تاني لحد دلوقتي.
- فقرة التكليف بالحضور هنا بتحدد **صراحة محكمة شئون الأسرة** (بخلاف
  النموذج الأول اللي بيقول "المحكمة" بشكل عام قابل للتخصيص لأي جهة)، ومعاها
  فقرة إضافية عن **إلزام المعلن إليه بالمصاريف ومقابل أتعاب المحاماة** —
  فقرة مش موجودة في النموذجين السابقين.

### 20.7 الخطوة الجاية (منفَّذة — راجع 20.8/20.9/20.10)

الجلسة اتوقفت مؤقتًا بطلب جيمي (توثيق أول، تنفيذ بعدين). البنود التلاتة
الأولى المطلوبة هنا اتقفلت في نفس الجلسة (راجع الأقسام التالية)؛ بند 3/4
(الانتقال لعرائض وباقي التصنيفات) لسه الخطوة الجاية الفعلية — راجع 20.11.

### 20.8 نموذج مرجعي — إعلان بالدعوى المدنية (النموذج الثالث)

جيمي رفع صورتين لنموذج ورقي فعلي (مكتب د. سمير عوض محمود المحامي — إدفو).
نفس تقسيمة صندوق "الموضوع" من 20.1، لكن الطبيعة مختلفة جوهريًا عن باقي
الأربعة: هو إعلان **مركّب** بيخدم دعوى مدنية متولدة جوه جنحة جنائية، مش
دعوى مدنية مستقلة.

- **صندوق الموضوع:** "إعلان بالدعوى المدنية فى جنحة [نوع الجنحة]" — بناء
  على طلب الطالب وتحت مسئوليته / وكيل الطالب / المحامي.
- **المتن:** بيسرد تفاصيل الجنحة الجنائية كاملة (رقم الجنحة/السنة، تاريخ
  الواقعة، وصف الاتهام، التقرير الطبي)، ثم بيوضّح إن الطالب (المجني عليه)
  ادّعى مدنيًا بمبلغ تعويض مؤقت معيّن في جلسة سابقة، وإن المحكمة قررت
  التأجيل تحديدًا للإعلان بالدعوى المدنية وسداد رسمها.
- **فقرة "بناء عليه" (التكليف بالحضور):** التكليف هنا لجلسة **جنائية**
  (سماع الحكم الجنائي بعد إنزال أقصى العقوبة)، **مقرون** بإلزام المعلن
  إليه بمبلغ التعويض المدني المؤقت + المصروفات + أتعاب المحاماة — خلط بين
  الشق الجنائي والمدني في نفس التكليف، وده أهم فرق عن نموذج "بأصل الصحيفة"
  (تكليفه مدني/عام بحت).
- مفيش استشهاد بمادة قانونية معينة (بخلاف نموذج "إعادة إعلان" اللي بيختم
  بالمادة 84/1).
- **ملاحظة جيمي:** هيبقى فيه حقول حرة/زيادة تتضاف يدوي (رقم الجنحة، تاريخ
  الواقعة، تاريخ الجلسة السابقة، الجلسة الجديدة، إلخ) — التفاصيل النهائية
  للحقول هتتحدد لما نقفل باقي النماذج (خارج نطاق هذه الجلسة التوثيقية).

### 20.9 نموذج مرجعي — إعلان بالأجل الإداري (النموذج الخامس)

جيمي رفع صورة نموذج ورقي فعلي (مكتب الأستاذ حاتم علي كثير المحامي — "إعلان
بالتأجيل الإداري لجلسة .../.../..."). نفس تقسيمة صندوق "الموضوع"، وهو أقصر
وأبسط النماذج الخمسة:

- **صندوق الموضوع:** "إعلان بالتأجيل الإداري لجلسة .../.../..." — بناء على
  طلب الطالب / ومحله المختار مكتب المحامي.
- **المتن:** مختصر جدًا — "أقام الطالب الدعوى رقم ... لسنة ... أمام محكمة
  ... وتداولت بالجلسات، وبجلسة .../.../... تم تأجيل الدعوى إداريًا لجلسة
  .../.../..."
- **بناء عليه:** نفس بنية النموذج الأول (محضر انتقل وأعلن وسلّم صورة)، لكن
  الغرض هنا **إعلام فقط** بالجلسة الجديدة "لسماع الحكم في الطلبات السابق
  إعلانه بها مع حفظ حقوق الطالب الأخرى" — مفيش دعوة لحضور بمضمون مختلف، هي
  استكمال لنفس المسار.
- **ملاحظة موجودة في النموذج الأصلي نفسه** (توثيقية): التأجيل الإداري بيحصل
  بسبب عدم حضور الدائرة (إجازة رسمية) أو انتخابات أو أي سبب تنظيمي تاني —
  مش سبب قانوني متعلق بالخصوم.
- مفيش استشهاد بمادة قانونية، ومفيش صندوق "جلسة سابقة" منفصل زي باقي
  النماذج — بس إشارة لجلسة سابق إعلانها.

### 20.10 النموذج السادس — قرار مؤكد

**✅ قرار مؤكد:** النموذج السادس (الفاضي/غير المخصص) هيبقى صندوق "الموضوع"
+ متن فاضيين بالكامل يملاهم المحامي بحرية (بدون أي نص افتراضي مقترح) — مفيش
تخصيص لنوع إعلان معين، ده الفرق الوحيد عنه وعن النماذج الخمسة التانية.

### 20.11 إغلاق تصنيف "إعلانات" — الحالة النهائية

تصنيف "إعلانات" اتقفل توثيقيًا بالكامل (6 نماذج):

| # | اسم النموذج | الحالة |
|---|---|---|
| 1 | إعلان بأصل الصحيفة | ✅ موثّق (20.4) |
| 2 | إعادة إعلان | ✅ موثّق (20.5) |
| 3 | إعلان بالدعوى المدنية | ✅ موثّق (20.8) |
| 4 | إعادة إعلان وعرض صلح (أسرة) | ✅ موثّق (20.6) |
| 5 | إعلان بالأجل الإداري | ✅ موثّق (20.9) |
| 6 | نموذج فاضي (إعلان غير مخصص) | ✅ محسوم (20.10) |

⚠️ **صفر تنفيذ كود لسه حتى الآن** — القسم كله (20) توثيق قرارات فقط، زي ما
اتفق مع جيمي من البداية.

### 20.12 الخطوة الجاية (منفَّذة — راجع قسم 21)

الانتقال لتصنيف "عرائض" اتنفّذ في نفس الجلسة — راجع القسم 21 التالي. باقي
التصنيفات الست (إنذارات/توكيلات/طلبات/أشكال/تظلمات/جنح مباشرة/عقود) لسه
الخطوة الجاية الفعلية.

---

## 21. تصنيف "عرائض" — قرار محتوى (26 أغسطس 2026)

⚠️ **توثيق قرارات فقط — صفر تنفيذ كود.**

### 21.1 القرار: نموذج فاضي واحد بس (الصيغ الشائعة مؤجَّلة)

جيمي وضّح إن أنواع صحف الدعاوى غير محدودة عمليًا (كل نوع دعوى له صياغة
موضوع/متن مختلفة)، وبعت صورة نموذج ورقي فعلي ("صيغة دعوى صحة توقيع" — مكتب
حسام الموافي للمحاماة والاستشارات القانونية) **كمثال توضيحي بحت للبنية
المتكررة**، مش كنموذج مزروع فعليًا.

**✅ قرار مؤكد:** تصنيف "عرائض" هيبدأ بـ**نموذج فاضي واحد بس**، بنفس
التقسيمة البصرية من 20.1 (صندوق "الموضوع" + متن رسمي) — كل الحقول والمتن
حر بالكامل يكتبه المحامي يدويًا حسب نوع الدعوى (صحة توقيع، فسخ عقد، مطالبة
مالية... إلخ)، بنفس فلسفة النموذج السادس الفاضي في "إعلانات" (20.10).

**الصيغ الجاهزة الشائعة** (زي ما اتعمل مع "إعلانات" بالخمس نماذج) —
**مؤجَّلة لجلسة لاحقة**، بطلب صريح من جيمي: "الصيغ الجاهزة دلوقتي لأنها مش
جاهزة معايا". هتتضاف فوق النموذج الفاضي الأساسي، مش بدل منه، لما جيمي
يجهزها.

### 21.2 البنية المرجعية (من صورة "صحة التوقيع" التوضيحية)

الشكل البنيوي المتكرر اللي هيُبنى عليه النموذج الفاضي (مش محتوى "صحة
التوقيع" نفسه):

- ترويسة مكتب المحامي (اسم/بيانات تواصل)
- صندوق "الموضوع" جانبي (نفس نمط 20.1)
- "إنه في يوم ... الموافق .../.../..."
- "بناء على طلب السيد/ [الطالب]، المقيم ... ومحله المختار مكتب الأستاذ/
  [المحامي]"
- "أنا محضر محكمة ... قد انتقلت في التاريخ المذكور أعلاه إلى حيث أقامه"
- "السيد/ [المعلن إليه]، المقيم ..."
- "مخاطبًا مع/ ____"
- "وأعلنته بالآتي" — متن الوقائع/الادعاء (حر بالكامل، يختلف حسب نوع
  الدعوى)
- طلبات ختامية (حرة بالكامل)

### 21.3 الحالة النهائية

تصنيف "عرائض" مقفول توثيقيًا بنموذج فاضي واحد.

---

## 22. تصنيفَي "إنذارات" و"جنح مباشرة" (26 أغسطس 2026)

⚠️ **توثيق قرارات فقط — صفر تنفيذ كود.**

### 22.1 القرار

جيمي أكّد إن تصنيفَي "إنذارات" و"جنح مباشرة" هيتبنوا **بنفس منطق "عرائض"**
(21.1) — مش تصميم بصري خاص بيهم:

**✅ قرار مؤكد:** كل تصنيف من الاتنين هياخد **نموذج فاضي واحد بس**، بنفس
التقسيمة البصرية من 20.1 (صندوق "الموضوع" + متن رسمي) — كل الحقول والمتن
حر بالكامل يكتبه المحامي يدويًا حسب حالة الإنذار/الجنحة المباشرة تحديدًا،
بنفس فلسفة النموذج السادس الفاضي في "إعلانات" (20.10) والنموذج الفاضي في
"عرائض" (21.1).

هيتبع نفس البنية المرجعية الموصوفة في 21.2 (ترويسة مكتب + صندوق موضوع +
"إنه في يوم..." + "بناء على طلب..." + "أنا محضر محكمة... انتقلت وأعلنت" +
متن حر + طلبات ختامية حرة) — مفيش صورة نموذج مرجعي منفصلة اتبعتت لأي منهم،
لأن البنية نفسها اتأكدت زي "عرائض" بالظبط.

**صيغ جاهزة شائعة** لأي من التصنيفين — نفس حالة "عرائض": **مؤجَّلة**، هتتضاف
فوق النموذج الفاضي لما تكون جاهزة معاك.

### 22.2 الحالة النهائية

أربع تصنيفات مقفولة توثيقيًا دلوقتي: إعلانات (6 نماذج)، عرائض/إنذارات/جنح
مباشرة (نموذج فاضي واحد لكل واحد).

---

## 23. إلغاء "أشكال"/"تظلمات" + قرار إزالة "توكيلات" من الإنتاج (26 أغسطس 2026)

⚠️ **توثيق قرارات فقط — صفر تنفيذ كود.** خطة الإزالة الخاصة بـ"توكيلات"
(23.3) مبدئية بس (skeleton)، مبنية على وصف المعمارية الموثّق في هذا التقرير
نفسه، مش على فحص فعلي للكود — لسه محتاجة فحص مباشر لملفات المشروع الحقيقية
وقت التنفيذ (زي ما اتفق عليه من البداية في كل جلسة كود سابقة).

### 23.1 إلغاء "أشكال" و"تظلمات" نهائيًا

**✅ قرار مؤكد:** جيمي ألغى تصنيفَي "أشكال" و"تظلمات" **نهائيًا** من نطاق
مستقبل "المستندات القانونية" في سند. الاتنين كانوا لسه في مرحلة التخطيط
البحت (صفر كود، صفر seed، مفيش حتى نموذج فاضي اتقرر ليهم) — الإلغاء هنا
مجرد شطب من قايمة التصنيفات المستقبلية، ومفيش أي أثر تنفيذي مطلوب.

### 23.2 إزالة "توكيلات" — قرار الإلغاء

**✅ قرار مؤكد:** جيمي قرر إزالة تصنيف "توكيلات" **بالكامل من الإنتاج
فعليًا** — مش بس شطبه من التقرير. ده مختلف جوهريًا عن 23.1 لأن "توكيلات"
واحد من التصنيفات الأربعة الأصلية (إنذارات/توكيلات/عرائض/طلبات) اللي
اتبنت فعليًا وباقالب "توكيل عام" مزروع (seeded) في الإنتاج ضمن مراحل 0-6
من خطة توليد المستندات الأصلية.

جيمي طلب **خطة تنفيذ لاحقًا** — يعني القرار محسوم، بس التنفيذ الفعلي مؤجَّل
لجلسة كود منفصلة (بنفس منطق سابقة "أولوية 7 — تجهيز ملف تسوية أسرة" في
قسم 19: قرار إلغاء + migration رولباك منفصلة، مش تعديل رجعي للـmigration
الأصلية).

### 23.3 خطة إزالة "توكيلات" — مبدئية (skeleton، تحتاج تأكيد وقت التنفيذ)

بناءً على وصف المعمارية الموثّق في هذا التقرير (قسم 2) ونمط الإزالة السابق
المتبع فعليًا في سند (قسم 19.3.3 — رولباك تسوية الأسرة)، الخطوات المتوقعة:

1. **الكود:** إزالة `'توكيلات'` من `DOCUMENT_CATEGORIES` في
   `useDocumentTemplates.ts`، وأي فلترة/عرض مرتبط بيه في `TemplatePicker`
   وباقي المكونات اللي بتقرأ من نفس المصدر.
2. **الداتابيز:** migration رولباك جديدة (بنفس نمط
   `02-family-settlement-rollback.sql`) — تشيل صف/صفوف `document_templates`
   الخاصة بتصنيف "توكيلات" (وأي `document_template_versions` مرتبطة)، أو
   تعطّلها (`is_active=false`) لو الحذف الفعلي خطر على مستندات اتولّدت
   فعلًا بيها قبل كده.
3. **⚠️ نقطة قرار حرجة لازم تتأكد وقت التنفيذ (مش دلوقتي):** لو فيه
   مستخدمين حاليين ولّدوا مستندات "توكيل عام" فعلية واتخزنت في
   `case_documents`/الـstorage قبل كده — الحذف من `document_templates`
   **مايمسحش** المستندات المولّدة القديمة دي (منطقيًا لازم تفضل موجودة في
   ملفات القضايا)، بس المستخدم مش هيقدر يولّد "توكيل عام" جديد تاني. محتاج
   تأكيد صريح إن ده المقصود بالظبط قبل أي migration فعلية.
4. **الفحص الفعلي المطلوب** (مش متاح دلوقتي — التقرير ده بدون ملفات
   المشروع الحقيقية في السياق): البحث عن أي مرجع مباشر تاني لـ"توكيلات"/
   `'توكيل عام'` في الكود (E2E tests، seed scripts، أي hardcoded reference)
   قبل التنفيذ الفعلي.

### 23.4 الحالة النهائية

- "أشكال" و"تظلمات": ملغيين نهائيًا، صفر متابعة مطلوبة.
- "توكيلات": قرار الإلغاء محسوم، **التنفيذ الفعلي (كود + migration) مؤجَّل**
  لجلسة كود منفصلة لما جيمي يجهز ملفات المشروع الحقيقية ويأكّد نقطة 23.3
  بند 3.
- تصنيفات المستندات القانونية المتبقية دلوقتي: إعلانات (6 نماذج) ✅، عرائض
  ✅، إنذارات ✅، جنح مباشرة ✅، طلبات (موجود أصليًا، لسه محتاج توثيق محتوى)،
  عقود (لسه محتاج توثيق محتوى).

---

## 24. تصنيف "طلبات" — النموذج الموحَّد (26 أغسطس 2026)

⚠️ **توثيق قرارات فقط — صفر تنفيذ كود.**

### 24.1 القرار: نموذج موحَّد واحد لكل الطلبات

جيمي بعت نص النموذج الموحَّد اللي هيتبني عليه تصنيف "طلبات" بالكامل — نموذج
واحد يغطي كل أنواع الطلبات (بخلاف "عرائض" اللي محتاج نموذج فاضي حر عام،
"طلبات" ليه بنية ثابتة موحَّدة بيتغير فيها بس الموضوع/المتن):

```
السيد الاستاذ / ...............
تحيه طيبه وبعد
مقدمه لسيادتكم / ............... المحامى
بصفتي وكيلا عن /...............
في الدعوي رقم / ..........

الموضوع
.............
.........
.......

وتفضلوا بقبول وافر الاحترام

                                              مقدمه لسيادتكم
                                                  المحامى
```

**✅ قرار مؤكد:** ده النموذج الموحَّد الوحيد لتصنيف "طلبات" بالكامل — مفيش
تعدد نماذج زي "إعلانات"، ومفيش نموذج فاضي حر زي "عرائض/إنذارات/جنح مباشرة"؛
دي بنية ثابتة بحقول متغيرة + متن حر.

### 24.2 الفرق البنيوي عن "إعلانات/عرائض" (مهم توثيقيًا)

النموذج ده **مختلف تمامًا** عن تقسيمة صندوق "الموضوع" + متن رسمي (20.1)
اللي بتحكم إعلانات/عرائض/إنذارات/جنح مباشرة:

- **مفيش صندوق جانبي** ("الموضوع" كبطاقة تعريف مستقلة) — هنا "الموضوع"
  جزء من متن الخطاب نفسه، مش صندوق منفصل.
- **مفيش شخصية "محضر"** (محضر محكمة بيعلن/بيسلّم صورة) — الصياغة هنا خطاب
  مباشر من المحامي (بصفته وكيلًا) لسيادة القاضي/المسؤول المختص، مش إعلان
  قانوني بواسطة محضر.
- **بنية خطاب رسمي كلاسيكية:** مخاطَب (السيد الأستاذ) → تحية → تعريف
  المحامي وصفته والموكل ورقم الدعوى → موضوع الطلب (حر) → خاتمة احترام →
  توقيع (مقدمه لسيادتكم / المحامي).

### 24.3 الحقول المتغيرة (المشتركة بين كل الطلبات)

| # | الحقل | ملاحظة |
|---|---|---|
| 1 | اسم/صفة المُخاطَب | "السيد الأستاذ / ..." — القاضي أو المسؤول المختص حسب نوع الطلب |
| 2 | اسم المحامي مقدّم الطلب | "مقدمه لسيادتكم / ... المحامى" |
| 3 | اسم الموكل | "بصفتي وكيلًا عن / ..." |
| 4 | رقم الدعوى | "في الدعوى رقم / ..." |
| 5 | متن الموضوع | حر بالكامل — يختلف حسب نوع الطلب (استعلام، تأجيل، صورة ضوئية، إلخ) |

### 24.4 التنسيق (Layout/Styling)

جيمي وضّح صراحة: **"والتنسيق هسيبهولك انت"** — يعني الشكل البصري/التنسيق
(محاذاة، خطوط، تباعد، هوامش) قرار تصميمي مفتوح لسند نفسها وقت التنفيذ
الفعلي، مش محتاج تأكيد إضافي من جيمي على مستوى البكسل — بس البنية النصية
والحقول المذكورة أعلاه (24.1-24.3) هي المُلزمة.

### 24.5 الحالة النهائية

تصنيف "طلبات" مقفول توثيقيًا بنموذج موحَّد واحد.

---

## 25. إلغاء "عقود" نهائيًا (26 أغسطس 2026)

⚠️ **توثيق قرار فقط — صفر تنفيذ كود.**

### 25.1 القرار

**✅ قرار مؤكد:** جيمي ألغى تصنيف "عقود" **نهائيًا** من نطاق مستقبل
"المستندات القانونية" في سند. زي "أشكال" و"تظلمات" (23.1)، "عقود" كان لسه
في مرحلة التخطيط البحت — مفيش أي كود أو seed مرتبط بيه، فالإلغاء هنا مجرد
شطب من قايمة التصنيفات المستقبلية، ومفيش أي أثر تنفيذي مطلوب (بخلاف
"توكيلات" في 23.2 اللي كانت مزروعة فعليًا في الإنتاج).

### 25.2 الحالة النهائية — كل تصنيفات "المستندات القانونية" محسومة الآن

جدول شامل بحالة كل التصنيفات اللي اتناقشت في نطاق هذا التقرير:

| # | التصنيف | الحالة النهائية |
|---|---|---|
| 1 | إعلانات | ✅ موثّق بالكامل — 6 نماذج (20.4-20.10) |
| 2 | عرائض | ✅ موثّق — نموذج فاضي واحد (21.1) |
| 3 | إنذارات | ✅ موثّق — نموذج فاضي واحد (22.1) |
| 4 | جنح مباشرة | ✅ موثّق — نموذج فاضي واحد (22.1) |
| 5 | طلبات | ✅ موثّق — نموذج موحَّد واحد (24.1) |
| 6 | توكيلات | 🗑️ **الكود جاهز ✅ (قسم 26)** — الـmigration مكتوبة، لسه محتاجة تشغيل فعلي منك على الإنتاج |
| 7 | أشكال | 🗑️ ملغاة نهائيًا (23.1) — صفر أثر تنفيذي |
| 8 | تظلمات | 🗑️ ملغاة نهائيًا (23.1) — صفر أثر تنفيذي |
| 9 | عقود | 🗑️ ملغاة نهائيًا (25.1) — صفر أثر تنفيذي |

**النتيجة:** خمس تصنيفات نشطة موثَّقة بالكامل توثيقيًا (إعلانات/عرائض/
إنذارات/جنح مباشرة/طلبات)، تصنيف واحد مقرر إزالته من الإنتاج ومحتاج جلسة
كود منفصلة (توكيلات)، وثلاث تصنيفات ملغاة نهائيًا بدون أي أثر (أشكال/
تظلمات/عقود).

قسم "محتوى/صياغة كل قالب مستند" من هذا التقرير (زي ما اتحدد في نطاق
التقرير الأصلي، قسم 1) **مقفول بالكامل** الآن. ~~الخطوة الجاية الفعلية
الوحيدة المتبقية من هذا المسار: جلسة كود منفصلة لتنفيذ إزالة "توكيلات" من
الإنتاج (23.3)~~ — **✅ نُفّذت بالكامل، راجع قسم 26 الجديد.** باقي كل حاجة
تانية جاهزة للانتقال لمرحلة التنفيذ الفعلي (schema/مكونات/رندر القوالب)
للتصنيفات الخمسة النشطة، وقت ما جيمي يقرر البدء.

---

## 26. تنفيذ إزالة "توكيلات" من الإنتاج (26 أغسطس 2026، رابع جلسة) — ✅ الكود جاهز، محتاج تشغيل الـmigration منك

بناءً على خطة السكيلتون في قسم 23.3، اتعمل فحص مباشر للكود الفعلي (مش
افتراض) للتأكد من كل مرجع حقيقي لتصنيف "توكيلات"، والتنفيذ الكودي اتقفل.

### 26.1 الفحص الفعلي — كل المراجع الحقيقية لتصنيف "توكيلات"

بحث شامل في الكود لقى إن كل انتشار كلمة "poa"/"power_of_attorney" في
الكودبيز (عشرات الملفات) خاص بحقل `power_of_attorney` على `case_parties`
(توكيل الطرف الفعلي في القضية — فيتشر مختلف تمامًا، مالوش أي علاقة
بتصنيف مستندات "توكيلات")، **ماعداش لمسه خالص**. المراجع الحقيقية
لتصنيف "توكيلات" (فيتشر توليد المستندات) اتحصرت في 4 ملفات كود + قالب
seed واحد:

1. `useDocumentTemplates.ts` — `'توكيلات'` جوه `DOCUMENT_CATEGORIES`
2. `TemplateCard.tsx` — إدخال `'توكيلات'` جوه `CATEGORY_BADGE_STYLE`
3. `CategoryPicker.tsx` — إدخال `'توكيلات'` جوه `CATEGORY_CARD_STYLE`
4. `caseTypeCategoryPriority.ts` — `'توكيلات'` جوه أولوية قاعدة
   "نفقة/طلاق/خلع/حضانة/رؤية/أحوال شخصية/أسرة" (أولوية 4)
5. `database/migrations/sql-migrations-phase7/02-document-generation-seed.sql`
   — قالب "توكيل عام" الأصلي (مش هيتلمس، ده تاريخي)

`generationApi.test.ts` فيه fixture اسمها `poa` بس دي مش مرتبطة بـ
`DOCUMENT_CATEGORIES` خالص — بتستخدم قالب "توكيل عام" كمجرد مثال عام
لاختبار محرك التوليد نفسه (ربط الحقول)، مش لاختبار قايمة التصنيفات. تُركت
من غير تعديل.

`AILegalAssistant.tsx` فيها سطر وصفي عام للمساعد الذكي بالشات ("مذكرات
وصحف دعاوى وتوكيلات") — نص تسويقي عام، مش مربوط بجدول
`document_templates`، برّه نطاق إزالة التصنيف. تُرك عمدًا؛ لو جيمي عايز
يتشال، محتاج قرار منفصل.

### 26.2 التعديلات الكودية المنفَّذة (✅ الأربعة كلهم)

**1. `src/features/documentGeneration/hooks/useDocumentTemplates.ts`**
`'توكيلات'` اتشالت من `DOCUMENT_CATEGORIES` — بقت 8 تصنيفات بدل 9.

**2. `src/features/documentGeneration/components/TemplatePicker/TemplateCard.tsx`**
إدخال `'توكيلات'` اتشال من `CATEGORY_BADGE_STYLE`.

**3. `src/features/documentGeneration/components/CategoryPicker.tsx`**
إدخال `'توكيلات'` اتشال من `CATEGORY_CARD_STYLE`. الشاشة نفسها
(`CategoryPicker`) بترندر الكاردز مباشرة من `DOCUMENT_CATEGORIES`
(`data-driven`)، فكارت "توكيلات" بيختفي تلقائيًا من الويزارد من غير أي
تعديل تاني في منطق العرض.

**4. `src/features/documentGeneration/lib/caseTypeCategoryPriority.ts`**
`'توكيلات'` اتشالت من قاعدة أولوية "نفقة/طلاق/أحوال شخصية" (كانت التالتة
في القايمة، بقت القايمة تصنيفين بس: `['عرائض', 'إعلانات']`). **ملحوظة
مهمة:** فيه تست حماية موجود فعليًا
(`caseTypeCategoryPriority.test.ts`) بيتأكد إن كل تصنيف مقترح من أي قاعدة
موجود فعليًا في `DOCUMENT_CATEGORIES` الحقيقية — لو اتسابت `'توكيلات'`
هنا من غير ما تتشال من هناك، التست ده كان هيفشل فورًا (بالظبط الحماية
اللي اتصمم عشانها). التعديل هنا ضروري، مش اختياري.

فحص نحوي (`node --experimental-strip-types --check`) سليم على الملفين
غير-JSX (`useDocumentTemplates.ts`, `caseTypeCategoryPriority.ts`).
الملفين التانيين `.tsx` وفيهم JSX فعلي، فالأداة دي مبتدعمهاش (قيد أداة
مش مشكلة كود) — اتراجعوا بصريًا سطر بسطر بدل كده.

### 26.3 Migration جديدة — ARCHIVE مش DELETE (✅ جاهزة، لسه محتاجة تشغيل منك)

`database/migrations/sql-migrations-phase8/01-remove-tawkilat-category.sql`

**القرار الحاسم اللي كان مفتوح في 23.3 بند 3 اتحسم هنا بطريقة أأمن من
الاتنين المطروحين أصلًا:** بدل الاختيار بين "امسح القديم" أو "سيبه"،
الحل هو `UPDATE document_templates SET status = 'archived' WHERE
category = 'توكيلات'` — مش `DELETE`. السبب التقني: `generated_documents.
template_id` مربوط بـFK **من غير `ON DELETE CASCADE`** (اتأكد من
`01-document-generation-schema.sql` نفسه)، يعني `DELETE` فعلي كان ممكن
يفشل بـforeign key violation لو أي مكتب ولّد "توكيل عام" قبل كده، أو
يحتاج تعديل الـschema عشان يسمح بالحذف. الـ`archive` كافي 100%:
`getActiveTemplates()` (في `templatesApi.ts`) بيفلتر على
`status = 'active'` بس، فالقالب بيختفي من الويزارد فورًا من غير ما يمسح
أي صف — أي مستند "توكيل عام" اتولّد فعلاً قبل كده يفضل زي ما هو تمامًا
في `case_documents`/`generated_documents`. **النتيجة: مفيش قرار إضافي
محتاج تأكيدك خالص بخصوص المستندات القديمة — الطريقة دي بطبيعتها
مامتلمسهاش.**

⚠️ **لسه محتاج منك:** تشغيل الـmigration فعليًا على الإنتاج (زي التلات
migrations السابقين في قسم 18/19)، لأن السندبوكس مفيهوش نت زي المعتاد.
تأكيد الاستعلام موجود جوه الملف نفسه كـcomment.

### 26.4 الحالة النهائية

- **الكود:** ✅ الأربعة ملفات اتعدّلوا وجاهزين للدمج.
- **الـMigration:** ✅ مكتوبة وجاهزة، ⏳ لسه محتاجة تشغيل فعلي منك على الإنتاج.
- **`npm run build`/`npm test`:** ⏳ لسه محتاج تأكيد يدوي منك (نفس قيد
  السندبوكس المتكرر من أول التقرير).
- تصنيف "توكيلات" دلوقتي **8 من 9 تصنيفات المستقبل بقوا نهائيين تمامًا**
  (الوحيد المتبقي فعليًا للمناقشة: لا حاجة — كل حاجة محسومة، الباقي
  تنفيذ فقط).

---

## 27. البنية التقنية لصندوق "الموضوع" (26 أغسطس 2026، ثامن جلسة) — ✅ منفَّذة، صفر محتوى قوالب

بعد إغلاق قسم "محتوى/صياغة كل قالب" (قسم 25) توثيقيًا بالكامل، بدأ التنفيذ
الفعلي. جيمي اختار صراحة نبدأ **بالبنية التقنية أولاً** (دعم صندوق "الموضوع"
في المعاينة/PDF/Word) قبل كتابة أي محتوى قوالب حقيقي — عشان محتوى النماذج
العشرة (إعلانات×6 + عرائض/إنذارات/جنح مباشرة/طلبات) يُكتب على بنية جاهزة
بدل ما يُعاد لاحقًا.

### 27.1 الفحص الأول — الفجوة المكتشفة

فحص مباشر لمحرك العرض/التصدير الحالي (`DocumentPreviewEditor.tsx` +
`exportApi.ts`) كشف إنه بيرندر **نص واحد متصل بس** (ترويسة مكتب + عنوان
القالب + متن `white-space:pre-wrap`) — مفيش أي دعم لتقسيمة صندوق جانبي +
متن رسمي (قسم 20.1)، وأصلًا `document_content_json` بيتبني من قسم واحد
فقط (`{type:'intro', text}`) لأن `body_template` عمود نصي واحد بلا تقسيم.

### 27.2 القرار المعماري: عمود `box_template` اختياري (مش تعديل `body_template`)

بدل تعديل شكل `body_template` نفسه أو جدول منفصل، القرار: عمود جديد
`box_template` (nullable) على `template_versions` — التوافق الرجعي كامل:
القوالب الأربعة الحالية (`box_template = NULL`) بتفضل بترندر بشكلها
الحالي بالحرف، صفر تغيير بصري عليها.

### 27.3 التعديلات المنفَّذة

| الملف | التعديل |
|---|---|
| `database/migrations/sql-migrations-phase8/02-document-box-template-column.sql` | **جديد** — `ALTER TABLE template_versions ADD COLUMN box_template text` (nullable). |
| `src/database.types.ts` | إضافة `box_template: string \| null` لـ`Row`/`Insert`/`Update` بتاع `template_versions` (تزامن مع الميجريشن). |
| `src/features/documentGeneration/types.ts` | `TemplateVersion.box_template` جديد؛ `DocumentContentSection.type` union اتوسّعت بقيمة `'subject_box'`. |
| `src/features/documentGeneration/api/generationApi.ts` | `renderDocumentContent()` بقت تاخد `boxTemplate` اختياري (باراميتر رابع) — لو موجود، بتضيف `subject_box` section الأول قبل الـ`intro`؛ لو مش موجود (كل القوالب الحالية)، نفس السلوك القديم بالحرف. `generateDocument()` بقت بتمرر `version.box_template`. |
| `src/features/documentGeneration/hooks/useGenerateDocument.ts` | `templateVersionRef` + مسار التوليد الأوفلاين اتوسّعوا لحمل/تمرير `boxTemplate`. |
| `src/features/documentGeneration/lib/offlineTemplateCache.ts` | `CachedTemplateData.boxTemplate` اختياري جديد (كاش قديم بدونه بيتعامل كـ`null` تلقائيًا). |
| `src/features/documentGeneration/components/DocumentPreviewEditor.tsx` | قسم `subject_box` بيترندر كبطاقة مبروزة (بوردر، عرض 70%، محاذاة يمين) بدل نص عادي متصل. |
| `src/features/documentGeneration/api/exportApi.ts` | `splitBoxSection()` جديدة تفصل قسم الصندوق عن باقي المتن؛ **PDF** (`exportToPdf`) بيرسم الصندوق كـ`<div>` مبروز بحدود قبل المتن؛ **Word** (`exportToDocx`) بيرسم الصندوق كفقرات محاطة ببوردر (`BorderStyle.SINGLE`) قبل فقرات المتن. |

### 27.4 تبسيط بصري مقصود (موثّق، مش قرار صامت)

الشكل الورقي الأصلي (20.1) فيه الصندوق **عائم في أعلى يمين** الصفحة
والمتن بيلف حواليه/تحته. التنفيذ هنا وضع الصندوق **فوق المتن مباشرة**
(مش عائم/ملفوف) — أبسط وأوثق بصريًا مع `html2canvas`/مكتبة `docx`، بدون
مخاطرة تراكب نصوص. لو الشكل النهائي محتاج الصندوق فعليًا عائم بجانب أول
سطرين من المتن، ده تحسين بصري لاحق منفصل، مش بلوكر لبدء كتابة المحتوى.

### 27.5 التحقق

`node --experimental-strip-types --check` سليم على كل الملفات غير-JSX
المعدَّلة (`types.ts`, `generationApi.ts`, `useGenerateDocument.ts`,
`offlineTemplateCache.ts`, `exportApi.ts`, `database.types.ts`).
`DocumentPreviewEditor.tsx` (JSX) اتراجع يدويًا فقط — نفس قيد أداة الـTSX
check غير المتاحة في السندبوكس، المذكور من أول التقرير. `npm run build`/
`npm test` الحقيقيين لسه محتاجين تأكيد منك، وكمان تشغيل الميجريشن الجديدة
(02) على الإنتاج — **بعد** ميجريشن إزالة "توكيلات" (01) اللي لسه معلّقة
هي كمان من قسم 26.

### 27.6 الحالة الآن — جاهز لكتابة المحتوى

البنية التقنية لصندوق "الموضوع" جاهزة. الخطوة الجاية: كتابة المحتوى
الفعلي (`body_template`/`box_template`/`template_fields`) للنماذج
العشرة، نموذج نموذج، بنفس بروتوكول المشروع المعتاد (تسليم بعد كل نموذج
أو مجموعة صغيرة، مش كل حاجة دفعة واحدة). محتاج منك تأكيد: نبدأ بأنهي
نموذج الأول من الـ6 بتوع "إعلانات" (الأرجح "إعلان بأصل الصحيفة" بما إنه
النموذج المرجعي الأساسي)، ولا نبدأ بـ"طلبات" (الأبسط، نموذج موحّد واحد
بلا صندوق) الأول؟

---

## 28. أول محتوى فعلي — "إعلان بأصل الصحيفة" (26 أغسطس 2026، تاسع جلسة)

اخترت (بتفويضك) نبدأ محتوى "إعلانات" بالنموذج المرجعي نفسه — "إعلان بأصل
الصحيفة" — لأنه بيمرّن مسار الصندوق التقني (قسم 27) اللي لسه متبنيش عليه
أي محتوى حقيقي، قبل ما نكمل باقي الخمسة.

### 28.1 الملف الجديد

`database/migrations/sql-migrations-phase8/03-elanat-asl-saheefa-seed.sql`
— نفس نمط seed القوالب الأربعة الأصلية (`DO $$ ... END $$`)، لكن هنا
بيملأ `box_template` كمان (العمود الجديد من قسم 27) جنب `body_template`.

### 28.2 المحتوى — مطابق لقسم 20.1/20.3/20.4 بالحرف

- **صندوق الموضوع:** ترويسة المحامي + "الموضوع: إعلان بأصل الصحيفة في
  الدعوى رقم .../لسنة .../نوع القضية" + "وذلك لجلسة ..." + "وكيل الطالب".
- **المتن:** الديباجة (يوم/ساعة/ناحية) → "بناء على طلب" (الطالب + صفته +
  عنوانه + محله المختار) → "أنا محضر محكمة ... انتقلت وأعلنت" (الخصم +
  صفته + عنوانه) → "مخاطبًا مع" → "وأعلنته بالآتي" (نص حر — ده تحديدًا
  محتوى النموذج المرجعي/العام) → "بناء عليه" (التكليف بالحضور) → "ولأجل
  العلم".
- **20 حقل** يغطوا الحقول المشتركة الخمسة من قسم 20.3 (الطالب/الخصم/رقم
  القضية المركّب/الجلسة السابقة/القادمة) + حقول الديباجة (تاريخ وساعة
  ومكان الانتقال) + `announcement_content` (textarea حر لمضمون الإعلان
  تحديدًا).
- **الربط التلقائي:** `applicant_name`/`addressee_name` بيتحلّوا تلقائيًا
  من `case_parties` (نفس منطق `resolveCaseBindings` الموجود بالفعل —
  `addressee_name` تحديدًا بيجيب الطرف التاني، مش الموكل)، و`case_number`/
  `court_name` من عمودي `cases` الموجودين. باقي الحقول (السنة/نوع القضية/
  التواريخ/العناوين/الصفات) يدوية — مفيش binding_source جاهز ليها في
  `resolveCaseBindings` حاليًا.

### 28.3 الحالة

الملف جاهز، لسه محتاج تشغيل على الإنتاج **بعد** ميجريشن 02 (عمود
`box_template`) — والاتنين بعد ميجريشن 01 (إزالة توكيلات) من قسم 26، لو
هتشغلهم في نفس الجلسة. `node --experimental-strip-types --check` مش منطبق
هنا (ملف SQL خالص)، اتفحص يدويًا بس (قوسين متوازنين + بنية DO block سليمة).

**الباقي:** 5 نماذج تانية من "إعلانات" + عرائض/إنذارات/جنح مباشرة/طلبات —
نفس البروتوكول، نموذج ورا التاني.

---

## 29. فحص CI أول تشغيل حقيقي بعد إضافة box_template (26 أغسطس 2026) — خطأ tsc واحد، اتصلح

جيمي رفع لوج CI حقيقي (بدون E2E — الوظيفة دي `build-and-test` بس: unit
tests + lint/type-check/build). النتيجة:

- **Unit tests:** ✅ 1066/1066 نجحوا (56 ملف)، صفر فشل.
- **Build (lint + tsc + vite):** ❌ خطأ واحد بس —

```
src/features/documentGeneration/__tests__/generationApi.test.ts(245,3):
error TS2741: Property 'box_template' is missing in type '{...}'
but required in type 'TemplateVersion'.
```

### 29.1 السبب

قسم 27 (عمود `box_template`) خلّى `TemplateVersion.box_template` حقل
**إلزامي** (`string | null`، مش اختياري) في `types.ts` — قرار مقصود
(يفرض على أي كود جديد يتعامل معاه بوعي، مش يتجاهله بالغلط). لكن دالة
`makeVersion()` في `generationApi.test.ts` (fixture مساعدة بتبني كائن
`TemplateVersion` كامل لتيستات القوالب الأربعة القديمة) اتكتبت قبل قسم 27
ومكانتش اتحدّثت. مفيش أي مكان تاني في الكود فيه نفس المشكلة (فحصت كل
استخدامات `body_template`/`TemplateVersion` في src/ — الملف ده كان
الوحيد).

### 29.2 الفيكس

سطر واحد بس — إضافة `box_template: null` جوه الكائن اللي بترجّعه
`makeVersion()` (نفس القيمة اللي القوالب الأربعة القديمة بترجعها فعليًا
من الداتابيز، لأنها اتزرعت قبل الميجريشن 02). صفر تغيير على أي تيست كيس
أو سلوك فعلي — تصحيح type-only.

### 29.3 التحقق

فحص شامل (`grep`) لكل مكان تاني بيبني `TemplateVersion` كـliteral في
`src/` — الملف ده كان الوحيد في الكود الفعلي (`useGenerateDocument.test.ts`
فيه mocks جزئية شكلها شبه، بس من غير type error فعلي في لوج الـCI، فمش
لمسته). `node --experimental-strip-types --check` سليم على الملف
المُصلَّح.

**الحالة الآن:** الفيكس جاهز. لسه محتاج `npm run build`/`npm test` حقيقيين
للتأكيد النهائي (نفس قيد السندبوكس المعتاد)، وبعدها المسار مفتوح نكمل
النموذج التاني ("إعادة إعلان").

---

## 30. ثاني محتوى فعلي — "إعادة إعلان" (26 أغسطس 2026، عاشر جلسة)

كمّلنا بنفس البروتوكول (نموذج ورا التاني) — النموذج الثاني من الستة تحت
"إعلانات"، حسب قسم 20.5.

### 30.1 الملف الجديد

`database/migrations/sql-migrations-phase8/04-elanat-eada-elan-seed.sql`
— نفس نمط ميجريشن 03 (`DO $$ ... END $$`)، بنفس صندوق "الموضوع" +
متن رسمي، لكن بمحتوى مختلف حسب 20.5.

### 30.2 الفروق الجوهرية عن النموذج الأول (موثّقة كمان جوه الميجريشن نفسها)

- بعد "أنا محضر محكمة ... قد انتقلت وأعلنت"، المتن بيدخل مباشرة في وصف
  الدعوى الأصلية ("أقام الطالب ضد المعلن إليه الدعوى رقم ... طالبًا في
  ختام صحيفة افتتاحها الحكم له بـ: ...") بدل نص حر عام.
- فقرة سبب التأجيل: تخلف المعلن إليه عن جلسة سابقة رغم إعلانه قانونًا.
- **مفيش** فقرة "بناء عليه"/التكليف بالحضور الرسمية — الاستشهاد القانوني
  بالمادة 84/1 مرافعات هو اللي بيختم المستند بدل كده.

### 30.3 قرار مهم — نص المادة 84/1 اتسيب حقل حر، مش نص ثابت

قسم 20.5 وصف الاستشهاد بالمادة 84/1 كـ"نص ثابت". لكن **معنديش تأكيد على
الصياغة الحرفية الدقيقة للمادة** في الجلسة دي، وده مستند هيتقدّم فعليًا
لمحكمة — خطأ في نص مادة قانونية أخطر من ترك حقل فاضي. بدل ما أخترع نص
وأحطه ثابت جوه `body_template`، سبته حقل حر (`article_84_citation`,
`textarea`, إلزامي) زي `announcement_content` في النموذج الأول بالظبط —
المحامي يكتب/يلصق الصياغة الرسمية بنفسه. لو عندك النص الدقيق جاهز، ممكن
نرجّعه fixed text في تعديل لاحق بسيط (سطر واحد في الميجريشن).

### 30.4 الحقول (21 حقل)

نفس الحقول المشتركة من 20.3 (طالب/خصم/رقم قضية مركّب/جلستين) + حقول
الديباجة (زي النموذج الأول) + 3 حقول جديدة خاصة بالنموذج ده: `circuit_number`
(رقم الدائرة)، `claim_summary` (طلبات الطالب في ختام الصحيفة)،
`article_84_citation` (الاستشهاد القانوني — حقل حر، راجع 30.3).
الربط التلقائي (`applicant_name`/`addressee_name`/`case_number`/
`court_name`) نفس منطق النموذج الأول بالحرف (`resolveCaseBindings`
الموجود).

### 30.5 التحقق

نفس مستوى الفحص اليدوي المتبع من قسم 28 (ملف SQL خالص، مفيش
`node --experimental-strip-types --check` منطبق) — قوسين متوازنين (44/44)
وبنية `DO $$ ... END $$` سليمة، اتفحصوا برمجيًا.

**الحالة الآن:** الملف جاهز، لسه محتاج تشغيل على الإنتاج بنفس ترتيب
الميجريشنز (01 → 02 → 03 → 04). **الباقي:** 4 نماذج تانية من "إعلانات"
(إعلان بالدعوى المدنية، إعادة إعلان وعرض صلح، إعلان بالأجل الإداري،
النموذج الفاضي) + عرائض/إنذارات/جنح مباشرة/طلبات.

**🆕 تحديث 26 أغسطس 2026:** جيمي أكّد تشغيل ميجريشنز 01→04 فعليًا على
الإنتاج. راجع قسم 31 للنموذج التالت.

---

## 31. تالت محتوى فعلي — "إعلان بالدعوى المدنية" (26 أغسطس 2026، حادي عشر جلسة)

النموذج الثالث من الستة تحت "إعلانات"، حسب قسم 20.8 — إعلان **مركّب**
بيخدم دعوى مدنية متولدة جوه جنحة جنائية.

### 31.1 الملف الجديد

`database/migrations/sql-migrations-phase8/05-elanat-dawa-madaniya-seed.sql`
— نفس نمط الميجريشنز 03/04 (`DO $$ ... END $$`).

### 31.2 الفروق الجوهرية عن النموذجين السابقين

- المتن بيسرد تفاصيل الجنحة الجنائية كاملة (نوع/رقم/سنة الجنحة، تاريخ
  الواقعة، وصف الاتهام، ملخص التقرير الطبي) **قبل** ما يوصل لموضوع
  الإعلان الفعلي.
- فقرة "بناء عليه" هنا تكليف **جنائي** (سماع الحكم بعد إنزال أقصى
  العقوبة) **مقرون** بإلزام مدني (مبلغ التعويض المؤقت + المصروفات +
  أتعاب المحاماة) — خلط جنائي/مدني في نفس التكليف، بخلاف النموذج الأول
  (مدني/عام بحت) والثاني (بلا تكليف، استشهاد بمادة بدل كده).
- مفيش استشهاد بمادة قانونية معينة هنا.

### 31.3 ملاحظة مفتوحة (موروثة من 20.8)

قسم 20.8 نفسه نبّه إن تفاصيل الحقول كانت لسه مفتوحة وقت التوثيق. الحقول
هنا (24 حقل) مبنية على السرد الوارد في 20.8 بالحرف — لو محتاج تفكيك حقل
زي "التقرير الطبي" لأكتر من حقل منفصل (رقم/تاريخ التقرير بدل نص حر
واحد)، ده تعديل بسيط في ميجريشن تالية.

### 31.4 التحقق

نفس الفحص اليدوي المتبع (SQL خالص، مفيش tsc منطبق) — قوسين متوازنين
(56/56) وبنية `DO $$ ... END $$` سليمة.

**الحالة الآن:** الملف جاهز، محتاج تشغيل على الإنتاج بعد 01→04.
**الباقي:** 3 نماذج تانية من "إعلانات" (إعادة إعلان وعرض صلح، إعلان
بالأجل الإداري، النموذج الفاضي) + عرائض/إنذارات/جنح مباشرة/طلبات.

**🆕 تحديث 26 أغسطس 2026:** جيمي أكّد تشغيل ميجريشن 05 فعليًا على
الإنتاج. راجع قسم 32 للنموذج الرابع.

---

## 32. رابع محتوى فعلي — "إعادة إعلان وعرض صلح" (26 أغسطس 2026، اتناشر جلسة)

النموذج الرابع من الستة تحت "إعلانات"، حسب قسم 20.6 — خاص بالأسرة.

### 32.1 الملف الجديد

`database/migrations/sql-migrations-phase8/06-elanat-eada-solh-osra-seed.sql`
— نفس نمط الميجريشنز السابقة (`DO $$ ... END $$`).

### 32.2 الفروق الجوهرية عن باقي النماذج

- سبب التأجيل تحديدًا "لإعادة الإعلان وعرض الصلح" — مش "تخلف عن الحضور"
  العام زي النموذج الثاني.
- المتن بيوضّح إن الطالب/ة بس اللي حضر الجلسة السابقة، ومفيش تمثيل من
  المعلن إليه.
- جملة وجدانية مميزة: "و[الطالب/ة] [ينتظر/تنتظر] قرار هذه المحكمة".
- التكليف بالحضور بيحدد صراحة **محكمة شئون الأسرة**، ومعاه إلزام
  بالمصاريف ومقابل أتعاب المحاماة.

### 32.3 قرار — حقول حرة للصيغة المذكرة/المؤنثة بدل التثبيت

النموذج الورقي المرجعي استخدم صيغة المؤنث بالكامل ("الطالبة"/"مثلت"/
"تنتظر") لأن الحالة المرجعية كانت طالبة. عشان القالب يصلح لأي جنس من
غير ما نثبّت مذكر أو مؤنث بالغلط، الصياغة المتغيّرة اتحطّت في 3 حقول حرة
(`applicant_label`, `attendance_verb`, `waiting_verb`) بدل نص ثابت —
نفس منطق الحقول الحرة في النماذج السابقة (announcement_content /
article_84_citation) بس هنا للنحو مش للمحتوى القانوني.

### 32.4 الحقول (23 حقل)

نفس الحقول المشتركة من 20.3 + حقول الديباجة + 3 حقول اللقب/الأفعال
المتغيّرة (32.3). الربط التلقائي نفس منطق النماذج السابقة
(`resolveCaseBindings`).

### 32.5 التحقق

قوسين متوازنين (49/49) وبنية `DO $$ ... END $$` سليمة.

**الحالة الآن:** الملف جاهز، محتاج تشغيل على الإنتاج بعد 01→05.
**الباقي:** نموذجين من "إعلانات" (إعلان بالأجل الإداري، النموذج الفاضي)
+ عرائض/إنذارات/جنح مباشرة/طلبات.

**🆕 تحديث 26 أغسطس 2026:** جيمي أكّد تشغيل ميجريشن 06 فعليًا على
الإنتاج، وطلب زيب محدّث للمشروع كامل (اتسلّم). راجع قسم 33 لإغلاق
تصنيف "إعلانات" بالكامل (6/6).

---

## 33. خامس وسادس محتوى فعلي — إغلاق تصنيف "إعلانات" بالكامل (26 أغسطس 2026، تلتاشر جلسة)

النموذجين الأخيرين من الستة، حسب قسم 20.9 (إعلان بالأجل الإداري) وقسم
20.10 (النموذج الفاضي) — بيقفلوا تصنيف "إعلانات" 6/6.

### 33.1 النموذج الخامس — "إعلان بالأجل الإداري"

`database/migrations/sql-migrations-phase8/07-elanat-agal-idari-seed.sql`
— أبسط وأقصر النماذج الخمسة المخصصة (قسم 20.9): إعلام فقط بجلسة جديدة
بعد تأجيل إداري/تنظيمي (مش قانوني)، استكمال لنفس المسار من غير مضمون
جديد، مفيش استشهاد قانوني ومفيش صندوق "جلسة سابقة" منفصل. 18 حقل. فحص
أقواس سليم (34/34).

### 33.2 النموذج السادس — "النموذج الفاضي"

`database/migrations/sql-migrations-phase8/08-elanat-namouzag-fady-seed.sql`
— قسم 20.10 حسم إن الصندوق والمتن يبقوا فاضيين بالكامل، يملاهم المحامي
بحرية بدون نص افتراضي مقترح.

**تفصيلة معمارية لازم تعرفها:** `renderDocumentContent()` بتتحقق من
`boxTemplate` بفحص truthy — لو سيّبنا العمود فاضي تمامًا أو NULL، صندوق
"الموضوع" مش هيترندر خالص (نفس سلوك القوالب القديمة اللي بلا صندوق).
عشان نحقق "صندوق فاضي يُملأ بحرية" فعليًا (مش "من غير صندوق")، الحل:
`box_template`/`body_template` كل واحد فيهم placeholder واحد بس
(`{{free_subject}}`/`{{free_body}}`) مربوط بحقل textarea إلزامي — كده
الصندوق بيترندر، لكن من غير أي نص قانوني افتراضي جواه. حقلين بس. فحص
أقواس سليم (23/23).

### 33.3 الحالة — تصنيف "إعلانات" مقفول 6/6

| # | اسم النموذج | ميجريشن | الحالة |
|---|---|---|---|
| 1 | إعلان بأصل الصحيفة | 03 | ✅ منفَّذ |
| 2 | إعادة إعلان | 04 | ✅ منفَّذ |
| 3 | إعلان بالدعوى المدنية | 05 | ✅ منفَّذ |
| 4 | إعادة إعلان وعرض صلح (أسرة) | 06 | ✅ منفَّذ |
| 5 | إعلان بالأجل الإداري | 07 | ✅ منفَّذ |
| 6 | نموذج فاضي | 08 | ✅ منفَّذ |

لسه محتاج منك: تشغيل ميجريشنز 07/08 فعليًا على الإنتاج (بعد 01→06).

**الباقي من التقرير كله:** المحتوى الفعلي لتصنيفات عرائض/إنذارات/جنح
مباشرة/طلبات (كل واحد فيهم كان "مقفول توثيقيًا بنموذج فاضي واحد" أو
مشابه — راجع أقسام 20-24 — التنفيذ الفعلي (seed migrations) لسه ما
بدأش لأي منهم).

---

## 34. سابع/تامن/تاسع محتوى فعلي — عرائض + إنذارات + جنح مباشرة (26 أغسطس 2026، أربعتاشر جلسة)

التنفيذ الفعلي (seed migrations) للتلات تصنيفات اللي كانوا "مقفولين
توثيقيًا بنموذج فاضي واحد" (أقسام 21/22) — نفس الهيكل المرجعي بالظبط،
تصنيف مختلف بس.

### 34.1 الهيكل المشترك (قسم 21.2، مطبَّق حرفيًا على التلاتة)

بخلاف "النموذج الفاضي" في إعلانات (08) اللي كان حرية كاملة بلا هيكل
(حقلين بس)، النموذج هنا **مبني على هيكل مرجعي ثابت**: ديباجة (محضر
انتقل) → بناء على طلب → أنا محضر محكمة ... انتقلت وأعلنت → مخاطبًا مع →
وأعلنته بالآتي (حر) → الطلبات الختامية (حر). صندوق "الموضوع" فيه سطر
واحد حر (`subject_free`) بدل نص ثابت، لأن نوع كل دعوى/إنذار/جنحة مختلف.

### 34.2 الملفات التلاتة

| الملف | التصنيف | المرجع |
|---|---|---|
| `09-araed-namouzag-fady-seed.sql` | عرائض | قسم 21 |
| `10-indharat-namouzag-fady-seed.sql` | إنذارات | قسم 22.1 |
| `11-jonah-mubashera-namouzag-fady-seed.sql` | جنح مباشرة | قسم 22.1 |

الثلاثة متطابقين بنيويًا (نفس الحقول الـ17، نفس الترتيب) — فرقهم بس
`category`/`name_ar`/`description` حسب التصنيف. **لا يوجد اعتماد فعلي
بين الميجريشنز الثلاثة** (تصنيفات مستقلة)، الترقيم بس بيعكس ترتيب
الجلسة، ممكن تتشغل بأي ترتيب بينهم بعد ميجريشن 02.

### 34.3 ملاحظة مهمة — الصيغ الجاهزة الشائعة لسه مؤجَّلة

الثلاثة نماذج دول "عامة" بس — قسم 21.1/22.1 وضّحوا إن الصيغ الجاهزة
الشائعة (زي ما اتعمل مع إعلانات بالخمس نماذج المخصصة) **مؤجَّلة بطلب
صريح من جيمي** ("مش جاهزة معايا دلوقتي"). النموذج العام هنا هو نقطة
البداية بس، مش نهاية المطاف لكل تصنيف من التلاتة.

### 34.4 التحقق

فحص برمجي لكل الملفات الثلاثة: قوسين متوازنين (39/39 لكل واحد)، بنية
`DO $$ ... END $$` سليمة، وتأكيد إن كل ملف بيحمل الـ`category` الصح
(مش نسخة ملوّثة من عرائض بالغلط بعد التعديل).

**الحالة الآن:** الثلاث ملفات جاهزين، محتاجين تشغيل على الإنتاج (بعد
ميجريشن 02، بأي ترتيب بينهم). **الباقي من التقرير كله:** تصنيف "طلبات"
(النموذج الموحَّد — قسم 24، مقفول توثيقيًا).

---

## 35. عاشر محتوى فعلي (والأخير) — "طلبات" النموذج الموحَّد (26 أغسطس 2026، خمستاشر جلسة)

آخر تصنيف من الخمسة النشطة (قسم 25.2) — بيقفل مرحلة كتابة المحتوى
الفعلي بالكامل لكل تصنيفات "المستندات القانونية" المخطط لها.

### 35.1 الملف الجديد

`database/migrations/sql-migrations-phase8/12-talabat-namouzag-mowahad-seed.sql`
— نفس نمط الميجريشنز السابقة، لكن بـ`box_template = NULL` عمدًا.

### 35.2 الفرق البنيوي الجوهري (قسم 24.2)

بخلاف كل التصنيفات التانية (إعلانات/عرائض/إنذارات/جنح مباشرة) اللي
بتستخدم صندوق "الموضوع" + متن محضر، "طلبات":
- **مفيش صندوق جانبي** — "الموضوع" جزء من متن الخطاب نفسه.
- **مفيش شخصية "محضر"** — خطاب مباشر من المحامي (بصفته وكيلًا) لسيادة
  القاضي/المسؤول المختص، مش إعلان قانوني بواسطة محضر.
- بنية خطاب رسمي كلاسيكية: مخاطَب → تحية → تعريف المحامي/الموكل/رقم
  الدعوى → موضوع الطلب (حر) → خاتمة احترام → توقيع.

عشان كده `box_template` اتسيب `NULL` بدل نص/placeholder — نفس سلوك
القوالب الأربعة الأصلية اللي معندهاش صندوق أصلًا، توافق كامل مع فحص
truthy في `renderDocumentContent()` (قسم 27/33.2)، بلا صندوق فاضي يظهر
غلط.

### 35.3 الحقول (5 حقول بس — أبسط نموذج لحد دلوقتي)

اسم/صفة المُخاطَب، اسم المحامي مقدّم الطلب، اسم الموكل (بيتحل تلقائيًا
`party.name`)، رقم الدعوى (`case.number`)، موضوع الطلب (حر بالكامل —
يغطي استعلام/تأجيل/صورة ضوئية/إلخ حسب قسم 24.3).

### 35.4 التنسيق البصري — قرار مفتوح لسند (موثّق، مش نسيان)

جيمي صرّح صراحة (24.4): "والتنسيق هسيبهولك انت" — المحاذاة/الخطوط/
التباعد/الهوامش قرار تصميمي وقت التنفيذ الفعلي، مش محتاج تأكيد إضافي.
البنية النصية والحقول (24.1-24.3) هي المُلزمة بس.

### 35.5 التحقق

قوسين متوازنين (25/25)، بنية `DO $$ ... END $$` سليمة.

### 35.6 🎉 الحالة الآن — كل التصنيفات النشطة الخمسة منفَّذة بالكامل

| # | التصنيف | نماذج | الحالة |
|---|---|---|---|
| 1 | إعلانات | 6 | ✅ منفَّذ (03-08) |
| 2 | عرائض | 1 | ✅ منفَّذ (09) |
| 3 | إنذارات | 1 | ✅ منفَّذ (10) |
| 4 | جنح مباشرة | 1 | ✅ منفَّذ (11) |
| 5 | طلبات | 1 | ✅ منفَّذ (12) |

**12 ميجريشن بالكامل** (01 إزالة توكيلات، 02 عمود box_template، 03-12
محتوى 10 نماذج عبر 5 تصنيفات) — كلهم جاهزين للتشغيل على الإنتاج
بالترتيب الرقمي (01→12؛ داخل مجموعة 03-12 الترتيب النسبي بين تصنيفات
مختلفة مرن، بس داخل نفس التصنيف زي إعلانات لازم يفضل بالترتيب).

**الباقي المفتوح من التقرير كله (قبل تحديث قسم 36 — راجعه، القيد اتحل):**
- ~~تأكيد `npm run build`/`npm test` حقيقيين لكل التعديلات (نفس قيد
  السندبوكس المعتاد من البداية).~~ ✅ **اتأكَّد فعليًا — قسم 36.**
- الصيغ الجاهزة الشائعة المؤجَّلة لعرائض/إنذارات/جنح مباشرة (قسم
  21.1/22.1) — لما تكون جاهزة.
- أي مراجعة أدق للقرارات المؤقتة المذكورة في المقدمة (نسخ المكتب، إلخ)
  لو لسه مطلوبة.

---

## 36. تأكيد CI حقيقي كامل (unit + build + E2E) — القيد المتكرر من أول التقرير اتحل (26 أغسطس 2026)

جيمي رفع لوجات CI حقيقية لتشغيلتين من نفس الكوميت (`4b3dec0c4dd0`، فرع
`main`)، بيغطوا سوا كل حاجة كان التقرير بيقول من الأول "محتاجة تأكيد يدوي
منك" بسبب قيد عدم وجود نت/`node_modules` في السندبوكس:

1. **`build-and-test`** (22:13 UTC) — نفس الوظيفة اللي فحصناها في قسم 29
   (يوم واحد فات، وقتها كان فيها خطأ `tsc` واحد اتصلح). دلوقتي:
   - **Unit tests (`vitest run`):** ✅ **1066/1066 نجحوا** (56 ملف تست، صفر
     فشل)، بما فيهم كل تستات الإيدج فانكشنز (`admin-actions`,
     `process-law-extract`, `embed-batch`, `embed-query`, `ai-chat`,
     `telegram-send`, `office-secrets`, `saas-admin`, `office-login`,
     `session-alerts`) والهوكس/الصفحات الفرونت-إند كلها.
   - **Build (`npm run lint && tsc --noEmit && vite build`):** ✅ سليم
     بالكامل — صفر أخطاء lint، صفر أخطاء type، الـbuild خلص في 7.44 ثانية.
     خطأ `box_template` المذكور في قسم 29 (الـfixture الناقصة في
     `generationApi.test.ts`) **اتأكَّد إنه انصلح فعليًا** ومحصلش تاني.
   - تحذيرين غير حرجين بس (متوقعين ومش مرتبطين بأي تعديل من التقرير ده):
     تحذير CSS minify بسيط، وتحذير حجم شنك (`index-COBVRi_D.js` ~1MB) —
     نفس تحذير الأداء العادي لأي build فيه مكتبات تصدير PDF/صور
     (`jspdf`, `html2canvas`).

2. **`e2e`** (22:54 UTC، بعد الـbuild-and-test بـ~40 دقيقة، نفس الكوميت) —
   أول تشغيل E2E حقيقي كامل بيوصل للتقرير من أول ما اتفتح:
   - **106 تست إجمالي عبر 3 فيوبورتات (chromium desktop/tablet/mobile) —
     104 نجحوا، 2 skipped، صفر فشل.** المدة: 16.7 دقيقة.
   - الاتنين اللي اتعملهم skip (`ai-assistant.spec.ts` — توليد مستند
     بنجاح + تجاوز السقف اليومي) **مش فشل ومش مرتبطين بأي حاجة في
     التقرير ده** — تستات mock قديمة لمسار الذكاء الاصطناعي بتتعمل لها
     skip أصلًا في الكونفيج الحالي (مفيش أي إشارة في اللوج لسبب جديد أو
     مؤقت).
   - أهم اللي اتغطى وعدّى بنجاح فيما يخص المستندات القانونية تحديدًا:
     `document-generation.spec.ts` (توليد مستند من قضية مفتوحة + تصدير
     PDF + ظهوره في مستندات القضية) ✅، `archive-tab.spec.ts` ✅،
     `docs.spec.ts` (رفع/بحث/حذف مستندات، ومنع الرفع أوفلاين) ✅ — دول
     أقرب مسارات E2E لمنطق قسم 17.6 (كاش الأوفلاين) وقسم 27
     (`box_template`).
   - الـ`global-teardown` نضّف بيانات التست بنجاح (45 قضية، 21 جلسة، 92
     طرف، إلخ) — مفيش data leakage بين التشغيلات.
   - تستات الـvisual regression الثلاثة (mobile/tablet/desktop) عدّت ✅ —
     يعني مفيش كسر بصري غير مقصود من أي تعديل CSS/layout حديث.

### 36.1 الخلاصة

القيد المتكرر ~15 مرة في التقرير ده ("السندبوكس مفيهوش نت، محتاج تأكيد
يدوي منك") **اتقفل رسميًا لكل الكود المنفَّذ لحد الكوميت `4b3dec0`** —
يعني كل بنود التنفيذ في قسم 16/17/18/26/27 اللي كانت مكتوب جنبها "لسه
محتاج `npm run build`/`npm test` حقيقي" بقت مؤكَّدة فعليًا. اللي لسه
مطلوب (لو حصل تعديل كود إضافي بعد الكوميت ده — زي ميجريشنز 03-12 أو أي
حاجة من قسم 28 لحد 35) هو تشغيل CI تاني على الكوميت الأحدث، مش نفس
القيد القديم.

---

## 37. إخفاء قسم "المستندات القانونية" بالكامل — مقصور على السوبر أدمن بس (26 أغسطس 2026)

بطلب جيمي: تاب/قسم "المستندات القانونية" (`LegalDocumentsPage.tsx`،
قسم 9 وما بعده من التقرير ده) بقى **مقصور بالكامل على حساب السوبر أدمن
الوحيد `m.gemy4231@gmail.com`** — نفس الحساب والنمط المستخدم فعليًا من
قبل لقفل قسم الـAI (`isAISuperAdmin` في `App.tsx`) وقسم "المكتبة
القانونية"/"بوابة إدارة المكاتب" في لوحة الإدارة (`SUPER_ADMIN_EMAIL`
في `AdminPanel.tsx`).

### 37.1 التنفيذ

نقطة تحكم واحدة كافت: `canGenerateDocuments` في `App.tsx` كانت
بتُستخدم **حصريًا** في كل نقاط عرض/دخول القسم (تأكدنا بالبحث الشامل
في الكود قبل التعديل) — مفيش أي استخدام تاني ليها في المشروع:

```
const canGenerateDocuments =
  checkPermission(profile, 'can_generate_documents') && isAISuperAdmin;
```

الإضافة دي (`&& isAISuperAdmin`) كفت لتغطية كل ده تلقائيًا من مصدر واحد:

- **CommandDock.tsx** (تاب "مستندات قانونية" في شريط الموبايل السفلي) —
  مابيتعملوش render خالص لغير السوبر أدمن.
- **DesktopSidebar.tsx / TabletDrawer.tsx** (عبر
  `requiresCanGenerateDocuments` في `navConfig.ts`) — نفس الشيء على
  الديسكتوب والتابلت.
- **App.tsx** (محتوى تاب `legalDocs` نفسه) — دفاع فعلي إضافي (مش بس
  إخفاء زرار التنقل) لأن التاب بيدعم deep linking بالـURL
  (`/legal-documents`)، فلو حد وصله برابط مباشر بيشوف رسالة "غير مصرح
  لك بهذا القسم" العادية بدل المحتوى.
- **AppModals.tsx** (زرار "توليد مستند" جوه `DocsSection.tsx` في تفاصيل
  القضية) — بيتبعتله `undefined` فمابيتعملوش render خالص، بنفس شرط
  `onGenerateDocument &&` الموجود من الأول.

النتيجة: محدش (حتى لو دوره `lawyer`/`admin` وعنده
`can_generate_documents=true` فعليًا على قاعدة البيانات) يقدر يشوف
القسم أو يوصله، من أي مكان في الواجهة (موبايل/تابلت/ديسكتوب) ولا حتى
برابط مباشر.

### 37.2 التأثير على تستات E2E

`e2e/document-generation.spec.ts` كانت بتسجّل دخول بحساب
`E2E_TEST_EMAIL` (حساب مكتب عادي، مش السوبر أدمن — نفس ملحوظة
`admin-legal-library.spec.ts` الموجودة بالفعل من قبل) وتجرب رحلة توليد
مستند حقيقية كاملة. بما إن زرار الدخول للقسم ماعادش بيظهر لحساب زي ده،
التست القديم:

- اتحوّل لـ`test.skip` (نفس نمط `ai-assistant.spec.ts` بالظبط — الكود
  فاضل كمرجع، مش متمسوح، لحد ما يحتاج حد يشغّله يدويًا بحساب السوبر
  أدمن الحقيقي).
- تست جديد اتضاف بدل منه، بنفس نمط `admin-legal-library.spec.ts`
  ("حساب مكتب عادي (مش سوبر أدمن) → قسم 'المستندات القانونية' مش ظاهر
  خالص") — بيتأكد إن زرار "توليد مستند" جوه تبويب مستندات القضية
  و`desktop-nav-legalDocs` مالهمش وجود (`toHaveCount(0)`) لحساب عادي.

⚠️ **قيد سندبوكس معتاد**: التعديل ده لسه محتاج نفس تأكيد CI حقيقي
(قسم 36) على كوميت جديد يشمله — مفيش نت/`node_modules` هنا للتحقق منه
فعليًا (lint/tsc/E2E). المنطق راجعته يدويًا سطر بسطر ومتأكد إنه سليم
(`canGenerateDocuments` مالهاش أي استخدام تاني غير مسار المستندات
القانونية دول الأربعة، فالتضييق عليها في مكان واحد آمن ومايأثرش على أي
صلاحية تانية زي `can_view_fees`/`isAdmin`).
