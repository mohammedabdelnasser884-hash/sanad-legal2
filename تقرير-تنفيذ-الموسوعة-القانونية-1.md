# تقرير تنفيذ — قسم "الموسوعة القانونية" (مرحلة 1: قاعدة البيانات والتخزين)

**التاريخ:** 13 سبتمبر 2026

## القرارات النهائية اللي اتقفلت قبل التنفيذ

1. **عداد التحميلات** — مضاف (`download_count` على `encyclopedia_forms`، يبدأ 0، يظهرلك إنت بس كسوبر أدمن).
2. **الترتيب** — أبجدي تلقائي بالكامل (على `name_ar` للمجلدات و`title` للنماذج). اتشال عمود `sort_order` من التصميم الأصلي في الخطة عشان مفيش داعي ليه.
3. **حذف مجلد فيه محتوى** — Cascade: حذف مجلد رئيسي بيحذف معاه أي مجلد فرعي جواه وكل النماذج المرتبطة، من غير أي تأكيد إضافي على مستوى القاعدة (التأكيد للمستخدم هيبقى مسؤولية الواجهة في مرحلة 3).

## اللي اتنفذ فعليًا (كود، مش لسه شغال على الإنتاج)

- **Migration جديدة:** `database/migrations/sql-migrations-phase28/01-encyclopedia-tables.sql`
  - جدول `encyclopedia_categories` (مجلدات، مستويين بس عبر تريجر `encyclopedia_enforce_two_level_depth`، `ON DELETE CASCADE` على `parent_id`)
  - جدول `encyclopedia_forms` (النماذج، `ON DELETE CASCADE` على `category_id`، عمود `download_count`)
  - تريجر `updated_at` تلقائي على الجدولين (دالة محلية `encyclopedia_set_updated_at`، معمولة خصيصًا للجدولين الجداد، مش بتلمس أي جدول تاني)
  - RLS: قراءة مفتوحة لأي `authenticated`، كتابة مقصورة على `is_super_admin()` — استخدمت الدالة الجاهزة الموجودة فعلاً في المشروع (مستخدمة في عشرات السياسات التانية) بدل `EXISTS` يدوي كان مقترح في الخطة الأصلية
- **`src/database.types.ts`:** أضفت تعريف الجدولين (Row/Insert/Update) في مكانهم الأبجدي الصحيح بين `clients` و`fee_payments`

## لسه محتاج منك (خطوات يدوية، زي كل مرة)

1. **تشغيل الـmigration** على Supabase (SQL Editor) — الملف فيه خطوات تأكيد (`pg_class`/`pg_policies`) في آخره تتأكد بيها إن RLS شغالة والسياسات الأربعة موجودة.
2. **إنشاء باكت Storage يدويًا** من الـDashboard:
   - الاسم: `encyclopedia-forms`
   - **Private** (مش Public)
   - سياسة الـStorage نفسها (قراءة `authenticated`، كتابة مقصورة على سوبر أدمن عبر Edge Function) هتتغطى في مرحلة 2 (الـEdge Function)، مش دلوقتي — الباكت الفاضي بس كفاية دلوقتي

## الخطوة الجاية (مرحلة 2 من الخطة)

بناء `encyclopedia-admin` Edge Function (createCategory/updateCategory/deleteCategory/uploadForm/updateForm/deleteForm) بتتحقق `is_super_admin` قبل أي عملية. هنبدأ فيها بعد ما تأكد تشغيل الـmigration وإنشاء الباكت.

**ملحوظة هحتاج قرارك فيها وقت الـEdge Function:** هيلبر فحص الرفع الموجود فعلاً (`validateUploadFile`) بيحدد حد أقصى **1 ميجا بس** لملفات PDF/Word — ده كان طلب صريح منك لمستندات القضايا. النماذج/الصيغ القانونية أحيانًا بتبقى أكبر من ميجا (خصوصًا لو فيها صور/شعارات). هل نفس الحد (1 ميجا) يتطبق على `encyclopedia-forms` كمان، ولا نحتاج حد مختلف (أعلى) خصيصًا للموسوعة؟
