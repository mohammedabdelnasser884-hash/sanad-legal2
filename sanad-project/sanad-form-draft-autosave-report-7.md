# تقرير حالة — إصلاح توقيت مسح مسودة الفورم عند الفشل
تاريخ: 3 أغسطس 2026

## الخلاصة
اتصلح البند التاني من "القرارات المفتوحة" في التقرير رقم 6: توقيت مسح
المسودة في `NewCaseModal`، `EditCaseModal`، `NewClientModal`،
`EditClientModal` بقى موحّد مع النمط اللي كان متبع أصلاً في
`StandaloneSessionDetailModal.tsx` — المسودة بتتمسح **بعد نجاح الحفظ
فعليًا**، مش بمجرد الضغط على زرار "حفظ".

## المشكلة اللي كانت موجودة
`draft.clearDraft()` كانت بتتنفذ فورًا بعد استدعاء `onSave(...)` من غير
انتظار نتيجته. بما إن `onSave` في الحقيقة async وبيرجع فشل واضح (رقم
قيد/رقم قومي مكرر، خطأ شبكة، conflict)، لو حصل انقطاع للتطبيق (كراش،
تحديث الصفحة) في نفس اللحظة اللي الحفظ فشل فيها، المستخدم كان يرجع
يفتح فورم فاضي تمامًا من غير أي استرجاع — رغم إن الحفظ نفسه فشل فعليًا.

## اللي اتعمل
### 1. `useCaseActions.ts`
- `handleSaveCase` بقت بترجع `Promise<boolean>` صريحة (`false` في كل
  نقاط الرجوع المبكر: فاليديشن، تكرار رقم القيد، خطأ الإدراج — و`true`
  في نهاية المسار الناجح). كانت من غير `return` خالص (يعني `undefined`
  دايمًا)، فمفيش طريقة كانت موجودة أصلاً للمستدعي يعرف بيها النتيجة.

### 2. `NewCaseModal.tsx`
- `onClick` بقى `async`، بينتظر `onSave(...)`، ويمسح المسودة بس لو
  `result !== false`.

### 3. `EditCaseModal.tsx` + الـwrapper في `CaseDetailView.tsx`
- الـwrapper بتاع `onSave` في `CaseDetailView.tsx` كان بيستدعي
  `onEdit?.(...)` وياخد النتيجة عشان يقفل المودال بس، من غير ما يرجّعها
  للمستدعي (`EditCaseModal`) — بقى دلوقتي بيرجّعها فعليًا (`return
  result;`).
- `EditCaseModal.tsx`: نفس تعديل `NewCaseModal.tsx` — `onClick` async،
  ينتظر النتيجة، يمسح المسودة بس لو نجحت.

### 4. `useClientActions.ts`
- `handleSaveClient` بقت بترجع `Promise<boolean>` بنفس منطق
  `handleSaveCase` بالظبط (فاليديشن الاسم، تكرار الاسم/الرقم القومي،
  خطأ الإدراج → `false`، النجاح → `true`).

### 5. `NewClientModal.tsx` و`EditClientModal.tsx` + الـwrapper في
`ClientDetailModal.tsx`
- نفس النمط: `onClick` async ينتظر `onSave`، والـwrapper في
  `ClientDetailModal.tsx` بقى يرجّع نتيجة `onEdit?.(...)` بدل ما
  يستخدمها داخليًا بس.

## الفحص
كل الملفات التسعة اللي اتلمست اتفحصت syntax-wise بـTypeScript
(`transpileModule`) — صفر أخطاء. اتعمل كمان مقارنة موازين الأقواس قبل
وبعد التعديل للتأكد إن أي فرق راجع بس للكود المُضاف (وهو متزن داخليًا)،
مش لخلل حقيقي.

## قرارات مفتوحة متبقية
- شكل تحذير الإغلاق لسه `window.confirm` الافتراضي (مش مودال مصمم
  بشكل التطبيق) — أولوية منخفضة، لسه قايم لحد ما تقرر تستبدله.

## الخطوة الجاية
لو حابب، نقدر:
1. نراجع سوا كمستخدم — تجربة فعلية: افتح فورم قضية/موكل جديد، اكتب
   بيانات هتفشل عمدًا (رقم قيد مكرر مثلاً)، أقفل التطبيق فجأة، افتحه
   تاني وشوف المسودة اتسترجعت.
2. نبدأ في شكل تحذير الإغلاق المخصص بدل `window.confirm`.
3. ننتقل لأي بند تاني في المشروع.
