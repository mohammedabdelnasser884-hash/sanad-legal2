# سند — تقرير حالة: المرحلة 6 (الأدمن) — دفعة 3، جزء 2: النسخ الاحتياطي (الجزء الآمن)

**التاريخ:** 25 يوليو 2026
**الحالة:** ✅ اكتمل بالكامل — المرحلة 6 (الأدمن) كلها مكتملة
**المرجع:** `سند-تقرير-حالة-المرحلة-6-دفعة-3-جزء-1-الأدمن-1.md`
**الزيب المرفق:** `sanad-project-updated-final-26.zip`

---

## الخلفية
دفعة 3 جزء 2 هي آخر جزء في قسم الأدمن كله (شاشة **النسخ الاحتياطي**). زي
ما اتفقنا في تقرير الجزء اللي فات: الجزء الآمن (إنشاء نسخة، عرض القايمة،
تنزيلها) يتغطى عادي زي أي قسم تاني، أما **تنفيذ استعادة فعلية** فمحتاج
قرار مسبق قبل ما يتكتب — لأن `handleRestoreBackup` بتحذف بيانات المكتب
الحالية بالكامل من كل الجداول (`RESTORE_DELETE_ORDER`) وتستبدلها بمحتوى
النسخة، بلا تراجع. حساب `E2E_TEST_EMAIL` هو نفسه المستخدم في كل تستات
E2E التانية — تنفيذ استعادة حقيقية عليه هيمسح أي بيانات تجريبية موجودة
وقت التشغيل.

## القرار
اتعرضت 3 خيارات لتغطية تنفيذ الاستعادة الفعلي (بدون تست تنفيذ حقيقي /
حساب تجريبي منفصل مخصص / تست منفصل يتشغّل بقصد لوحده). **القرار: خيار
1 — بدون تست تنفيذ حقيقي.** التغطية بتقف عند فتح المودال + فاليديشن حقل
التأكيد + الإلغاء (زي ما هو موصوف تحت)، ومن غير نية لكتابة أي تست بيدوس
"استعادة الآن" فعليًا في متصفح حقيقي.

## اللي خلص

### `BackupSection.tsx` + `AdminPanelSectionConfirms.tsx`
اتضافت `data-testid` للجزء الآمن بالكامل:
- زرار "إنشاء نسخة احتياطية الآن" (`admin-backup-create-button`)
- زرار "تحديث" القايمة (`admin-backup-refresh`)
- حالة "لا توجد نسخ احتياطية بعد" (`admin-backup-empty`)
- بطاقة النسخة (`admin-backup-card`)، زرار "تنزيل JSON"
  (`admin-backup-download`)، زرار "استعادة" اللي بيفتح المودال
  (`admin-backup-restore-button`)
- مودال تأكيد الاستعادة: حقل كتابة "استعادة"
  (`admin-backup-restore-confirm-input`)، زرار الإلغاء
  (`admin-backup-restore-cancel`)، زرار "استعادة الآن"
  (`admin-backup-restore-confirm-button`)

**تست جديد:** `e2e/admin-backup.spec.ts` (4 حالات، بس **بلا أي دوسة فعلية
على زرار "استعادة الآن"**):
1. إنشاء نسخة جديدة → توست نجاح → تظهر أول القايمة بباچ "الأحدث"
2. زرار "تحديث" بيعمل من غير خطأ والقايمة تفضل ظاهرة
3. تنزيل نسخة كملف JSON (`page.waitForEvent('download')`، بالتأكد من
   اسم الملف `sanad-backup-YYYY-MM-DD.json`)
4. فتح مودال التأكيد، فاليديشن حقل الكتابة (الزرار معطّل من غير كتابة
   أو بكتابة غلط، ويتفعّل بكتابة "استعادة" بالظبط)، وإلغاء المودال من
   غير أي تنفيذ

## تحقق الصيغة
فحصت `BackupSection.tsx`, `AdminPanelSectionConfirms.tsx`, وملف التست
الجديد بـ `tsc --noEmit --ignoreConfig` منفردين — نفس قايمة أخطاء بيئة
الساندبوكس المعروفة من كل التقارير السابقة (`react`/`@playwright/test`/
`@supabase/supabase-js` غير مثبتين، `import.meta.env`، `@types/node`
غير مثبت)، صفر أخطاء صياغة في أي سطر اتعدّل فعليًا.

**لازم تتأكد بنفسك قبل الدمج:**
1. `npm test` محليًا — نفس ملحوظة الدفعات اللي فاتت: التعديلات كلها
   إضافة `data-testid` بس، صفر تغيير منطق (`useAdminBackup.ts` لسه زي
   ما هو من غير أي تعديل).
2. `npm run test:e2e -- admin-backup.spec.ts` للملف الجديد.

---

## حالة خطة E2E الكاملة (تحديث)

| # | المرحلة | الحالة |
|---|---|---|
| 1 | الموكلين | ✅ مكتملة |
| 2 | الجلسة المستقلة | ✅ مكتملة |
| 3 | القضايا (أطراف متعددة) | ✅ مكتملة |
| 4 | الأتعاب | ✅ مكتملة |
| 5 | مساعد الذكاء الاصطناعي (وضع `generate`) | ✅ مكتملة |
| 6 | الأدمن — كل الأقسام (أرشيف، نشاط، مكتبة، مكتب، مستخدمون، جلسات، أمان، بوابة، نسخ احتياطي) | ✅ **مكتملة بالكامل** |
| 7 | باقي Tier 2 | ⬜ لسه ما بدأتش |
| 8 | Tier 3 — Smoke دفعة واحدة | ⬜ لسه ما بدأتش |

## الزيب المرفق
`sanad-project-updated-final-26.zip` — فيه كل حاجة من
`sanad-project-updated-final-25.zip` زي ما هي، زائد:
- `src/features/admin/backup/BackupSection.tsx` (testids)
- `src/features/admin/AdminPanelSectionConfirms.tsx` (testids مودال تأكيد الاستعادة)
- `e2e/admin-backup.spec.ts` (جديد — 4 تستات، بدون دوسة فعلية على "استعادة الآن")

---

## الخطوة الجاية
المرحلة 6 (الأدمن) بكامل أقسامها التسعة مكتملة رسميًا. الخطوة الجاية
الطبيعية: **المرحلة 7 — باقي Tier 2** من خطة E2E الكاملة.
