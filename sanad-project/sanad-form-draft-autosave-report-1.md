# تقرير حالة — خطة حفظ مسودات الفورمات تلقائيًا
تاريخ: 3 أغسطس 2026

## الهدف
منع فقدان بيانات أي فورم (قضية/جلسة مستقلة/موكل) لو المستخدم خرج من
التطبيق فجأة قبل ما يدوس "حفظ" (مكالمة، تطبيق تاني ياخد الفوكس، إغلاق
من نظام التشغيل...).

## الحل
حفظ مسودة تلقائي في localStorage أثناء الكتابة (debounced) + تحذير
بسيط قبل إغلاق الفورم لو فيه بيانات غير محفوظة.

---

## ✅ اللي اتعمل

### 1. البنية التحتية المشتركة (ملفان جديدان)
- **`src/shared/hooks/useFormDraft.ts`** — حفظ/استرجاع/مسح المسودة:
  - حفظ تلقائي في localStorage بعد ~800ms من آخر تغيير
  - مفتاح مميز لكل مستخدم (عن طريق `supabase.auth.getSession()`) عشان
    محامي تاني على نفس الجهاز ميشوفش مسودة زميله
  - بيرجّع `restoredDraft` لو لقى مسودة محفوظة وقت فتح الفورم
  - `isEmpty` اختيارية لتجنب تخزين/عرض مسودة فاضية فعليًا
- **`src/shared/hooks/useUnsavedChangesGuard.ts`** — تحذير قبل الإغلاق:
  - بيقارن الفورم الحالي بحالته الأولية وقت الفتح
  - لو فيه فرق، بيعرض `window.confirm` قبل ما ينفّذ `onClose` الفعلي
  - (قرار: نافذة المتصفح الافتراضية دلوقتي، مش مودال مصمم بشكل
    التطبيق — أسرع وأقل خطورة للخطوة الأولى)

### 2. توسيع `usePartyFields.ts` (هوك أطراف الدعوى المشترك)
- إضافة `replaceParties` و`replaceLegalTitles` — لاستبدال array الأطراف
  والمسمى القانوني دفعة واحدة وقت استرجاع مسودة
- إضافة فقط، صفر تغيير في السلوك الحالي (تدفق الإضافة/الحذف/التعديل
  اليدوي زي ما هو)

### 3. الدمج في `NewCaseModal.tsx` (فورم قضية جديدة) — أول فورم مكتمل
- بيحفظ: حقول الفورم + كل أطراف الدعوى + المسمى القانوني الجامع
- عند الفتح: لو فيه مسودة، بترجع تلقائيًا مع توست "تم استرجاع بيانات
  كنت بتكتبها قبل كده"
- زرار الإغلاق (✕) والضغط على الخلفية بقوا يسألوا تأكيد لو فيه بيانات
  غير محفوظة
- المسودة بتتمسح لما تدوس "حفظ" وتعدّي الفاليديشن (حتى لو الحفظ فشل
  سيرفريًا بعد كده — الفورم بيفضل مفتوح والبيانات في الذاكرة زي ما هي،
  بس مش هترجع تتسترجع تلقائيًا لو التطبيق اتقفل فجأة قبل إعادة المحاولة)

### 4. الدمج في `EditCaseModal.tsx` (فورم تعديل قضية) — مكتمل
- نفس منطق NewCaseModal بالحرف، مع فرق واحد: المفتاح `edit-case:<caseId>`
  عشان مسودة قضية متختلطش بمسودة قضية تانية
- ملحوظة اتضحت أثناء التنفيذ: `EditCaseModalForm` (الفورم الحقيقي)
  أصلاً بيتبني بس بعد ما بيانات القضية (وأطراف case_parties) تتحمّل من
  السيرفر بالكامل (فيه شاشة تحميل منفصلة قبلها) — يعني الـbaseline
  الأولي كان أصلاً بيانات القضية الحقيقية من أول رندر، فمحتجناش
  `enabled=false` زي ما كان متوقع في الخطة الأصلية
- الفورم ده مالوش overlay قابل للإغلاق بالضغط عليه (بعكس NewCaseModal)
  — زرار ✕ بس هو المطلوب ربطه بالتحذير، وده اللي حصل
  - الأب (`CaseDetailView.tsx`) بينتظر نتيجة الحفظ الفعلية قبل ما يقفل
    المودال — نفس قرار "امسح المسودة أول ما تدوس حفظ" اتطبّق هنا كمان
    للاتساق مع NewCaseModal

### 5. الدمج في `NewClientModal.tsx` (فورم إضافة موكل) — مكتمل
- بيغطي حقول الفورم النصية بس — ملفات الصور (البطاقة/التوكيل) مش قابلة
  للتخزين في localStorage أصلاً (مش JSON)، فمش جزء من المسودة ولا يمكن
  تسترجع بعد إغلاق التطبيق فجأة
- **قرار اتخد أثناء التنفيذ (مش في الخطة الأصلية):** الموديل ده بيتفتح
  أحيانًا بـ`initialData` (من جوه قضية/جلسة، زرار "إنشاء موكل جديد من
  هذه البيانات"). في الحالة دي **الحفظ التلقائي معطّل تمامًا** — السبب:
  مفيش معرّف ثابت للسياق ده نقدر نبني بيه مفتاح مسودة مميز، فلو سبناه
  شغال بمفتاح عام كان ممكن مسودة اتكتبت من سياق معين (طرف في قضية X)
  ترجع تتسترجع غلط في سياق تاني تمامًا (طرف في قضية Y). تحذير الإغلاق
  (لو فيه بيانات غير محفوظة) لسه شغال في الحالتين

### 6. الدمج في `EditClientModal.tsx` (فورم تعديل موكل) — مكتمل
- نفس منطق EditCaseModal — بيانات الموكل محمّلة فعليًا من الـprop
  (`client`) من أول رندر، فمفيش داعي لـenabled=false. المفتاح
  `edit-client:<clientId>`
- الفورم فيه خطوة تأكيد وسيطة ("التعديل هيتطبق في كل مكان الموكل ده
  ظاهر فيه — متأكد؟") قبل الحفظ الفعلي — المسودة بتتمسح عند تأكيد
  الحفظ الفعلي (زرار "نعم، احفظ في كل مكان")، مش عند مجرد ظهور شاشة
  التحذير

كل الملفات اتفحصت syntax-wise بـTypeScript (`transpileModule`) وتمام.

---

## ⏳ الباقي

### فورمات لسه محتاجة الدمج (بنفس الـ2 hook)
1. **`NewStandaloneSessionModal.tsx`**
2. **`StandaloneSessionDetailModal.tsx`** — بيانات محمّلة من السيرفر
   زي EditCaseModal (محتاج فحص هل عنده نفس نمط شاشة التحميل المنفصلة
   ولا لأ)

### قرارات مفتوحة (اتأجلت عمدًا من الخطوة الأولى، مش أخطاء)
- **شكل تحذير الإغلاق:** `window.confirm` الحالي شغال بس شكله مش
  موحّد مع باقي مودالات التأكيد في التطبيق (زي `ExitConfirmModal`) —
  ممكن نستبدله بمودال مخصص بعدين لو حبيت
- **حالة فشل الحفظ:** المسودة بتتمسح أول ما تدوس "حفظ" (مش بعد تأكيد
  النجاح فعليًا) — تبسيط مقصود، فيه احتمال ضيق إن المسودة تتمسح ثم
  يفشل الحفظ سيرفريًا ثم يتقفل التطبيق فجأة قبل إعادة المحاولة

---

## الترتيب المقترح للخطوات الجاية
`NewStandaloneSessionModal.tsx` → `StandaloneSessionDetailModal.tsx`،
كل فورم لوحده مع ZIP كامل بعد كل خطوة.
