# مراجعة شاملة: خطة تفكيك الأعمدة القديمة (Legacy Columns) — تحديث 10

> امتداد للتقرير السابق (`مراجعة-شاملة-خطة-تفكيك-legacy-columns-9.md`) بعد
> (1) التحقق من المرحلة C و(2) تنفيذ المرحلة D فعليًا. القسمين 0-2
> والخريطة الكاملة في القسم 1، وأقسام B.1-B.4 تحت، لسه صحيحين زي ما هم —
> النقلة هنا في قسمي C وD الجديدين والمراحل الباقية وجدول الحالة.

---

## تصحيح: `SessionUpdateModal.tsx` مش من طبقة العرض فعليًا

الجدول الأصلي (قسم 1، الطبقة ج) حط `SessionUpdateModal.tsx` كملف عرض قرائي
🔴 خطورة. بعد فحص الكود فعليًا وقت تنفيذ B.1: **ده غير دقيق**. الملف مالوش
أي حقل يعرض plaintiff/defendant للمستخدم أصلًا (الفورم بتاعه بس: "ما تم
في الجلسة" + تاريخ الجلسة القادمة + "المطلوب"). الاستخدام الوحيد لعمودي
plaintiff/defendant فيه هو **كتابة** (INSERT) نسخة الجلسة القادمة
للمسار المستقل (isStandalone) — يعني الملف ده فعليًا من **الطبقة أ
(الكتابة)** مش الطبقة ج، ومكانه الصح Phase F (لما تتشال الأعمدة، لازم
الـinsert ده يتوقف عن كتابتها) مش B.1.

**فجوة حقيقية اكتشفتها بالمناسبة:** السطر ده تحديدًا بيعمل dual-write
لجلسة **جديدة** (مش موجودة قبل كده) — يعني هو مصدر كتابة رابع للأعمدة
القديمة، غير التلات المذكورين في الطبقة أ الأصليين
(`NewCaseModal.tsx`, `NewStandaloneSessionModal.tsx`, `useCaseActions.ts`).
لازم يُضاف لقائمة الطبقة أ في Phase F (البند رقم 1) قبل أي `DROP COLUMN`.
مفيش أي تغيير سلوك اتعمل على الملف ده في B.1 — بس تصحيح للتصنيف والخطة
المستقبلية.

---

## المرحلة B.1 — الكالندر ✅ **مكتملة (6 أغسطس 2026)**

### الأساس المشترك (جديد)
- **`src/shared/parties/partiesDisplay.ts`** — `derivePartiesDisplay()` /
  `derivePartiesLine()`: بيبنوا نص "فلان ضد علان" (أو "فلان وآخرون ضد
  علان" لو أكتر من شخص في نفس الجهة) من صفوف `case_parties` الفعلية،
  مع رجوع تلقائي بالكامل لعمودي `plaintiff`/`defendant` (والمسمى
  القانوني) القديمين لو مفيش صفوف `case_parties` خالص — صفر تغيير سلوك
  لأي بيانات قديمة لسه معتمدة على الأعمدة دي.
- **`src/shared/parties/useSessionsPartiesMap.ts`** — بيجيب صفوف
  `case_parties` لمجموعة جلسات دفعة واحدة (نداءين بالكتير — مش نداء لكل
  جلسة)، مبني على نفس نمط `fetchPartiesByCaseId` المُثبت فعليًا في
  `supabase/functions/session-alerts/index.ts`، لكن ممتد كمان لمستوى
  الجلسة (`session_id`) عشان الجلسات المستقلة (نفس الفجوة المذكورة في
  الطبقة هـ للـEdge Function، لكن هنا للواجهة).

### الملفات المعدّلة
| الملف | التغيير |
|---|---|
| `SessionCard.tsx` | بقى بياخد `parties` prop اختياري، وبيستخدم `derivePartiesDisplay` بدل قراءة `plaintiff`/`defendant`/`*_legal_title` مباشرة. ده المكوّن المشترك خلف كل كروت الجلسات في الكالندر (اليوم المفتوح + عرض الأسبوع). |
| `CalendarTab.tsx` | بيجيب `partiesIndex` عبر `useSessionsPartiesMap` ويمرره لكل `SessionCard`. |
| `MonthListTab.tsx` → `MonthWeekView.tsx` | نفس الفكرة، ممررة عبر طبقة إضافية (`MonthWeekView` بقى بياخد `partiesIndex` prop). |
| `MissedTab.tsx` | سطر العنوان الاحتياطي (لما مفيش عنوان قضية/جلسة) بقى بيستخدم `derivePartiesLine` بدل تجميع `plaintiff + ' ضد ' + defendant` يدويًا. |
| `UpcomingWidget.tsx` | نفس إصلاح `MissedTab.tsx` بالظبط. |
| `UpcomingSessionsList.tsx` | نفس الإصلاح — عرضها الداخلي مش عن طريق `SessionCard` (كود منفصل)، فاتعدّل مباشرة بنفس الطريقة. |

### معيار الإنجاز
الشاشة بتعرض صح لقضية عندها أكتر من مدعي/مدعى عليه واحد — أي جلسة مرتبطة
بقضية أو مستقلة عندها صفوف `case_parties` فعلية هتعرض كل الأطراف (باسم
أول شخص + "وآخرون" لو أكتر من واحد على نفس الجهة) بدل الطرف الأساسي بس.
أي جلسة/قضية لسه من غير صفوف `case_parties` (الأغلبية حاليًا — راجع
أرقام المرحلة A: 92 قضية بس عندها `plaintiff` قديم فعلي) بترجع بالضبط
لنفس العرض القديم، صفر فرق ملحوظ.

### ملاحظة فحص — `MonthListTab.tsx`
الأعمدة الإضافية اللي بتتجاب في الاستعلام هناك (`plaintiff_national_id`,
`plaintiff_power_of_attorney`, `defendant_national_id`) اتأكد إنها **مش
معروضة فعليًا في أي مكان** في `MonthListTab`/`MonthWeekView`/`DayCard` —
type-only fields مش لها استخدام عرض حاليًا. مفيش تغيير مطلوب عليها في
B.1 (مفيش باگ عرض متعلق بيها)، لكن لو اتشالت الأعمدة القديمة في Phase F
هتحتاج تتشال من الـselect والـinterface هنا كمان.

### تحقق كود ثابت (بدون تشغيل build كامل — بيئة بدون `node_modules`)
اتشغّل `tsc` (نسخة عالمية، بدون تحميل تبعيات المشروع) على كل الملفات
المعدّلة والجديدة — الأخطاء الوحيدة اللي رجعت كانت "cannot find module"
متوقعة (react/`@supabase/supabase-js` مش مثبتين في بيئة المراجعة دي) وأخطاء
موجودة أصلًا في ملفين لم يُلمَسا (`caseSessionLinkingShared.ts`,
`notifications.ts` — بتاعة إعلانات `window.__dbWrite`/`window.__pendingSubscription`
العالمية، مش من شغل B.1). **صفر أخطاء متعلقة فعليًا بالكود المعدّل نفسه.**
التوصية: شغّل `npm run type-check` و`npm test` محليًا (فيهم `node_modules`)
قبل الدمج — ده تحقق ثابت بس، مش بديل عن تشغيل فعلي.

---

## المرحلة B.2 — الداشبورد ✅ **مكتملة (6 أغسطس 2026)**

### الفكرة
نفس الأساس المشترك بتاع B.1 (`derivePartiesDisplay` + `useSessionsPartiesMap`
+ `lookupParties`) اتطبّق على `DashboardTab.tsx` — من غير أي تعديل على
`useDashboardFeed.ts` نفسه (الـselect هناك أصلًا بيجيب كل الأعمدة
اللازمة، القديمة والجديدة، فمفيش داعي لتغييره).

### الملف المعدّل
| الملف | التغيير |
|---|---|
| `DashboardTab.tsx` | `buildSessionCard` (المستخدمة في الثلاث بطاقات: اليوم/القادم/الفائتة) بقت بتحسب `partiesIndex` واحد لكل جلسات الداشبورد مجمّعة (`todaySessions` + `upcomingSessions` + `missedSessions`) عبر `useSessionsPartiesMap`، وبتستخدم `derivePartiesDisplay(lookupParties(s, partiesIndex), legacyFallback)` بدل قراءة `plaintiff`/`defendant`/`*_legal_title` مباشرة من `linkedCase`/`s.cases`/`s`. الـfallback القديم (كل الترتيب: `linkedCase` → `s.cases` embed → عمود الجلسة نفسها لو مستقلة) اتحفظ زي ما هو بالظبط، بس بقى بيُستخدم فقط لو مفيش صفوف `case_parties` فعلية. |

### معيار الإنجاز
نفس معيار B.1 بالظبط لكن على شاشة الداشبورد: أي جلسة (اليوم/الأسبوع
القادم/الفائتة) مرتبطة بقضية أو مستقلة وعندها صفوف `case_parties` فعلية
هتعرض كل الأطراف (الاسم الأول + "وآخرون" لو أكتر من واحد على نفس الجهة)
في كارت الداشبورد بدل الطرف الأساسي بس. أي جلسة/قضية لسه من غير صفوف
`case_parties` بترجع بالضبط لنفس العرض القديم — صفر فرق ملحوظ.

### تحقق كود ثابت (بدون تشغيل build كامل — بيئة بدون `node_modules`)
اتشغّل `tsc` (نسخة عالمية) على `DashboardTab.tsx` والملفين المشتركين من
B.1 (`partiesDisplay.ts`, `useSessionsPartiesMap.ts`). الأخطاء الوحيدة
اللي رجعت: "cannot find module" متوقعة (react/`@supabase/supabase-js`
مش مثبتين في بيئة المراجعة) + خطأ موجود أصلًا في `supabaseClient.ts`
(`ImportMeta.env` بدون types بتاعة Vite، مش من شغل B.2 ولا حتى في ملف
اتلمس هنا). **صفر أخطاء متعلقة فعليًا بـ`DashboardTab.tsx` نفسه.**
التوصية زي B.1 بالظبط: شغّل `npm run type-check` و`npm test` محليًا
قبل الدمج.

---

## المرحلة B.3 — البحث ✅ **مكتملة (6 أغسطس 2026)**

### ملحوظة مهمة اكتشفتها أثناء التنفيذ
تغطية **البحث نفسه** في `case_parties` (تطابق اسم أي طرف من أطراف
الدعوى، مش بس الطرف الأساسي المخزّن في `cases.plaintiff/defendant`) كانت
**منفّذة فعليًا من قبل** في `useUniversalSearch.ts` (موثّقة في الكود هناك
كـ"مرحلة 9" — خطة تانية سابقة، مش B.3). يعني `db.from('case_parties')
.select('case_id').ilike('name', pattern)` بيرجّع `case_id` لأي قضية
فيها طرف (مش بس الأساسي) اسمه مطابق، والكود بيجيب بيانات القضايا دي
كمان (`extraCaseIds`) لو مش موجودة أصلًا في نتيجة تطابق `title`/`plaintiff`/
`defendant` المباشر. **الفجوة الفعلية كانت في طبقة العرض بس**: نتيجة
القضية في قائمة البحث كانت بتعرض عمودي `plaintiff`/`defendant` القديمين
مباشرة (بدل تعدد الأطراف الفعلي)، فقضية اتطابقت بسبب طرف تاني (مش
الأساسي) كانت بتظهر في النتائج من غير ما السطر يوضح مين الطرف اللي
اتطابق فعليًا.

### الملف المعدّل
| الملف | التغيير |
|---|---|
| `UniversalSearchModal.tsx` | نتيجة كل قضية في قائمة البحث بقت بتحسب `partiesIndex` (عبر `useSessionsPartiesMap`، بتمرير `{id: c.id, case_id: c.id}` لكل قضية مطابقة — استخدام مباشر لنفس الملف المشترك بدون أي تعديل عليه، بما إن `case_id` للقضية هو `id` بتاعها نفسها) وبتستخدم `derivePartiesDisplay` بدل قراءة `c.plaintiff`/`c.defendant` مباشرة في سطر العرض. |
| `useUniversalSearch.ts` | **بدون تغيير** — تغطية البحث في `case_parties` خلصت من قبل (مرحلة 9)، والـselect والـinterfaces هنا لسه محتاجة عمودي `plaintiff`/`defendant` كـfallback (مُستخدمة في `UniversalSearchModal.tsx` الآن كـfallback بس، مش عرض مباشر). |

### معيار الإنجاز
نفس معيار B.1/B.2: قضية اتطابقت في نتيجة البحث بسبب اسم طرف (أساسي أو
غير أساسي) موجود في صفوف `case_parties` فعلية، هتعرض سطر الأطراف الصحيح
(بما فيه "وآخرون" لو أكتر من طرف على نفس الجهة) بدل الطرف الأساسي بس.
أي قضية لسه من غير صفوف `case_parties` بترجع بالضبط لنفس عرض
`plaintiff`/`defendant` القديم — صفر فرق ملحوظ.

### تحقق كود ثابت (بدون تشغيل build كامل — بيئة بدون `node_modules`)
اتشغّل `tsc` على `UniversalSearchModal.tsx`، `useUniversalSearch.ts`،
وملفي B.1 المشتركين. نفس نمط B.1/B.2: الأخطاء الوحيدة "cannot find
module" المتوقعة + خطأ `ImportMeta.env` الموجود أصلًا في
`supabaseClient.ts` (غير متعلق بأي ملف اتلمس في B.3). **صفر أخطاء
متعلقة فعليًا بالملفين المعدّلين.** التوصية زي كل مرحلة سابقة: شغّل
`npm run type-check` و`npm test` محليًا قبل الدمج.

---

## المرحلة B.4 — الأنواع المركزية ✅ **مكتملة (6 أغسطس 2026)** — قفل المرحلة B

### الفكرة
على عكس B.1/B.2/B.3 (كل واحدة عدّلت شاشة عرض بعينها)، B.4 كانت مقصودة
من البداية إنها **إضافة بحتة على النوع المركزي `MappedCase`** (`useAppData.ts`)
مش تعديل شاشة تانية — لأن ملفات كتير جدًا بتستورد `MappedCase` (أكتر من
45 ملف)، وأي تعديل غير إضافي فيه كان هيبقى خطر تكسير واسع. الهدف: أي
شاشة مستقبلية (أو شاشة لسه معتمدة على `plaintiff`/`defendant` مباشرة
وخارج نطاق B.1-B.3) تقدر تستخدم `derivePartiesDisplay`/`derivePartiesLine`
من غير ما تحتاج تعمل نداء `useSessionsPartiesMap` منفصل بتاعها.

### الملف المعدّل
| الملف | التغيير |
|---|---|
| `useAppData.ts` | (1) حقل جديد **اختياري** `parties?: PartyDisplayRow[]` على `MappedCase` (اختياري عمدًا — عشان أي مكان في الكود بيبني كائن `MappedCase` يدويًا، زي كل ملفات الاختبار اللي بتستخدم `makeCase()`/mocks، ميتكسرش). (2) دالة جديدة `fetchPartiesMapByCaseIds()` (نفس نمط batching بتاع `useSessionsPartiesMap` في B.1 بس كدالة عادية مش hook، لأنها بتتنادى جوه `useCallback` موجودة أصلًا). (3) `fetchCases`/`searchCases` الاتنين بقوا بيجيبوا `case_parties` دفعة واحدة لكل صفحة/نتيجة بحث (نداء واحد إضافي بالكتير، مش نداء لكل قضية) ويملّوا `parties` على كل قضية ناتجة. |

### ليه اختياري (`parties?`) مش إجباري
لو الحقل إجباري، أي كائن `MappedCase` مبني يدويًا في مكان تاني (اختبارات،
mocks، أي كود مستقبلي) كان هيبقى ناقص خاصية إجباريًا ويكسر الـtype check.
بما إن الهدف من B.4 "صفر تغيير سلوك"، الحقل اختياري: القضايا الجاية من
`fetchCases`/`searchCases` (المسار الحقيقي الوحيد اللي بيعرض بيانات فعلية
للمستخدم) هتلاقيه معبّى دايمًا (`[]` لو مفيش صفوف، مش `undefined`)، وأي
كود تاني بيبني `MappedCase` يدويًا (اختبارات) يفضل شغّال زي ما هو من غير
أي تعديل مطلوب عليه.

### معيار الإنجاز
`cases` القادمة من `fetchCases`/`searchCases` كل واحدة فيها `parties`
معبّى (مصفوفة، فاضية أو بصفوف فعلية) — جاهز الاستخدام المباشر من أي شاشة
مستقبلية من غير نداء DB إضافي. **صفر تغيير على أي حقل موجود قبل كده**،
وصفر تغيير على أي مكان بيبني `MappedCase` يدويًا خارج `useAppData.ts`.

### تحقق كود ثابت (بدون تشغيل build كامل — بيئة بدون `node_modules`)
اتشغّل `tsc` على `useAppData.ts` مع `partiesDisplay.ts`. الأخطاء الوحيدة:
"cannot find module" المتوقعة + خطأ `ImportMeta.env` الموجود أصلًا في
`supabaseClient.ts` (نفس الخطأ اللي ظهر في كل مرحلة سابقة، مش من شغل
B.4). **صفر أخطاء متعلقة فعليًا بـ`useAppData.ts` نفسه.** ملحوظة إضافية
خاصة بـB.4: بما إن الحقل الجديد اختياري، مفيش خطر انكسار على أي ملف تاني
بيستورد `MappedCase` (أكتر من 45 ملف فحصتهم بالاسم — كلهم بيستخدموا
الحقول الموجودة بس، محدّش بيبني كائن `MappedCase` كامل يدويًا غير ملفات
الاختبار عبر `as MappedCase`/mocks جزئية). التوصية زي كل مرحلة سابقة:
شغّل `npm run type-check` و`npm test` محليًا قبل الدمج — خصوصًا هنا لأنه
أوسع ملف اتلمس في المرحلة B كلها.

---

## المرحلة C — توليد المستندات/AI ✅ **تحقّقت (مش تنفيذ جديد) — 6 أغسطس 2026**

### القرار المنتجي اللي اتاخد
سألتك: توليد المستندات بالـAI لازم يدعم كل الأطراف (يذكرهم كلهم بالاسم)
لو أكتر من مدعي/مدعى عليه، وجاوبت: **أيوه، يدعم كل الأطراف**.

### الاكتشاف
بعد الفحص، طلع إن الدعم ده **موجود ومنفّذ فعليًا من قبل** — مش من خطة
تفكيك الأعمدة القديمة الحالية، لكن من خطة سابقة منفصلة اسمها "خطة تطوير
أطراف الدعوى" (تواريخ التعليقات في الكود بتاريخ 23-24 يوليو 2026، قبل ما
خطة legacy-columns دي تبدأ أصلًا). يعني القرار المنتجي اللي اتاخد دلوقتي
(6 أغسطس) بيتماشى فعليًا مع اللي اتبنى من شهر تقريبًا:

| الملف | الوضع الفعلي |
|---|---|
| `useAIDocumentGenerator.ts` | بياخد `caseParties` prop، وبيبني `partiesDetailBlock` (قائمة كاملة بكل شخص باسمه وصفته لكل جهة فيها شخصان فأكتر) بيتضاف لنص الـprompt المرسل للـAI عبر `buildFullPartiesText` (من `shared/parties/partyDisplay.ts`). |
| `useAIAssistant.ts` | بيجيب `case_parties` فعليًا (فتش مستقل بـ`case_id`) ويمررها كـ`caseParties` لـ`useAIDocumentGenerator`. |
| `CaseSummary.tsx` | نفس الفكرة بالحرف — `buildCaseContextText` بتستخدم `buildFullPartiesText` لو الجهة فيها أكتر من شخص. |
| `CaseDataExtract.tsx` | نفس الفكرة، في نص الملخص القابل للنسخ وفي العرض المباشر (`InfoRow`) للشاشة. |
| `AILegalAssistant.tsx` | بيملّى `docFields.plaintiff/defendant` (سطر الترويسة المختصر القابل للتعديل يدويًا من المستخدم) من `plaintiff_legal_title || plaintiff` — ده تصميم مقصود ومنطقي: سطر الترويسة المختصر لازم يكون نص واحد قابل للتعديل، وتفاصيل كل شخص بالاسم بتتضاف تلقائيًا في جسم المستند عبر `partiesDetailBlock` فوق، مش في حقل الترويسة نفسه. |

### الخلاصة
**لا يوجد كود لازم يتغيّر في C.** القرار المنتجي اللي اتاخد اتأكد إنه
متحقق فعليًا في الكود الحالي. الملاحظة الوحيدة المسجّلة: تقرير الخطة
الأصلي (تحديثات 1-9) افترض إن C لسه معلّقة محتاجة قرار — الافتراض ده
كان مبني على معرفة بالخطة الحالية بس (legacy-columns)، من غير رؤية خطة
"تطوير أطراف الدعوى" السابقة اللي غطّت C فعليًا بشكل مستقل ومنفصل.

---

## المرحلة D — Edge Function ✅ **مكتملة (6 أغسطس 2026)**

### الفجوة
`fetchPartiesByCaseId` في `supabase/functions/session-alerts/index.ts`
كانت بتجيب أطراف `case_parties` بس لما فيه `case_id` (قضية مرتبطة). أي
جلسة **مستقلة** (case_id فاضي) كانت بترجع دايمًا لعمودي
`plaintiff`/`defendant` القديمين على مستوى الجلسة نفسها — حتى لو كانت
الجلسة دي عندها فعليًا صفوف `case_parties` مسجّلة بـ`session_id` (تعدد
أطراف). نفس الفجوة دي كانت موصوفة بالظبط في تحديث 6 (المرحلة الجاية D)،
واتقفلت جزئيًا على مستوى الواجهة بس في B.1 (`useSessionsPartiesMap.ts`).

### الملف المعدّل
| الملف | التغيير |
|---|---|
| `session-alerts/index.ts` | (1) `SESSION_COLS` بقى فيه `id` (كان ناقص — لازم لعمل lookup بـ`session_id`). (2) دالة جديدة `fetchPartiesBySessionId()` — نفس نمط `fetchPartiesByCaseId` بالحرف بس بـ`session_id` بدل `case_id` (نفس التوسيع اللي عمله `useSessionsPartiesMap.ts` في B.1 على الواجهة، هنا جوه الـEdge Function). (3) `buildPartyLines()` بقى بياخد `sessionId`/`partiesBySessionId` اختياريين إضافيين — لو موجودين وفيه صفوف مسجّلة بـ`session_id`، بتُستخدم بنفس أولوية `partiesByCaseId`. (4) نداءي `sendSessionAlert` (جلسات الغد/بعد غد) والجلسات الفائتة الاتنين بقوا بيجيبوا `partiesBySessionId` لمعرّفات الجلسات المستقلة في الدفعة (case_id فاضي) ويمرروها لـ`buildPartyLines`. |

### ليه اختياريان (`sessionId`/`partiesBySessionId`) مش إجباريان
نفس فلسفة B.4 بالظبط: باراميترين اختياريين إضافيين آخر التوقيع — أي نداء
قديم لـ`buildPartyLines` (لو فيه مكان تاني مش اتلمس هنا) يفضل شغّال زي ما
هو من غير أي تعديل، وأي جلسة عندها `case_id` (الحالة الغالبة) بتفضل تاخد
أولوية `partiesByCaseId` زي الأول بالظبط.

### معيار الإنجاز
جلسة **مستقلة** (case_id فاضي) عندها صفوف `case_parties` فعلية مسجّلة
بـ`session_id` (تعدد أطراف) هتظهر بكل أطرافها في رسالة تيليجرام (جلسات
الغد/بعد غد + الجلسات الفائتة)، بدل عمودي `plaintiff`/`defendant`
المفردين بس. أي جلسة مستقلة لسه من غير صفوف `case_parties` بترجع بالضبط
لنفس عرض الأعمدة القديمة — صفر فرق ملحوظ. جلسات القضايا (case_id موجود)
صفر تغيير — نفس مسار `partiesByCaseId` القديم بالحرف.

### تحقق كود ثابت (بدون تشغيل build كامل — بيئة Deno بدون نداء فعلي)
اتشغّل `tsc` على `index.ts`. الأخطاء الوحيدة: استيراد `esm.sh` واسم
`Deno` العالمي — متوقعان تمامًا في بيئة مراجعة بدون Deno runtime، مش
متعلقين بالتعديل. **صفر أخطاء بنيوية/type متعلقة فعليًا بالتعديل نفسه**
(كل نداءات `buildPartyLines` الاتنين اتحدّثوا بنفس التوقيع الجديد، ومفيش
نداء تالت متروك بالتوقيع القديم). التوصية: نفّذ اختبار يدوي فعلي (تشغيل
الـEdge Function على بيئة staging مع جلسة مستقلة تجريبية عندها أكتر من
طرف) قبل النشر — مفيش بيئة Deno هنا لتشغيله فعليًا.

---

## المراحل الباقية

- **E — نافذة مراقبة:** بعد ما B+C+D يخلصوا — **كلهم خلصوا/اتأكدوا
  دلوقتي**، فـE بقت جاهزة تتاخد.
- **F — التفكيك الفعلي:** زي ما هي، + بند `SessionUpdateModal.tsx` (مصدر
  كتابة رابع، راجع تصحيح تحديث 6 فوق) لازم يُضاف لقائمة "امسح كل الأكواد
  اللي لسه بتكتب للأعمدة القديمة" قبل أي `DROP COLUMN`.

---

## جدول الحالة المحدّث

| الطبقة | الحالة الفعلية | يمنع التفكيك؟ |
|---|---|---|
| الكتابة (Write) | ✅ dual-write سليم — **+ فجوة مكتشفة:** `SessionUpdateModal.tsx` مصدر كتابة رابع (INSERT جلسة قادمة مستقلة)، لازم يُضاف لبند 1 في Phase F | لأ (لسه dual-write، مش مانع دلوقتي) |
| القفل/الربط (Edit-lock) | ✅ خلص فعليًا (المراحل 1-3) | لأ |
| العرض القرائي (Display) — **الكالندر** | ✅ **B.1 مكتملة** (7 ملفات) | لأ (بعد B.1) |
| العرض القرائي (Display) — **الداشبورد** | ✅ **B.2 مكتملة** (`DashboardTab.tsx`) | لأ (بعد B.2) |
| العرض القرائي (Display) — **البحث** | ✅ **B.3 مكتملة** (`UniversalSearchModal.tsx`؛ تغطية البحث نفسه في `case_parties` كانت جاهزة من قبل) | لأ (بعد B.3) |
| العرض القرائي (Display) — **الأنواع المركزية** | ✅ **B.4 مكتملة** (`MappedCase.parties` — حقل اختياري جاهز لأي شاشة مستقبلية) | لأ — **المرحلة B كلها مقفولة** |
| المستندات/AI | ✅ **C اتحقّقت** — الدعم موجود فعليًا من خطة سابقة منفصلة ("تطوير أطراف الدعوى") | لأ |
| Edge Function | ✅ **D مكتملة** — `fetchPartiesBySessionId` جديدة + `buildPartyLines` بيغطي الجلسات المستقلة كمان | لأ |
| قاعدة البيانات (تأكيد) | ✅ اتأكدت — صفر بيانات هتضيع (المرحلة A) | لأ |
