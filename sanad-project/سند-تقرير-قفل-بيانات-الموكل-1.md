# تقرير — قفل بيانات الموكل داخل القضية/الجلسة + التوجيه لفورم التعديل

## الملخص
بيانات الموكل (الاسم/الرقم القومي/العنوان/بيانات التوكيل) بقت **عرض فقط** لأي طرف
مربوط بموكل حقيقي، سواء كان الطرف الأساسي أو أي طرف تاني اتربط لاحقًا عن طريق
"ربط بموكل من النظام". زرار "✏️ عدّل من ملف الموكل" بيظهر لكل طرف من دول، وبيوديك
مباشرة لفورم تعديل الموكل نفسه (مش شاشة تفاصيل بس محتاجة ضغطة تانية) — بعد ما يقفل
فورم القضية/الجلسة الحالي تلقائيًا (عشان ميفضلش شايل بيانات قديمة لو رجعت له).

بعد الحفظ في فورم تعديل الموكل، ظهر تنبيه واضح إن التعديل ده هيتطبق في كل مكان
الموكل ظاهر فيه، ولازم تأكيد قبل ما الحفظ يتم فعليًا.

**حقل "صفته" (capacity) فضل قابل للتعديل دايمًا** — دا دور الطرف في القضية دي
بالذات، مش بيانة موكل.

**فورمات الإنشاء (NewCaseModal / NewStandaloneSessionModal) ما اتلمستش** — القفل
والتوجيه بس في فورمات التعديل، زي ما اتفقنا.

## الملفات اللي اتعدلت
1. `src/features/cases/EditCaseModal.tsx` — القفل عام على أي طرف عنده client_id +
   زرار تعديل لكل طرف مربوط (مش بس الأساسي)
2. `src/features/calendar/sessions-calendar/StandaloneSessionDetailModal.tsx` —
   نفس التعديل بالظبط لجلسة مستقلة
3. `src/features/cases/CaseDetailView.tsx` — تمرير الموكل الصح (مش الأساسي دايمًا)
4. `src/App.tsx` — حالة جديدة `selectedClientEditMode` لفتح فورم التعديل مباشرة
5. `src/app/AppModals.tsx` — توصيل الحالة الجديدة لـ ClientDetailModal
6. `src/features/clients/ClientDetailModal.tsx` — خاصية `initialEditMode`
7. `src/features/clients/EditClientModal.tsx` — تنبيه تأكيد بعد الحفظ

## data-testid جديدة (لو حابب تضيف اختبارات E2E لاحقًا)
- `edit-case-open-client-profile-{partyId}` — زرار تعديل لأي طرف غير أساسي (قضية)
- `edit-standalone-session-open-client-profile-{partyId}` — نفس الفكرة (جلسة)
- `edit-client-impact-warning` / `edit-client-impact-confirm` / `edit-client-impact-cancel`

## اتفحص syntax-wise
كل الملفات المعدّلة اتفحصت بـ tsc بمعزل عن باقي المشروع (مفيش node_modules هنا
عشان أعمل build كامل) — مفيش أخطاء parsing. لازم build حقيقي (CI أو Vercel) للتأكد
الكامل زي المعتاد.

## حاجات محتاجة عينك عليها وقت الاختبار الحقيقي
- جرب تفتح قضية فيها أكتر من طرف مربوط بموكل (مش بس الأساسي) وشوف الزرار الجديد
  بيظهر صح وبيوديك لفورم تعديل الموكل الصح
- جرب تعدل موكل عن طريق الزرار ده وشوف التنبيه بيظهر، وبعد التأكيد البيانات بترجع
  محدّثة في القضية لما تفتحها تاني
- تأكد إن "صفته" (capacity) لسه قابلة للتعديل حتى لو الطرف مقفول
- تأكد إن دروب-داون "ربط بموكل من النظام" لسه شغال (فك ربط/تغيير موكل) للأطراف
  الغير أساسية
