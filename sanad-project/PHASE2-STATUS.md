# المرحلة 2 — الإصلاح الحرج على مستوى RLS (C-1) + المكمّل (M-2)

## 2.1 — تعديل `current_tenant_id()` — ✅ تم كتابة الـ migration
**الملف الجديد:** `database/migrations/sql-migrations-phase3/03-current-tenant-id-is-active-fix.sql`

التعديل: إضافة شرط `and p.is_active = true` في نهاية الدالة، بنفس نمط "شرط + تعليق شارح" المستخدم بالفعل في باقي شروطها (`status <> 'suspended'`, `trial_ends_at`). باقي تعريف الدالة (`SECURITY DEFINER`, `STABLE`, `search_path`) اتنقل زي ما هو بالظبط بدون أي تغيير.

**ملاحظة أمان:** لو `profiles.is_active` كانت `NULL` لأي صف قديم (نادر — العمود عنده `DEFAULT true`)، الشرط `p.is_active = true` هيرجع `NULL`/false، يعني الصف مش هيتحسب — ده سلوك آمن (fail-closed: منع افتراضي) مش خطر.

## 2.2 / 2.3 — تشغيل ملف الاختبار — ✅ تم، النتيجة نظيفة
**النتيجة الفعلية اللي رجعت من تشغيل صاحب المشروع:**
```json
{"summary":{"total_count":34,"failed_count":0,"passed_count":34},"failed_tests":null}
```
34 من 34 اختبار ناجح، صفر فشل — يشمل PART G (نجحت بعد التعديل) وكل الأجزاء A-F (regression check، صفر كسر في عزل التينانت الحالي).

## 2.5 — النشر على القاعدة الحية — ✅ تم فعليًا
لا يوجد بيئة staging منفصلة (المشروع كله تجريبي بلا عملاء/بيانات حقيقية حاليًا)، فالتعديل في 2.1 اتشغّل مباشرة على نفس القاعدة الوحيدة الموجودة. **المرحلة 2 مكتملة بالكامل رسميًا.**

## 2.4 — M-2 (ربط تعطيل المستخدم بتسجيل خروج إجباري تلقائي) — ✅ مُنفَّذة بالفعل، مفيش حاجة تتعمل
**اكتشاف مهم أثناء المراجعة:** رجعت لـ `useAdminUsers.ts` عشان أنفّذ الربط، ولقيت إنه **متنفذ بالفعل**:

```ts
const toggleUserActive = async (user: ProfileRow) => {
    const newState = user.is_active === false ? true : false;
    const { error } = await db.from('profiles').update({ is_active: newState }).eq('id', user.id);
    ...
    if (!newState && user.user_id) {
      try {
        await callAdminAction({ action: 'force_signout', user_id: user.user_id });
      } catch (e) {
        // ... تسجيل الخطأ وتوضيح للأدمن لو فشل إنهاء الجلسات
      }
    }
    ...
};
```

الدالة دي متصدَّرة من الهوك ومستخدمة فعليًا في `UsersSection.tsx` (زرار تفعيل/تعطيل السريع). يعني تعطيل المستخدم بيستدعي `force_signout` تلقائيًا في نفس العملية، ومفيش اعتماد على إن الأدمن يفتكر يدوس زرار "تسجيل خروج من كل الأجهزة" بشكل منفصل.

**تحديث تصنيف M-2 في التقرير:** البند ده **متحقّق بالفعل في نسخة الكود المرفوعة** — يبدو إنه اتحل في جولة سابقة قبل هذا الفحص. **صفر كود جديد مطلوب هنا.**

## المرحلة 2 — مكتملة بالكامل ✅
| البند | الحالة |
|---|---|
| 2.1 | ✅ الدالة اتعدلت |
| 2.2/2.3 | ✅ 34/34 اختبار ناجح |
| 2.4 (M-2) | ✅ كانت متعملة بالفعل |
| 2.5 | ✅ النشر تم (مفيش بيئة منفصلة) |
