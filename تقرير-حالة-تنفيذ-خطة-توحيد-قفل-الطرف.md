# تقرير حالة تنفيذ خطة توحيد قفل الطرف + فلو الأتعاب

> هذا التقرير بيتحدّث بعد كل مرحلة تتنفّذ فعليًا. مرجعه الأساسي هو
> `تقرير-خطة-تنفيذ-توحيد-قفل-الطرف-3-2-2.md` (الخطة الأصلية المبنية على
> فحص الكود) — هنا بس تتبّع "إيه اللي اتعمل فعليًا" مقابل "إيه اللي لسه".

**آخر تحديث:** بعد تنفيذ المرحلة 2 من مسار الطرف (توصيل السيرفس فعليًا
بالفورمات + إصلاح باگ 5.1/5.5).

**الترتيب المتفق عليه:** مسار الطرف كامل (0.5+0.6 → 1 → 2 → 3) أولًا، ثم
مسار الأتعاب (المرحلة 5) بعده.

---

## ✅ اتعمل فعليًا

### مسار الطرف — المرحلة 1: `PartyDomainService`

**الملفات الجديدة:**
- `src/shared/parties/partyDomainService.ts` — السيرفس نفسه.
- `src/shared/parties/partyDomainService.test.ts` — تستات تغطي الحالات
  الخمسة + كل دوال `canX`.

**إيه اللي فيه:**
- `getPartyState(party, ctx)` — بيرجّع واحدة من 5 حالات موحّدة:
  `MANUAL` / `LINKED` / `ORPHAN_PARTY` / `ORPHAN` / `PRIMARY_CLIENT`
  (مطابق لتحديث المراجعة الرابعة في الخطة، مش النسخة القديمة اللي
  كانت 4 حالات بس).
- `isLinkedState()` / `isOrphanState()` / `isPartyOrphaned()` — دوال
  فحص مختصرة مبنية على الحالة الموحّدة.
- `canEditPartyFields()` — بديل `renderPartyReadOnly` القديم
  (`!!party.client_id`). دلوقتي طرف orphan (أساسي أو ثانوي) بيرجع
  `true` (قابل للتعديل) بدل ما يفضل مقفول بلا سبب.
- `canToggleIsClient()` — بديل شرط تعطيل زرار ⭐.
- `canUnlinkParty()` — **ده تحديدًا إصلاح باگ 5.1 (dead-end)**: بيرجع
  `true` لأي طرف عنده `client_id`، بما فيه `ORPHAN_PARTY` — يعني طرف
  ثانوي اتحذف موكله بقى ليه مسار فك ربط واضح بدل ما يفضل عالق.
- `canCreateNewClientFromParty()` — `true` لـ `MANUAL` و`ORPHAN_PARTY`
  بس (مش `ORPHAN` — الموكل الأساسي بيتم ربطه/فكه من تاب بيانات القضية،
  نفس القرار المعماري الموجود بالفعل).
- `getPartyStateMessage()` — نص موحّد بديل للتوستات المتفرقة.
- `isOrphanedLink(clientId, linkedClient)` — دالة مساعدة لمستوى
  القضية/الجلسة (الهيدر وتاب البيانات) اللي معاها كائن الموكل مباشرة
  مش قائمة `clients` كاملة.

**قرارات تصميم اتاخدت أثناء البناء (يستاهلوا توثيق):**
- الفرق بين `ORPHAN` و`ORPHAN_PARTY` بيتحدد بمقارنة `client_id` بتاع
  الطرف مع `primaryClientId` اللي بيتمرر في الـ`context` — مش بعلم
  إضافي على الطرف نفسه.
- ملاحظة الـRLS/الصلاحيات المذكورة في الخطة (قسم "ملاحظة مراجعة —
  صلاحيات/RLS") **اتوثّقت كتعليق داخل الكود نفسه** ولسه مفتوحة زي ما
  هي — السيرفس بياخد `clients` زي ما بتوصله من الفورم (نفس افتراض
  الكود الحالي)، ومفيش تمييز بين "موكل محذوف فعلاً" و"موكل مش مرئي
  للمستخدم الحالي بس". القرار ده لسه محتاج توضيح قبل أي محاولة تفرقة
  مستقبلية.

**مش اتعمل في المرحلة دي (وده متعمّد، مش نسيان):**
- **مفيش أي ملف فورم اتلمس لسه** — `EditCaseModal.tsx`،
  `StandaloneSessionDetailModal.tsx`، `casePartiesValidation.ts`،
  `InfoSection.tsx`، `CaseDetailView.tsx` كلهم زي ما هما بالظبط. الخطة
  الأصلية بتفصل بناء السيرفس (مرحلة 1) عن التوصيل الفعلي في الفورمات
  (مرحلة 2) عمدًا — عشان كل مرحلة تتراجع عنها لوحدها لو احتاج الأمر من
  غير ما تكسر فورمات شغالة دلوقتي. يعني **باگ 5.1 لسه موجود فعليًا في
  التطبيق** لحد ما المرحلة 2 تتنفذ وتوصّل السيرفس بالفورمات.

**⚠️ تنبيه تنفيذي:** التستات اتكتبت وراجعتها منطقيًا يدويًا (compile +
تشغيل مباشر بـ Node بعد إزالة أنواع TypeScript) وطلعت صح، لكن
**`npm test` (vitest) ما اتشغلش فعليًا** — بيئة التنفيذ هنا مقطوعة عن
الإنترنت فمش قادرة تجيب `node_modules`. لازم تشغّل
`npm test -- partyDomainService` عندك أول ما تستلم الملف للتأكد
النهائي قبل ما نبني عليه المرحلة 2.

---

### مسار الطرف — المرحلة 2: توصيل `PartyDomainService` بالفورمات

**الملفات المعدّلة:**
- `src/shared/parties/usePartyFields.ts`
- `src/shared/lib/casePartiesValidation.ts` (+ `casePartiesValidation.test.ts`)
- `src/features/cases/EditCaseModal.tsx`
- `src/features/calendar/sessions-calendar/StandaloneSessionDetailModal.tsx`
- `src/features/cases/case-detail/InfoSection.tsx`

**إيه اللي اتصلح فعليًا (مش بس إعادة تسمية):**

1. **باگ 5.1 (dead-end) — اتصلح في الملفين:**
   - `EditCaseModal.tsx`: طرف ثانوي orphan دلوقتي بياخد بانر تحذير واضح
     (بدل مفيش أي إشارة)، ودروب-داون "ربط بموكل من النظام" بيرجع لقيمة
     "— بدون ربط —" بدل ما يعرض قيمة `client_id` شبحية مالهاش خيار
     مطابق، وزرار "➕ إنشاء موكل جديد من هذه البيانات" بقى يظهر تاني
     (كان مخفي بالكامل لأي طرف عنده `client_id` حتى لو orphan).
   - `StandaloneSessionDetailModal.tsx`: الملف ده معهوش أصلًا أي دروب-داون
     ربط/فك ربط (بعكس `EditCaseModal.tsx`) — **أضفنا أول زرار "🔓 فك
     الربط عن هذا الطرف" فعليًا للملف ده**، متاح لأي طرف ثانوي عنده
     `client_id` (حي أو orphan)، + بانر تحذير عند orphan. ده أول إصلاح
     حقيقي (مش إعادة صياغة) للـdead-end في الملف ده تحديدًا، لأنه ماكانش
     عنده أي مسار فك ربط خالص قبل كده.

2. **باگ 5.5 (فاليديشن بتتجاهل orphan) — اتصلح:**
   `casePartiesValidation.ts` بقت بتاخد `orphanedPartyIds` اختياري.
   `usePartyFields.ts` بقى بياخد `domainContext` اختياري وبيحسب الأطراف
   الـorphan تلقائيًا (`getPartyState`) ويغذّي بيها الفاليديشن. اسم طرف
   orphan بقى يخضع لفحص الصيغة (ثلاثي/ثنائي) زي أي اسم يدوي، مش يتخطى
   الفحص للأبد لمجرد إنه لسه شايل `client_id` قديم.

3. **`renderPartyReadOnly` بقى مبني على `getPartyState()`:** طرف orphan
   (أساسي أو ثانوي) بياناته بقت قابلة للتعديل الحر فعليًا دلوقتي — مش
   بس النية الموثّقة في التعليقات القديمة (كان فيه تناقض: التعليق بيقول
   "الحقول رجعت حرة" بينما الشرط الفعلي `!!party.client_id` كان بيقفلها
   لسه لو `client_id` لسه موجود في الصف حتى لو orphan).

4. **`isOrphaned` اتوحّد في 4 أماكن** (كان مكرر يدويًا في كل واحد):
   `EditCaseModal.tsx`، `StandaloneSessionDetailModal.tsx` (نسختين)،
   `InfoSection.tsx` — كلهم بقوا بينادوا `isOrphanedLink()` نفسها.

**تيستات جديدة/محدّثة:**
- `usePartyFields.test.ts` — 3 تستات جديدة لـ`domainContext`.
- `casePartiesValidation.test.ts` — 4 تستات جديدة لـ`orphanedPartyIds`
  (`describe('orphanedPartyIds — إصلاح باگ 5.5')`).
- ⚠️ نفس تنبيه المرحلة 1: راجعتها منطقيًا يدويًا، **ما اتشغلتش فعليًا**
  (لسه مفيش نت لتثبيت `node_modules`). شغّل `npm test` عندك للتأكيد.

**مش اتعمل في المرحلة دي (متعمّد، موثّق كفجوة مش نسيان):**
- **5.3 (الهيدر `CaseDetailView.tsx`):** `linkedClients` لسه بتتجاهل
  الأطراف الـorphan بصمت (بدون أي مؤشر). القرار: التعديل ده UI جديد
  (مش استبدال شرط موجود) وأقرب لطبيعة المرحلة 3 (Badges)، فسيبناه ليها
  عمدًا بدل ما نلمس الهيدر دلوقتي بتغيير سلوك مش متفق عليه.
- **5.4 (badges في `CasesTab.tsx` وعرض `sessionParties`):** استعلام
  `sessionParties` في `StandaloneSessionDetailModal.tsx` لسه بيجيب
  `side,name,capacity` بس (بلا `client_id`) — مفيش بيانات كفاية لإضافة
  badge هناك من غير تعديل الاستعلام. نفس القرار: مؤجل لحد المرحلة 3.
- **0.6 (فحص `client_id` وقت استرجاع مسودة `useFormDraft`):** لسه ما
  اتلمسش مباشرة. **لكن** بما إن `renderPartyReadOnly`/`renderPartyExtra`
  بقوا مبنيين على `getPartyState()` اللي بيتحسب من `partyFields.parties`
  الحيّة (مش لقطة وقت التحميل الأول)، فلو مسودة قديمة رجّعت `client_id`
  لموكل اتمسح، الطرف هيظهر تلقائيًا كـ`ORPHAN_PARTY` (بانر + حقول حرة)
  بمجرد ما `replaceParties()` تتنفذ — **الأثر العملي لـ6.1 اتغطى
  كنتيجة جانبية**، لكن التحسينات الإضافية المقترحة في 6.2/6.3 (تأكيد
  المستخدم قبل الاسترجاع، `savedAt` timestamp) لسه مش متنفذة، وده متعمّد
  (خارج نطاق "توحيد القفل" الأساسي، أقرب لتحسين UX منفصل).
- **NewCaseModal.tsx / NewStandaloneSessionModal.tsx:** مش جزء من
  قايمة الملفات الرسمية في الخطة لمرحلتي 1/2 (فورمات إنشاء جديد، مش
  تعديل) — سيناريو orphan فيهم نادر جدًا (لازم الموكل يتمسح أثناء ما
  المستخدم لسه بيملى فورم إنشاء لسه مفتوح) ومش متغطى هنا.

---

## 🔜 الباقي

### مسار الطرف
1. **المرحلة 3** — Badges/UI (🟢🔵🟠🟣) + نص سبب القفل تحت الحقل +
   preview قبل فك الربط، في `PartyFields.tsx`/`PartyFieldsGroup.tsx`.
   دي كمان الفرصة الطبيعية نقفل فيها الفجوات المؤجلة من المرحلة 2:
   مؤشر orphan في هيدر `CaseDetailView.tsx` (5.3)، و`client_id` في
   استعلام `sessionParties` عشان تبان badge في عرض الجلسة المستقلة (5.4).
2. *(المرحلة 4 — تفكيك الـLegacy columns — خارج التسلسل عمدًا، قرار
   منفصل بتوقيتها زي ما موضّح في الخطة الأصلية).*

### مسار الأتعاب (بعد ما مسار الطرف يخلص)
4. **المرحلة 5** — RPC ذرّية لفلو "إنشاء أتعاب جديدة + دفعة مقدّمة"
   (`useFeesActions.ts`) على نمط `record_fee_payment` الموجودة، بدل
   3 استعلامات منفصلة بلا transaction.
5. **المرحلة 6 (القديمة)** — تنبيه عند فشل تسجيل الجلسة الأولى في
   `useCaseActions.ts` (أولوية متوسطة).

---

## ملخص سريع لصاحب القرار

| المرحلة | الحالة |
|---|---|
| 0.5 + 0.6 (تحقيق) | ✅ متوثّق في الخطة الأصلية |
| 1 — `PartyDomainService` | ✅ اتعمل |
| 2 — توصيل السيرفس بالفورمات (باگ 5.1 + 5.5) | ✅ **اتعمل الآن** (محتاج `npm test` عندك للتأكيد) |
| 3 — Badges/UI (+ فجوتي 5.3/5.4 المؤجلتين) | ⏳ التالي |
| 5 — RPC الأتعاب | ⏳ |
| 6 — تنبيه الجلسة الأولى | ⏳ |
| 4 — Legacy columns | 🔒 خارج التسلسل — قرار منفصل |
