# خطة تنفيذ: إعادة هيكلة "الموسوعة القانونية" إلى "الموارد القانونية"

## 0. نظرة عامة

القسم الحالي "الموسوعة القانونية" هيتحول لقسم أب اسمه **"الموارد القانونية"**، وتحته قسمان:

1. **الصيغ والنماذج** — نفس شاشة التصفح الموجودة حاليًا بالضبط، بدون أي تغيير في منطقها أو بياناتها.
2. **دليل المحامي** — دليل خدمات رسمية قابل للبحث والتصنيف (وزارة العدل، الشهر العقاري، الضرائب... إلخ)، مبني من الصفر.

---

## 1. القرارات المؤكدة

| القرار | التفصيل |
|---|---|
| آلية الاختيار | الضغط على "الموارد القانونية" يفتح بوكس منبثق (نفس تصميم بوكس "المزيد" الحالي في الموبايل) فيه أيقونتين: الصيغ والنماذج / دليل المحامي |
| الرجوع | زرار الرجوع من جوه أي قسم يرجع مباشرة للقائمة الرئيسية |
| صلاحيات "دليل المحامي" | نفس نموذج "الصيغ والنماذج": القراءة مفتوحة لأي مستخدم مسجّل دخول، الإضافة/التعديل/الحذف مقصورة على السوبر أدمن فقط (`is_super_admin()`) |
| تنظيم "دليل المحامي" | **تصنيفات**، مش قائمة مسطحة |
| بحث | نفس نمط البحث الموجود في "الصيغ والنماذج" |
| بيانات كل رابط | عنوان، وصف مختصر، نوع الجهة، تاريخ آخر مراجعة (يدوي من الأدمن) |
| **ترتيب الروابط داخل كل تصنيف** | **يدوي — محسوم (15 سبتمبر 2026)**: أزرار "لأعلى/لأسفل" على كل رابط داخل تصنيفه، مش سحب وإفلات. الرابط الجديد بيتحط آخر الترتيب تلقائيًا (مش أول حاجة) لحد ما الأدمن يرتّبه بنفسه |
| هيكل التصنيفات | التقسيم الثماني الكامل معتمد كما هو (التفصيل الكامل في الملحق أ) |
| المفضلة ⭐ | مؤجلة لمرحلة 7 بعد الإطلاق |
| تجميع محتوى الروابط نفسه | مسؤولية بي — مش جزء من التنفيذ التقني |
| التعامل مع الخدمات الفرعية بدون بوابة مستقلة | راجع التفصيل الكامل في الملحق أ |

## 2. قرارات لسه مفتوحة
لا يوجد قرارات مفتوحة حاليًا — قرار #1 (ترتيب الروابط) اتحسم في هذا التسليم، وقرار #2 (شكل بوكس الديسكتوب) كان محسوم بالفعل من قبل.

---

## 3. المراحل التقنية

### المرحلة 1 — قاعدة البيانات — ✅ منفّذ
جدولا `lawyer_guide_categories`/`lawyer_guide_links` + RLS.

**تحديث (هذا التسليم):** ميجريشن إضافي `database/migrations/sql-migrations-phase32/01-lawyer-guide-links-sort-order.sql` — إضافة عمود `sort_order integer NOT NULL DEFAULT 0` لجدول `lawyer_guide_links` (نفس فكرة العمود الموجود بالفعل في `lawyer_guide_categories`)، لحسم قرار الترتيب اليدوي. **لازم تُطبَّق يدويًا في SQL Editor قبل استخدام أزرار الترتيب في الواجهة.**

### المرحلة 2 — Edge Function — ✅ منفّذ (محدَّث في هذا التسليم)
توسيع `encyclopedia-admin` بـ6 actions. **تحديث:** `createLink`/`updateLink` بقوا يقبلوا `sort_order?` اختياري كمان — **لازم `supabase functions deploy encyclopedia-admin` تاني بعد هذا التسليم** (حتى لو كنت نشرتها قبل كده على نسخة مرحلة 2/3 السابقة).

### المرحلة 3 — واجهة الأدمن — ✅ منفّذ (محدَّث في هذا التسليم)
كل حاجة من التسليم السابق + **الترتيب اليدوي الجديد**:
- `useAdminLawyerGuide.ts`: الروابط بقت بتتجاب مرتّبة `category_id, sort_order, title`. عند إضافة رابط جديد، بيتحسب `sort_order` تلقائيًا كـ`(أكبر ترتيب موجود في نفس التصنيف) + 1` (يعني بيتحط آخر القائمة). دالة جديدة `handleReorderGuideLink(link, 'up'|'down')` — بتبدّل `sort_order` مع الجار المباشر في نفس التصنيف (نداءين `updateLink` متوازيين، صفر action جديدة في السيرفر).
- `LawyerGuideSection.tsx`: زوج أزرار "لأعلى ▲ / لأسفل ▼" صغير على شمال كل بطاقة رابط (بس جوه تصنيف مفتوح — مش في نتائج البحث اللي بتخلط روابط من تصنيفات مختلفة). الزرار بيتعطّل تلقائيًا لو الرابط أول أو آخر واحد في تصنيفه.
- `database.types.ts`/`supabaseClient.ts`: تحديث `lawyer_guide_links` وunion `EncyclopediaActionPayload` بعمود/حقل `sort_order` الجديد.

### المرحلة 4 — واجهة المستخدم وإعادة هيكلة التنقل — ⬜ لم يبدأ
### المرحلة 5 — تسميات ونصوص (Rename sweep) — ⬜ لم يبدأ
### المرحلة 6 — اختبارات (E2E + Edge Function) — ⬜ لم يبدأ
### المرحلة 7 (مؤجلة) — ⬜ مؤجلة عمدًا

---

## 3.1 جدول حالة تنفيذ المراحل

| المرحلة | الوصف | الحالة | ملاحظات |
|---|---|---|---|
| 1 | قاعدة البيانات (+ عمود sort_order للروابط) | ✅ منفّذ | ميجريشن phase32 محتاج تطبيق يدوي |
| 2 | Edge Function (+ sort_order في createLink/updateLink) | ✅ منفّذ | محتاج إعادة نشر (`supabase functions deploy`) |
| 3 | واجهة الأدمن (+ ترتيب يدوي بأزرار لأعلى/لأسفل) | ✅ منفّذ | هذا التسليم — محتاج تشغيل CI منك قبل النشر |
| 4 | واجهة المستخدم + إعادة هيكلة التنقل | ⬜ لم يبدأ | — |
| 5 | تسميات ونصوص (Rename sweep) | ⬜ لم يبدأ | — |
| 6 | اختبارات (E2E + Edge Function) | ⬜ لم يبدأ | — |
| 7 | المفضلة ⭐ + توسّع تدريجي | ⬜ مؤجلة عمدًا | خارج نطاق v1 |

---

## 3.2 طريقة التسليم

**ملفات هذا التسليم (زيب):**
- `src/features/admin/lawyer-guide/hooks/useAdminLawyerGuide.ts` (معدّل)
- `src/features/admin/lawyer-guide/LawyerGuideCategoryModal.tsx` (بدون تغيير عن التسليم السابق)
- `src/features/admin/lawyer-guide/LawyerGuideLinkModal.tsx` (بدون تغيير — الترتيب مش حقل في المودال، بيتحكم فيه بس بأزرار الواجهة)
- `src/features/admin/lawyer-guide/LawyerGuideSection.tsx` (معدّل)
- `src/features/admin/AdminPanel.tsx` (معدّل)
- `src/features/admin/AdminPanelModals.tsx` (بدون تغيير عن التسليم السابق)
- `src/types.ts` (بدون تغيير عن التسليم السابق)
- `src/supabaseClient.ts` (معدّل)
- `src/database.types.ts` (معدّل)
- `src/constants.ts` (بدون تغيير عن التسليم السابق)
- `supabase/functions/encyclopedia-admin/index.ts` (معدّل)
- `database/migrations/sql-migrations-phase32/01-lawyer-guide-links-sort-order.sql` (جديد)

**نسخة منفصلة خارج الزيب (مرفقة كملف مستقل):** نفس ملف الميجريشن `01-lawyer-guide-links-sort-order.sql` — طبّقه في SQL Editor قبل أي حاجة تانية، وبعدين انشر الـEdge Function.

**خطوات النشر بالترتيب:**
1. طبّق `01-lawyer-guide-links-sort-order.sql` في Supabase SQL Editor.
2. `supabase functions deploy encyclopedia-admin`.
3. انشر الفرونت إند (بعد ما تشغّل `npm run build`/`vitest` بنفسك محليًا للتأكد).

لا يوجد ملفات تحتاج حذف يدوي.

---

## 4. ملاحظة عامة
لا يوجد أي تغيير في منطق أو بيانات "الصيغ والنماذج" الحالية.

---

## ملحق أ — خريطة التصنيفات والروابط الكاملة
بدون تغيير عن التقرير السابق (نفس التقسيم الثماني الكامل بروابطه).
